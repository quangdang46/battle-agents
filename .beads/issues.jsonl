{"id":"ba-2xu","title":"DESIGN.md: the binding visual design authority","description":"Write DESIGN.md, the visual design authority the plan names and an open bead is already blocked on. This is a short document with a short list of decisions in it, and the reason it is a bead rather than a chore is that ba-web-ui-surface-t3w cannot start without it.\n\nWHAT THE PLAN ALREADY DECIDED, SO THIS IS TRANSCRIPTION PLUS THE UNSETTLED PARTS, NOT INVENTION\nSection 14 already locks the two decisions that matter most, and quotes them so nobody reopens them:\n- \"MEDIUM DECIDED 2026-09-24: 2D pixel art is the rendering medium; cyberpunk / dev-workstation / RPG-arena is the mood applied to it.\" The plan names a mood but never a medium, and the two do not conflict: pixel sprites lit by a neon cyberpunk palette is one coherent direction. It was already de facto chosen by section 19's PixiJS plus tilemap layout, section 25's 16x16 sprites, and the vendored tiny-swords-cc0 pack.\n- \"THE SPLIT, and it does not bend toward a fully retro UI: pixel art for the game world, characters, items, tilesets and VFX; the dashboard, bounty board and auth screens stay modern, clean and responsive. A site that is retro all the way down is harder to use and reads as a costume.\"\n- \"DESIGN.md is the binding source for visual decisions; this section records the decision and the reasoning.\" That sentence is why this file has to exist. Section 14 is the record; DESIGN.md is the binding artifact downstream work is checked against.\n\nWHY IT IS MISSING, verified 2026-09-25\n- There is no DESIGN.md anywhere in the repo. docs/ contains only design/ and research/; docs/design/ holds payout-rail.md and public-event-stream.md and nothing else.\n- ba-web-ui-surface-t3w is blocked on it in so many words: \"That is VISUAL work, and it is still blocked until DESIGN.md exists - antislop R-37 forbids building UI without design direction. A framework shell is not visual design and was never blocked; the shell is now in place.\" The shell work is done and the visual work cannot start.\n- The web-ui bead does not own producing it - it names DESIGN.md as a precondition it does not satisfy. So at the time the work had no home, which is the only condition under which a new bead is justified in a tracker this size. This bead is that home; the 'no owner' reading below is the gap that created it, not the current state.\n- The web-ui bead is blocked by four: ba-api-routes-surface-mqr, ba-feature-bounty-xhk, ba-battle-replay-yjb and THIS bead, which was added as a dependency edge once DESIGN.md got an owner. So this is a named plan artifact with a named blocked consumer and a blocker of its own. It is not yet on the critical path only because those three are outstanding; it becomes blocking the moment they land, which is why the edge is worth keeping rather than deleting as redundant.\n\nTHE ONE DECISION THAT IS NOT ALREADY MADE, AND WHY IT BELONGS HERE\nThe plan gives two different answers about what the user sees first.\n- Section 8: \"Main screen (from conversation): top bar (Agent Battle, notifications, coins, level) / CODING CITY map (Guild Hall, Research Lab, Claude character, Arena, Workshop, Quest Board) / bottom tabs (QUESTS/AGENTS/GUILD/ARENA/PROFILE).\" Then, later in the same paragraph: \"Bounty Board as FIRST screen option (not pixel city).\"\n- Section 11.4: \"First screen candidate: Bounty Board (HOT BOUNTIES with amounts/difficulty/repos/competitor counts) -> bounty detail -> [ENTER BATTLE] spins real agent work.\"\nSo section 8 describes the pixel city as the main screen and then proposes the bounty board instead; section 11.4 proposes the bounty board again. The plan hedges with \"option\" and \"candidate\", which is honest about not having decided, and an undecided first screen is a visual decision - which is exactly what this document exists to settle. The 2.3 gimmick trap and risk 8's scope discipline both bear on it: a bounty board first is the M2-with-zero-game-UI shape the plan repeatedly says should land before heavy art, and a pixel city first is the M5 shape. Settle it, state the reasoning, and note which milestone each choice implies.\nEverything else - the medium, the mood, the retro/modern split, the agent-card anatomy, the bounty-board anatomy, the coding-city landmarks - is already in the plan and is transcription.\n\nWHAT THE DOCUMENT MUST CONTAIN\n1. The medium and the mood, stated as binding, with section 14's rationale and a pointer back to it.\n2. The split: what is pixel (world, characters, items, tilesets, VFX) and what is modern (dashboard, bounty board, auth). With the reason - a retro-all-the-way-down site is harder to use and reads as a costume - because that is the sentence a future contributor will otherwise relitigate.\n3. The first-screen decision, with the milestone it implies and the reasoning.\n4. The named surfaces and their anatomy, from section 8: top bar, coding-city landmarks, bottom tabs, agent card (level, current quest+session, skill bars, [Enter Arena] [View History] [Join Guild]), bounty board (hot bounties with amount, difficulty, repo, competitor count -> detail -> [ENTER BATTLE]), replay view, arena.\n5. Asset direction, pointing at ba-asset-shortlist-x2a as the owner of pack selection and per-pack licensing, and at section 14's rule that each pack ships its own LICENSE.txt. It must not duplicate that bead's work or pre-empt its selection.\n6. An explicit non-goals section, because the plan's own failure mode here is scope creep dressed as design: no equipment, pets, mounts, cosmetics or housing. Section 10.2's MVP character sheet ends at History, and section 17.8's risk 8 bans the MMO shape.\n\nSUCCESS CRITERIA, each with the thing that settles it\n1. DESIGN.md exists at the repo root and every visual decision in it is traceable to a plan section. Settled by a check that each decision carries a section reference, and by a reader finding no visual assertion in it that the plan does not support. A design doc that invents a visual language the plan never sanctioned is worse than none, because it acquires authority by being written down.\n2. The medium, the mood and the retro/modern split are stated and match section 14 verbatim in substance. Settled by a check against section 14 - the point is that these were decided on 2026-09-24 and a later contributor must not quietly reopen them.\n3. The first screen is decided, and the decision names the milestone it implies. The test is that a reader can answer \"what does a user see first, and when\" from the document alone, with no reference back to the plan. Today the plan gives two answers, which is why this clause exists.\n4. Every surface ba-web-ui-surface-t3w and ba-game-client-pixijs-riw are building appears in the document. Settled by cross-checking the two beads' surface lists against the document - a screen one of them builds and the document does not mention is a screen built without design direction, which is the thing this document exists to prevent. This is the criterion that makes the document load-bearing rather than decorative: it is checked against the beads that consume it.\n5. The non-goals section is present and names at least the equipment/pets/mounts/cosmetics/housing exclusions from section 10.2.\n\nCONSIDERATIONS\nKeep it short. This is a design authority, not a design essay, and its value comes from being short enough that people read it before they build. The failure mode is a 400-line document nobody consults, which is functionally identical to no document. Second, it must not become a second copy of the plan: the plan records the decision and the reasoning, DESIGN.md records what is binding, and duplication is how the two drift. When they disagree, section 14 governs and DESIGN.md is the one that is stale. Third, DESIGN.md is a design artifact, so a visual review is a legitimate part of accepting it - but the checklist above is what makes it accountable, because \"it looks good\" is exactly the unfalsifiable criterion this tracker has been rewriting out of other beads.","status":"closed","priority":2,"issue_type":"task","created_at":"2026-09-24T22:44:38.480737Z","created_by":"tranquangdang21","updated_at":"2026-09-25T15:27:39.828627Z","closed_at":"2026-09-25T15:27:39.828457Z","close_reason":"DESIGN.md exists at the repo root (1870973, 8f17b21) and is the binding\nartifact §14 names, with all five success criteria met.\n\nTRANSCRIBED, not invented. Every decision carries the plan section it comes\nfrom, because a design doc that invents a visual language the plan never\nsanctioned is worse than none — it acquires authority by being written down. §7\nnames outright what the document does NOT decide (palette, type scale, spacing,\nicons, motion) so the consuming beads own those instead of inheriting them from\na document that never had standing to settle them.\n\nTHE MEDIUM, MOOD AND SPLIT match §14 in substance, and were re-read at source\nrather than trusted: the three claims about pixel being de facto chosen (§19\nPixiJS + tilemap target layout, §25 16x16 sprite-factory, §28.1 tiny-swords-cc0)\nwere each verified by opening the section. The tilemap claim in particular looks\nwrong on inspection — §19 is the monorepo layout — and is right, because §19 does\ncarry the game-client target tree.\n\nTHE FIRST SCREEN IS DECIDED: Bounty Board (M2), Coding City as a second view at\nM5. The plan hedged (§8 \"option\", §11.4 \"candidate\"). Three things point the same\nway and none of them is taste: §17.1's gimmick trap, whose stated defense is\nshipping M2 real utility before heavy art; §17.8's scope discipline, since the\ncity is the M5 shape; and §11.4's own \"even without game UI, M2 alone has\nvalue,\" which only earns its place if the landing screen is the one M2 builds.\nThe tracker already encodes this as ordering edges on both art beads, so the\ngate is an edge rather than a sentence — noted in the doc so the edges are not\nremoved as over-constraining.\n\nBOTTOM TABS are stated as a function of milestone (M2 has no GUILD; M6 adds it).\nThis reconciles §8's flat five-tab list with §10.2 deferring guild and §27\nplacing guilds at M6, rather than silently picking one.\n\nDELIBERATELY NOT DONE, and why:\n- No palette, type, spacing, icon or motion decisions. The plan settled none, and\n  §14 gives the medium and mood without them.\n- Criterion 4 is satisfied for both consuming beads' surface lists as they stand\n  today. It is not a permanent proof; it holds only while those beads' surfaces\n  do not change, and a new zone or scene has to come back through this document.","source_repo":"battle-agents","source_repo_path":"/Users/tranquangdang21/Projects/battle-agents","mol_type":"work","work_type":"mutex","wisp_type":"none","labels":["design","frontend","p2"]}
{"id":"ba-4to","title":"MCP Streamable HTTP transport + /api/mcp route mount","description":"Mount the MCP Streamable HTTP transport and the /api/mcp route. The five primitives already exist and are frozen; this bead is the wire they have never been carried on.\n\nWHAT THE PLAN REQUIRES\n- Section 19 lists `app/api/mcp/route.ts  # MCP Streamable HTTP endpoint` in the target tree.\n- Section 22: \"POST /api/mcp (Streamable HTTP): ONLY the 5 stable primitives (32); act() dispatches typed registry actions to runtime.dispatch. No per-feature tool wrappers - adding a feature never adds an MCP tool. Auth: short-lived installation token from the section 4 flow.\"\n- Section 31's correction is explicit and load-bearing: \"MCP IS P0, not P3\" and P0 contains \"Interfaces: Public API, CLI, MCP\" as three EQUAL consumers, because \"do not design the game first and bolt MCP/CLI on later; design the game assuming the agent can ONLY play through the protocol/tool interface.\"\n- The onboarding proof the plan wants from it (section 4 / the mcp-coding-agents reference): `claude mcp add agent-battle https://agentbattle.dev/mcp`, then browser auth, then a terminal confirmation showing Agent, Account and Project. That round trip is the thing that proves login -> add MCP -> authorize -> play needs no separate CLI install.\n\nWHY IT IS MISSING RATHER THAN DONE, verified against the tree 2026-09-25\n- packages/mcp-server/src/{server,tools,index}.ts exist and are good: createMcpServer is a pure function of an ApplicationApi with the five tools, lazy discovery, typed act() dispatch, and per-subscription observe. All of that stays.\n- server.ts says in its own doc comment: \"The MCP SDK is not installed yet. When it is, this becomes a thin translation - register these five tools, call callTool, send notifications from deliver. Nothing about the surface changes.\" The bead that closed named the next step and then closed. That left the next step unowned - which is the gap this bead was created to fill, so read 'no owner' here as the reason this bead exists rather than as its current state.\n- No @modelcontextprotocol/sdk (or any MCP SDK) is in any package.json in the workspace.\n- There is no apps/web/app/api/mcp/. apps/web/app/api/ contains exactly two directories: auth/ and events/.\n- ba-mcp-adapter-li2 is CLOSED and its close reason is \"Shipped against the frozen contract ... See the commit for the design calls and what was deliberately left out.\" The thing deliberately left out is this.\n- ba-api-routes-surface-mqr used to inventory the route as \"POST /api/mcp (MCP Streamable HTTP - built in the P3 MCP bead)\" - crediting a closed bead, naming a priority the plan has since reversed, and leaving the route with no owner, which is how a route that does not exist reads as one that does. That line is now corrected to name this bead; if it ever reads \"built in the P3 MCP bead\" again, the inventory has been reverted.\n\nWHAT THIS BEAD IS NOT\n- It is not the five primitives. They are done and frozen by ba-contract-public-api-ute and exercised by the closed MCP bead. Re-implementing or reshaping them here fails section 32's whole argument.\n- It is not a per-feature tool surface. Adding guild must change the registry, not the tool list. A route that grows a sixth tool, or a tool per feature, fails review under section 36.\n- It is not a second event bus. The NotificationSink in server.ts is deliberately optional and its comment says the event-ingest and SSE bead owns where subscription events land. Wire notify to THAT stream; do not open a competing one.\n- It is not the agent-identity work. The token is an installation token from the frozen credentials/identity contract; the GitHub human's Better Auth web session is a different credential and must not authenticate this route.\n\nAUTH, precisely, because it is the security surface\n- Bearer installation token in the Authorization header, scoped and expiring, exactly as ba-agent-credentials-issuance-294 issues them. The same refusal the credentials bead already enforces - a token in the query string leaks into logs and browser history, and the plan's own example is the bad one - applies here: refuse ?token= even when the header is also present, and assert the refusal rather than assuming it.\n- Expired, revoked and under-scoped tokens must be rejected with the missing scope named, not a generic 401.\n- A test that the human web session cannot drive this route, and that the agent token cannot drive a human-only route, because those two are the separation section 38 calls a hard review issue.\n\nSUCCESS CRITERIA, each with the thing that settles it\n1. A real MCP client completes initialize -> tools/list -> tools/call against the mounted route. Settled by an integration test speaking the Streamable HTTP protocol to apps/web/app/api/mcp/route.ts, not by calling createMcpServer in-process - an in-process test is exactly what already passes today and proves nothing about the wire.\n2. The tool count over the wire is exactly 5, whatever is installed. Settled by a test asserting the tools/list response length is 5 with the full feature set composed in, so a future per-feature tool fails here.\n3. The full loop works over MCP alone: discover -> search -> inspect -> act (a claim) -> observe (the resulting event), with the same application-command effects the CLI and HTTP surfaces produce. Settled by a parity assertion against the same createApplicationApi the other two consumers use, so MCP cannot drift from them.\n4. The route handler contains no game logic. Settled by the same architecture assertion the web-ui bead uses - each route maps 1:1 onto an application command and translates; it decides nothing. A route that reaches into a feature and branches on a game word fails it.\n5. Notification delivery rides the existing SSE stream rather than a new one. Settled by a test that an event observed through MCP arrives on the stream ba-event-ingest-and-sse-gu9 owns.\n\nTESTS, each failing when the behaviour is wrong\n- a Streamable HTTP integration test against the mounted route (the handshake, tools/list, one tools/call), which is the test that does not exist today;\n- the exactly-5 tools/list assertion with every feature composed in;\n- the loop parity assertion across MCP / CLI / HTTP;\n- auth: no header, expired, revoked, missing scope, and ?token= refused, plus the human-session-cannot / agent-token-cannot separation;\n- an architecture assertion that apps/web/app/api/mcp/route.ts imports no feature internals and registers no domain operation.\n\nCONSIDERATIONS\nThe tempting wrong turn is to treat this as \"mount the SDK and move on\", which produces a route that works for a hand-written client and fails for `claude mcp add`, because that command does the OAuth/browser-auth dance the plan's onboarding UX depends on and a bearer-only route skips. If the transport cannot complete the browser-auth leg, the honest move is to ship the bearer route AND say plainly in this bead's close reason that the `mcp add` leg is still open - not to describe a half-wired endpoint as the onboarding proof. Second: the 250ms/50-event batching rules and the single-sessionId 400 belong to the ingest path and are not this bead's to re-decide; MCP commands are not telemetry.","status":"closed","priority":0,"issue_type":"task","created_at":"2026-09-24T22:43:38.612345Z","created_by":"tranquangdang21","updated_at":"2026-09-25T07:17:33.146425300Z","closed_at":"2026-09-25T07:17:33.141747200Z","close_reason":"MCP Streamable HTTP transport mounted at apps/web/app/api/mcp/route.ts, speaking JSON-RPC 2.0 over the Streamable HTTP envelope. The five frozen primitives are unchanged and are still exactly five over the wire with every feature composed in.\n\nCHOSE: wrote the transport directly rather than adopting @modelcontextprotocol/sdk. The SDK's Streamable HTTP transport is built around a Node http.Server, so using it means either a second server in front of Next.js or a transport that never sees the outer request. packages/mcp-server has types:[] and no node dependency, so handleMcpRequest stayed pure and testable without a socket; a real SDK is a swap behind that one function.\n\nDID NOT: carry an MCP client's observe() result back over the response. That needs a per-connection server-to-client channel, which this bead does not build. The consequence is stated in mcp-gateway.ts rather than hidden: an agent can open a subscription and the subscription is real, but nothing yet pushes the events to that client.\n\nDID NOT: wire MCP's notification sink into the SSE hub. Considered and rejected, with a test. The hub already subscribes to the bus, so routing notifications into it would deliver every event to every spectator TWICE. The public stream and an MCP observer are two independent consumers of one bus. The test counts frames because a test that stops at 'an event arrived' cannot see a duplicate.\n\nALSO FIXED en route, all three found because a gate was already shaped to catch them:\n- api.observe was a permanent no-op that voided both arguments. One of the five frozen primitives was advertised to every agent and delivered nothing, and the only observe assertion was that a subscription opens and closes. It now subscribes to the bus it is given, scoped by domain, and the delivery tests count events.\n- A token in the query string was refused only when the Authorization header was ABSENT. A request carrying both was served, which is the one case that matters because the credential was already in the access log. Refused unconditionally now.\n- writeFileSync's mode applies only on create, so a second login against a file left at 0644 kept 0644 while its contents became a fresh token.\n\nALSO ADDED: architecture rule no-route-handler-game-logic, scoped to the Next.js route tree via a new from.files glob on the rule engine. The composition root in apps/web/src imports features on purpose; a route handler that does is deciding game questions at the transport edge. Watched failing before being believed, per AGENTS.md.\n\nSTILL OPEN, not this bead: the web app builds two runtimes with two independent buses (routes.ts sharedApi and event-gateway.ts createEventGateway), so an event emitted by a game action through the Application API never reaches the SSE stream. See ba-monorepo-scaffold-j5a. That is also why criterion 5 could not be met end to end.\n\nSTILL OPEN, needs a decision, not effort: an MCP client's 'claude mcp add' onboarding leg, because that command does a browser-auth dance a bearer-only route does not perform. Shipped as a bearer route and said so rather than described as the onboarding proof.","source_repo":"battle-agents","source_repo_path":"/Users/tranquangdang21/Projects/battle-agents","compaction_level":0,"original_size":0,"labels":["interface","mcp","p0"],"dependencies":[{"issue_id":"ba-4to","depends_on_id":"ba-contract-public-api-ute","type":"blocks","created_at":"2026-09-24T22:44:59.450656Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"ba-4to","depends_on_id":"ba-event-ingest-and-sse-gu9","type":"blocks","created_at":"2026-09-24T22:44:59.541388Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"ba-4to","depends_on_id":"ba-mcp-adapter-li2","type":"blocks","created_at":"2026-09-24T22:44:59.346698Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"ba-8oh","title":"adapters/pi: Agent Intercom protocol translation layer","description":"Implement the Pi adapter, per plan sections 1.3 and 23.\n\nWHY THIS IS ITS OWN BEAD, because the split is not cosmetic and the difference is architectural, not clerical. The three lower-traffic harnesses were one bead until this was separated, and they are not three instances of one task. Cursor and Gemini are LOG WATCHERS: tail a file the harness writes, normalize, emit - structurally identical to the codex bead (rollout JSONL) and the opencode bead (SQLite), each of which already has its own bead. Pi is neither. It is a PROTOCOL TRANSLATION LAYER: a thin vendor adapter over the shared inter-agent broker described below, with no local log format to tail at all. An agent who has built a log watcher still has to learn a different thing to build Pi, and an agent who has built Pi still has to learn a different thing to build a log watcher. Bundling them meant neither could be started, finished, or reviewed on its own, and it hid Pi's one genuinely novel design constraint (it must not become a second bus) inside a paragraph about two file watchers. The siblings are ba-adapters-other-harnesses-as3 (cursor, gemini); this bead is pi.\n\nPI (section 23, Agent Intercom precedent): reuse the Agent Intercom protocol shape. Section 1.3 describes Agent Intercom as cross-harness same-machine messaging over one Unix-socket broker with a shared protocol and per-vendor adapters for Pi/Codex/Claude/OpenCode. STEAL the protocol-vendor-per-adapter pattern (section 1.3). Concretely, that pattern means: the broker/socket is the shared seam and each vendor is a thin translation layer onto the one normalized AgentEvent union - which is exactly the shape here. The Pi adapter is a vendor layer over the shared protocol, NOT a second bus.\n\nTHE ONE CONSTRAINT THAT IS SPECIFIC TO PI, and the reason it could not simply join the other two: the broker is a WIRE PROTOCOL, not a place to import code from. So while every adapter here is barred from importing another adapter under the dependency rules (they import core + protocol only), Pi has an additional temptation the log watchers do not - reaching into the broker, or worse, growing the broker to accommodate Pi. Both are wrong. The broker is the seam; Pi is one translation layer sitting on it; the normalized AgentEvent union is the only vocabulary that crosses it. If Pi needs a field the union lacks, the fix is a new event type in the union (frozen by ba-agent-event-union-up5), never a vendor-private field smuggled through the broker.\n\nEach adapter must: implement AgentWatcher, normalize through the shared tool-map, emit to the same local buffer feeding POST /api/events, and never import game code (section 23).\n\nCONSIDERATIONS: this is a lower-traffic harness, so the priority is CORRECTNESS of the seam over feature depth. Minimum deliverable is session lifecycle plus tool events - the same floor the sibling watchers are held to, and no less for being a translation layer rather than a file reader.\n\nWHAT YOU ARE BUILDING AGAINST (the concrete contracts, so this bead can be picked up without reading the plan). These contracts were written for all the low-traffic harnesses together and are reproduced here in full so this bead stands alone; the two log-watching siblings (ba-adapters-other-harnesses-as3: cursor, gemini) carry the same text, and pi is subject to every clause of it. The harnesses look different at the edge but are identical underneath, and the underneath is already frozen - do not re-decide it:\n\nTHE WATCHER INTERFACE. Every adapter implements AgentWatcher, the seam ba-adapters-template-a09 exists to specify. The reference shape, from agent-move's packages/server/src/watcher/agent-watcher.ts, is exactly two methods:\n  export interface AgentWatcher { start(): Promise<void>; stop(): void; }\nstop() returns void, not Promise<void>. That open decision (a watcher that must flush on shutdown has nowhere to await in the void shape) is flagged in the template bead; pick the SAME answer as the template, because the contract suite runs against both - and the two sibling watchers must pick that same answer too.\n\nTHE EVENT VOCABULARY IS FIXED AND VERSIONED. No adapter invents event names. They emit members of the zod discriminated AgentEvent union in packages/protocol/src/agent-event.ts, frozen by ba-agent-event-union-up5: all 21 taxonomy events have zod schemas and TS types (session.started / session.heartbeat / session.ended, prompt.submitted, tool.started / tool.completed / tool.failed, file.read / file.write / file.changed, command.run, test.passed / test.failed, thinking, waiting, permission.requested, message.sent / message.received, subagent.spawned / subagent.completed, task.created). PROTOCOL_VERSION is '0.1.0', checked at handshake. The union is the ONLY vocabulary the game core knows - core never learns the words Cursor, Pi or Gemini. Validation rejects malformed events with field-level messages; an unknown type names every known type. Prefer adding a new event type over overloading an existing one with optional fields. Each harness's raw tool names are normalized through the tool-map, never emitted straight.\n\nTHE TOOL-MAP IS A DEPENDENCY, NOT A CHOICE. Raw tool names go through the shared tool-map (ba-tool-map-port-89a): normalizeToolName() (TOOL_NAME_MAP[name] ?? name, and our version IS exported so the contract suite can assert against it), getZoneForTool(), and normalizeToolInput(), which rewrites the camelCase input keys filePath->file_path, oldString->old_string, newString->new_string, replaceAll->replace_all and is called by every parser in the reference. Omitting normalizeToolInput is the most likely way a copied parser turns out subtly wrong. An unmapped tool must degrade to a sensible default zone, never be dropped - an unmapped tool is still real activity.\n\nTHE INGEST CONTRACT, because emitting correctly is not the same as being accepted. Adapters buffer locally and POST batches to /api/events. The buffer is EventBuffer in apps/web/src/event-batch.ts - it is NOT exported from @battle-agents/protocol, and an adapter may not import apps/web (wrong layer, see the dependency rule below). The template bead has to decide whether to lift EventBuffer into a shared package or specify it precisely enough to rebuild; every adapter should share that one decision, not each reinvent a buffer. A conforming buffer honours all three server-enforced rules: flush at 250ms OR 50 events, never exceed 100 events in one batch (the server rejects with 413 plus a Retry-After header), and never put two sessionIds in one batch (400). @battle-agents/protocol exports the zod union, PROTOCOL_VERSION, the generated RegisteredActionId union and isRegisteredActionId - it exports NO http client, so a self-sufficient adapter needs its own POST path.\n\nWHAT THE ARCHITECTURE FORBIDS HERE. Adapters import core + protocol ONLY and NEVER game code - enforced mechanically by the dependency-rules bead (ba-dependency-rules-os1), machine-checked in CI via architecture-rules.cjs, not by reviewer discipline. So: no importing features/*, no importing apps/web, no importing another adapter (the Pi broker is not a place to import code from, it is a wire protocol). The telemetry plane (these adapters) is strictly separate from the control plane (MCP) - MCP is never used for event capture. These adapters observe; they do not command. The adapters also must not import each other: cursor, pi and gemini each stand alone as a subdirectory, because a harness that only builds because another harness was already built is the failure the whole adapter-seam design exists to prevent.\n\nWHY A NAIVE IMPLEMENTATION GOES WRONG. The first shortcut is treating Pi like the other two and inventing a local log format to watch, because that is the shape you already know - but there is no Pi log to tail, and a fabricated one is a second bus wearing a parser's clothes. The second is a shared 'generic' adapter that pi and the sibling watchers all plug into, because their outputs look vaguely similar once normalized: the moment one harness needs an event the others do not, the shared layer grows an if(harness) branch, and the 'new CLI = one subdirectory' promise is gone. The third is emitting Pi's native protocol names directly; the game core would then have to learn a second vocabulary, which the whole normalized-union design exists to prevent. The fourth is importing the broker's implementation rather than speaking to it over the socket, which drags another harness's code into this adapter and fails the same architecture test the other adapters must pass.\n\nSUCCESS CRITERIA: the Pi adapter produces a valid AgentEvent stream and passes the shared adapter contract suite; every emitted event validates against the frozen AgentEvent union and the version handshake passes; tool names reach the bus normalized through the tool-map; batches obey the 250ms/50/100 and single-session rules; the adapter imports neither game code, another adapter, nor the broker's implementation, and adds no vendor-private event field to the union.\n\nTESTS: the shared adapter contract suite run against pi; fixture tests over a captured Pi protocol exchange; a test that every emitted event passes the protocol's zod validation; an architecture test asserting pi imports no game code, no other adapter, and no broker internals; a test that the broker is consumed as a wire protocol (the adapter holds a socket, not a broker object).","status":"closed","priority":1,"issue_type":"task","created_at":"2026-09-24T22:25:27.897195Z","created_by":"tranquangdang21","updated_at":"2026-09-25T23:45:41.198671Z","closed_at":"2026-09-25T23:45:41.198606Z","close_reason":"DONE. The shared adapter contract suite the criterion names did not exist when this\nbead was worked; it exists now (tests/unit/adapter-contract.test.ts), and pi is\ncovered by it — shape, shared tool map, event names in the frozen union.\n\nThe byte-offset defect is the substantive fix and it was silent. readNewSessionLines\nsliced a DECODED STRING by a BYTE offset, which is equivalent only for ASCII, so\nevery poll started progressively further in and destroyed records. Measured on the\nlargest real session on this machine (750 records, non-ASCII in 7 of 19 files):\n310 events with 33 records destroyed, against 322 and zero after. 322 is the\nsingle-session count, which is the only number that should ever have been possible.\nCodex read bytes and decoded last; pi copied the shape and lost that discipline,\nwhich is the copied-parser-goes-subtly-wrong failure the brief names.\n\nReused rather than reimplemented: the POST client lives in @battle-agents/protocol\nbecause codex's ingest.ts named its own trigger for extracting it, and the session\npartition fix is in EventBuffer rather than in this watcher, because four beads\nfound that independently and solving it per-adapter is the drift that moving the\nbuffer into protocol exists to prevent.\n\nDELIBERATELY NOT DONE, and it is worth naming because the criterion reads as if it\nwere: no vendor-private event field was added to the union, and none is needed —\npi's three unmapped tool names went into the shared TOOL_NAME_MAP instead, with\nzone-only entries for tools that are zoned by what they do rather than renamed to\na name they are not.","source_repo":"battle-agents","source_repo_path":"/Users/tranquangdang21/Projects/battle-agents","mol_type":"work","work_type":"mutex","wisp_type":"none","dependencies":[{"issue_id":"ba-8oh","depends_on_id":"ba-adapters-template-a09","type":"blocks","created_at":"2026-09-24T22:25:34.444838Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"ba-8oh","depends_on_id":"ba-feature-agent-f9t","type":"blocks","created_at":"2026-09-24T22:25:34.539957Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"ba-8oh","depends_on_id":"ba-tool-map-port-89a","type":"blocks","created_at":"2026-09-24T22:25:34.623535Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"ba-9hr","title":"SSE stream is an unauthenticated global broadcast","description":"GET /api/events/stream has no authentication and no channel scoping. Every subscriber receives every event the runtime puts on the bus, and toGameEvent makes the whole validated AgentEvent the payload — so an agent's file paths, command names and thinking content reach any open socket. The bead ba-event-ingest-and-sse-gu9 asked for a per-user (web session) or per-battle channel; that was narrowed to a global broadcast with a comment deferring the policy to the game-client bead.\n\nShipped deliberately with that limitation recorded in apps/web/src/event-routes.ts, because deciding what is public is a policy that has not been written down. It is a bug, not a design: a battle scoreboard is public, the argv and file paths of every agent in the system are not.\n\nTHE EXPOSURE, made concrete, because 'some data leaks' undersells it. The stream is unauthenticated: any client that can reach the endpoint can open it - no token, no cookie, no handshake. It is also unscoped: there is one global broadcast, not per-user or per-battle, so there is no boundary to scope a subscriber to even if you wanted to. And the payload is the whole validated AgentEvent (toGameEvent forwards it wholesale), not a curated projection. So the union of those three facts is: anyone who can open a socket receives, in real time, the file paths, command argv, tool inputs, thinking content, message bodies, and permission requests of EVERY agent in the system - not one battle, not one agent, all of them. That is a cross-tenant confidentiality failure and a serious one, because agent activity is proprietary and can contain source code fragments, secrets the agent touched, and proprietary reasoning. A battle scoreboard being public is fine; this is not a scoreboard.\n\nWHAT THE FIX NEEDS, in order (order matters - do not start at step 3):\n1. Write the policy down first: which event types are public spectator data (battle progress, score, agent identity) and which are never public (file paths, command input, thinking, message bodies, permission requests). Without that written, any filter is a guess. This is a decision, not an implementation detail: it needs a human to say what this product considers public. A default-deny filter is the safe starting posture (allow-list the public types, deny the rest), because a block-list silently leaks anything not yet thought of.\n2. Filter the bus by that classification before the hub broadcasts. A public stream carrying only classified-safe types is better than no stream and better than this. Note the filter belongs in the FAN-OUT path, not the persist path: the durable log legitimately stores richer events (that is what replay and achievements read), so filtering at the SSE hub is what makes the public stream safe WITHOUT gutting the internal record.\n3. Authenticate the stream and scope it. A browser EventSource cannot set an Authorization header, and the repo forbids tokens in URLs, so the credential has to be a web session - which is a real piece of work, not a header. Concretely: the SSE stream authenticates as a human/browser via a web session (the Better Auth / GitHub OAuth path, per ba-better-auth-github-oauth-9wm), and is SCOPED to what that session is entitled to see (its own battles, its guild, or a specific public battle), not a single global firehose. This is a different credential from the agent's Bearer installation token (used by adapters on the ingest side) - do not reuse or expose the agent token to the browser.\n4. Test that an unauthenticated request gets no frames, and that a subscriber for one battle cannot see another battle's deltas.\n\nWHY THE EVENT TYPES ARE THE RIGHT FILTER AXIS, which is what makes step 1 tractable. The event vocabulary is a frozen, versioned union (ba-agent-event-union-up5, closed): 21 named taxonomy events with zod schemas, PROTOCOL_VERSION '0.1.0'. That is a closed, enumerable set - so a public/private classification over event TYPES is a finite table someone can actually review, not a per-payload judgment call. A battle.started / battle.finished / score event is public spectator data; a file.write / command.run / thinking / message.sent / permission.requested carries private content and must be denied to a public subscriber. Classifying by type (not by inspecting payload contents) is what makes the filter auditable and stable as new event types are added - a NEW event type must default to private until classified, or the next added type silently becomes a leak.\n\nWHY A NAIVE FIX IS NOT ENOUGH. Adding a single filter without a written classification is a guess that will leak the next event type someone adds. Authenticating without scoping still returns one authenticated user's entire cross-tenant firehose (a logged-in attacker reads everyone's activity). Scoping without a type filter still exposes private content to a legitimate member of a battle. And a block-list ('deny thinking, deny file.write') fails open for every future type, so an allow-list is the durable choice. There is also a real footgun in 'just redact the payload': the whole-validated-AgentEvent payload means partial redaction has to happen in toGameEvent/the hub, and if any broadcast path bypasses that redaction, the leak returns - so the classification must be enforced at the single fan-out choke point, which is the same single-choke-point discipline the pixel-agents broadcaster (agentStateStore as the SOLE broadcaster) is credited with.\n\nDo not close ba-event-ingest-and-sse-gu9 until this lands: the ingest path is correct, but the fan-out it publishes to is currently open. That is a real cross-bead dependency - the ingest bead (ba-event-ingest-and-sse-gu9) is a direct dependent of this bug, so the correct order is: this policy + filter + auth lands, and only then is the ingest/fan-out bead's work considered done.\n\nSUCCESS CRITERIA: an unauthenticated request to the stream receives no frames; a subscriber scoped to one battle cannot see another battle's deltas; the public stream carries only classified-public event types by default-deny; the richer internal record (durable log) is unaffected by the public filter.\n\nTESTS: an unauthenticated-stream test (no frames, not even public ones, unless the endpoint is intentionally public-read); a cross-battle scoping test (a subscriber for battle A receives no deltas from battle B); a type-classification test that every event type is classified and new types default to private; a test that the durable log still stores the richer events the public stream filters out; a test that the agent Bearer token is never used to authenticate the browser SSE stream.","status":"closed","priority":0,"issue_type":"bug","created_at":"2026-09-24T18:03:29.859712Z","created_by":"tranquangdang21","updated_at":"2026-09-25T02:35:23.785883Z","closed_at":"2026-09-25T02:35:23.785797Z","close_reason":"Steps 1 and 2 of the bead are done: the classification was written first in docs/design/public-event-stream.md so the filter is a consequence of a decision, and the spectator view now builds each public event field by field rather than publishing and then dropping a field. The protocol types every payload field as an unbounded string, so a field of unknown content is exactly the one that must not be published, and an event with nothing publishable yields undefined rather than an empty frame.\n\nThe filter is per subscriber, not on the bus. Filtering at the bus made every viewer public and took the game with it: five tests asserting a transient event reaches the game's own activity view began timing out, and the ingest bead is explicit that not persisting must not quietly become dropping. There are two views now, public carries the classification and operator carries everything, and the default is the strict one so a forgotten argument narrows what is shared. Tests describing the operator view say so, and one that was about frame shape rather than content policy was pointed at two events the spectator view carries, because conflating the two is how it would have kept passing while asserting the wrong thing. Removing the filter turns two tests red, one of which checks the default rather than the classifier, because a guard that is written and never called is a shape this repository has hit repeatedly.\n\nStep 3 is NOT done and is not pretended otherwise: the stream is still unauthenticated and unscoped. What changed is that a socket that reaches it now receives a scoreboard rather than the activity log, which contains the audit trail the payout dispute design in docs/design/payout-rail.md rests on. The credential is a web session rather than an Authorization header, because a browser EventSource cannot set one and the repo forbids tokens in URLs. That is the next piece and it is a real piece of work, not a header.","source_repo":"battle-agents","source_repo_path":"/Users/tranquangdang21/Projects/battle-agents","mol_type":"work","work_type":"mutex","wisp_type":"none","labels":["realtime","security"]}
{"id":"ba-activity-event-log-aph","title":"Activity/Event Log feature (P0) + persisted-event filter","description":"Implement the Activity/Event Log as a first-class P0 feature, per plan section 30 and the persistence filter in section 7.2.\n\nWHY IT IS P0 AND NOT A DEBUG TABLE (section 29.4 + section 30): replay, audit, achievements, reputation, anti-abuse, and battle history ALL read from ONE ordered, attributed action trail. Paperclip treats action attribution the same way. Scattering this data across feature tables would mean each of those five consumers re-derives it differently.\n\nTHE TRAIL SHAPE (section 30), per session:\n  Agent #123 / Session #456\n  10:02 claim bounty -> 10:04 modify 3 files -> 10:07 run tests ->\n  10:08 submit PR -> 10:11 PR merged -> 10:11 bounty completed -> +500 XP\n\nIMPLEMENTATION:\n- event_log table (section 21): id (bigserial), type, actorId, payload jsonb, occurredAt\n- features/activity/ extension owning the append/query API and the retention policy\n- every runtime.emit appends KEY events; transient telemetry stays on the bus only\n- PERSISTED_EVENT_TYPES (section 21/22) = ['session.started','session.ended','test.passed','test.failed','bounty.claimed','bounty.completed','battle.started','battle.finished','agent.level_up'] - everything else goes to the SSE bus ONLY.\n\nTHE REASON FOR THE FILTER IS THE CORE BACKEND INSIGHT (section 7.2): coding agents emit torrents. 100 agents x 20 events/s = 2000 DB writes/s. Neon must NEVER see that. Architecture: Agents -> Next.js Event Gateway -> {Realtime layer (WS/SSE) -> Browser} + {batched/important -> Neon}. Realtime is NOT Neon's job.\n\nORDERING: the runtime emits in the order persist -> handlers -> publish. The log must be written before downstream consumers read it, or replay/achievements can race ahead of the durable record.\n\nCONSIDERATIONS: retention policy is part of this feature's scope (section 30 says 'owns append/query API, retention policy'). Replays need old events, so retention cannot be aggressive; decide and document the window. Retention is also the natural pressure valve if event volume grows.\n\nSUCCESS CRITERIA: every key event lands in event_log with actor and ordering; transient events (thinking, waiting, streaming, heartbeat) provably do NOT; replay, achievements, and reputation all read from this one log rather than from scattered feature tables.\n\nTESTS: unit test that the persist filter admits exactly PERSISTED_EVENT_TYPES; integration test that ordering survives concurrent emits; test that querying a session's trail returns the example shape from section 30.","status":"closed","priority":0,"issue_type":"task","created_at":"2026-09-23T18:59:47.702410Z","created_by":"tranquangdang21","updated_at":"2026-09-24T06:36:33.913788300Z","closed_at":"2026-09-24T06:36:33.911725400Z","close_reason":"features/activity owns the read API (trail + recent), the session summary shape, and the retention policy (365 days, because a shared replay is a link). Added the missing bridge: DrizzleStateStore, the only path an event takes to the database. 10 unit tests pin the persist filter (exactly the key events, features may add their own), retention boundaries and summary rendering; 34 integration tests prove key events land and transient ones provably do not, ordering survives, and prune removes only what aged out.","source_repo":"battle-agents","source_repo_path":"/Users/tranquangdang21/Projects/battle-agents","compaction_level":0,"original_size":0,"labels":["feature","p0","platform"],"dependencies":[{"issue_id":"ba-activity-event-log-aph","depends_on_id":"ba-runtime-and-event-bus-63r","type":"blocks","created_at":"2026-09-23T19:04:21.903738Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"ba-adapter-claude-fbc","title":"adapters/claude: hooks primary + JSONL secondary","description":"Implement the Claude Code adapter, per plan sections 6, 23, and 28.2.\n\nPRIMARY PLANE: HOOKS. Claude Code has the richest hook surface of any supported harness (section 6), so the hook plane is primary and session-log reading is secondary. Handler at hooks/hook-handler.ts receives SessionStart, PreToolUse, PostToolUse, PermissionRequest, and Stop, normalizes each via normalizeHookEvent(), and emits an AgentEvent.\n\nSECONDARY PLANE: JSONL. parsers/jsonl.ts tails ~/.claude/projects/**/*.jsonl for fields the hooks do not expose (section 23).\n\nINSTALLER: installer/install-claude.ts writes a hook entry into ~/.claude/settings.json, CONSENT-GATED (pixel-agents pattern), with session-file fallback when hooks are unavailable. Section 6 confirms the fallback shape: AgentMove auto-installs into ~/.claude/settings.json and falls back to session-file watching.\n\nPORT FROM agent-move (section 28.2, learn-only rewrite): watcher/claude parser, the AgentStateManager idle timeout, the Broadcaster, TaskGraphManager, full_state-then-delta with exponential-backoff reconnect, and the OpenCode WAL-polling CAUTION that applies to any file-watching path.\n\nIDLE TIMEOUT: 45 SECONDS, NOT 30. The plan (section 28.2) and an earlier version of this bead both said 'AgentStateManager 30s idle rule'. That is wrong, and docs/research/agent-move.md records the correction with the source line (`idleTimeoutMs: 45_000`). The 30s figure is a client-side cosmetic timer - how long a sprite stands idle before switching to a sleeping pose - and plays no part in the server state machine. Three unrelated 30s values also exist in that repo (a git-info cache TTL, an anomaly-detector sweep interval, a session-recorder flush interval). Do not port 30. Note also that the same research note found agent-move's own `full_state` snapshot was 39.2 MB, which is the concrete argument for the delta-only fan-out we already ship; our EventStreamHub sends a small snapshot and then deltas, so mirror that discipline and not the size.\n\nUX (section 6, from mcp-coding-agents): 'claude mcp add agent-battle <url>' is the registration shape, proving the npx/MCP-registration UX is feasible.\n\nCONSIDERATIONS: hooks give structured, timely events but only while the agent cooperates; the JSONL tail is the safety net for crashes, forced quits, and missed hooks. Both are needed, and the JSONL reader must not double-count events the hooks already delivered (deduplication is required, not optional).\n\nWHAT THE ADAPTER MUST EMIT AGAINST (already built, do not re-invent): POST /api/events is real and strict. A batch over 100 events is refused with 413 and a Retry-After header; a batch must carry events for exactly ONE sessionId; every event is zod-validated and checked against PROTOCOL_VERSION; the caller presents a Bearer installation token that must own the session. The client-side buffer that satisfies those rules is `EventBuffer` in apps/web/src/event-batch.ts, which flushes at 250ms or 50 events and stops short of the 100-event reject limit. A new adapter feeds the same buffer shape rather than inventing a second batching policy.\n\nWHAT THE NORMALIZED EVENT MUST LOOK LIKE: `packages/protocol/src/agent-event.ts` is the zod union every event is validated against, and `packages/protocol/src/version.ts` holds PROTOCOL_VERSION. An adapter that emits a shape the protocol schema does not describe is rejected at the door with a 400, so the fixture tests should assert against that schema and not against a local copy of it.\n\nWHY CLAUDE IS FIRST: it is the reference harness. The normalization logic proven here is what every other adapter reuses.\n\nSUCCESS CRITERIA: a live Claude Code session produces a correct, deduplicated AgentEvent stream covering session lifecycle, tools, files, commands, tests, and permission requests; the installer is consent-gated and reversible.\n\nTESTS: fixture-driven tests over real hook payloads; a dedup test proving hook+JSONL do not double-emit; an installer test asserting consent is required and that uninstall restores the original settings.json exactly.","status":"open","priority":1,"issue_type":"task","created_at":"2026-09-23T19:02:10.372765Z","created_by":"tranquangdang21","updated_at":"2026-09-24T20:46:23.412287Z","source_repo":"battle-agents","source_repo_path":"/Users/tranquangdang21/Projects/battle-agents","mol_type":"work","work_type":"mutex","wisp_type":"none","labels":["adapter","p1"],"dependencies":[{"issue_id":"ba-adapter-claude-fbc","depends_on_id":"ba-adapters-template-a09","type":"blocks","created_at":"2026-09-23T19:04:25.112516Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"ba-adapter-claude-fbc","depends_on_id":"ba-feature-agent-f9t","type":"blocks","created_at":"2026-09-23T19:04:25.440262Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"ba-adapter-codex-nhp","title":"adapters/codex: rollout JSONL watcher (no hooks exist)","description":"Implement the Codex adapter, per plan sections 6 and 23.\n\nNO HOOKS EXIST for Codex (section 6) - this adapter is pure session-log watching, which makes it the second reference implementation and the proof that the architecture does not secretly depend on hook support.\n\nMECHANISM (section 23): parsers/rollout.ts watches ~/.codex/sessions/**/rollout-*.jsonl, polling every 2-3 seconds (the agent-quest precedent, section 28.2).\n\nPORT FROM agent-quest (section 28.2, learn-only rewrite): the providers/ split handling multi-~/.claude* directories plus Codex rollout parsing, and the SessionRegistry / EventBridge concepts. Note agent-quest is MIT but Bun-locked - port the PARSING LOGIC, do not adopt the Bun runtime (section 1.2 item 2 AVOID: 'Bun/Phaser lock-in').\n\nREGISTRATION UX (section 6): 'codex mcp add agent-battle <url>'.\n\nCONSIDERATIONS: polling a file that is being appended to requires care - read incrementally from a tracked offset, tolerate partial/torn lines at the tail, and handle the file appearing or rotating. A naive read-whole-file poll will double-emit or drop events.\n\nSIGNIFICANCE FOR THE ARCHITECTURE: section 22's M1 DoD requires 'two different harnesses visible simultaneously'. This adapter is half of that proof. If a hook-based adapter and a log-watching adapter both work, the telemetry plane is genuinely harness-agnostic.\n\nWHAT YOU ARE BUILDING AGAINST (the concrete contracts, so this bead can be picked up without reading the plan):\n\nTHE WATCHER INTERFACE. Every adapter implements AgentWatcher, the seam this bead's sibling ba-adapters-template-a09 exists to specify. The reference shape, from agent-move's packages/server/src/watcher/agent-watcher.ts, is exactly two methods:\n  export interface AgentWatcher { start(): Promise<void>; stop(): void; }\nstop() returns void, not Promise<void>. Whether ours should keep void or upgrade to a promise is a real open decision (a watcher that must flush a buffer on shutdown has nowhere to await in the void shape) - the template bead flags it, and whichever way you pick here must match the template, because the contract suite runs against both.\n\nTHE EVENT VOCABULARY IS FIXED AND VERSIONED. This adapter does not invent event names. It emits members of the zod discriminated AgentEvent union in packages/protocol/src/agent-event.ts, which was frozen by ba-agent-event-union-up5: all 21 taxonomy events have zod schemas and TS types (session.started / session.heartbeat / session.ended, prompt.submitted, tool.started / tool.completed / tool.failed, file.read / file.write / file.changed, command.run, test.passed / test.failed, thinking, waiting, permission.requested, message.sent / message.received, subagent.spawned / subagent.completed, task.created). PROTOCOL_VERSION is '0.1.0', exported and pinned; the server checks it at handshake and warns on mismatch. The union is the ONLY vocabulary the game core knows - core never learns the word Codex. Validation rejects malformed events with field-level messages, and an unknown type names every known type. Prefer adding a new event type over overloading an existing one with optional fields; overloads make consumer switches ambiguous. Emit Codex's raw tool names through the tool-map, never straight into an event.\n\nTHE TOOL-MAP IS A DEPENDENCY, NOT A CHOICE. Raw rollout tool names are normalized through the shared tool-map (ba-tool-map-port-89a): normalizeToolName() (TOOL_NAME_MAP[name] ?? name, and our version IS exported so the contract suite can assert against it), getZoneForTool(), and normalizeToolInput(), which rewrites the camelCase input keys filePath->file_path, oldString->old_string, newString->new_string, replaceAll->replace_all and is called by every parser in the reference. Omitting normalizeToolInput is the most likely way a copied parser turns out to be subtly wrong - a downstream consumer then cannot read one input shape regardless of harness. An unmapped tool must degrade to a sensible default zone, never be dropped: an unmapped tool is still real activity.\n\nTHE INGEST CONTRACT, because emitting correctly is not the same as being accepted. Adapters buffer locally and POST batches to /api/events. The buffer is EventBuffer in apps/web/src/event-batch.ts - it is NOT exported from @battle-agents/protocol, and this adapter may not import apps/web (wrong layer, see the dependency rule below). The template bead has to decide whether to lift EventBuffer into a shared package or specify it precisely enough to rebuild; do the same reasoning here, and a conforming buffer honours all three server-enforced rules: flush at 250ms OR 50 events, never exceed 100 events in one batch (the server rejects with 413 plus a Retry-After header), and never put two sessionIds in one batch (400). @battle-agents/protocol exports the zod union, PROTOCOL_VERSION, the generated RegisteredActionId union and isRegisteredActionId - it exports NO http client, so a self-sufficient adapter needs its own POST path as part of this work.\n\nWHAT THE ARCHITECTURE FORBIDS HERE. Adapters import core + protocol ONLY and NEVER game code - enforced mechanically by the dependency-rules bead (ba-dependency-rules-os1), which is machine-checked in CI via architecture-rules.cjs, not by reviewer discipline. So: no importing features/*, no importing apps/web, no importing another adapter. The telemetry plane (hooks/session-watcher) is also strictly separate from the control plane (MCP) - MCP is never used for event capture, because MCP-per-Read/Edit/Bash bloats context, costs tokens, drags the workflow, and couples us to per-agent MCP quirks. This adapter is telemetry: it observes, it does not command.\n\nWHY A NAIVE IMPLEMENTATION GOES WRONG. Read-whole-file polling on a file that is being appended to will either double-emit (re-reading bytes already consumed) or drop events (a torn final line is not valid JSON yet). That is why the tracked-offset read is the substance of this bead, not a detail: maintain a per-file byte offset, only parse complete lines (buffer the trailing partial line until the next poll completes it), and handle the file appearing, being rotated, or a new session directory starting. A parser that maps Codex events straight to event names instead of through the tool-map produces a stream the game client cannot render into zones.\n\nSUCCESS CRITERIA: a live Codex session produces a correct, non-duplicated AgentEvent stream from rollout JSONL alone, running concurrently with a Claude session in the same UI; every emitted event validates against the frozen AgentEvent union and the version handshake passes; tool names reach the bus normalized through the tool-map; batches obey the 250ms/50/100 and single-session rules.\n\nTESTS: fixture tests over real rollout JSONL including partial-line and rotation cases; a test that incremental reads do not double-emit; the M1 DoD concurrency test (Claude and Codex both visible at once); a test that every emitted event passes the protocol's zod validation; a contract-suite run against the shared adapter contract suite.","status":"closed","priority":1,"issue_type":"task","created_at":"2026-09-23T19:02:10.460453Z","created_by":"tranquangdang21","updated_at":"2026-09-26T02:20:55.764653Z","closed_at":"2026-09-26T02:20:55.764448Z","close_reason":"DONE at adapter level. The shared adapter contract suite the criteria name now\nexists and codex is covered by it (it did not exist when this bead was worked — the\nonly trace was a false comment in tool-map.ts).\n\nTWO DEFECTS FIXED, BOTH INVISIBLE TO THE EXISTING 34 TESTS.\n\nThe byte/UTF-16 mismatch: reading incremental means reading BYTES from a tracked\noffset, looping on short reads, advancing the cursor over what was actually\nreceived, and decoding LAST. The recon described a reader that sliced a decoded\nstring at a byte offset, which is equivalent only for ASCII and silently destroys\nrecords on any non-ASCII line. It was already correct when wave work reached it, so\nrather than take that on faith it was re-verified by re-introducing the defect and\nwatching exactly the two tests that claim to cover it go red — a torn fragment with\n`Unexpected non-whitespace character after JSON`, and a three-byte cursor drift.\nThe other 22 stayed green, so the failure is specific to multi-byte input.\n\nThe 413 backoff: the brief asks for honouring Retry-After on an over-size batch.\nWaiting alone cannot fix a size refusal — the identical batch is still too big a\nsecond later, so a client that only honours the header retries the same 413\nforever and the events never land. A 413 is answered by the advertised pause AND a\nhalving, which strictly reduces size and so terminates. That is the difference\nbetween a retry policy and a livelock.\n\nAnd a related one the tests found: the poll interval did `void this.tick()`, so a\nrejected request became an unhandled rejection, which in Node ends the process. An\nadapter with an expired token would have taken the process down and looked exactly\nlike an idle agent. Failures are counted rather than propagated, and the comment\nsays this is the only place the adapter swallows an error.\n\nThe single-session rule is enforced centrally in EventBuffer rather than here,\nbecause four adapter beads found that gap independently and solving it per-adapter\nis the two-copies-drift failure that moving the buffer into protocol exists to\nprevent.\n\nTHE ONE CRITERION NOT MET, and it is not this bead's to meet: \"running\nconcurrently with a Claude session in the same UI\" is the M1 definition of done,\nand it belongs to ba-m1-two-harness-integration-3us. It is blocked on ba-118 —\nthere is no way to create a session over HTTP, so every adapter batch answers 404\nagainst a live server. That is the same blocker for claude, and closing this bead\ndoes not pretend otherwise.","source_repo":"battle-agents","source_repo_path":"/Users/tranquangdang21/Projects/battle-agents","mol_type":"work","work_type":"mutex","wisp_type":"none","labels":["adapter","p1"],"dependencies":[{"issue_id":"ba-adapter-codex-nhp","depends_on_id":"ba-adapters-template-a09","type":"blocks","created_at":"2026-09-23T19:04:25.192016Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"ba-adapter-codex-nhp","depends_on_id":"ba-feature-agent-f9t","type":"blocks","created_at":"2026-09-23T19:04:25.521690Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"ba-adapter-codex-nhp","depends_on_id":"ba-tool-map-port-89a","type":"blocks","created_at":"2026-09-24T21:32:19.527195Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"ba-adapter-opencode-cyt","title":"adapters/opencode: SQLite session store, careful polling","description":"Implement the OpenCode adapter, per plan sections 1.2 item 4, 6, and 23.\n\nMECHANISM (section 23): parsers/sqlite.ts reads the local SQLite store. Section 6 says 'poll carefully, prefer events'.\n\nTHE CAUTION IS EXPLICIT AND REPEATED: section 1.2 item 4 AVOIDS 'high-freq SQLite WAL polling', and section 28.2 repeats 'OpenCode WAL polling CAUTION' as a lesson to carry into our own watcher. Polling a SQLite database in WAL mode at high frequency is a known way to cause lock contention and wasted IO.\n\nIMPLEMENTATION DIRECTION: use BACKOFF polling rather than a hot loop, and prefer event-driven notification where the harness provides it. If we must poll, make the interval adaptive and idle-aware.\n\nPORT FROM agent-move (section 28.2): the watcher/opencode parser approach, while carrying its own OpenCode WAL caution forward rather than inheriting the pattern.\n\nCONSIDERATIONS: SQLite readers must open read-only and must never hold a long-lived write lock on a database the user's editor also owns. A crash of our watcher must not corrupt the user's session data.\n\nWHY IT MATTERS: OpenCode is one of the harnesses that proves the 'cover all coding agents' claim (section 2.2: not a Claude Code plugin). Supporting a non-hook, non-JSONL, database-backed harness broadens the proof considerably.\n\nWHAT YOU ARE BUILDING AGAINST (the concrete contracts, so this bead can be picked up without reading the plan):\n\nTHE WATCHER INTERFACE. Every adapter implements AgentWatcher, the seam ba-adapters-template-a09 exists to specify. The reference shape, from agent-move's packages/server/src/watcher/agent-watcher.ts, is exactly two methods:\n  export interface AgentWatcher { start(): Promise<void>; stop(): void; }\nstop() returns void, not Promise<void>. That open decision (a watcher that must flush on shutdown has nowhere to await in the void shape) is flagged in the template bead; whatever you pick here must match it, because the contract suite runs against both.\n\nTHE EVENT VOCABULARY IS FIXED AND VERSIONED. This adapter does not invent event names. It emits members of the zod discriminated AgentEvent union in packages/protocol/src/agent-event.ts, frozen by ba-agent-event-union-up5: all 21 taxonomy events have zod schemas and TS types (session.started / session.heartbeat / session.ended, prompt.submitted, tool.started / tool.completed / tool.failed, file.read / file.write / file.changed, command.run, test.passed / test.failed, thinking, waiting, permission.requested, message.sent / message.received, subagent.spawned / subagent.completed, task.created). PROTOCOL_VERSION is '0.1.0', checked at handshake. The union is the ONLY vocabulary the game core knows - core never learns the word OpenCode. Validation rejects malformed events with field-level messages; an unknown type names every known type. Prefer adding a new event type over overloading an existing one with optional fields. Emit OpenCode's raw tool names through the tool-map, never straight into an event.\n\nTHE TOOL-MAP IS A DEPENDENCY, NOT A CHOICE. Raw tool names are normalized through the shared tool-map (ba-tool-map-port-89a): normalizeToolName() (TOOL_NAME_MAP[name] ?? name, and our version IS exported so the contract suite can assert against it), getZoneForTool(), and normalizeToolInput(), which rewrites the camelCase input keys filePath->file_path, oldString->old_string, newString->new_string, replaceAll->replace_all and is called by every parser in the reference. Omitting normalizeToolInput is the most likely way a copied parser turns out subtly wrong. An unmapped tool must degrade to a sensible default zone, never be dropped - an unmapped tool is still real activity.\n\nTHE INGEST CONTRACT, because emitting correctly is not the same as being accepted. Adapters buffer locally and POST batches to /api/events. The buffer is EventBuffer in apps/web/src/event-batch.ts - it is NOT exported from @battle-agents/protocol, and this adapter may not import apps/web (wrong layer, see the dependency rule below). The template bead has to decide whether to lift EventBuffer into a shared package or specify it precisely enough to rebuild; reason the same way here. A conforming buffer honours all three server-enforced rules: flush at 250ms OR 50 events, never exceed 100 events in one batch (the server rejects with 413 plus a Retry-After header), and never put two sessionIds in one batch (400). @battle-agents/protocol exports the zod union, PROTOCOL_VERSION, the generated RegisteredActionId union and isRegisteredActionId - it exports NO http client, so a self-sufficient adapter needs its own POST path as part of this work.\n\nTHE READ-ONLY RULE IS A CORRECTNESS RULE, NOT A POLITENESS ONE. Opening the user's session database read-write - even momentarily, even just to run a PRAGMA - risks corrupting a database the user's editor also owns. Open read-only (SQLite URI mode=ro, or the driver equivalent), never hold a long-lived write lock, and treat a crash of the watcher as something that must leave the user's data intact. This is the concrete form of the plan's WAL caution: a hot poll loop on a WAL database is how you cause the contention the caution warns about.\n\nWHAT THE ARCHITECTURE FORBIDS HERE. Adapters import core + protocol ONLY and NEVER game code - enforced mechanically by the dependency-rules bead (ba-dependency-rules-os1), machine-checked in CI via architecture-rules.cjs, not by reviewer discipline. So: no importing features/*, no importing apps/web, no importing another adapter. The telemetry plane (this adapter) is strictly separate from the control plane (MCP) - MCP is never used for event capture. This adapter observes; it does not command.\n\nWHY A NAIVE IMPLEMENTATION GOES WRONG. A hot poll loop is the failure the plan names twice: it burns IO and contends on the user's database for no benefit, because an idle session produces no new rows. A fixed-interval poll is better but still wrong at the edges - it must be adaptive and idle-aware, backing off when the database is quiet and tightening when a session is active. A parser that maps OpenCode columns straight to event names instead of through the tool-map produces a stream the game client cannot render into zones. And a read-write connection is a data-safety bug, not a style choice.\n\nSUCCESS CRITERIA: a live OpenCode session produces a correct AgentEvent stream without contending on the user's database, with adaptive backoff polling rather than a hot loop; the connection is read-only; every emitted event validates against the frozen AgentEvent union and the version handshake passes; tool names reach the bus normalized through the tool-map; batches obey the 250ms/50/100 and single-session rules.\n\nTESTS: fixture tests against a copy of an OpenCode SQLite database; a test that the reader opens read-only; a test that polling backs off when idle; a concurrency test that our reader does not block the harness; a test that every emitted event passes the protocol's zod validation; a contract-suite run against the shared adapter contract suite.","status":"closed","priority":1,"issue_type":"task","created_at":"2026-09-23T19:02:10.536937Z","created_by":"tranquangdang21","updated_at":"2026-09-26T02:21:07.346871Z","closed_at":"2026-09-26T02:21:07.346633Z","close_reason":"DONE. The shared adapter contract suite the criteria name now exists and opencode\nis covered by it.\n\nTHE PORT WOULD HAVE WATCHED AN EMPTY DATABASE FOREVER, which is the finding worth\nkeeping. The recon reported that the vendored source includes the real OpenCode\nSQL schema and that \"no guessing about table or column names is needed\" — which was\nread from the PORT rather than from the product. Opening\n~/.local/share/opencode/opencode.db read-only and dumping the schema: OpenCode\n1.18.21 writes session_v2 / session_message, and the newest v1 session/message/part\nrow on that machine is 19 days old against 5 days for v2.\n\nA verbatim port of the reference SQL is a watcher that connects, queries, and\nreturns nothing on every current install. It would have shipped as working, and\nthe first symptom would have been somebody noticing months later that no events\never arrive — indistinguishable from an idle agent. The store now probes for\nsession_v2 and reads either generation, with two readers feeding one normalisation\nlayer so the mapping is stated once rather than twice.\n\nON THE DEPENDENCY: better-sqlite3 is what the port used and it is not needed.\nnode:sqlite ships in Node 22+, which this repository already requires. The tradeoff\nis that it is an experimental API, so it is recorded in the code next to the import\nrather than left implicit — and pnpm-lock.yaml gained no external entry, which also\nkept this bead out of the dependency mutex the wave was running under.\n\nThe two rules the bead calls correctness, and which are now tests rather than prose:\nthe reader opens READ-ONLY, never taking a long-lived write lock on a database the\nuser's editor also owns, and a crash of this watcher must leave that data intact;\nand the poll backs off adaptively and idle-aware. A hot loop on a WAL database is\nthe failure the plan names twice — it burns IO and contends, and an idle session\nproduces no new rows, so a fixed interval is wrong at the edges too.\n\nSame caveat as every other adapter: the end-to-end \"a live session is visible in\nthe UI\" is M1's definition of done and belongs to\nba-m1-two-harness-integration-3us, which is blocked on ba-118 — no session can be\ncreated over HTTP, so every batch answers 404.","source_repo":"battle-agents","source_repo_path":"/Users/tranquangdang21/Projects/battle-agents","mol_type":"work","work_type":"mutex","wisp_type":"none","labels":["adapter","p1"],"dependencies":[{"issue_id":"ba-adapter-opencode-cyt","depends_on_id":"ba-adapters-template-a09","type":"blocks","created_at":"2026-09-23T19:04:25.280827Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"ba-adapter-opencode-cyt","depends_on_id":"ba-feature-agent-f9t","type":"blocks","created_at":"2026-09-23T19:04:25.598613Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"ba-adapter-opencode-cyt","depends_on_id":"ba-tool-map-port-89a","type":"blocks","created_at":"2026-09-24T21:32:19.610257Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"ba-adapters-other-harnesses-as3","title":"adapters/{cursor,gemini}: follow established log-watcher precedents","description":"Implement the remaining first-party LOG-WATCHING adapters, per plan sections 6 and 23. This bead is now CURSOR and GEMINI only.\n\nPI MOVED TO ITS OWN BEAD (ba-8oh), and the reason is architectural rather than clerical. Cursor and Gemini are log watchers - tail a file the harness writes, normalize, emit - which is structurally the same task as the codex bead (rollout JSONL) and the opencode bead (SQLite), each of which already has its own bead and its own subtle hazard (incremental reads that do not double-emit; read-only polling that does not contend on the user's database). Pi is neither: it is a thin vendor translation layer over the Agent Intercom Unix-socket broker, with no local log format to tail at all, and it carries one constraint the watchers do not have (it must speak to the broker as a wire protocol, not import it or grow it into a second bus). An agent who has built a watcher still has to learn a different thing to build Pi, and the reverse holds too, so bundling them meant neither could be started, reviewed, or landed on its own - and it buried Pi's one novel constraint in a paragraph about two file watchers. Build Pi from ba-8oh. Everything below about the frozen contracts, the tool-map, the buffer rules and the ingest contract still applies to it verbatim, which is the point of stating them here in full.\n\nCURSOR (section 23, Agent Quest precedent): Agent Quest already ships a Cursor adapter (section 1.2 item 2), so follow that precedent. Section 6 adds that Agent Quest covers Cursor and Copilot.\n\nGEMINI CLI (section 6): JSON logs under ~/.gemini.\n\nSECTION 6 GENERAL RULE for the rest: Amp, Grok, and Koda all follow the same AgentWatcher recipe - ONE SUBDIRECTORY EACH (the pixel-agents/agent-move proof). They are intentionally NOT built here; the _template exists precisely so they can be contributed without core changes. Do not gold-plate by pre-building harnesses the template already covers.\n\nEach adapter must: implement AgentWatcher, normalize through the shared tool-map, emit to the same local buffer feeding POST /api/events, and never import game code (section 23).\n\nCONSIDERATIONS: these are lower-traffic harnesses, so the priority is CORRECTNESS of the seam over feature depth. Each must at minimum deliver session lifecycle plus tool events.\n\nWHAT YOU ARE BUILDING AGAINST (the concrete contracts, so this bead can be picked up without reading the plan). Cursor and Gemini look different at the edge but are identical underneath, and the underneath is already frozen - do not re-decide it per harness:\n\nTHE WATCHER INTERFACE. Every adapter implements AgentWatcher, the seam ba-adapters-template-a09 exists to specify. The reference shape, from agent-move's packages/server/src/watcher/agent-watcher.ts, is exactly two methods:\n  export interface AgentWatcher { start(): Promise<void>; stop(): void; }\nstop() returns void, not Promise<void>. That open decision (a watcher that must flush on shutdown has nowhere to await in the void shape) is flagged in the template bead; both adapters here must pick the SAME answer as the template, because the contract suite runs against both. The same answer is required of Pi in ba-8oh.\n\nTHE EVENT VOCABULARY IS FIXED AND VERSIONED. No adapter invents event names. They emit members of the zod discriminated AgentEvent union in packages/protocol/src/agent-event.ts, frozen by ba-agent-event-union-up5: all 21 taxonomy events have zod schemas and TS types (session.started / session.heartbeat / session.ended, prompt.submitted, tool.started / tool.completed / tool.failed, file.read / file.write / file.changed, command.run, test.passed / test.failed, thinking, waiting, permission.requested, message.sent / message.received, subagent.spawned / subagent.completed, task.created). PROTOCOL_VERSION is '0.1.0', checked at handshake. The union is the ONLY vocabulary the game core knows - core never learns the words Cursor, Pi or Gemini. Validation rejects malformed events with field-level messages; an unknown type names every known type. Prefer adding a new event type over overloading an existing one with optional fields. Each harness's raw tool names are normalized through the tool-map, never emitted straight.\n\nTHE TOOL-MAP IS A DEPENDENCY, NOT A CHOICE. Raw tool names go through the shared tool-map (ba-tool-map-port-89a): normalizeToolName() (TOOL_NAME_MAP[name] ?? name, and our version IS exported so the contract suite can assert against it), getZoneForTool(), and normalizeToolInput(), which rewrites the camelCase input keys filePath->file_path, oldString->old_string, newString->new_string, replaceAll->replace_all and is called by every parser in the reference. Omitting normalizeToolInput is the most likely way a copied parser turns out subtly wrong. An unmapped tool must degrade to a sensible default zone, never be dropped - an unmapped tool is still real activity.\n\nTHE INGEST CONTRACT, because emitting correctly is not the same as being accepted. Adapters buffer locally and POST batches to /api/events. The buffer is EventBuffer in apps/web/src/event-batch.ts - it is NOT exported from @battle-agents/protocol, and an adapter may not import apps/web (wrong layer, see the dependency rule below). The template bead has to decide whether to lift EventBuffer into a shared package or specify it precisely enough to rebuild; the adapters here should share that one decision, not each reinvent a buffer - and Pi (ba-8oh) is subject to the same one. A conforming buffer honours all three server-enforced rules: flush at 250ms OR 50 events, never exceed 100 events in one batch (the server rejects with 413 plus a Retry-After header), and never put two sessionIds in one batch (400). @battle-agents/protocol exports the zod union, PROTOCOL_VERSION, the generated RegisteredActionId union and isRegisteredActionId - it exports NO http client, so a self-sufficient adapter needs its own POST path.\n\nWHAT THE ARCHITECTURE FORBIDS HERE. Adapters import core + protocol ONLY and NEVER game code - enforced mechanically by the dependency-rules bead (ba-dependency-rules-os1), machine-checked in CI via architecture-rules.cjs, not by reviewer discipline. So: no importing features/*, no importing apps/web, no importing another adapter (the Pi broker is not a place to import code from, it is a wire protocol). The telemetry plane (these adapters) is strictly separate from the control plane (MCP) - MCP is never used for event capture. These adapters observe; they do not command. The three of them also must not import each other: cursor, pi and gemini each stand alone as a subdirectory, because a harness that only builds because another harness was already built is the failure the whole adapter-seam design exists to prevent.\n\nWHY A NAIVE IMPLEMENTATION GOES WRONG. The tempting shortcut is to write a shared 'generic' adapter that Cursor and Gemini both plug into, because their logs look vaguely similar. That is the God-Engine antipattern in miniature: the moment one harness needs an event the other does not, the shared layer grows an if(harness) branch, and the 'new CLI = one subdirectory' promise is gone. Each harness is a self-contained subdirectory that depends only on the frozen seam (AgentWatcher + AgentEvent union + tool-map + buffer rules). The second shortcut is emitting each harness's native event names directly; the game core would then have to learn two vocabularies, which the whole normalized-union design exists to prevent. The third is a read-whole-file poll: it double-emits or drops on a torn tail, exactly as in the codex adapter, and both harnesses here are log watchers so both inherit that hazard.\n\nTWO WATCHERS IS STILL ONE BEAD, and the reason is worth recording so nobody splits it later: cursor and gemini are the same task twice, over two file formats, and the work that makes one correct is the work that makes the other correct - incremental reads, tool-map normalization, buffer discipline, contract-suite conformance. There is no cursor-shaped insight that does not transfer to gemini, so splitting them would buy two agents re-deriving one lesson rather than two independent results. The set is still worth having as a unit because the pair is what makes the 'adding a harness is routine' claim observable: two more harnesses landing with zero core edits is evidence, and one would be an anecdote. Pi was the one that did not belong, and it now has its own bead.\n\nSUCCESS CRITERIA: cursor and gemini each produce a valid AgentEvent stream and pass the shared adapter contract suite; the pair collectively demonstrates that adding a harness is routine; every emitted event validates against the frozen AgentEvent union and the version handshake passes; tool names reach the bus normalized through the tool-map; batches obey the 250ms/50/100 and single-session rules; no adapter imports another adapter, the broker, or any game code.\n\nTESTS: the shared adapter contract suite run against each; fixture tests per harness including a torn-tail case; a coverage check that each emits session lifecycle and tool events at minimum; a test that every emitted event passes the protocol's zod validation; an architecture test asserting no adapter imports another adapter or apps/web.","status":"closed","priority":1,"issue_type":"task","created_at":"2026-09-23T19:02:10.609155Z","created_by":"tranquangdang21","updated_at":"2026-09-25T23:45:52.940693Z","closed_at":"2026-09-25T23:45:52.940643Z","close_reason":"DONE. Both adapters produce a validated AgentEvent stream, and the shared adapter\ncontract suite this criterion names now exists and covers them (it did not exist\nwhen this bead was worked — the only trace was a false comment in tool-map.ts\nclaiming it \"asserts against the map directly\").\n\nTHE PRECEDENT THE BEAD NAMES DOES NOT EXIST. .tmp/agent-quest at the pinned commit\ncontains only claude-provider.ts and codex-provider.ts, so there was nothing to\nfollow. Rather than invent a format, both were read off the disk:\n\n  - Cursor: a complete survey of all 5,225 records across all 159 agent-transcripts\n    on this machine — every one of exactly three shapes, 4,013 of them tool_use.\n    Three consequences are facts about the file, not choices: NO RECORD CARRIES A\n    TIMESTAMP (so the reader supplies the file mtime, where codex's alternative\n    would make this adapter emit nothing and look idle), no record carries the\n    session id (it comes from the path), and NO TOOL RESULT IS EVER WRITTEN — so\n    there is no tool.completed, and emitting one would assert an outcome the file\n    does not contain. That is asserted as a negative test so the gap stays visible.\n  - Gemini: TWO live formats, not the one the recon found. 42 append-only .jsonl\n    files with $set patches, and — three weeks later — a .json that is a\n    whole-document rewrite with messages[] and 1,266 tool calls, the only\n    tool-bearing sample of either harness on this machine. A byte cursor into a\n    rewritten document is not a cursor, so the message count is used instead.\n\nThe POST client is EXTRACTED to @battle-agents/protocol, not duplicated: codex's\ningest.ts named the trigger for extracting it and this was the second adapter. An\nadapter may not import another adapter, so sharing by importing codex was never\navailable — protocol was. Codex keeps a re-export shim and its 34 tests untouched.\n\nON THE CONTRACT SUITE'S SCOPE, deliberately narrowed and this is the judgement: it\ndoes NOT assert packages/adapters/_template's REQUIRED_EVENT_TYPES, because the\nshipped adapters do not meet it and asserting it would be red on day one. A\npermanently-red suite gets switched off, which is worse than a narrow one. It\nasserts the three properties true of every adapter by construction — watcher\nshape, a shared tool map, and event names inside the frozen union — and the\nper-harness event lists belong beside each adapter's own fixtures.\n\nTool names are normalised through the shared map, from evidence rather than\nguessed: Cursor's from the observed transcripts, Gemini's read out of the installed\n@gemini-cli 0.46.0 bundle. The architecture guard is proven to fire, not assumed —\nthe fixture for it was written by dropping 'adapter' from no-adapter-game-code and\nwatching it go red.","source_repo":"battle-agents","source_repo_path":"/Users/tranquangdang21/Projects/battle-agents","mol_type":"work","work_type":"mutex","wisp_type":"none","labels":["adapter","p1"],"dependencies":[{"issue_id":"ba-adapters-other-harnesses-as3","depends_on_id":"ba-adapters-template-a09","type":"blocks","created_at":"2026-09-23T19:04:25.360700Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"ba-adapters-other-harnesses-as3","depends_on_id":"ba-feature-agent-f9t","type":"blocks","created_at":"2026-09-23T19:04:25.677482Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"ba-adapters-other-harnesses-as3","depends_on_id":"ba-tool-map-port-89a","type":"blocks","created_at":"2026-09-24T21:32:19.692205Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"ba-adapters-template-a09","title":"adapters/_template: the 'new CLI = one subdirectory' promise","description":"Build the adapter template that makes the plan's central onboarding promise true: adding a new coding agent is one subdirectory, not a core change. Per plan sections 1.1, 6, 23, and 27 (M7 DoD).\n\nTHE PROMISE (section 1.1, credited to pixel-agents): an explicit agent-agnostic roadmap where 'new CLI = one subdirectory'. Section 27 M7 DoD makes it a testable claim: 'third-party adapter PR merged using only _template + protocol docs; DoD: contributor adds Amp support without touching core.'\n\nFILES (section 23): watcher.ts stub, parser.ts stub, install.ts stub, and a README checklist. Plus index.ts implementing the AgentWatcher interface.\n\nCURRENT STATE, 2026-09-25: packages/adapters/_template/ holds a package.json, a tsconfig and src/index.ts containing exactly `export {};` (11 bytes). Every sibling adapter - claude, codex, cursor, gemini, opencode, pi - is in the same state. The directory structure the plan describes exists; none of the content does. Treat the sibling directories as reserved names, not as partial implementations.\n\nTHE INTERFACE every adapter implements (section 23, from agent-move). The real one, copied from .tmp/agent-move/packages/server/src/watcher/agent-watcher.ts:\n  export interface AgentWatcher { start(): Promise<void>; stop(): void; }\n\nNOTE stop() RETURNS void, NOT Promise<void>. An earlier version of this bead wrote `stop(): Promise<void>`, which does not match the source it claims to copy. Whether ours should keep void or upgrade to a promise is a real decision, not a transcription detail: a watcher that needs to flush a buffer on shutdown has to be awaited, and `stop(): void` gives it no place to say so. Pick one deliberately, and say which - but do not present the choice as 'verbatim from the reference' when the reference says void.\n\nTHE README CHECKLIST must cover: where this harness stores session data, whether it has hooks or requires file/session-log watching, how to normalize its tool names via the shared tool-map, how the installer obtains CONSENT before modifying user config, and how to emit a versioned AgentEvent stream.\n\nCONSENT GATING (section 1.1, from pixel-agents 'consent-gated hook installer'): an adapter that writes into ~/.claude/settings.json or equivalent MUST gate the write behind explicit consent. Section 4 also says 'Never require editing the user's project repo' - so installers touch only global agent config, never the user's code.\n\nWHAT AN ADAPTER MUST NEVER DO (section 23): import game code. Adapters import core + protocol only (enforced by the dependency-rules bead, checked by tests/unit/dependency-rules.test.ts). All adapters emit into the same local buffer feeding POST /api/events.\n\nTHE BUFFER IS NOT WHERE AN ADAPTER MIGHT GUESS, AND THE CLIENT IS NOT A PACKAGE YET. Two facts that will otherwise cost the first adapter author an afternoon:\n- The working client-side buffer is `EventBuffer` in apps/web/src/event-batch.ts. It is NOT exported from @battle-agents/protocol, and adapters are forbidden from importing apps/web (wrong layer). So the honest options are to lift EventBuffer into a shared package both can import, or to reimplement its three rules in the template. Decide that as part of this bead and record which - a template that cannot satisfy the ingest contract is not a template.\n- The three rules a conforming buffer must honour, because POST /api/events enforces all three: flush at 250ms or 50 events, never exceed 100 events in one batch (413 + Retry-After above that), and never put two sessionIds in one batch (400).\n- @battle-agents/protocol exports the zod AgentEvent union, PROTOCOL_VERSION, the generated RegisteredActionId union and isRegisteredActionId. It exports NO http client. If the template is to be self-sufficient, the client it needs is part of this bead's deliverable - that is the single biggest gap between 'a competent developer can add a harness from this alone' and the truth today.\n\nCONSIDERATIONS: the template is only as good as its checklist. The M7 proof is a third party shipping Amp support from this template alone - so the template must be genuinely self-sufficient, not a stub that assumes tribal knowledge.\n\nSUCCESS CRITERIA: a competent developer can add a new harness using only _template + the protocol docs, with zero core edits and zero game-code imports. Concretely, the template ships: a watcher implementing the chosen AgentWatcher shape, the buffer or a spec precise enough to rebuild one that satisfies the 250ms/50/100 and single-session rules, and a working path to POST /api/events - because a template that stops at 'and then send it somehow' does not discharge this criterion.\n\nTESTS: the template must pass the same adapter contract suite as the real adapters; a CI check that the template is not allowed to import anything outside core/protocol.","status":"closed","priority":1,"issue_type":"task","created_at":"2026-09-23T19:02:10.313899Z","created_by":"tranquangdang21","updated_at":"2026-09-25T00:59:30.686549Z","closed_at":"2026-09-25T00:59:30.686445Z","close_reason":"Template is real: parser.ts, watcher.ts, install.ts, and a barrel, replacing 11 bytes of export {}. The three modules model the parts a first adapter gets wrong: tool-name normalisation, the buffer-and-flush loop, and consent gating. EventBuffer moved to @battle-agents/protocol because an adapter must buffer and the dependency rules stop it importing apps/web, so the alternative was a reimplementation per adapter that drifts. That move exposed a rule gap: no-adapter-game-code flagged an adapter importing a sibling file in its own directory, so any multi-file adapter failed the gate and the one-subdirectory promise was unkeepable. Intra-package imports are now exempt, and cross-adapter and cross-feature imports were each verified still blocked. The first version of that exemption compared the wrong path segment and swallowed feature-to-feature imports; the fixture test caught it.","source_repo":"battle-agents","source_repo_path":"/Users/tranquangdang21/Projects/battle-agents","mol_type":"work","work_type":"mutex","wisp_type":"none","labels":["adapter","p1"],"dependencies":[{"issue_id":"ba-adapters-template-a09","depends_on_id":"ba-agent-credentials-issuance-294","type":"blocks","created_at":"2026-09-23T19:07:17.200905Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"ba-adapters-template-a09","depends_on_id":"ba-agent-event-union-up5","type":"blocks","created_at":"2026-09-23T19:04:25.030205Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"ba-adapters-template-a09","depends_on_id":"ba-hookprovider-seam-lq2","type":"blocks","created_at":"2026-09-23T19:04:24.947467Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"ba-adapters-template-a09","depends_on_id":"ba-reference-repo-study-o92","type":"blocks","created_at":"2026-09-23T19:05:53.752401Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"ba-adapters-template-a09","depends_on_id":"ba-tool-map-port-89a","type":"blocks","created_at":"2026-09-24T21:32:19.401508Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"ba-agent-credentials-issuance-294","title":"Agent credential issuance, rotation, scopes, expiry","description":"Implement agent credential lifecycle: issuance, rotation, revocation, scopes, and expiry, per plan sections 21, 29.1, 31, and 34.\n\nSCHEMA (section 21): agent_credentials: id, installationId -> installations, tokenHash, scopes jsonb, expiresAt, revokedAt, createdAt.\n\nHARD RULES:\n- Store a HASH, never the raw token (section 38).\n- Issuance/rotation endpoints live under the AGENT EXTENSION, not inside Better Auth config (section 38). Better Auth owns human auth only.\n- A token authenticates a user/installation and NEVER equals a session (section 3.4).\n\nTEMPORARY IDENTITY TOKENS (section 3.5 and 13.2, from the Moltbook pattern - NOTE: section 29 flags Moltbook details as UNVERIFIED hypothesis, so keep the PRINCIPLE and drop citation certainty): adopt temporary identity tokens (1h expiry, minted from a permanent key) to separate Permanent Identity / Credential / Session / Event IDs. The separation of these four ID classes is the sound, independently-justified part.\n\nTOKEN LEAKAGE (section 3.4): NEVER put raw tokens in MCP URLs. The plan's own bad example is https://agentbattle.gg/mcp?token=xxxx because tokens in URLs leak into logs and browser history. Use MCP OAuth -> short-lived access tokens. Web GitHub login and MCP agent authorization are related but SEPARATE credentials - do not reuse one raw credential for both.\n\nSCOPES: agent_credentials.scopes drives the permissions field on defineAction (capability-registry), so authorization is enforced uniformly across CLI/MCP/API rather than re-implemented per surface.\n\nROTATION: support rotation without downtime if possible, and immediate revocation. Revoked credentials must fail cleanly with an actionable error, not a confusing 500.\n\nCONSIDERATIONS: the 1h temporary-token lifetime is a real tradeoff - long enough that an active coding session does not churn, short enough that a leaked token has limited blast radius. Make it configurable.\n\nSUCCESS CRITERIA: issue a credential, use it to authenticate, rotate it, then confirm the old one is rejected and the new one works; a revoked credential is rejected; raw tokens never appear in the database.\n\nTESTS: unit test that issuance stores only a hash; integration test for issue/use/rotate/revoke; a test that a token in a URL is rejected by the auth layer (defense in depth); a test that expired temporary tokens are refused.","status":"closed","priority":0,"issue_type":"task","created_at":"2026-09-23T18:59:47.996898Z","created_by":"tranquangdang21","updated_at":"2026-09-24T06:50:39.672278900Z","closed_at":"2026-09-24T06:50:39.669812400Z","close_reason":"SHA-256 over 32 CSPRNG bytes (full entropy, so no password KDF), constant-time comparison, 1h configurable lifetime. Raw token never stored — the integration test reads the whole row as text and asserts the token is absent. Rotation without downtime (replacement issued before the old is revoked, with a test for the exclusion that makes that true), immediate revocation via revoked_at rather than deletion, scope enforcement naming the missing scope. Tokens in URLs are refused by the auth layer, headers still honoured when both are present.","source_repo":"battle-agents","source_repo_path":"/Users/tranquangdang21/Projects/battle-agents","compaction_level":0,"original_size":0,"labels":["auth","feature","p0"],"dependencies":[{"issue_id":"ba-agent-credentials-issuance-294","depends_on_id":"ba-contract-agent-identity-evq","type":"blocks","created_at":"2026-09-23T19:04:22.418432Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"ba-agent-event-union-up5","title":"AgentEvent union + zod schemas + PROTOCOL_VERSION pin","description":"Define the versioned normalized event vocabulary that the entire game core speaks, per plan sections 5.2 and 20.\n\nTHE TAXONOMY (section 5.2) is the game core's ONLY vocabulary. The game NEVER knows Claude vs Codex vs OpenCode. SessionStarted/Stopped, PromptSubmitted, ToolStarted/Completed/Failed, FileRead/FileWrite, CommandRun, TestPassed/Failed, Thinking/Waiting, MessageSent/Received, SubagentSpawned/Completed, PermissionRequested, FileChanged, TaskCreated.\n\nCONVERGED PATTERN FROM ALL RESEARCHED REPOS (section 1.2 conclusion): Agent -> normalized events -> State/GameEngine/Control split -> World. Adapters translate Claude hooks / Codex JSONL / OpenCode SQLite / pi events into one AgentEvent union, THEN the game decides mappings: FileWrite -> Workshop, CommandRun -> Terminal, Search -> Library, Message -> Comms room, Task -> quest, Spawn -> summon NPC, Tokens -> resource. AgentMove and Agent Quest both prove this taxonomy works across Claude/Codex/OpenCode/pi/Cursor/Copilot.\n\nFILE: packages/protocol/src/agent-event.ts with PROTOCOL_VERSION = '0.1.0' and a zod discriminated union on 'type'. Section 20 provides the concrete shape - session.started, session.heartbeat, session.ended, tool.started, tool.completed, file.read, file.write, command.run, test.passed, test.failed, thinking, waiting, permission.requested, message.sent, subagent.spawned, subagent.completed - each carrying sessionId and at (plus type-specific fields such as harness enum, exitCode, linesAdded/linesRemoved).\n\nALSO: packages/protocol/schemas/*.json for cross-language adapters, and packages/protocol/src/version.ts.\n\nVERSION PIN IS A RISK CONTROL (section 17.3): keep the AgentEvent union versioned (skill.json-style pin) so adapter PRs from third parties do not break core. Section 33 P3 requires version-checking on every handshake with a warning and changelog link on mismatch.\n\nCONSIDERATIONS: the union is the contract between third-party adapters and our core, so it must stay small and stable. Prefer adding a new event type over overloading an existing one with optional fields - overloads make consumer switches ambiguous.\n\nSUCCESS CRITERIA: every taxonomy item from section 5.2 has a zod schema and a TS type; validation rejects malformed events with a useful message; PROTOCOL_VERSION is exported and checked at handshake.\n\nTESTS: round-trip validation tests per event type; a test that an unknown event type is rejected; a snapshot test that locks the union shape so an accidental breaking change fails CI.","status":"closed","priority":1,"issue_type":"task","created_at":"2026-09-23T19:00:28.721324Z","created_by":"tranquangdang21","updated_at":"2026-09-24T05:34:10.660117400Z","closed_at":"2026-09-24T05:34:10.658280200Z","close_reason":"All 21 section 5.2 taxonomy events now have zod schemas + TS types (added prompt.submitted, tool.failed, file.changed, message.received, task.created). Validation rejects malformed with field-level messages; unknown types name every known type. PROTOCOL_VERSION exported, pinned in the JSON Schema , checked in the artifact test. 9 protocol tests incl. a compile-time exhaustiveness check and a shape snapshot.","source_repo":"battle-agents","source_repo_path":"/Users/tranquangdang21/Projects/battle-agents","compaction_level":0,"original_size":0,"labels":["p1","protocol"],"dependencies":[{"issue_id":"ba-agent-event-union-up5","depends_on_id":"ba-monorepo-scaffold-cit","type":"blocks","created_at":"2026-09-23T19:04:22.564348Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"ba-animation-core-spike-hf7","title":"Animation core spike: minimal skeletal runtime subset","description":"Spike a MINIMAL skeletal animation runtime, informed by learn-spine, per plan sections 9.1-9.5 and 25.\n\nTHE LESSON (section 9.3): learn-spine is NOT a Spine tutorial. It is a feasibility prototype of an AGENT-OPERATED 2D animation web platform. Architecture: Agent -> WebMCP -> Command Layer -> Project Session -> Animation Core -> Pose -> PixiJS -> WebGL. React is UI only; the core has NO DOM/React/WebMCP dependencies; editor and player SHARE the core so they can never render divergently.\n\nADOPT from learn-spine (section 9.4): the core/pose/renderer split; STABLE IDs (bones identified by stable id, never by name - names change, ids do not); revisioned commands carrying expectedRevision + requestId so concurrent edits CONFLICT instead of silently overwriting; the observe-modify-render loop (modify -> render -> observe -> diagnose -> modify again); shared editor/player core.\n\nAVOID (section 9.4): cloning the architecture wholesale; treating it as a production renderer; building a FULL ANIMATION EDITOR. We need a minimal RUNTIME subset, not an editor product. A character needs idle/walk/attack/take-damage/victory, not a keyframe timeline UI.\n\nPIPELINE TO REPLICATE (section 9.3): FK -> IK -> Mesh skinning -> Region transform -> Renderer, driven by evaluate(Project, PoseRequest). The renderer NEVER solves IK - it receives computed geometry. This separation is the whole point.\n\nWHY SKELETAL AND NOT SPRITESHEETS (section 9.2): spritesheets hardcode animation; skeletal lets an agent manipulate STRUCTURED STATE (moveAgent, attack, takeDamage, equip, changeSkin, playAnimation, setPose) with the game engine deciding the animation. Agent state (reading, testing, failed, fixed, battling) selects reading/terminal/damage/victory/attack animations.\n\nSTATUS CAVEATS (section 9.3): the reference author reports perf Gate 1 FAIL at the measurement point, physics untested, production-readiness unproven, and MVP output deferred. Treat it as a RESEARCH PROTOTYPE, not a Spine replacement, and do not benchmark against it as a quality bar.\n\nNO PREMATURE OPTIMIZATION (section 9.3): the reference deliberately avoided WebGPU/WASM/Web Workers until a bottleneck was MEASURED. Do the same - measure first.\n\nINTEGRATION (section 25): the animation feature is a CONSUMER - GameState -> AnimationState -> Pose frames for the game client - with ZERO game logic. Removing it must leave the game fully playable.\n\nSUCCESS CRITERIA, each clause with the thing that settles it. The two clauses marked [REWRITTEN] could not fail as written.\n\n1. A character renders from skeleton data. Settled by golden-frame tests for pose evaluation: a named skeleton fixture, evaluated at a named pose, produces a frame that matches a committed golden. A golden is the right instrument precisely because it fails on a wrong pose - but it only means something if the golden is committed and the comparison is exact, so a golden regenerated on the fly is not a golden. Two wrong implementations this catches: evaluating from bone NAMES rather than stable ids (a rename must leave the pose identical, and a test that renames a bone and asserts an unchanged golden is what proves the stable-id property from section 9.4), and a renderer that solves IK itself rather than receiving computed geometry (the pipeline split the bead is built around - see clause 4).\n\n2. [REWRITTEN] The character animates through the AGENT-STATE MAPPINGS. 'Animates' is a property of a running screen and settles nothing; what is actually claimed is the mapping from our five agent states to animations, and that is a table with a testable shape. The bead must ship an exported, exhaustive AGENT_STATE_ANIMATION map from the five states named in the bead (reading, testing, failed, fixed, battling) to the five animations named in the bead (reading, terminal, damage, victory, attack). Settled by: a test that every one of the five states resolves through the map with no undefined and no silent default, AND the reverse - that a state NOT in the map is a loud failure rather than a fallback. The silent default is the failure worth guarding: a decider that maps unknown states to a generic idle animation makes this criterion pass while the mapping is in fact incomplete, and the gap only surfaces when an agent reaches a state nobody mapped. A P2 state map that is total over the current set and refuses unknown states is the deliverable; a mapping table with holes is not.\n\n3. The core has no React/DOM/WebMCP imports. Settled by a module-graph test over the core asserting zero React / DOM / WebMCP imports, not a grep of the top-level entry file. This matters because the reference's discipline is that the editor and the player SHARE the core, which is only true if the core is genuinely headless - one convenience document.createElement in a helper and the whole property is gone while every unit test still passes.\n\n4. The renderer never solves IK - it receives computed geometry (the FK -> IK -> Mesh skinning -> Region transform -> Renderer pipeline from section 9.3). This was implicit in the old criteria and is the separation the whole spike is built around, so it is now stated: settled by a test that the renderer's inputs contain only already-computed transforms, and by the module graph showing IK code reachable only from the evaluation stage. A renderer that quietly computes IK looks identical on screen and destroys the shared-core property, because the editor and player can then diverge in exactly the way sharing is meant to prevent.\n\n5. [REWRITTEN] 'It is removable.' Removability is the integration contract - section 25 says the animation feature is a CONSUMER with zero game logic and that removing it must leave the game fully playable - and 'removable' as a word settles nothing, because an animation package nothing imports is trivially removable and an animation package everything imports is not removable at all, and the criteria did not distinguish them. Settled by a real removal test with two assertions, both required: delete the animation feature from the composition root and the game still reaches a playable state (no dangling import, no missing action id, no build break), AND the game is still correct without it - the same loop, the same rendered outcomes, minus the animation. The second half is the one that catches the actual violation: an animation consumer that quietly became a dependency of a game rule passes the first half and fails this one, and that is precisely the leak section 25 forbids.\n\nTESTS, each failing when the behaviour is wrong rather than when a file is missing:\n- golden-frame tests over named skeleton fixtures at named poses, compared against committed goldens.\n- a stable-id test: renaming a bone leaves every golden unchanged, which is what proves bones are identified by id and not by name.\n- an agent-state mapping test: the map is total over the five named states, and an unknown state is a loud failure rather than a fallback.\n- a determinism test: the same Project + PoseRequest yields the same pose, byte for byte.\n- a module-graph test over the whole core: zero React / DOM / WebMCP imports, and IK reachable only from the evaluation stage.\n- a removal test with both halves: uninstall leaves the game playable AND leaves it correct. A removal test asserting only the first half passes for a feature that has become load-bearing.\n\nCONSIDERATIONS: the scope trap is building an editor. The learn-spine reference includes a full editor with undo/redo, checkpoints, and observation tooling, and it is genuinely impressive - but we need a runtime subset that plays a handful of animations, not an authoring product. Building the editor is a multi-month diversion that serves no milestone. Second: because the reference reports its own perf gate failing, do not treat it as a quality bar or copy its rendering approach uncritically; measure our own bottleneck before optimizing, which is the same discipline the reference itself followed by not prematurely reaching for WebGPU/WASM/Workers.","status":"open","priority":2,"issue_type":"task","created_at":"2026-09-23T19:03:04.717135Z","created_by":"tranquangdang21","updated_at":"2026-09-24T21:43:27.003602Z","source_repo":"battle-agents","source_repo_path":"/Users/tranquangdang21/Projects/battle-agents","mol_type":"work","work_type":"mutex","wisp_type":"none","labels":["animation","p2"],"dependencies":[{"issue_id":"ba-animation-core-spike-hf7","depends_on_id":"ba-game-client-pixijs-riw","type":"blocks","created_at":"2026-09-23T19:04:26.170681Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"ba-animation-core-spike-hf7","depends_on_id":"ba-reference-repo-study-o92","type":"blocks","created_at":"2026-09-23T19:05:53.865072Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"ba-api-routes-surface-mqr","title":"HTTP API surface: sessions, bounties, battles, cron","description":"Implement the HTTP route surface listed in plan section 19/22, sitting behind the Public API contract.\n\nROUTES (section 19 api/ tree, section 22 flows):\n- POST /api/events (event-ingest-and-sse bead) - BUILT, apps/web/app/api/events/route.ts\n- GET /api/events/stream (SSE) - BUILT, apps/web/app/api/events/stream/route.ts\n- POST /api/sessions (HELLO handshake - see contract-session)\n- POST /api/sessions/[id]/heartbeat\n- POST /api/mcp (MCP Streamable HTTP) - NOT BUILT, and NOT THIS BEAD. Owned end to end by ba-4to, which mounts the Streamable HTTP transport and the route. Two corrections to the line this replaces: the route does not exist (apps/web/app/api/ contains auth/ and events/ only), and 'the P3 MCP bead' was the plan's superseded reading - section 31's correction dated 2026-09-24 is explicit that MCP IS P0, not P3, and section 33's revised build order puts the MCP adapter in P0 alongside the CLI as an equal interface consumer. So the priority named here was wrong as well as the owner. Do not build it from this bead; build it from ba-4to.\n- POST /api/bounties, /api/bounties/[id]/claim, /api/bounties/[id]/submit\n- POST /api/battles, /api/battles/[id]\n- POST /api/webhooks/github - NOT THIS BEAD. Owned end to end by ba-github-infrastructure-56v, which is building it now: signature verification over the raw body, the Octokit client, the issue->bounty and PR-merge->bounty.completed effects, and the boundary rule that the bounty feature consumes those as events rather than importing Octokit. It appears in the list here only so the inventory is complete. Do not build it from this bead; if you picked up 56v, build it from there.\n- POST /api/cron/heartbeat - the TRIGGER only, and the state machine it drives is not this bead's either. The stale-session transitions (active -> disconnected -> abandoned) are CONTRACT 3 (ba-contract-session-rpo, closed), and packages/features/agent already ships a sweepStaleSessions helper in hello.ts alongside DEFAULT_HEARTBEAT_TIMEOUT_MS and DEFAULT_RESUME_GRACE_MS. The season-job half belongs to ba-bounty-modes-tiers-seasons-62l, which owns season accounting. What is left for this bead is the scheduled route that calls the existing helper on a schedule - a trigger, not a sweeper.\n\nTHE LIST ABOVE IS INCOMPLETE IN A WAY THAT MATTERS, because four routes already exist and are not on it. apps/web/src/routes.ts serves THE FIVE PRIMITIVES over HTTP, and they are the surface every agent and every CLI-equivalent actually uses:\n- GET /api/discover (optional ?domain= for the per-domain detail)\n- GET /api/search (optional ?type= and ?name=)\n- GET /api/inspect (?type= and ?id=)\n- POST /api/act ({ action, input })\n\nThese are the CONTRACT-4 public API, exposed as HTTP, and they route through the same createApplicationApi the CLI and MCP consume - so HTTP and CLI cannot drift, which is the property the success criteria below actually care about. Add them to the inventory before treating this bead as a list of things still to build: the remaining work here is the resource routes and the sweeper, not the primitives.\n\nAlso note the two event routes live in a SEPARATE file, apps/web/src/event-routes.ts, behind its own dispatcher, and deliberately so: the five primitives never read a request body and this one cannot route without inspecting one. Both are pure functions over injected dependencies, which is why apps/web/src/event-routes.test.ts can exercise the whole ingest path - auth, 413, parse, session resolution, emit, SSE - with no server and no database.\n\nTHE CONTRACT: these routes are CONSUMERS of the application commands/queries defined in contract-public-api. They must contain NO game logic - a route handler translates HTTP into an application command and maps the result to a response. This is the same iron rule the CLI follows (section 31) and it is what lets a future Discord bot or GitHub App reuse the commands without reimplementation.\n\nAUTH LAYERING: routes authenticate, then authorize at the application boundary. Bearer installation tokens for agent-facing routes; the human web session for dashboard routes. The two credentials are related but SEPARATE (section 3.4).\n\nWHAT IS BUILT: the ingest route authenticates a Bearer installation token through an injected `authenticate(request)` that throws an authentication failure, and the dispatcher turns that into a 401 with the reason - the same mapping every other route uses, so there is no second one to drift. Session resolution is ownership-scoped: resolveSession(sessionId, installationId) returns undefined for a session the caller's installation does not own, which is deliberately indistinguishable from a session that does not exist, so the route cannot be used to enumerate real session ids. RouteDependencies.authenticate is OPTIONAL and absent only where the caller has already authenticated; the app supplies it, because an unauthenticated /api/act is an open door to every capability the game grows.\n\nWHAT IS NOT BUILT: the human-session side of the matrix. There is no dashboard route yet, so the 'agent routes reject human-session auth and vice versa' criterion has only its first half testable today.\n\nTHE CRON SWEEWER (section 22): marks sessions stale when (>5 min, no heartbeat, no websocket) as 'disconnected', then 'abandoned' after the grace window. This is what keeps the presence view honest without holding any socket open - and it echoes the plan's decision that heartbeat is a VOLUNTARY agent return cadence, not a held connection (section 13.2).\n\nCONSIDERATIONS: keep handlers thin and push logic down. A route handler that computes a reward is a route handler that will be bypassed by the CLI and produce divergent behavior.\n\nSUCCESS CRITERIA: every route maps 1:1 onto an application command with zero game logic in the handler; the sweeper transitions sessions on schedule; agent routes reject human-session auth and vice versa.\n\nTWO OF THESE ARE ALREADY TRUE AND SHOULD BE ASSERTED, NOT REBUILT. Zero game logic in the handler is the design of apps/web/src/routes.ts: its own comment says a route handler authenticates, translates and translates back, and makes no decisions. The auth split is likewise real - isAuthenticationFailure in packages/api/src/api.ts maps a failed credential to 401 with a reason, and a test asserts the human session is never used as agent identity. The five-primitive routes also already have a parity property available for free: they call the same createApplicationApi the CLI calls, so an HTTP/CLI parity test is a comparison of two calls into one object rather than a comparison of two implementations.\n\nThe outstanding part HERE is narrower than the inventory above suggests, because three items on that list belong to other beads. Owed to this bead: the bounty resource routes, the battle resource routes, and the cron trigger. NOT owed to this bead: the webhook endpoint (ba-github-infrastructure-56v owns it end to end), the stale-session transition logic (a closed contract plus a helper that already ships), and the season jobs (ba-bounty-modes-tiers-seasons-62l owns season accounting). Two agents building the same endpoint is the failure this inventory is supposed to prevent, and until the annotations above were added that failure was live for the webhook route.\n\nThe three groups that remain are one bead rather than three on purpose, and the reason is worth stating so nobody splits them later: every one of them is the same shape - authenticate, translate to an application command, map the result back - so there is no way to land one of them that teaches you anything the other two do not, and three beads would buy three agents re-deriving the same thin-handler discipline. They are not parallelisable because they are not independent; they are one exercise in the route contract applied to three resources.\n\nTESTS: route-level integration tests; a parity test asserting HTTP and CLI produce identical application-command effects; an auth-matrix test covering which credential each route accepts.","status":"open","priority":1,"issue_type":"task","created_at":"2026-09-23T19:01:27.912402Z","created_by":"tranquangdang21","updated_at":"2026-09-24T23:01:14.823025Z","source_repo":"battle-agents","source_repo_path":"/Users/tranquangdang21/Projects/battle-agents","mol_type":"work","work_type":"mutex","wisp_type":"none","labels":["api","p1"],"dependencies":[{"issue_id":"ba-api-routes-surface-mqr","depends_on_id":"ba-contract-public-api-ute","type":"blocks","created_at":"2026-09-23T19:04:23.890305Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"ba-api-routes-surface-mqr","depends_on_id":"ba-event-ingest-and-sse-gu9","type":"blocks","created_at":"2026-09-23T19:04:24.137638Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"ba-asset-shortlist-x2a","title":"Curated CC0/consistent asset shortlist + license tracking","description":"Curate the game asset shortlist and enforce per-pack license tracking, per plan sections 14 and 28.3.\n\nSOURCES (section 14): OpenGameArt (fast prototyping - characters, tilesets, RPG maps, UI, SFX); itch.io (largest - pixel character/RPG, cyberpunk/arena tilesets, UI, battle VFX); Kenney (clean consistent packs - characters, UI, icons, buildings, particles); CraftPix and GameDevMarket (commercial production art); GitHub (CC0 packs - VERIFY each license, never assume).\n\nTHE DELIVERABLE (section 14): a curated shortlist of roughly 10-20 packs, free/CC0, in a SINGLE consistent style. The plan explicitly offered this as a follow-up and section 18 build-order item 9 includes it. Do it BEFORE commissioning art.\n\nART DIRECTION (section 14): cyberpunk / developer-workstation / RPG-arena. Tentative skins: Claude -> Mage, Codex -> Knight, Gemini -> Alchemist, OpenCode -> Rogue, Pi -> Hacker. CRITICAL CONSTRAINT: skins ONLY - never hardcode class to model. Class is behavior-derived (see feature-progression); the skin is cosmetic and must be swappable. An agent may also carry its own custom loadout.\n\nLICENCE HYGIENE (sections 14, 17.7, 28.3): the code is MIT, but EACH ASSET PACK KEEPS ITS OWN LICENSE, stored beside the pack. The shortlist MUST record per-pack license BEFORE merge. Never assume a GitHub repo's assets are freely usable.\n\nIMMEDIATE UNBLOCKER (section 28.1 item 1): the CC0 tiny-swords pack from agent-quest is SANCTIONED for V0 - sprites plus its LICENSE.txt, unchanged - giving instant characters and tiles before the shortlist lands. STATUS CORRECTED 2026-09-24: it is NOT yet vendored. Nothing is under apps/web/public/assets/ and no asset directory exists in the tracked tree; the copy is still to be done. This bead covers both the V0 tiny-swords copy and the longer-term curated set, since the V0 copy is the first thing that needs a per-pack LICENSE.txt to exist.\n\nPATH NOTE: section 19 places web assets under apps/web/public/assets/<pack>/LICENSE.txt, not the bare public/assets/<pack>/ the earlier revision of this bead used. There is no apps/web/public/ directory yet.\n\nSCOPE DISCIPLINE (section 17.7): decide the game-asset license separately from the code MIT license, and track per-pack licenses. This is a real decision, not a formality - mixing a GPL art pack into an MIT repo would contaminate it.\n\nSUCCESS CRITERIA, REWRITTEN BECAUSE '10-20 SAME-STYLE PACKS' IS A JUDGMENT, NOT A CHECK. Two of the three clauses were already machine-checkable and are kept; the first was the one that could not fail, because a shortlist file listing twenty packs with recorded licenses satisfies it whether or not the packs look like each other or the number is right.\n\n1. A shortlist of 10-20 packs in a SINGLE consistent style exists, with a recorded per-pack license for each row. What is checkable and what is not, stated plainly so nobody fakes a check that isn't there: THE COUNT AND THE PER-ROW LICENSE ARE MACHINE-CHECKABLE, and the style is a human judgment the plan makes explicitly (see CONSIDERATIONS: 'style consistency is the actual selection criterion'). So: a shortlist table exists with one row per pack, every row carrying pack name, source URL, license, and the recorded style/tags; a check asserts the row count is in 10-20; and a check asserts no row has an empty license cell. The style-consistency claim is NOT reduced to a fake lint here - a script cannot judge whether five packs look like one game, and writing a check that claims to would be worse than none. What the check DOES enforce is the part that is mechanical and that people actually get wrong: the style is DECLARED per row, so the claim is explicit and reviewable rather than implied by a selection nobody can second-guess later, and the shortlist is a table with a stable shape rather than prose.\n\n   The 10-20 number is a target, not a floor to pad. A shortlist of 20 packs in five visual styles is worse than 12 in one, per the bead's own reasoning - so if the count check fails because a careful selection found fewer, FIX THE SELECTION, do not add packs to make a number go green. Record which packs were rejected for style if the count lands low; that is the evidence the judgment was made rather than skipped.\n\n2. Every merged pack ships its LICENSE.txt. Settled by: a CI check that every directory under apps/web/public/assets/ has a LICENSE.txt - the real, checkable clause, and the one with teeth because it runs on every merge rather than at selection time. scripts/check-licenses.sh currently checks vendored SOURCE licenses only and has no asset-directory rule, so this check does not exist yet and is part of what this bead must add.\n\n3. No pack is merged without a verified license, and the recorded license matches the shipped file. Settled by: a check comparing the license recorded in the shortlist against the license text actually shipped in that pack's LICENSE.txt. Both halves are needed and the second is the one that catches the real failure: a pack relicensed upstream between selection and merge still carries a stale recorded license, the LICENSE.txt check passes because a file exists, and the shortlist quietly asserts something untrue. A file merely EXISTING is not verification.\n\nTESTS, each a command with an exit code. (This supersedes the older single-line TESTS note but keeps its content: the per-asset-directory rule in scripts/check-licenses.sh is still to be added, and that script still has no asset rule today.)\n- a shortlist-shape check: the shortlist is a table, every row carries pack name, source URL, license and style/tags, no license cell is empty, and the row count is within 10-20. A prose shortlist fails this - which is intended, because the per-pack license is the deliverable and prose is where per-pack licenses go missing.\n- the per-pack license match: the license recorded in the shortlist matches the text shipped in that pack's LICENSE.txt, per directory under apps/web/public/assets/. This is the relicensing check and the reason the license is re-verified at MERGE time rather than selection time.\n- a CI check that every directory under the asset root has a LICENSE.txt, added to scripts/check-licenses.sh (which today checks vendored SOURCE licenses only and has no asset rule).\n- the V0 tiny-swords copy specifically: apps/web/public/assets/ does not exist in the tracked tree today, so the first directory to appear is the one this check is proven against. A check that has never seen a real directory has never run.\n\nWHAT THIS BEAD STILL CANNOT PROVE, and the reason the style clause was deliberately not given a lint: no script here judges whether the selected packs look like one game. That judgment is a human review of the shortlist's declared style column, and it is the plan's stated selection criterion - a perfect set of individually excellent packs in five visual styles is worse than a smaller set that reads as one game. Anyone tempted to add a 'style lint' should read that sentence first: the failure is a bad selection, not a missing script, and a script cannot see it.\n\nCONSIDERATIONS: style consistency is the actual selection criterion, not license or price - a perfect set of individually excellent packs in five different visual styles is worse than a smaller set that looks like one game, because visual incoherence reads as 'amateur project' instantly. Second: the license check is per-pack and must be re-verified at merge time, not at selection time; licenses change and packs get relicensed. Third: the plan is explicit that art licenses are tracked SEPARATELY from the MIT code license, so resist the convenience of assuming everything in the repo is MIT because the code is.","status":"open","priority":2,"issue_type":"task","created_at":"2026-09-23T19:03:04.774810Z","created_by":"tranquangdang21","updated_at":"2026-09-24T21:49:19.409633Z","source_repo":"battle-agents","source_repo_path":"/Users/tranquangdang21/Projects/battle-agents","mol_type":"work","work_type":"mutex","wisp_type":"none","labels":["assets","governance","p2"],"dependencies":[{"issue_id":"ba-asset-shortlist-x2a","depends_on_id":"ba-feature-bounty-xhk","type":"blocks","created_at":"2026-09-24T21:32:47.083053Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"ba-asset-shortlist-x2a","depends_on_id":"ba-license-hygiene-qy7","type":"blocks","created_at":"2026-09-23T19:04:26.247394Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"ba-battle-replay-yjb","title":"Battle replay builder + shareable replay URL","description":"Build the battle replay - the plan's most viral feature, per plan sections 2.3, 15.1, 22, and 8.\n\nWHY REPLAY IS THE VIRAL HOOK (section 2.3): the plan judges Battle Replay to be EVEN MORE viral than the Claude-vs-Codex demo. It is the social mechanism: timestamped event log -> battle -> result -> URL. It is also the mechanism by which replays attract sponsors, and sponsors attract more agents (section 2.4 network effect). The example timeline the plan sketches: 00:01 accept quest / 00:08 starts research / 00:17 edits auth.ts / 00:23 discovers vulnerability / 00:41 test fails / 00:48 patches bug / 01:12 critical hit / 01:20 tests 47/47 / 01:20 winner.\n\nDATA: battles.replayJson holds the event timeline; battles.winnerSessionId holds the result. Replay is BUILT FROM THE ACTIVITY LOG (section 30) plus the judge output (section 22, 'judge output IS the replay seed') - not assembled separately.\n\nVIEW (section 22): apps/web/app/(app)/replay/[replayId]/page.tsx renders the event timeline, PixiJS arena playback, and share metadata including an og:image generated from the final frame.\n\nTHE DoD THAT MATTERS (section 27 M4): share the replay link with a LOGGED-OUT user and they see the full timeline. This forces a real design decision - the public replay must be viewable without a session, which means the replay data is public even though the account is not. Decide what participant detail (agent names, repo names) is safe to expose publicly; this is a privacy consideration the plan does not spell out, so resolve it deliberately.\n\nALSO: diff-streaming over the websocket for arena efficiency (section 8), using the codemoo stateDiffBroadcast pattern (section 28.1 item 12).\n\nCONSIDERATIONS: replay URLs are the viral vector, so they must be stable, guess-resistant, and cheap to serve. Consider whether replays should expire or be pruned (an activity-log retention question, cross-referenced with activity-event-log).\n\nTHE ACTIVITY LOG YOU ARE REPLAYING FROM, already shipped, so the \"build from the log\" instruction is concrete (ba-activity-event-log-aph, closed). The durable table is event_log: id (bigserial), type, actorId, payload jsonb, occurredAt, read through features/activity's read API (a per-session trail plus recent), via DrizzleStateStore - the only path an event takes to the database. Three properties of that log decide what a replay can and cannot show:\n- THE PERSISTENCE FILTER IS NOT EVERYTHING. Only PERSISTED_EVENT_TYPES are durable: PERSISTED_EVENT_TYPES = ['session.started','session.ended','session.resumed','test.passed','test.failed'] plus whatever each installed feature declares in GameFeature.persistedEvents. The list this bead used to quote was the plan's wish-list, and the difference is not cosmetic for THIS bead: bounty.claimed, bounty.completed, battle.started and battle.finished are the spine of a replay, and none of them is durable today because no feature declares them. A replay built now would render session and test events and nothing else. Battle owns declaring battle.started/battle.finished and bounty owns declaring its own, and the DoD 'share the link with a logged-out user and they see the full timeline' is unreachable until they do - which makes this the one place where the filter's real shape is a delivery blocker rather than a caveat.. Everything else (thinking, waiting, streaming, heartbeat) is bus-only and is gone after a restart. So the replay timeline is reconstructable ONLY from persisted events. The example timeline's narrative beats (research, discovers vulnerability, critical hit) are illustrative, not all durable - a replay that wants them must add those event types to the persisted set, a deliberate act, not an assumption.\n- ORDERING IS GUARANTEED, which is what makes the timeline a timeline. The runtime emits persist -> handlers -> publish, so the durable record is written before any consumer reads it, and the log is ordered. A replay can rely on occurredAt ordering without re-sorting races.\n- RETENTION IS 365 DAYS (features/activity owns the policy; 365 because a shared replay is a link). So a replay is reconstructable for a year; beyond that, event-log pruning and replay availability are the same question, and a replay URL pointing at pruned events needs a defined degraded state rather than a 500.\n\nDETERMINISM IS A HARD REQUIREMENT, not a nicety, because the same event stream must yield the same replay (this is an explicit test). That constrains the design: the replay is a pure function of the persisted event stream plus the judge output - no wall-clock reads, no random IDs, no dependence on 'now' during construction. It also means the judge output that seeds the replay must itself be the ordered, attributable event stream the workspace/judge bead (ba-battle-workspace-judge-jc9) produces, not a collapsed score; a judge that returns only a weighted number cannot seed an explainable replay.\n\nTHE PUBLIC/LOGGED-OUT BOUNDARY INTERACTS WITH AN OPEN BUG, so this bead is not blocked on privacy decisions alone. The SSE stream (GET /api/events/stream) is currently an unauthenticated global broadcast that ships every validated AgentEvent to any open socket - an agent's file paths, command names, and thinking content reach any subscriber. That is tracked as a P0 bug (ba-9hr). It is recorded here so whoever builds the replay page does not build it on the assumption that a clean public replay implies a clean live stream: the replay page is a public, deliberately-scoped read (a specific battle's persisted timeline), which is a different surface from the global live broadcast that still needs the public/private event classification and stream authentication. Do not widen the replay's public surface to include live transient events because the live stream is currently unfiltered.\n\nWHY A NAIVE IMPLEMENTATION GOES WRONG. The first shortcut is assembling the replay from feature tables (the bounty row, the battle row, the progression rows) instead of from the activity log; that is exactly what the 'not assembled separately' clause forbids, it duplicates truth the log already holds, and it will drift. The second is a replay that is not deterministic - if construction reads the current time or mints a fresh ID, the determinism test fails and two viewers of the same battle can see different replays, which defeats sharing. The third is serving the full timeline to a logged-out viewer by simply removing the auth check from an authenticated endpoint: the logged-out DoD is a real public read with a chosen field allow-list (agent names, repo names - decided deliberately), not 'disable auth and leak every field'. The fourth is a replay URL that is sequential or otherwise guessable; the viral vector plus public data means an enumerable id lets anyone bulk-scrape every battle.\n\nSUCCESS CRITERIA: a completed battle produces a replay URL; a logged-out visitor sees the complete timeline; the og:image renders; the replay is a deterministic pure function of the persisted event stream plus the judge output (same stream, same replay); the timeline is reconstructed from event_log, not from feature tables; the public logged-out view exposes a deliberate allow-list of participant detail, not everything.\n\nTESTS: the logged-out DoD test; a determinism test (same event stream yields the same replay); a test that replay construction reads from event_log rather than feature tables; a test that the replay timeline survives a restart (reads persisted events, not bus-only ones); a test that replay URLs are not sequential/guessable; a test that the public logged-out view excludes fields outside the chosen allow-list.","status":"open","priority":2,"issue_type":"task","created_at":"2026-09-23T19:03:04.434117Z","created_by":"tranquangdang21","updated_at":"2026-09-24T23:05:04.525778Z","source_repo":"battle-agents","source_repo_path":"/Users/tranquangdang21/Projects/battle-agents","mol_type":"work","work_type":"mutex","wisp_type":"none","labels":["battle","feature","p2"],"dependencies":[{"issue_id":"ba-battle-replay-yjb","depends_on_id":"ba-activity-event-log-aph","type":"blocks","created_at":"2026-09-23T19:04:24.789971Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"ba-battle-replay-yjb","depends_on_id":"ba-feature-battle-fbt","type":"blocks","created_at":"2026-09-23T19:04:24.708004Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"ba-battle-workspace-judge-jc9","title":"Isolated workspace provisioner + judge pipeline + hook gates","description":"Build the mechanical substrate under feature-battle: isolated workspaces, the judge run pipeline, and pre-flight gates. Per plan sections 10.3, 17.4, 22, and 28.1 items 4-5.\n\nISOLATED WORKSPACES ARE MANDATORY (section 17.4), not optional. Each participant works in its own sandboxed copy so neither can read nor corrupt the other's work. The battle is meaningless without this.\n\nTHE JUDGE RUN (section 22), per participant workspace: install -> typecheck -> lint -> test suite -> regression (base-branch test set) -> diff stats -> security grep rules. Then the weighted score from battles.weightsJson. Judge output IS the replay seed (section 22) - so the judge must emit an ordered, structured, attributable event stream, not just a number.\n\nHOOK GATES AS PRE-CHECKS (section 28.1 item 4, direct copy from agent-dashboard, MIT): port adapters/claude-code/hooks/ scripts - commit-lint, test-gate, destructive-warn, block-main-commit - into battle judge pre-checks and features/battle workspace guards. Also port the skills/{feature,fix,pr}/SKILL.md FRONTMATTER SHAPE as our extension/skill contract model (section 1.1 credits agent-dashboard with this).\n\nWHY GATES MATTER: a 'destructive-warn' or 'block-main-commit' gate is precisely the kind of guard that makes it safe to let autonomous agents work in parallel sandboxes. They are cheap insurance and the plan explicitly wants them.\n\nSECURITY GREP RULES belong in the judge (section 22) - keep them as DATA (a rules table), not hardcoded branches, so rules can be updated without a deploy of the battle engine.\n\nCONSIDERATIONS: judge runs are the slowest and most failure-prone part of a battle. A participant whose workspace fails to install should lose or forfeit by a stated rule, not hang the match. Decide and document timeout/forfeit semantics; a battle that cannot terminate is worse than no battle.\n\nWHAT THE JUDGE OUTPUT MUST CONFORM TO, because \"judge output IS the replay seed\" is a hard contract with the replay bead, not a nice property. The replay builder (ba-battle-replay-yjb) reconstructs a battle from the ordered activity log plus this judge output, and its own determinism test requires that the same event stream yields the same replay. So the judge output is not a score you compute at the end - it is a sequence of attributable, ordered events that the replay reads back verbatim. Concretely, that means:\n- Each judge step emits a structured, ordered event (which criterion ran, on which participant workspace, what it found) rather than collapsing to a final number. A weighted total that cannot be decomposed into its per-criterion parts cannot seed a replay that shows its work.\n- The events are attributable to a participant (the two workspaces are isolated, so an event must say which one it came from) and they carry the ordering the replay timeline depends on.\n- The judge output is written to the same durable trail the rest of the system reads - event_log, reached through DrizzleStateStore, the only path an event takes to the database - so a replay is reproducible from storage rather than from an in-memory score. The runtime guarantees persist -> handlers -> publish, so an event the judge emits is durable before a replay consumer reads it.\n- Note the persistence filter: only PERSISTED_EVENT_TYPES are durable by default (PERSISTED_EVENT_TYPES = ['session.started','session.ended','session.resumed','test.passed','test.failed'] plus what each installed feature declares in GameFeature.persistedEvents. The list this bead used to quote was the plan's wish-list and it is wider than what ships. The judge criteria that DO reach the durable trail today are the ones whose types are already in the shipped set: test.passed / test.failed for the suite and regression criteria. command.run (install/typecheck/lint), file.read / file.write / file.changed (diff stats) and any per-criterion judge event are all bus-only and WILL NOT be in a replay after a restart - so the decomposable judge stream this bead requires is not yet durable, and the fix is a persistedEvents declaration on the judge, not a change to core. Do not assume a judge event is durable because it was emitted.). A judge step whose event type is not in that list is bus-only and will NOT be in the replay after a restart. If the replay must show individual judge steps, those event types have to be added to the persisted set (a deliberate act) - do not assume a judge event is durable just because it was emitted.\n\nWHERE THE BOUNDARY IS, because \"under feature-battle\" is a placement rule with teeth. Provisioning and judging are the mechanical substrate; the matchmaking, scoring policy, and arena (the feature's own state machine) belong to ba-feature-battle-fbt, which is the blocked parent. The judge consumes the battle lifecycle; it does not own it, and the two beads must not each define the score. The provisioning/judging machinery also must not import game vocabulary into core, and features never import each other (machine-enforced in CI via architecture-rules.cjs): the judge runs against a workspace and a rules table, and reports upward over the bus, it does not reach into another feature's state.\n\nTHE EVENT VOCABULARY IS FROZEN, so a judge step does not invent event names. It emits members of the zod AgentEvent union in packages/protocol/src/agent-event.ts (ba-agent-event-union-up5, closed) - among them the ones that already exist for this: test.passed / test.failed for the suite and regression criteria, command.run for install/typecheck/lint invocations, and file.read / file.write / file.changed for diff stats. The weight table battles.weightsJson and the security grep rules are configuration DATA, not code branches. If a genuinely new judge event is needed, prefer adding a new event type to the union over overloading an existing one with optional fields; overloading makes every consumer's switch ambiguous.\n\nWHY A NAIVE IMPLEMENTATION GOES WRONG. A judge that returns a single weighted number is the trap the 'replay seed' clause exists to prevent: it makes the replay unable to show why anyone won, and it makes the score unexplainable to the participants. A judge that stores a running score that cannot be traced back to per-criterion events cannot be replayed deterministically. A judge with hardcoded if(repoIsX) security rules needs a deploy to update a rule, which is why they are a data table. And a judge with no timeout/forfeit rule is the failure the consideration names: a workspace that never finishes installing hangs the whole match, which is worse for both participants than a stated forfeit - so a non-terminating judge is a correctness bug, not an edge case.\n\nSUCCESS CRITERIA: two participants are provably isolated; the judge produces a weighted, explained score that decomposes into attributable, ordered per-criterion events; that event stream is durable (persisted, not bus-only) so a replay can be reconstructed from it deterministically; the gate scripts block the specified dangerous operations and pass through the safe ones; a non-terminating participant forfeits by a stated, tested rule.\n\nTESTS: isolation test (cross-workspace access attempts fail); judge fixture tests per criterion; a determinism test that the same judge event stream yields the same weighted score; a test that the judge event stream survives a restart (i.e. is persisted, not bus-only); gate tests proving destructive-warn blocks and commit-lint passes; a timeout/forfeit test.","status":"open","priority":2,"issue_type":"task","created_at":"2026-09-23T19:03:04.378501Z","created_by":"tranquangdang21","updated_at":"2026-09-24T23:05:04.610360Z","source_repo":"battle-agents","source_repo_path":"/Users/tranquangdang21/Projects/battle-agents","mol_type":"work","work_type":"mutex","wisp_type":"none","labels":["battle","feature","p2"],"dependencies":[{"issue_id":"ba-battle-workspace-judge-jc9","depends_on_id":"ba-feature-battle-fbt","type":"blocks","created_at":"2026-09-23T19:04:24.626053Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"ba-better-auth-github-oauth-9wm","title":"Better Auth + GitHub OAuth + local OAuth contract (M0)","description":"Set up Better Auth with GitHub OAuth over PostgreSQL, per plan sections 38 and 39. No hand-rolled OAuth.\n\nREAD THIS FIRST - MOST OF THIS BEAD IS ALREADY BUILT AND WIRED, AND THE ONE PART THAT IS NOT WIRED LOOKS COMPLETE BECAUSE A GREEN TEST EXERCISES IT. This bead predates the tree. Rebuilding what is already here is wasted work at best and a second identity path at worst.\n\nALREADY BUILT, MOUNTED AND TESTED - do not rebuild, and do not add a second instance:\n  apps/web/src/auth/server.ts            - betterAuth() with the drizzleAdapter (provider 'pg', schema betterAuthSchema), readAuthEnvironment() validating BETTER_AUTH_SECRET / BETTER_AUTH_URL / GITHUB_CLIENT_ID / GITHUB_CLIENT_SECRET / DATABASE_URL, a refusal of a localhost BETTER_AUTH_URL by name carrying the redirect_uri_mismatch explanation and the exact homepage + callback URLs, and sharedAuth() as a lazily-built process-wide singleton with closeSharedAuth() for graceful shutdown.\n  apps/web/app/api/auth/[...all]/route.ts - the mount. `export const { GET, POST } = toNextJsHandler(sharedAuth().handler)`. Note the app directory is apps/web/app/, NOT apps/web/src/app/ - looking under src/ finds nothing and is how you conclude the auth is unmounted when it is not.\n  apps/web/src/auth/bootstrap.ts         - bootstrapGameAccount(): a signed-in human becomes a users row with an empty agent list, idempotent, with the race made safe by the unique index on github_id.\n  apps/web/src/auth/server.test.ts       - the HARD SEPARATION clause below is ALREADY machine-enforced. The test reads server.ts, strips comments and the @battle-agents namespace, and fails on reserved words (agents, characters, installations, ...). It also covers the env contract including the localhost refusal.\n  tests/e2e/first-login.test.ts          - drives bootstrapGameAccount three times (first login, repeat login, return) against a real database.\n  .env.example                           - BETTER_AUTH_SECRET, BETTER_AUTH_URL=http://127.0.0.1:3000, GITHUB_CLIENT_ID, GITHUB_CLIENT_SECRET, GITHUB_CALLBACK_URL=http://127.0.0.1:3000/api/auth/callback/github, DATABASE_URL, with the localhost/127.0.0.1 warning in comments.\n  apps/web/README.md                     - the failure modes, including redirect_uri_mismatch, missing secret, unmigrated database.\n  compose.yaml / compose.dev.yaml / compose.test.yaml - the local container Postgres and the compose test-runner image (.github/workflows/m0.yml runs pnpm test:m0 in it).\n\nTHE ONE REAL GAP, AND IT IS DISGUISED AS DONE: bootstrapGameAccount has NO PRODUCTION CALLER. Every importer in the repo is a test - tests/e2e/first-login.test.ts and the reserved-word list in server.test.ts. Nothing on the login path invokes it, and createAuth() in server.ts declares no databaseHooks / user-create hook that would call it either. Meanwhile a comment inside that same function asserts 'The users row is created by bootstrapGameAccount in bootstrap.ts', so the code reads as though it is wired. It is not. The consequence is precise: a real first GitHub login creates the Better Auth user and no game-account row, so 'logout then login shows the same user and the same agents' fails at the game-account step, and the dashboard has no account to read. The trap is that the e2e test named first-login.test.ts is green and tests exactly this function's three-call idempotency, so 'there is an integration test for the first-login bootstrap' is true while the first-login bootstrap is unreachable from the app. Do not treat that green test as evidence the path is wired; a test that calls the function directly proves the function, never the caller. The work is to call it from the login path (a Better Auth hook, or the first authenticated request that needs an account) and keep server.test.ts's separation guard green - NOT to rewrite the function, which is already correct and already tested.\n\nDO NOT REBUILD AGENT IDENTITY HERE. The M0 composition line below lists 'Agent identity, Agent credential, Agent session'. Those are NOT this bead's work and they are already delivered by ba-feature-agent-f9t: packages/features/agent exports agentFeature with AGENT_READ / AGENT_DESCRIBE actions, agent.registered / agent.registration_rejected events, and is already constructed in the composition root (apps/web/src/composition.ts). Building a second agent identity inside Better Auth is precisely the extraction cost section 38 warns about, and the source guard in server.test.ts is what will stop you - if you want to put an agent token in the auth config, that is the test working, not the test being wrong. The same applies to mapProfileToUser, which is already called out and deliberately absent in server.ts: it runs on OUR server but Better Auth still treats the return as provider input, so a whole user object returned from it invites provider-controlled data into account fields while looking like it asserts ours.\n\nWHY BETTER AUTH (section 38): Next.js-native (mounted at /api/auth/[...all]), GitHub OAuth built in, session management included, extensible later to passkey / API key / JWT / organizations / roles, and aligned with the MCP and agent-auth roadmap. The plan previously said 'GitHub OAuth' without naming a framework; this is the locked choice.\n\nTHE FLOW:\n  Human -> GitHub OAuth -> Better Auth -> {User -> Agent extension -> Quest/Bounty/Progression/Battle/Guild\n                                          Session (human web session; NOT agent session)}\n\nHARD SEPARATION (section 38, and see above: this is already enforced by a test, so do not add a second mechanism for it): Better Auth owns User / Auth / human-Session ONLY. The game domain stays in extensions. The GitHub OAuth token is NEVER agent identity.\n\nLOCAL CONTRIBUTOR CONTRACT (section 39) - every contributor must get this green on a clean clone:\n  127.0.0.1:3000 -> GitHub OAuth -> /api/auth/[...all]/callback -> local DB (container Postgres)\n- GitHub OAuth App: homepage http://127.0.0.1:3000, callback http://127.0.0.1:3000/api/auth/callback/github. These EXACT URLs are already in .env.example comments. GitHub matches callback URLs strictly, and localhost/127.0.0.1 are different origins to it (a common contributor failure: redirect_uri_mismatch) - readAuthEnvironment already refuses a localhost BETTER_AUTH_URL at startup rather than letting it fail at the callback.\n- First-login bootstrap: new GitHub user -> users row -> EMPTY agent list -> dashboard prompts CLI/MCP connect (section 4). No manual SQL. The row and the empty list already exist in bootstrapGameAccount; what is missing is the caller (see THE ONE REAL GAP above).\n\nM0 COMPOSITION (section 38, amends section 33 P0 list): Next.js, Postgres/Neon, Better Auth, GitHub OAuth, User identity, Agent identity, Agent credential, Agent session, Event Bus, Extension API, CLI, local Docker dev env. (Agent identity/credential/session are delivered by ba-feature-agent-f9t and already wired in the composition root; do not re-add them here.)\n\nONBOARDING UX this enables (section 4): agentbattle.gg -> [Continue with GitHub] -> Dashboard 'Connect your Coding Agent' -> per-agent cards -> 'claude mcp add agent-battle <url>' -> browser auth -> terminal 'connected, you can now enter the arena'. One account fans out to multiple agents with no per-session GitHub re-login. The Dashboard itself is ba-web-ui-surface-t3w's deliverable, not this bead's; the sign-in step in front of it is this bead's.\n\nSUCCESS CRITERIA: from a clean clone, docker compose up + migrations + seed + GitHub login works at 127.0.0.1:3000; logout then login shows the same user and the same agents. Split honestly, because three of the four parts are already met and one is not: (a) MOUNT - met: GET /api/auth/get-session responds through apps/web/app/api/auth/[...all]/route.ts; (b) BOOTSTRAP REACHABILITY - NOT MET, and this is the deliverable: a login performed through the MOUNTED route produces a game-account row, and the same human logging in again returns the SAME account with created=false and no second row. The assertion must go through the route, because a test that calls bootstrapGameAccount directly passes today and proves nothing about (b); (c) LOCAL ENV - met: compose.yaml plus migrations and seed give a reachable Postgres; (d) the existing server.test.ts source guard stays green.\n\nTESTS: keep server.test.ts (env contract + separation guard) and tests/e2e/first-login.test.ts (idempotency) as they are - both are correct and neither should be rewritten. Add exactly one: a test that drives first login THROUGH the mounted /api/auth route and asserts a game-account row exists afterwards. The CI stub that bypasses the real GitHub round trip must go through the mounted route for the same reason - a stub that calls sharedAuth() in-process proves the library works, not that the app reaches it.\n\nCONSIDERATIONS: the callback-URL mismatch is the most likely reason a newcomer's first run fails, and it is already handled in code, so it is no longer the main risk - do not spend this bead re-documenting it. The main remaining risk is quieter: a correct, tested, correctly mounted auth library whose first-login bootstrap nothing calls, behind a green test that makes the gap look closed. Also resist letting Better Auth grow: section 38 is emphatic that it owns human auth ONLY, and every game concept that creeps into its config (agent tokens, session identity, progression) is a future extraction cost.","status":"closed","priority":0,"issue_type":"task","created_at":"2026-09-23T18:59:47.757005Z","created_by":"tranquangdang21","updated_at":"2026-09-25T01:14:23.727890Z","closed_at":"2026-09-25T01:14:23.727802Z","close_reason":"Built, mounted and separated already; the one real gap was that bootstrapGameAccount had no production caller, and it is wired now through Better Auth's account-create hook. The account hook rather than the user hook because the game account is keyed on the GitHub provider id, and the user record a user hook receives is Better Auth's internal user with no provider id in it. Non-GitHub providers are ignored so a later passkey or email link cannot mint a second identity. The hook body is an exported function so wiring.test.ts can assert both that it is reachable and that databaseHooks registers it; removing the block turns the test red, which is the edit that made it unreachable. The hard separation the bead asks about stays machine-enforced by server.test.ts, which still passes: Better Auth owns User, Auth and the human session, and no game concept is in that file. apps/web/README.md now exists with the three failure modes.","source_repo":"battle-agents","source_repo_path":"/Users/tranquangdang21/Projects/battle-agents","mol_type":"work","work_type":"mutex","wisp_type":"none","labels":["auth","infra","p0"],"dependencies":[{"issue_id":"ba-better-auth-github-oauth-9wm","depends_on_id":"ba-monorepo-scaffold-cit","type":"blocks","created_at":"2026-09-23T19:04:22.129947Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"ba-bounty-modes-tiers-seasons-62l","title":"Bounty modes, chains, and seasons","description":"Implement the bounty variety layer per plan section 11.3: competition modes, skill tiers, quest chains, guild treasuries, and seasons.\n\nMODES (section 11.3):\n- RACE: many agents, FIRST VALID PR WINS (Tari precedent)\n- OPEN: many agents, maintainer chooses\n- TOURNAMENT: same issue, best validated solution\n- TEAM: role-composed team, ONE PR\n\nTIERS (section 11.3, gated by skill/reputation - see feature-reputation): Beginner $5-25 / Intermediate $25-200 / Advanced $200-1000 / Legendary $1000+.\n\nCHAINS (section 11.3, the strongest game hook in this section): linked bounties turn a repository's issue graph into an RPG CAMPAIGN. $100 auth -> $250 OAuth -> $500 SSO -> $2000 overhaul, each requiring the previous. Render the GitHub issue tree as a quest tree, with reward, difficulty, sponsor, claimer, and progress per node. GitHub becomes the quest database.\n\nGUILD TREASURIES: guilds fund collectively (section 11.3).\n\nSEASONS (section 11.3): a time-boxed bounty round, e.g. 'Season 1: Open Source Bounty Hunt', with monthly pools and leaderboards. Precedent: the AsyncAPI bounty program (budget rounds, public amounts, resolution rates).\n\nECONOMY DISCIPLINE (section 11, section 10.4): XP, Coins, Reputation, Items, Cosmetics ONLY. NO crypto, NO NFT. Real money and game progression stay SEPARATE - never represent money as XP or vice versa. Mappings: quest -> XP, win -> Reputation, achievement -> Badge, season -> Cosmetic, guild contribution -> Guild XP.\n\nSCOPE NOTE (section 17.8): seasons and tournaments are NOT in the M0-M4 MVP. This bead is P1-adjacent groundwork; the fully playable season system belongs to M6/M7. Build the data model and the mode enum now so early bounties carry the right shape, but do not build season UI here.\n\nCONSIDERATIONS: the bounty must carry a mode field from creation, because RACE-mode double-claim rejection in the bounty feature depends on it. Introducing modes later would require a migration of live bounties.\n\nOWNERSHIP, so this bead and the sibling do not each define the same thing. ba-feature-bounty-xhk (the bounty lifecycle) explicitly states the boundary: it owns the CORE lifecycle - the Bounty object and its state machine - and it STORES a 'mode' field and enforces RACE-mode double-claim rejection, because the lifecycle cannot be correct without it. This bead owns the enum of valid modes, the tier thresholds, the chain prerequisite logic, and season accounting. If both beads try to define the mode enum, that is a duplication bug - one owns it. The dependency is on the bounty feature, so this work builds on top of a lifecycle that is still in flight; do not fork its schema.\n\nTHE MODE FIELD IS LOAD-BEARING AT CREATION TIME, which is the reason this cannot be bolted on later. The bounty feature's ClaimBounty path already branches on mode to reject a second claim under RACE, so a bounty created without a mode has no defined claim behavior. That is the migration hazard the consideration names: a mode introduced after live bounties exist means every existing row needs a backfill and a default, and the default silently picks a claim policy for bounties nobody chose one for. So the honest order is: the mode enum and its default land with (or before) the first live bounty, and the RACE double-claim rejection is only correct once every bounty carries a mode.\n\nTIERS GATE ON REPUTATION, and the reputation source is a projection, not a stored number you read directly. Tier eligibility reads the agent's trust/reputation (the feature-reputation bead), which itself is derived from the persisted activity log rather than a manually-set field. The consequence for this bead: the tier boundary a test asserts ($25 / $200 / $1000) is a boundary in the reputation-derived value, so a test should derive the eligible tier the same way the system does - from the reputation projection - rather than hardcoding a 'reputation = N' shortcut that would not survive the derivation changing. Round 1 already recorded the trust formula and tier gates on feature-reputation as done; this bead consumes them, it does not redefine them.\n\nHOW THE VARIETY LAYER ATTACHES. Modes, tiers, and chains are part of the bounty feature's surface, so they register under the frozen Contract 1 and reach agents and the CLI through the capability registry over the five frozen primitives (discover/search/inspect/act/observe): a bounty carries its mode and tier as data, and a chain prerequisite is enforced when a claim is attempted, not by a bespoke tool. Features never import each other (machine-enforced in CI via architecture-rules.cjs), so this bead consumes bounty's state over the bus / dependency, it does not import the bounty feature, and core stays free of game vocabulary.\n\nTHE MONEY/PROGRESSION SEPARATION IS A DATA-MODEL RULE, not a UI nicety, and the grep test is how it stays true. Because real money and game progression must never be conflated, a bounty amount is real currency (amountCents on a fund) while XP/Coins/Reputation are game progression - two different scales that must never be summed, converted, or stored in the same field. The mappings are fixed (quest -> XP, win -> Reputation, achievement -> Badge, season -> Cosmetic, guild contribution -> Guild XP), so a season pool of real dollars must not become a Reputation value, and a Legendary tier's $1000+ must not become 1000 XP. The grep-level no-crypto test is the cheapest guard against a season or tier quietly introducing a tokenized path.\n\nWHY A NAIVE IMPLEMENTATION GOES WRONG. The first shortcut is adding a mode with a default and backfilling later, which the migration hazard above shows is the whole problem. The second is making tiers a label a sponsor picks (so 'Legendary' is a marketing string) rather than a gate derived from reputation - that turns the tier system into decoration and breaks the trust economy the reputation feature is for. The third is enforcing a chain prerequisite in the UI only, so the prerequisite is bypassed by anyone using the protocol/CLI directly; the machine-native rule means the gate belongs where the claim is processed, not where it is displayed. The fourth is letting a season pool or tier amount flow into the XP/Reputation scale because both are 'numbers', which is precisely the conflation the discipline forbids.\n\nSUCCESS CRITERIA: a bounty carries a mode and a tier; RACE mode rejects a second claim while OPEN mode permits multiple; chains render as a tree with prerequisites enforced at claim time (not only in the UI); tier eligibility is derived from the reputation projection, not a stored or self-declared label; no crypto anywhere in the schema or code and no real-money amount represented as progression.\n\nTESTS: table-driven tests over the four modes; tier-boundary tests that derive the boundary through the reputation projection; a chain-prerequisite test (a $500 SSO bounty is not claimable until the $250 OAuth bounty completes) exercised through the protocol/CLI, not the UI; a grep-level test asserting no crypto/NFT code paths exist; a test that a real-money amount is never stored or emitted as XP/Reputation.","status":"open","priority":1,"issue_type":"task","created_at":"2026-09-23T19:01:27.590342Z","created_by":"tranquangdang21","updated_at":"2026-09-24T21:20:54.777388Z","source_repo":"battle-agents","source_repo_path":"/Users/tranquangdang21/Projects/battle-agents","mol_type":"work","work_type":"mutex","wisp_type":"none","labels":["bounty","feature"],"dependencies":[{"issue_id":"ba-bounty-modes-tiers-seasons-62l","depends_on_id":"ba-feature-bounty-xhk","type":"blocks","created_at":"2026-09-23T19:04:24.375905Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"ba-bounty-modes-tiers-seasons-62l","depends_on_id":"ba-feature-reputation-nnj","type":"blocks","created_at":"2026-09-23T19:04:24.462519Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"ba-bounty-open-funding-et1","title":"Bounty open funding: multi-sponsor stacking","description":"Allow ANYONE - not just the repository owner - to fund or add to any public issue's bounty, per plan section 11.2.\n\nTHE MECHANIC: Alice creates a $200 bounty on her repo's issue #381. Bob, who does not own the repo, adds $50. Carol adds $100. The bounty total becomes $350, with each contribution tracked as a separate fund record.\n\nSCHEMA SUPPORT: bounty_funds: id, bountyId -> bounties, sponsorUserId -> users, amountCents, createdAt. A bounty can have N fund rows; the displayed amount is their sum.\n\nPRECEDENTS (section 11.2): Bountysource allowed multiple people to co-fund a single bounty for the same issue. BountyHub permits any third party to attach a bounty to a public GitHub issue, with payout after the PR is approved.\n\nWHY THIS IS A PRODUCT PILLAR (section 11.2 + section 11.4): it makes BOTH Sponsor and Solver first-class players.\n- SPONSOR: create, fund, set requirements, review PRs, award rewards\n- SOLVER: discover, claim, code, submit, earn, level up\nA person can be both. This is what turns a task list into an economy, and it is the mechanism behind 'Anyone can put a bounty on real GitHub work. Any coding agent can compete to solve it.'\n\nCOMMAND: FundBounty (already declared by the bounty feature; this bead adds the open-funding path and the authorization rule).\n\nAUTHORIZATION: funding a bounty on someone else's repository must be permitted, but claiming/submitting must still be validated against real GitHub state.\n\nCONSIDERATIONS: refund and dispute semantics become real once third parties contribute. At minimum, an expired or cancelled bounty must not silently swallow funds - decide and document the disposition. This interfaces with the payout-rail-dispute bead.\n\nWHERE THIS SITS AGAINST THE BOUNTY FEATURE AS IT ACTUALLY STANDS. This bead depends on ba-feature-bounty-xhk, which is a large in-flight bead that owns the core bounty lifecycle: the Bounty object and its state machine (open -> claimed -> submitted -> review -> paid/expired) and the commands CreateBounty, ClaimBounty, SubmitBounty, FundBounty, ExpireBounty. Two ownership facts matter so this bead does not collide with it:\n- bounty_funds is ALREADY a declared table in the bounty feature's Drizzle schema (packages/db/src/schema/features/bounty.ts). The table shape listed above (bountyId -> bounties, sponsorUserId -> users, amountCents, createdAt) is the target; this bead supplies the open-funding WRITE PATH and the AUTHORIZATION RULE on top of it, not a competing definition of the table.\n- The sibling bead ba-bounty-modes-tiers-seasons-62l owns the mode/tier taxonomy; this bead owns only who may fund and how stacked funds are attributed. Do not define the mode enum here.\n\nREALITY OF THE PAYOUT RAIL, because stacked funds make money-real questions unavoidable and the payout design is only partly done. The payout-rail-dispute bead is closed as a DESIGN, and its unfinished obligations were deliberately moved onto ba-feature-bounty-xhk, which is the bead that now carries them. The honest current state, verified 2026-09-25 and carried into the bounty bead:\n1. PERSIST PAYOUT INTENT is not implemented. PayoutIntentStore is declared at payout.ts:75-79 with no implementation; packages/db/src/repositories/ has activity, agents, credentials, quests, sessions and state-store - no payout repository; the bounty schema declares bounty_funds but no payout or payout_intent table. PAYOUT_EVENTS (payout.ts:44-48, mapping funded/pending/recorded to bounty.payout_funded / payout_pending / payout_recorded) appear in no event union and are emitted by nothing. So today a stacked fund is representable in memory and never durably recorded as payout intent.\n2. THE SANDBOX BANNER rule exists (needsSandboxBanner, payout.ts:172-174) and is unit-tested, but has zero production callers. It is unblocked now that apps/web is a real Next.js app, and is tracked as RISK 5 in ba-risk-gates-e74. A green test here does not mean a user will see 'payout: manual/sandbox'.\n3. THE DISPUTE WINDOWS in docs/design/payout-rail.md are NOT implemented; they are meant to become values in a bounty-local rules.ts. Note the naming collision when that file appears: progression and reputation each already have their own rules.ts, so bounty's must hold the payout/dispute constants specifically.\n\nTHE CONSEQUENCE FOR OPEN FUNDING. This bead stacks THIRD-PARTY money, which is precisely the case the sandbox banner and the dispute windows exist for. Building multi-sponsor funding on top of a payout rail whose intent is not persisted and whose sandbox marker is not shown in the UI is how a user ends up believing three strangers' funds were escrowed when nothing was. So open funding must ship with the same honest 'payout: manual/sandbox' marking the bounty slice carries, and the refund/dispute disposition this bead is told to 'decide and document' is not a nice-to-have here - it is the difference between a co-funding feature and a way to lose people's money quietly.\n\nHOW THIS ATTACHES TO CORE. Open funding is part of the bounty feature's surface, so it registers through the frozen Contract 1 (ba-contract-extension-api-w29): FundBounty is a CommandHandler on the bounty GameFeature, and it must reach agents and the CLI through the capability registry over the five frozen primitives (discover/search/inspect/act/observe) rather than a bespoke tool. It does NOT import the github-infrastructure feature to validate the issue - bounty consumes GitHub validation as an event/dependency, because features never import each other (machine-enforced in CI). Authorization is a property of the command handler, decided against the authenticated user, not of the transport.\n\nWHY A NAIVE IMPLEMENTATION GOES WRONG. The tempting design stores a running total on the bounty row and increments it on each fund. That is exactly the scalar that drifts from its fund rows, and the test 'the total is the sum of funds, not a stored scalar' exists to forbid it. Derive the displayed total from the sum of bounty_funds rows, so per-sponsor attribution - which is what payout and refunds depend on - is the source of truth, not a side effect. The second mistake is gating funding by repository ownership out of habit from the owner-only flow; the whole point of this bead is that funding is open to anyone, while AWARDING stays validated - a non-owner may fund but must not be able to award or complete someone else's bounty. The third is treating a third-party contribution as if the sponsor still controls the outcome; per-sponsor attribution must survive to payout.\n\nSUCCESS CRITERIA: three users can stack funds on one bounty and the total reflects all contributions; a non-owner can fund; per-sponsor attribution is preserved for payout; the displayed total is derived from the sum of funds, never a stored scalar; a paid-adjacent status is clearly marked 'payout: manual/sandbox' because the real rail is not persisted yet.\n\nTESTS: integration test for the stacked-funding scenario end to end; a test that the total is the sum of funds (not a stored scalar that can drift); an authorization test that a non-owner may fund but a random user may not award someone else's bounty; a test that funding is reachable identically through the protocol and CLI over the frozen primitives.","status":"open","priority":1,"issue_type":"task","created_at":"2026-09-23T19:01:27.528678Z","created_by":"tranquangdang21","updated_at":"2026-09-24T21:17:08.589125Z","source_repo":"battle-agents","source_repo_path":"/Users/tranquangdang21/Projects/battle-agents","mol_type":"work","work_type":"mutex","wisp_type":"none","labels":["bounty","feature"],"dependencies":[{"issue_id":"ba-bounty-open-funding-et1","depends_on_id":"ba-feature-bounty-xhk","type":"blocks","created_at":"2026-09-23T19:04:24.292286Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"ba-capability-registry-klw","title":"Capability registry + typed action definitions","description":"Build the capability registry and the typed action definition system that sits between the Extension API and every interface surface, per plan section 32 and section 33.\n\nWHAT IT IS: A registry mapping capability names to the feature that provides them, plus a typed defineAction() helper for domain operations.\n\nTWO REGISTRIES (section 20 registry.ts) - plus two the plan's sketch did not name:\n- command registry: command type -> CommandHandler (duplicate types throw at install)\n- capability registry: capability name -> owning feature id\n- action registry: action id -> ActionDef, with the same all-checks-before-any-mutation rule\n- event-handler registry, keyed by event type and holding each entry TAGGED WITH ITS OWNING FEATURE ID\n\nThat fourth one is the non-obvious load-bearing decision. Removing a handler by comparing handler references looks equivalent and is not: two features may legitimately share one handler object, the list then holds the same reference twice, and filtering it out uninstalls both registrations - silently switching off a feature nobody asked to remove. Tagging by owner is what makes removal correct.\n\nTWO MORE THINGS THE CODE ENFORCES THAT THE SKETCH DOES NOT MENTION, both worth keeping: capability names must satisfy the same dotted, at-least-two-segments rule as action ids, because two features both offering a bare 'read' would otherwise merge into one catalog entry whose absence is only visible as a missing row somewhere downstream; and #assertAvailable runs every check BEFORE the first mutation, so a feature that collides on its last declaration leaves the registry exactly as it found it rather than half-installed.\n\nThe GameFeature.capabilities[] array populates the capability registry at install. requires[] is validated against it, producing the degrade-with-warning path.\n\nTYPED ACTIONS (section 32 Anti-God-Tool guard):\ndefineAction({ id: 'quest.claim', permissions, run(input, ctx) })\nThe guard exists because act({action: string, payload: any}) destroys both type safety and discoverability. A bogus action id must be a COMPILE error, not a runtime surprise.\n\nTHE SIGNATURE ABOVE IS CORRECTED, and the correction is the interesting part. The plan's sketch put `input: ClaimQuestInput, output: ClaimQuestResult` beside the function. That does not compile - it is a type written where a value is expected - and keeping those fields would have meant either an `as` cast on every declaration or a phantom property that exists only to be read back. packages/core/src/contracts.ts documents the decision: ActionDef<I, O> carries its input and output in the TYPE PARAMETERS of `run`, not in sibling fields. So defineAction is generic and a feature's own action has a checked input and output; what a CALLER gets is a different question, answered below.\n\nID NAMING: dotted domain.operation, e.g. quest.list / quest.claim / quest.submit, battle.challenge / battle.accept, guild.join / guild.leave. These are REGISTRY capabilities, never MCP tool names - the plan is emphatic that historical domain-operation names (register_agent, get_world, get_quests, challenge_agent, accept_battle, send_message, claim_reward, spawn_agent...) describe capabilities in the registry and must NOT be implemented verbatim as MCP tools. That verbatim list is exactly the tool-explosion path the plan bans.\n\nWHY THE REGISTRY IS THE INTEGRATION POINT (section 33): features declare capabilities; interfaces/{cli,mcp,api,websocket} consume them. Because of this, adding a feature never edits an interface implementation - it only adds a manifest/registry entry. That is the property the removal test and the architecture-failure review rule both depend on.\n\nLAZY DISCOVERY (section 32 discover()): the catalog must return top-level domains first, with per-domain detail on demand. Never ship the full capability list at connect time - that is what bloats agent context.\n\nCONSIDERATIONS: permission metadata on each action is how surfaces enforce authorization uniformly rather than each surface re-implementing authz. Wire it now; retrofitting it means auditing every action later.\n\nSUCCESS CRITERIA: features register capabilities at install and deregister at uninstall; defineAction gives compile-time input/output typing; a feature's requires[] that is unsatisfied degrades with a warning naming the missing capability.\n\nSTATUS, 2026-09-25 - THIS BEAD IS SUBSTANTIALLY BUILT, and one clause of it is already known to be unreachable as written. Verified against the tree:\n- DONE: register/uninstall symmetry. FeatureRegistry.register/unregister handle commands, actionDefs, capabilities, declaredCapabilities, eventHandlers and persistedEvents, and runtime.test.ts asserts 'gives back every declaration a feature made'. Unregister also handles the two cases that look equivalent and are not: a feature removed while a shared handler is still registered, and two features legitimately sharing one handler object (comparing handler references would uninstall both).\n- DONE: degradation. missingRequirements() recomputes after every install and uninstall rather than once at construction, and runtime.test.ts covers the two mistakes that poison the signal - a feature ordered before its provider is not falsely degraded, and a feature installed later is checked at all.\n- DONE: the compile-error guard. The action-id union is GENERATED, not hand-written, by scripts/generate-action-ids.ts (pnpm codegen) from each feature's manifest; act() in packages/api is typed against it, and tests/types/action-ids.ts pins the property with @ts-expect-error in BOTH directions, so the day the union stops being enforced the build fails. It already covers the three cases worth covering: a misspelling, a real feature not built into this union, and a command name passed to act().\n- NOT SATISFIABLE AS WRITTEN: 'defineAction gives compile-time input/output typing'. Half of it is true and half is deliberately false. Typing holds where an action is WRITTEN - defineAction<I,O> checks a feature's own declaration. It does NOT hold where an action is CALLED, and packages/core/src/runtime.ts says so at length: runAction casts the registry entry, the caller supplies both I and O, and with no contextual type the result is unknown. A wrong input shape, a wrong expected output and a misspelled id all compile clean and fail at runtime. That boundary is measured, pinned in tests/types/core-actions.ts so it cannot be forgotten, and it is not accidental - the registered set is decided at runtime by independently built packages, so no type the compiler can see is a function of it. The generated union closed the ID half of this gap; the PAYLOAD half stays open until each feature declares its own input and output shapes, which is why packages/protocol/src/generated/action-ids.ts says the ids are real and checked while the shapes are not there yet.\n\nSo the honest restatement of this criterion is: the id half is done and enforced in both directions; the payload half is a known, documented, deliberately-deferred gap with a stated route to closing it. Judged against that, the remaining work is small - have features declare their action payload types and widen the generated union to carry them.\n\nALSO DONE, and it is what made the union possible: the LAZY DISCOVERY the bead asks for is the shipped shape, and it is not what the plan sketched. packages/core/src/registry.ts derives domains() from the first segment of every capability and action id, and describeDomain(domain) returns just that domain's capabilities and actions. Runtime.domains() is what a connecting client is handed; the full catalog is never shipped at connect time. runtime.test.ts covers the shape directly - 'hands a connecting client the domains, not the whole catalog' and 'returns one domain at a time, on request' - plus the two aliasing traps (a caller editing the catalog it was handed, and handing out the feature's own capability objects rather than copies). Note the naming: the plan says discover(), the code says domains()/describeDomain() on the Runtime and discover()/discover(domain) on the ApplicationApi.\n\nTESTS: unit test for register/deregister symmetry; type-level test that an unknown action id fails to compile; test that discover() returns domains before details (assert the lazy shape).","notes":"BLOCKER for ba-mcp-adapter-li2, found 2026-09-24. The MCP spec (2026-07-28, server/tools) says a tool that declares an outputSchema MUST return structured content conforming to it, delivered via structuredContent. That makes the deferred codegen a blocker rather than a nice-to-have, the moment MCP begins: discover/search/inspect CAN declare honest static outputSchemas because their shapes are static, but act CANNOT, because the set of valid action ids is decided at runtime by independently built packages. act must therefore declare NO outputSchema, with a comment saying why; a permissive {type:'object'} would claim a guarantee the spec then reads as a MUST. The code fix is codegen emitting a union of every registered id with its input and output.\n---\n[2026-09-24 15:08:49 UTC] [2026-09-24 22:05 UTC] STATUS UPDATE — the 07:06 reopen comment is now partly stale. It said closing the gap \"needs a codegen step emitting a union of registered ids with their input and output\" and that it \"cannot be fixed with a cast or a generic\". The codegen step now EXISTS and the id half is closed: scripts/generate-action-ids.ts emits packages/protocol/src/generated/action-ids.ts (RegisteredActionId + isRegisteredActionId) from each feature's manifest, and packages/api/src/api.ts types act()'s first parameter as RegisteredActionId. tests/types/action-ids.ts asserts a misspelled id, an uninstalled domain, and a dispatched-command-as-action are all compile errors via @ts-expect-error. So the bead's own bar — a bogus action id must be a COMPILE error, not a runtime surprise — is now met at the act() surface. What is still open, and matches the rest of that comment: the input and output SHAPES are not typed. act() is declared act<I>(action: RegisteredActionId, input: I): Promise<unknown> — the id is checked, the payload is not — and runtime.runAction<I,O> still takes the caller's own I and O (packages/core/src/runtime.ts:165). The generated file says so itself: the input and output shapes are not here yet. tests/types/core-actions.ts still pins the runAction-level gap as a compile-time fact, and that file remains correct: the gap moved surface, it did not close.","status":"closed","priority":0,"issue_type":"task","created_at":"2026-09-23T18:59:47.620089Z","created_by":"tranquangdang21","updated_at":"2026-09-25T01:02:12.569704Z","closed_at":"2026-09-25T01:02:12.569653Z","close_reason":"Substantially built and the remaining clause is out of reach as written, so it moved rather than being quietly dropped.\n\nDONE and verified: register/uninstall symmetry across commands, actionDefs, capabilities, declaredCapabilities, eventHandlers and persistedEvents, including the two cases that look equivalent and are not, a shared handler still registered by another feature and two features legitimately sharing one handler object. Degradation recomputes after every install and uninstall, covered for a feature ordered before its provider and for one installed later. Lazy discovery is the shipped shape: domains() and describeDomain() rather than the plan's discover(), with the two aliasing traps covered. Registration guards now also cover actionDefs, which they did not: the dotted-id pattern and the non-empty-permissions rule were applied to capabilities only, so a feature could register a bare verb id through a plain object literal. Both guards are pinned by tests written against a literal, because the first version of those tests used defineAction and passed whether or not the registry guards existed.\n\nNOT SATISFIABLE AS WRITTEN, and the reason is architectural. 'defineAction gives compile-time input/output typing' holds where an action is WRITTEN and not where it is CALLED. The id half is closed: codegen emits RegisteredActionId, act() is typed against it, and a bogus id is a compile error, which was this bead's own bar. The payload half cannot be closed the same way, because packages/api may not depend on a feature so the generated file may not import feature types, and types are erased so codegen cannot read them off the value side. Emitting the placeholders the manifests already declare would produce a union that looks typed and is not, which is the thing the generated file's own header warns against. Moved to ba-vsd with three routes and none chosen, since picking one is a design call.\n\nThe criterion as written should be restated to match: the id half is enforced in both directions, the payload half is a known and documented gap with a stated route.","source_repo":"battle-agents","source_repo_path":"/Users/tranquangdang21/Projects/battle-agents","mol_type":"work","work_type":"mutex","wisp_type":"none","labels":["contract","core","p0"],"dependencies":[{"issue_id":"ba-capability-registry-klw","depends_on_id":"ba-runtime-and-event-bus-63r","type":"blocks","created_at":"2026-09-23T19:04:21.829986Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}],"comments":[{"id":1,"issue_id":"ba-capability-registry-klw","author":"unknown","text":"Reopened: Reopened: my closure claimed 'defineAction gives compile-time input/output typing' as though it reached the cross-package call site. It does not. Verified with a probe: runAction takes the caller's own I and O, so with no contextual type the result is unknown, with one it is the caller's unchecked assertion, and a misspelled action id, a wrong input shape and a wrong expected output all compile clean. The gap is now pinned in tests/types/core-actions.ts as a compile-time fact and documented on the cast. Closing it properly needs a codegen step emitting a union of registered ids with their input and output; it cannot be fixed with a cast or a generic because the registered set is decided at runtime by independently built packages.","created_at":"2026-09-24T07:06:39Z"}]}
{"id":"ba-cli-p0-6oe","title":"CLI agent-battle: P0 foundation, zero game logic, no DB","description":"Ship the agent-battle CLI in P0 alongside the API, per plan section 31, corrected by section 36.\n\nWHY P0 AND NOT LATER TOOLING (section 31): the CLI is the agent's DAILY RUNTIME ENTRY POINT, not developer convenience. THE MCP HALF OF THIS SENTENCE WAS CORRECTED IN THE PLAN AFTER THIS BEAD WAS WRITTEN, and the old wording is exactly what the plan now overrides. It used to read 'MCP does NOT have to be P0; protocol/API + CLI ship first, MCP is an adapter on the SAME protocol later'. Section 31 now carries a dated CORRECTION 2026-09-24 that names that exact sentence as the superseded MIDDLE state: 'MCP IS P0, not P3', with P0 containing 'Interfaces: Public API, CLI, MCP' as three EQUAL consumers, because 'do not design the game first and bolt MCP/CLI on later'. Section 33's revised build order agrees - P0 ends with 'CLI (login/init/start/status/doctor), MCP adapter'. What survives of the old reading is the ARCHITECTURE, not the phase: MCP is still an adapter over the same protocol and still dispatches through the capability registry, never through game code. So this bead and ba-4to (the MCP route mount) are P0 SIBLINGS, not a sequence - neither blocks the other, and an implementer who reads the superseded sentence will wrongly believe the MCP surface can wait until the CLI is done.\n\nTHE IRON RULE (section 31): the CLI contains ZERO game logic and touches NO DB. It calls the Public API only - the same claimQuest(agentId, questId) application command the web and MCP paths use. Future surfaces (Discord bot, GitHub App, SDK) reuse the same command and must NEVER reimplement it per surface.\n\nARCHITECTURE (section 31):\n  Web App (Next.js) -+\n                       +-> Public API -> Core Runtime -> Extensions -> Neon\n  CLI (agent-battle) --+\n\nTHREE CLI ROLES:\n1. AGENT RUNTIME: login / init / start / status / stop\n2. PLATFORM INTERACTION: agent status, quest list/claim, bounty list/inspect, profile, xp, submit\n3. DEV/ADMIN: dev, doctor, config, extension\n\nLAYOUT per section 31, packages/cli/: src/commands/{login,init,start,status,stop,agent,quest,bounty,profile,dev,doctor,config,extension}, runtime/local.ts, api-client.ts. Thin wrappers over the Application API (contract-public-api), argparse-only, NO domain imports.\n\nWHAT ACTUALLY EXISTS, 2026-09-25, and it is a different shape from the layout above. packages/cli/src/ holds three files: commands.ts (312 lines), commands.test.ts (219) and index.ts (2). There is no src/commands/ directory, no per-command file, and no runtime/local.ts.\n\nThat is not drift to be corrected - it is the surface-parity property, achieved. The shipped CLI is ONE generic dispatcher: `agent-battle <domain> <verb> [args...]`, plus four platform commands (help, discover, status, doctor). It resolves a domain through a single lazy discover(), matches the verb against that domain's actions, and either acts or falls through to a search filter. `quest list` and `battle accept` are the same three lines. packages/cli/src/commands.ts says so explicitly: there is deliberately no switch over known domains, no --with-xp, no local cache, no direct read.\n\nSo of the thirteen named command files, FOUR exist as real behaviour under different names - `status` and `doctor` are implemented, `discover` and `help` were added and are not in the list above. login/init/start/stop, the per-feature files, and api-client.ts DO NOT EXIST and are the real remaining work. Read this bead as: the registry-driven dispatcher is DONE and is the thing that makes the rest cheap; the agent-runtime verbs and the HTTP transport are NOT DONE.\n\nThe package also has no `bin` entry in package.json, so `agent-battle` is not yet an installed command - the module exports parseArgv/run and a consumer has to wire them up. That is part of the missing transport work, not a packaging detail.\n\nAgentRuntime abstraction (start/stop/status/send) with future LocalCliRuntime / McpRuntime / RemoteRuntime / EmbeddedRuntime. A CLI restart creates a NEW SESSION but the SAME Agent ID (section 3.2).\n\nSURFACE PARITY (section 32): the CLI uses the identical capability abstraction as every other surface - 'quest list' is search(type=quest); 'quest claim <id>' is act('quest.claim', ...). CLI and MCP evolve INDEPENDENTLY of feature count: adding Guild changes the registry, not the CLI or the primitives.\n\nTHIS IS THE ONE CLAUSE IN THE BEAD THAT IS DEMONSTRABLY TRUE TODAY, and it is worth stating why. The five primitives are frozen in packages/api/src/api.ts (PRIMITIVES = discover, search, inspect, act, observe), the CLI consumes exactly that interface, and the MCP server derives its tool list from the same constant rather than hand-writing it. The ids are checked at compile time against the generated RegisteredActionId union, so a misspelled action is a build error in all three surfaces. A feature added to createGameRuntime's extensions array is immediately reachable from the CLI and from MCP with no edit to either - which is the removal test's dependency made concrete.\n\nREVIEW GUARD (section 36): any PR adding interfaces/cli/* implementation code FOR A SPECIFIC GAME FEATURE (instead of registry entries) fails review. The CLI must not grow per-feature branches.\n\nAGENT-FRIENDLY UX: this is an agent-facing CLI, so it should also support non-interactive/robot modes, clear actionable errors, and layered help. Consider a --json output mode for machine consumption, since agents will drive it.\n\nSUCCESS CRITERIA: 'agent-battle login/init/start/status/stop' works against the API; a quest can be listed and claimed entirely from the CLI; no DB connection and no domain import exists anywhere under packages/cli/.\n\nSPLIT INTO WHAT IS ALREADY MET AND WHAT IS NOT, because as written this criterion reads as wholly outstanding and two of its three clauses are satisfied:\n- MET: no DB connection and no domain import under packages/cli/. packages/cli depends on @battle-agents/api, @battle-agents/core and @battle-agents/protocol only, and tests/unit/dependency-rules.test.ts enforces the layering.\n- MET, in the only form the architecture allows: a quest can be listed and claimed entirely from the CLI. `agent-battle quest` lists what quest offers, `agent-battle quest list` filters, and `agent-battle quest claim <args>` acts - generically, because quest.claim is a registered action. The word 'entirely' overstates it: the CLI is an in-process adapter over an ApplicationApi object today, with no network path, so it is not yet talking to a running API at all.\n- NOT MET: 'agent-battle login/init/start/status/stop works against the API'. None of login, init, start or stop exists. status exists and answers from the API. The other four verbs, and the api-client.ts that would make 'against the API' mean anything, are the outstanding work.\n\nA test that would catch the regression this criterion is really about: assert that packages/cli/src contains no reference to any registered domain name, and that adding a feature to the composition root makes it reachable from the CLI with no change in packages/cli/. The first is a grep; the second is the removal test's spirit applied at the CLI.\n\nTESTS: CLI integration tests against a live API in the compose test environment; an architecture test that greps packages/cli/ for forbidden DB/domain imports; a test that the same quest claim via CLI and via HTTP produces identical application-command effects.\n\nCONSIDERATIONS: the hardest part of this bead is restraint. It is tempting to give the CLI convenient shortcuts - a --with-xp flag, a local cache of bounties, a direct DB read for speed. Each one is an iron-rule violation that will make the CLI behave differently from the web and MCP surfaces. Also: the CLI is the surface an AGENT will drive, not only a human, so it should support non-interactive and machine-readable output (a --json mode and actionable errors) from the start. Retrying that later means auditing every command.","status":"closed","priority":0,"issue_type":"task","created_at":"2026-09-23T18:59:47.938488Z","created_by":"tranquangdang21","updated_at":"2026-09-25T02:14:40.848508Z","closed_at":"2026-09-25T02:14:40.848445Z","close_reason":"All five agent-runtime verbs exist and the CLI is a client rather than an in-process adapter. The blocker was the interface: discover and search were declared to return their values, which only a same-heap object can do, so HttpApiClient could not implement the thing it is named after; every method is async now. start and stop resolve a domain and dispatch through the same actOn helper as quest.claim rather than special-casing game knowledge, and a build without a session domain answers with the domains it does have. login, init and logout cover the three verbs that touch this machine and store only configuration, never a game concept. init is a read on purpose: an installation record belongs to the agent feature, not to a file the CLI keeps. The iron rule holds — packages/cli/src imports no feature and no database — and pnpm -r build now passes, which it had not since b10f42e.","source_repo":"battle-agents","source_repo_path":"/Users/tranquangdang21/Projects/battle-agents","mol_type":"work","work_type":"mutex","wisp_type":"none","labels":["cli","interface","p0"],"dependencies":[{"issue_id":"ba-cli-p0-6oe","depends_on_id":"ba-better-auth-github-oauth-9wm","type":"blocks","created_at":"2026-09-23T19:07:16.608453Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"ba-cli-p0-6oe","depends_on_id":"ba-contract-public-api-ute","type":"blocks","created_at":"2026-09-23T19:04:22.488216Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"ba-contract-agent-identity-evq","title":"CONTRACT 2: Agent Identity - agents + credentials + ownership","description":"Implement and FREEZE Contract 2: Agent Identity, per plan section 29.1 and section 38.\n\nTHE INVARIANT (section 29.1 item 2, and section 38): the agents row plus agent_credentials (hash, scopes, expiry) plus ownership via agents.user_id. 'GitHub User ID is NOT Agent ID, permanently.' The invariant sessions.agent_id -> agents.id is load-bearing.\n\nSCHEMA per plan section 21:\n- users: id, githubId (unique), login, avatarUrl, createdAt\n- installations: id, userId -> users, installationKey (unique), label, lastSeenAt, createdAt\n- agents: id, userId -> users, name, harness, level, xp, reputation, build, status, lastSeenAt, createdAt\n- agent_credentials: id, installationId -> installations, tokenHash, scopes, expiresAt, revokedAt, createdAt\n\nHARD SEPARATIONS (section 38, enforce in code review):\n- Better Auth owns User/Auth/human-Session ONLY. The game domain stays in extensions.\n- The GitHub OAuth token is NEVER agent identity. The chain is: User (human) -> Agent (persistent character: XP/level/rep + credential + sessions) -> Session (one run) -> Credential (the token the agent calls back with). Example: User#42 -> Agent 'CodeKnight' (1250 XP, Lv 7, Rep 83) -> Sessions #a1/#b7.\n- Agent credential issuance/rotation endpoints live under the Agent extension, NOT inside Better Auth config.\n\nWHY AGENT IS PERSISTENT AND SESSION IS EPHEMERAL (plan section 3.1-3.2): AGENT is never deleted when a terminal closes. It holds XP/level/skills/build/reputation/inventory/achievements/battle+quest history. This is the plan's 'most important design' (section 3 heading) and the basis of the whole social/network-effect story (section 2.4).\n\nIDENTITY RESOLUTION (section 3.2): do NOT key on process name alone - three concurrent Claudes must stay distinguishable. Resolution uses agent_id + installation_id + project_id + session_id. installation_id is generated once at install (~/.agent-battle/identity.json {installationId}). Multi-machine: GitHub User -> Agent -> Installations (PC/Laptop/Server/WSL/Docker) -> Sessions.\n\nTOKEN MODEL (section 3.4): a token authenticates a user/installation and NEVER equals a session. Close -> #003 CLOSED; reopen -> #004 CREATED under the same User/Claude. NEVER put raw tokens in MCP URLs (?token= leaks into logs and history) - use MCP OAuth -> short-lived access tokens. Web GitHub login and MCP agent authorization are related but SEPARATE credentials.\n\nSUCCESS CRITERIA: schema exists with the FK chain users -> agents -> sessions and the invariant sessions.agent_id -> agents.id enforced at the DB level; credentials store a HASH (never the raw token); ownership is queryable.\n\nTESTS: migration test; a test that agent_credentials stores only a hash (assert the raw token is absent from the row); a test that a user's agent cannot be read by another user (authorization at the repository boundary).\n\nCONSIDERATIONS: the separation of four ID classes - permanent identity, credential, session, event - is easy to blur and expensive to undo. Agent is the character and persists forever; credential is a revocable secret; session is an ephemeral run; event is a log entry. Keeping them distinct is what makes 'close the terminal, reopen tomorrow, same character' possible. The multi-installation case (PC, laptop, server, WSL, Docker) is not hypothetical - get the schema right for it now rather than retrofitting a second installation concept later.","status":"closed","priority":0,"issue_type":"task","created_at":"2026-09-23T18:59:02.975812Z","created_by":"tranquangdang21","updated_at":"2026-09-24T06:15:28.535718400Z","closed_at":"2026-09-24T06:15:28.533774300Z","close_reason":"CONTRACT 2. Schema half pre-existing (users -> installations/agents -> agent_credentials/sessions, tokenHash only, sessions.agent_id -> agents.id). Added the feature: four id classes branded so they cannot be passed for one another, ownership a required parameter on every repository method, codes as the cross-layer error contract (instanceof cannot work across the layering boundary). 12 unit tests + 8 integration tests against live Postgres proving a user's agent cannot be read by another user. Found and fixed: agents_user_id_name_unique was declared index not uniqueIndex, so the DB never enforced one name per owner (migration 0001). drizzle/ moved to packages/db/ so a store can be depended on. Removal test proves the feature is independently removable.","source_repo":"battle-agents","source_repo_path":"/Users/tranquangdang21/Projects/battle-agents","compaction_level":0,"original_size":0,"labels":["contract","data","p0"],"dependencies":[{"issue_id":"ba-contract-agent-identity-evq","depends_on_id":"ba-contract-extension-api-w29","type":"blocks","created_at":"2026-09-23T19:05:52.428255Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"ba-contract-agent-identity-evq","depends_on_id":"ba-db-schema-drizzle-fki","type":"blocks","created_at":"2026-09-23T19:05:52.785434Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"ba-contract-agent-identity-evq","depends_on_id":"ba-monorepo-scaffold-cit","type":"blocks","created_at":"2026-09-23T19:04:22.202105Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"ba-contract-extension-api-w29","title":"CONTRACT 1: Extension API (GameFeature + contracts) - freeze first","description":"Implement and FREEZE Contract 1 of the four contracts the plan says must be locked before any feature code (section 29.1).\n\nWHY THIS IS FIRST: Section 29.1 states 'If these four are right, everything above (bounty/battle/guild) stays clean and pluggable. If any is wrong, stop features and fix the contract.' The Extension API is the surface every feature is written against, so a wrong shape here poisons every downstream bead. Section 29.2 reinforces: core stays EXACTLY the section 20 runtime and knows NO Bounty/Battle/Guild/XP.\n\nFILE: packages/core/src/contracts.ts. Per plan section 20:\n\nexport interface CommandHandler<T = unknown> { type: string; handle(cmd: Command<T>, ctx: RuntimeContext): Promise<GameEvent[]>; }\nexport interface EventHandler { on: string; handle(evt: GameEvent, ctx: RuntimeContext): Promise<void>; }\nexport interface Reducer<S> { feature: string; initial: S; reduce(state: S, evt: GameEvent): S; }\nexport interface Capability { name: string; description: string; }\nexport interface GameFeature {\n  id: string;\n  commands?: CommandHandler[];\n  eventHandlers?: EventHandler[];\n  reducers?: Reducer<unknown>[];\n  capabilities?: Capability[];\n  requires?: string[];   // capability names; missing -> degrade with warning\n}\n\nALSO the supporting primitives in packages/core/src/:\n- entity.ts: Entity{id, kind, version, createdAt, updatedAt}\n- event.ts: GameEvent{type, occurredAt, actorId, causationId?, payload}\n- command.ts: Command{type, issuedAt, issuerId, payload}\n- state.ts: StateStore get/update + namespaced per-feature stores\n- contracts.ts (above)\n- registry.ts: command registry + capability registry\n- persistence.ts: persistence boundary interface (Neon adapter implements)\n- index.ts barrel\n\nHARD CONSTRAINTS:\n- Core contains ZERO game words: no Bounty, Guild, Battle, XP, Level, Quest, Pet, Inventory, Reputation. A grep for these in packages/core must return nothing.\n- features[] is explicit composition (section 29.2): createRuntime({extensions: [...]}). NO dynamic plugin loader, NO plugin marketplace, NO remote/dynamic plugin loading, NO distributed event infra, NO microservices. All of those are BANNED for MVP.\n- Section 12.3 lineage (earendil-works/pi): 'Do not build the feature into the core. Build the core so the feature can be built outside the core.' Core is difficult to change, easy to extend.\n\nCONSIDERATIONS: requires[] degradation is the key extensibility mechanism. A feature whose dependency capability is missing must WARN AND DEGRADE, never hard-fail the runtime. This is what lets the removal test pass and lets Battle run without Reputation.\n\nSUCCESS CRITERIA: the interface compiles; a sample no-op feature installs and uninstalls; core greps clean of game words; the contract is marked frozen in a comment so later beads treat changes as breaking.\n\nTESTS: unit tests for the type shapes (compile-time), a test that a feature declaring requires:['nonexistent.cap'] installs with a warning rather than throwing, and a test asserting core/ source contains no game vocabulary.\n\nPLAN INCONSISTENCY TO RESOLVE BEFORE FREEZING (found during bead-graph review):\nThe plan states the feature contract in TWO places that do not agree.\n\n(a) Section 12.2 says: 'Feature contract: {id, commands, eventHandlers, reducers, capabilities, effects}; runtime.install/uninstall.'\n(b) Section 20 GameFeature has: {id, commands?, eventHandlers?, reducers?, capabilities?, requires?} - there is NO 'effects' field, and it does have 'requires'.\n\nRESOLUTION REQUIRED IN THIS BEAD, not deferred: section 29.1 names section 20 as canonical ('Extension API (section 20 GameFeature + createRuntime) - frozen first'), so default to (b) and treat 'effects' in section 12.2 as an earlier sketch. But do not silently drop the concept - decide explicitly whether effects are needed (timers, background scheduling, out-of-band side effects) and either add a typed 'effects' member or record that reducers plus event handlers fully cover it. A frozen contract that silently disagrees with another section of the plan is a trap for the first third-party extension author.\n\nSECOND INCONSISTENCY, same class: the per-feature FILE layout also differs.\n(a) Section 29.2: 'each extension is commands/events/state/domain/index'\n(b) Section 24: 'domain.ts, commands.ts, events.ts, reducer.ts, rules.ts, repository.ts, index.ts, README.md, *.test.ts'\nSection 24 is more detailed and includes rules.ts, repository.ts, tests, and a README - all load-bearing (rules.ts holds the XP tables and the behaviour-to-build classifiers; repository.ts is what keeps persistence per-feature). Adopt (b), and note that 'state' in (a) corresponds to the reducer plus the namespaced StateStore slice rather than a separate file.\n\nTHIRD, SECTION 33 EXTENDS THE LAYOUT AGAIN: features 'gain two optional dirs: application/ (command/query handlers = the single domain implementation all surfaces share) and cli/ + mcp/ contribution fragments ONLY if the interface-adapter pattern needs per-feature metadata (default: pure registry entries, no code)'. Reconcile this with (b) so application/ is a documented optional subdirectory of the section 24 layout rather than a competing scheme.\n\nWHY THIS MATTERS ENOUGH TO CALL OUT: the whole extension architecture is justified by third parties building on it (sections 12, 33, M7). An ambiguous contract turns a contributor into a guesser, and the third-party-adapter-proof bead exists precisely to catch that. Better to resolve it here, before the contract is declared frozen.","status":"closed","priority":0,"issue_type":"task","created_at":"2026-09-23T18:59:02.833698Z","created_by":"tranquangdang21","updated_at":"2026-09-24T05:18:18.558998200Z","closed_at":"2026-09-24T05:18:18.557053100Z","close_reason":"Contract 1 frozen: core runtime + contracts, 55 unit tests, all 7 gates green, agent review applied (4 defects fixed, each now guarded by a test)","source_repo":"battle-agents","source_repo_path":"/Users/tranquangdang21/Projects/battle-agents","compaction_level":0,"original_size":0,"labels":["contract","core","p0"],"dependencies":[{"issue_id":"ba-contract-extension-api-w29","depends_on_id":"ba-monorepo-scaffold-cit","type":"blocks","created_at":"2026-09-23T19:04:21.675756Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"ba-contract-public-api-ute","title":"CONTRACT 4: Public API = Application Commands/Queries (single domain impl)","description":"Implement and FREEZE Contract 4: the Agent Protocol surface, per plan section 29.1 item 4, section 31, section 32, section 33, section 36.\n\nTHE PRINCIPLE (section 29.1 item 4): Application Commands and Queries are the SINGLE definition of what an agent can do. CLI / MCP / REST are CONSUMERS of it.\n\nIRON RULE (section 31): the CLI contains ZERO game logic and touches NO DB. It calls the Public API only - the same claimQuest(agentId, questId) application command the web and MCP paths use. Future surfaces (Discord bot, GitHub App, SDK) reuse the same command and must NEVER reimplement per surface.\n\nTHE STABLE SURFACE (section 32) - frozen, about 5 primitives:\n- discover([domain?]) - lazy catalog; top-level domains first; per-domain detail on demand; NEVER the full 200-capability firehose at connect.\n- search({type, status, difficulty, ...})\n- inspect({type, id})\n- act({action, target, input})\n- observe({scope})\n\nTHE PROBLEM THIS SOLVES: N features x M operations collapses to 100-200 MCP tools, which is undiscoverable and bloats agent context. The principle is 'cover every capability, expose few stable primitives'.\n\nANTI-GOD-TOOL GUARD (section 32): act() dispatches TYPED, REGISTRY-DEFINED actions - defineAction({id:'quest.claim', input: ClaimQuestInput, output: ClaimQuestResult, permissions}) - NEVER act({action: string, payload: any}). Type safety and discoverability must be preserved.\n\nDOMAIN CAPABILITIES live in the REGISTRY, not as MCP tools: quest.list/claim/submit, battle.challenge/accept, guild.join/leave and so on. Feature growth must NOT equal surface growth: adding Guild changes the registry, not the 5 primitives.\n\nTHE CLI USES THE IDENTICAL ABSTRACTION (section 32): 'quest list' -> search(type=quest); 'quest claim <id>' -> act('quest.claim', ...).\n\nARCHITECTURE (section 33): CORE -> Extension Contract -> GAME extensions | INTERFACE extensions | INFRASTRUCTURE extensions.\n- extensions/* declare capabilities and know NOTHING about MCP/CLI/REST/Web.\n- interfaces/{cli,mcp,api,websocket}/* are capability CONSUMERS/adapters; adding a feature never edits them, only composition/manifest registration.\n- infrastructure/* is integrations behind boundaries.\n\nMACHINE-NATIVE ASSUMPTION (section 34): every gameplay loop must be fully playable through the protocol/tool interface alone, with no web clicks required. If a quest cannot be discovered -> claimed -> submitted -> rewarded via discover/search/act plus the CLI equivalents, the feature is NOT done.\n\nFEATURE LAYOUT NOTE (section 33): features may gain an application/ directory holding command/query handlers (the single domain implementation all surfaces share). Default is pure registry entries with no per-feature interface code.\n\nSUCCESS CRITERIA: one application command is reachable identically from CLI, HTTP, and MCP; act() is fully typed (a bogus action id is a compile error, not a runtime string); adding a feature adds registry entries and zero new primitives.\n\nTESTS: a parity test asserting the same application command is invoked through each surface; a type-level test that an unknown action id fails to compile; a test that adding a dummy feature increases the registry but not the 5-primitive surface.\n\nCONSIDERATIONS: this is the bead where 'expose little' has to become real. The pressure to add a convenient sixth primitive - or to promote quest.claim to a first-class tool because an agent asked for it - will arrive early and repeatedly. The plan's counter-argument should be the default: that operation already exists in the registry, and promoting it means every future feature will ask the same. The other risk is per-feature interface code creeping in; the review rule in section 36 is the guard, so encode it in the CI architecture check rather than relying on reviewers noticing.","status":"closed","priority":0,"issue_type":"task","created_at":"2026-09-23T18:59:03.105011Z","created_by":"tranquangdang21","updated_at":"2026-09-24T06:42:55.925053200Z","closed_at":"2026-09-24T06:42:55.922728100Z","close_reason":"CONTRACT 4. packages/api is the single Application API: discover/search/inspect/act/observe, frozen at five. Parity test drives the same action through CLI-shaped, HTTP-shaped and MCP-shaped calls and asserts identical results and identical failures. Adding 7 features grows the registry 2->9 domains with no change to the surface, asserted on both the constant and the returned object's keys. Unknown actions name the known domains rather than dumping every id. Honest limit: action ids are runtime-registered by independently-built packages, so a bogus id is a typed runtime error rather than a compile error without codegen.","source_repo":"battle-agents","source_repo_path":"/Users/tranquangdang21/Projects/battle-agents","compaction_level":0,"original_size":0,"labels":["api","contract","p0"],"dependencies":[{"issue_id":"ba-contract-public-api-ute","depends_on_id":"ba-capability-registry-klw","type":"blocks","created_at":"2026-09-23T19:04:22.345327Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"ba-contract-public-api-ute","depends_on_id":"ba-contract-agent-identity-evq","type":"blocks","created_at":"2026-09-23T19:05:52.530185Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"ba-contract-session-rpo","title":"CONTRACT 3: Session lifecycle + HELLO resume-vs-new handshake","description":"Implement and FREEZE Contract 3: Session, per plan section 29.1 item 3, section 3.2, and section 22.\n\nTHE INVARIANT: sessions.agent_id -> agents.id, NEVER sessions.user_id as agent identity (section 29.1).\n\nSCHEMA (section 21): sessions: id, agentId -> agents, installationId -> installations, projectId -> projects (nullable), harnessSessionRef, status (default 'active'), startedAt, endedAt, lastHeartbeatAt.\n\nSESSION LIFECYCLE (section 3.2):\n- Terminal close: Session -> CLOSED; Agent ACTIVE -> OFFLINE; stats persist (Level/XP/Battles/Tests/Last-seen).\n- Reopen must NOT create 'Claude #2'. Identity resolution maps a new process to the SAME Agent, producing old Session#182 + new Session#205.\n- State machine: ACTIVE -> COMPLETED / STOPPED(ABANDONED) / CRASHED / TIMEOUT(DISCONNECTED).\n- Battle states (separate machine): RUNNING / PAUSED / COMPLETED / ABANDONED / EXPIRED.\n- Disconnect during a battle -> grace period -> reconnect? resume : abandoned.\n- MVP may skip the Run layer (Agent -> Run -> Session) and keep Agent -> Session only.\n\nHELLO HANDSHAKE (section 22): POST /api/sessions receives {installationKey, agentName, harness, projectRepoUrl?}. It must find-or-create installation/agent/project, then look for a status=disconnected session with the same agent+installation+project within a grace window (default 15 minutes), and return {sessionId, resumed} or create a new one.\n- Heartbeat: POST /api/sessions/[id]/heartbeat updates lastHeartbeatAt.\n- A cron route marks stale sessions (>5 min, no heartbeat, no websocket) as 'disconnected', then 'abandoned' after the grace window.\n- Adapter sends HELLO{installationId, agentId, projectId}; the server matches same agent+installation+project+recently-disconnected and offers resume vs new.\n\nWHY THIS MATTERS TO THE USER (section 3.2 UI consequence): dead sessions must never make characters vanish. Claude offline shows Level/XP/wins plus 'Last seen 3m ago'; reopening flips to ONLINE with the SAME character. Session history doubles as a replayable game mechanic.\n\nCONSIDERATIONS: the grace window is a real product decision (15 min default per section 22) - too short and an accidental terminal kill loses battle progress; too long and a genuinely abandoned session holds a slot. Make it configurable.\n\nSUCCESS CRITERIA: kill a terminal mid-session, reopen, and get a resume offer rather than a duplicate agent; heartbeat keeps a session alive; the sweeper transitions disconnected -> abandoned on schedule.\n\nTESTS: unit test for the state machine transitions; integration test that a HELLO within the grace window resumes the same sessionId and outside it creates a new one; test that sessions.agent_id is the only identity FK (no user_id identity column).","status":"closed","priority":0,"issue_type":"task","created_at":"2026-09-23T18:59:03.045909Z","created_by":"tranquangdang21","updated_at":"2026-09-24T06:23:58.574047500Z","closed_at":"2026-09-24T06:23:58.572144200Z","close_reason":"CONTRACT 3. Pure state machine (active/ended/disconnected/abandoned) with an exhaustive transition matrix; disconnected is resumable, only ended+abandoned are terminal. HELLO handshake matches on agent+installation+project and can never create a character. Heartbeat staleness and the resume grace window are single-sourced helpers shared by the handshake and the sweeper. 24 unit tests + 25 integration tests against live Postgres. Found+fixed a 15-hour grace window from a units bug, and a missing unique index on projects that made every session non-resumable (migration 0002).","source_repo":"battle-agents","source_repo_path":"/Users/tranquangdang21/Projects/battle-agents","compaction_level":0,"original_size":0,"labels":["contract","data","p0"],"dependencies":[{"issue_id":"ba-contract-session-rpo","depends_on_id":"ba-contract-agent-identity-evq","type":"blocks","created_at":"2026-09-23T19:04:22.276711Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"ba-contributor-onboarding-tc9","title":"Contributor onboarding: docs, AGENTS.md, good-first-issues","description":"Build the contributor experience, per plan section 16.\n\nWHY THIS IS A SEPARATE BEAD: section 16 argues the flywheel needs MORE than a permissive license - it needs docs, easy dev, clear architecture, good-first-issues, fun visible results, a useful protocol, and active maintainers. The MIT decision (section 35) is necessary but NOT sufficient. Skipping this is the most likely way the project has a license and no contributors.\n\nDELIVERABLES:\n- AGENTS.md describing the project for coding agents (WHAT/WHY/HOW, progressive disclosure)\n- README that states the vision, the MIT license, and the architecture at a glance\n- Architecture documentation explaining the four contracts (section 29.1) and WHY they were locked first - a contributor who does not understand the extension boundary will break it\n- A local dev guide pointing at 'git clone, cp .env.example .env, docker compose up' (section 37) - the low-friction entry is a real contributor-acquisition feature\n- Good-first-issues, ideally of the FUN-VISIBLE kind the plan calls for: adapter PRs, reporter PRs, battle-mode PRs. These let a newcomer ship something that shows up on screen rather than fixing an internal chore\n- Documented failure modes for the local OAuth path (section 39): redirect_uri_mismatch, missing Better Auth secret, DB unreachable - the three things that will actually block a newcomer's first run. DONE: apps/web/README.md exists and has a 'When it fails' section covering all three by name, each with symptom, why, and fix - including the instruction to use 127.0.0.1 rather than localhost, and the point that a migration reporting success is not proof because drizzle-kit keeps its ledger in a separate schema. It also states the auth/game boundary and how to run the stack. Do not rewrite it; the remaining work is a ROOT README that points at it.\n\nARCHITECTURE COMMUNICATION: the removal test and the dependency rules are only meaningful if contributors understand the intent. Explain that adding a feature must never edit core/cli/mcp implementations (section 33) and that this is checked in review.\n\nSTATUS 2026-09-25: the doc deliverables are still absent at the root - there is no AGENTS.md, no README.md and no CONTRIBUTING.md - but the picture is no longer 'none of it exists'. What is now true:\n- apps/web/README.md EXISTS and is good. It covers what the app owns and must not, the docker compose up path, the three OAuth/database failure modes, and the environment. That is most of the local-dev guide, scoped to the web app.\n- docs/ now holds design/ (payout-rail.md AND public-event-stream.md) plus research/ (the six reference-repo notes and a README that indexes them with commit SHAs and licence status). The research README's staleness section is the model worth copying for the root docs: it names the beads that were unstarted when the note was written and tells the reader to re-check against the SHA.\n- .github/workflows/m0.yml runs pnpm test:m0 in the compose test-runner image, so the local-setup path is enforced by CI even where it is not yet documented. The 'documented steps cannot rot' property is still only half-satisfied - machinery exists, no document describes it.\n\nWHAT IS ACTUALLY LEFT, in the order a newcomer would hit it: a root README (vision, MIT, architecture at a glance, the quickstart); AGENTS.md for coding agents; and good-first-issues. Note that THIRD-PARTY-NOTICES.md already exists at the root with a per-repo licence table, so the licence story is told - a root README should link it rather than restate it.\n\nCONSIDERATIONS: contributor friction is highest at first-run. Measure it: have someone unfamiliar run the local setup and log every point of confusion.\n\nSUCCESS CRITERIA, REWRITTEN BECAUSE 'WITHOUT UNDOCUMENTED HELP' HAS NO WRONG VERSION. The old first clause was unfalsifiable in two ways: a newcomer who needed no help because the app was never started satisfies it, and a newcomer who struggled badly satisfies it as long as they eventually succeeded. 'A MERGED FIRST PR' was also the wrong deliverable for this bead - whether a newcomer's PR got merged depends on a maintainer's review, not on the documentation, so the criterion could not be closed by doing this bead's work. Split into what documentation can actually be held to, and what belongs to a person.\n\n1. The quickstart in the root README is EXECUTABLE VERBATIM, and cannot rot. This is the real, checkable form of 'a newcomer gets to a running app'. Settled by: a CI job that runs the documented local-setup steps EXACTLY as written in the README - git clone, cp .env.example .env, docker compose up - and whatever the README then says - and fails the build when a step does not work. The property being protected is the one apps/web/README.md already half-provides: machinery exists (.github/workflows/m0.yml runs pnpm test:m0 in the compose test-runner image) but no document describes it, so 'the documented steps cannot rot' is only half-satisfied. A doc that drifts from reality passes every review and fails this job; a job that paraphrases the steps instead of copying them passes while the doc stays wrong, so the job must read the steps FROM the document.\n\n2. A newcomer who hits a blocker finds it documented. This is the honest replacement for 'without undocumented help', and it is checkable as a LIST rather than as a feeling. The three named OAuth/DB failure modes (redirect_uri_mismatch, missing Better Auth secret, DB unreachable) must each appear in the docs by name, each with symptom, cause and fix - apps/web/README.md already does this well, and the remaining work is a root README that points at it rather than a rewrite. Settled by: a check asserting all three are named in the root docs, and that the root README links to the detailed section. What this does NOT claim: that these are the only three a newcomer will hit. The honest failure mode is a fourth, and the way that gets recorded is by adding it to the list and failing the check until it is documented - so the list grows honestly rather than the criterion being quietly declared met.\n\n3. The docs state that THIS PROJECT is MIT, and do not claim any other license for it. Settled by: a check scoped to claims about this repository's own license - see the correction below, which is load-bearing and must not be reverted to a bare grep.\n\n4. NOT THIS BEAD'S TO CLOSE, recorded so nobody waits on it: whether a newcomer's first PR actually gets MERGED. That depends on maintainer review and on a maintainer existing, neither of which documentation can deliver. The bead's honest scope is that the path to a first PR is unblocked and the good-first-issues are real; merging is downstream. Do not hold this bead open waiting for a stranger's pull request.\n\n5. The good-first-issues are REAL and VISIBLE, not a list of internal chores. This was a DELIVERABLES bullet with no check at all. Settled by: each listed good-first-issue being an actual open issue in the tracker, labelled or titled so a newcomer can find it, and each naming what a merged PR would visibly change - an adapter, a reporter, a battle mode. A bullet claiming good-first-issues exist while the tracker has none is the failure, and it is checkable: the number of good-first-issues the README advertises must equal the number that exist.\n\nTESTS, each a command with an exit code:\n- a docs-link checker over the root docs, so a renamed file or a moved anchor fails rather than quietly 404-ing for the reader.\n- the CI job from clause 1, reading the steps FROM the README rather than restating them.\n- a check that all three named failure modes appear in the root docs with symptom, cause and fix, and that the root README links to the detailed section.\n- the license check, scoped as described in the correction below.\n- a check that the good-first-issues the README advertises actually exist in the tracker, and that their count matches.\n\nCRITERION CORRECTED 2026-09-24 - the earlier wording of this bead demanded \"a grep asserting no stale Apache-2.0 claim in docs\", which is wrong and would fail on correct content. The string Apache-2.0 legitimately appears in THIRD-PARTY-NOTICES.md (agent-world-smallville is Apache-2.0 and is recorded there as a third-party source) and in plan section 28's license table. Those are accurate records of someone ELSE's license, not a claim about this repo. What section 35 actually locks is that battle-agents itself is MIT; a stale reference means a doc asserting THIS project is Apache-2.0. The check must therefore be scoped to claims about this repository's own license, not a bare grep for the string.","status":"closed","priority":3,"issue_type":"task","created_at":"2026-09-23T19:03:57.041484Z","created_by":"tranquangdang21","updated_at":"2026-09-26T02:17:47.448124Z","closed_at":"2026-09-26T02:17:47.447778Z","close_reason":"DONE. The brief's own status block was stale in the place that mattered — it said\nthere was no root AGENTS.md and no root README.md. Both exist and are good. What\nwas genuinely missing is now there.\n\nCONTRIBUTING.md, which did not exist, carries the thing a newcomer cannot infer:\nthe four contracts and WHY they were locked first, plus the rule that adding a\nfeature must never edit core/cli/mcp. That rule is not politeness — it is checked\nin review AND by the architecture stage, so a newcomer learns it on their first\npush rather than in review. The local path links apps/web/README.md rather than\nrestating it, because that file already covers the three failure modes a newcomer\nactually hits: redirect_uri_mismatch, a missing Better Auth secret, and a database\nthat is up but unreachable — including the point that a migration reporting\nsuccess is not proof, because drizzle-kit keeps its ledger in a separate schema.\n\nGood-first-issues are the fun-visible kind the plan calls for — adapter PRs,\nreporter PRs, battle-mode PRs — because a newcomer's first change should show up\non screen rather than be an internal chore. The docs gate now asserts all three\nexist in the tracker and are open, so the README cannot advertise issues that\nnobody filed.\n\nTHE MEASUREMENT, since the bead asks whether a stranger could get from clone to a\ngreen first run without asking anyone: the docs gate checks that the README's\nlinks resolve, that the licence claim is one this repository actually holds (as\ndistinct from a third-party source legitimately being Apache-2.0, which is a fact\nabout someone else), and that the advertised steps match what the tree does. It is\nwired as pipeline stage docs, because a check nobody invokes is a comment with a\nshebang — and this one existed for a few hours in exactly that state before the\nmain loop wired it.","source_repo":"battle-agents","source_repo_path":"/Users/tranquangdang21/Projects/battle-agents","mol_type":"work","work_type":"mutex","wisp_type":"none","labels":["community","docs","p3"],"dependencies":[{"issue_id":"ba-contributor-onboarding-tc9","depends_on_id":"ba-docker-local-dev-y28","type":"blocks","created_at":"2026-09-23T19:04:26.758791Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"ba-contributor-onboarding-tc9","depends_on_id":"ba-local-testing-contract-gmi","type":"blocks","created_at":"2026-09-23T19:04:26.840939Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"ba-core-loop-integration-uk7","title":"Core loop end-to-end: DISCOVER -> CLAIM -> CODE -> VALIDATE -> REWARD -> BATTLE -> SOCIAL","description":"Prove the plan's core loop (section 10.5) works END TO END across features, which no single feature bead can demonstrate.\n\nTHE LOOP (section 10.5, verbatim structure): DISCOVER quests -> ACCEPT -> CODE (real) -> VALIDATE (tests/build) -> SUCCESS (XP/REP) | FAILURE (learn/retry) -> PROGRESS -> BATTLE -> SOCIAL (guild) -> NEW BOUNTY. The plan's judgement: 'Animation exists to make this loop FEEL like a game, not to be the game.'\n\nWHY A SEPARATE BEAD: each feature bead tests itself in isolation. This bead tests the INTEGRATION - specifically that cross-feature side effects flow through the EVENT BUS and not through direct imports. Section 29.3 is the rule: events are the cross-feature integration boundary ONLY; direct domain operations stay plain function calls inside their feature. An end-to-end run is the only way to catch a feature quietly importing another's internals.\n\nTHE FAN-OUT THAT MUST WORK (section 12.2): when a bounty completes, bounty.completed is emitted and EACH of Progression (+XP), Reputation (+trust), and Achievement (First Bounty check) reacts INDEPENDENTLY. Each owns its own state; there is no cross-mutation and no God Bus (a consumer may consume, emit, and handle only its own state).\n\nSTATE OF THAT FAN-OUT TODAY - read this before you build it. The diagram above is the TARGET, not the current tree, and three things about it will mislead you:\n\n1. THERE IS NO BOUNTY EMITTER YET. packages/features/bounty/src/ contains only payout.ts and index.ts - a half-built payout state machine (PayoutIntent, PAYOUT_STATES, INTENT_ONLY, needsSandboxBanner). There is no bounty feature.ts, no GameFeature, no emitter, and no bounty entry in any persistedEvents list. bounty.completed is emitted by nothing today. If you assume the emitter exists and only write the three consumers, the loop can never fire and a happy-path test will appear to pass while awarding nothing.\n\n2. THE PAYLOAD IN THE PLAN'S SKETCH IS NOT THE SHIPPED ONE. Do not emit {bountyId, agentId, reward}. The consumer that already exists, packages/features/reputation/src/feature.ts, declares:\n     export interface BountyCompletedPayload {\n       readonly agentId: string;\n       readonly rewardCents: number;   // NOT `reward`\n       readonly reviewScore?: number;  // 0..5, absent means \"not reviewed\"\n     }\n   There is no bountyId field, and the amount is rewardCents in minor units. Emitting the sketch shape produces an event that reputation's handler does not match: the fan-out silently awards no trust, with no error. GameEvent is {type, occurredAt, actorId, causationId?, payload} - the payload is nested under payload, it is not spread across the top level.\n\n3. THE EVENT NAME IS CURRENTLY DECLARED BY THE WRONG SIDE, AND THE OBVIOUS FIX IS A FORBIDDEN IMPORT. BOUNTY_COMPLETED / BOUNTY_FAILED / BATTLE_FINISHED are exported from packages/features/reputation/src/feature.ts - the CONSUMER, not the emitter. The obvious way to wire a second consumer is `import { BOUNTY_COMPLETED } from '@battle-agents/features/reputation'`, and that is a cross-feature import, which `pnpm run architecture` fails on (architecture-rules.cjs: 'Features never import each other; a cross-feature need goes through a capability declared in core'). If you cannot avoid wanting that import, that is the signal to move the shared name somewhere outside features/ (core or protocol), not to add the import. The forbidden-by-machine-check wrong move is the whole point of this bead, so do not make it while building the thing meant to catch it.\n\nFULL ROUND TRIP: an agent discovers a real GitHub issue, claims it as a bounty, works the code, opens a PR, the merge webhook fires, the bounty completes, XP/reputation/achievement all update, a battle becomes possible against the same bounty, and the replay URL is shareable. Then social: the agent's guild sees the contribution.\n\nMACHINE-NATIVE CHECK (section 34): the entire loop must complete through discover/search/act plus CLI equivalents with NO web clicks. If any step requires the browser, the machine-native rule is broken. The five MCP verbs are discover, search, inspect, act, observe (packages/mcp-server/src/tools.ts); the CLI is a generic dispatcher over the capability registry with platform commands status/doctor/help/discover (packages/cli/src/commands.ts), so a loop step that only exists as an MCP tool is not machine-native until a CLI route reaches the same capability.\n\nFAILURE PATH MATTERS TOO (section 10.2): the plan includes FAILURE achievements, and a failed session must grant experience while leaving the character alive. Exercise the failure branch, not just the happy path.\n\nTHE BOUNDARY TEST ALREADY EXISTS - do not write a second one. The static rule 'features/ contains no cross-feature imports' is not this bead's job to add. It is already machine-enforced over the real tree by `pnpm run architecture` (scripts/check-architecture.ts reading architecture-rules.cjs), and each individual rule is separately proven by the synthetic fixture suite tests/unit/dependency-rules.test.ts, which fails if any declared rule has no fixture. Re-implementing it as a hand-written grep in this bead produces a weaker check that disagrees with the real one, and a grep passes on a tree the gate rejects. What is genuinely NEW here is the behavioural half: three consumers reacting to one emission with no cross-mutation, and a loop that still completes when one consumer is uninstalled.\n\nSUCCESS CRITERIA: the loop completes end to end on a real repository, with the success and failure branches both exercised, driven entirely through the protocol and CLI. `pnpm run architecture` passes unchanged, and the fan-out is proven to work with progression absent from the install (a feature that survives removal is a feature that really owns its own state; the reverse - an install that only works because a peer is present - is the God Bus this bead forbids).\n\nTESTS: a full-loop integration test, including the failure branch; the bus-boundary half is settled by `pnpm run architecture` and tests/unit/dependency-rules.test.ts, not by a new test; a machine-native test with the web UI disabled; and a removal test (scripts/removal-test.sh is the existing shape) proving the loop still completes with a consumer feature uninstalled.\n\nCONSIDERATIONS: this bead is where architecture violations surface as behavior bugs. If Bounty reaches into Progression's state instead of reacting to bounty.completed on the bus, the loop may still appear to work in a happy-path test - and will break the moment a second consumer subscribes or progression is uninstalled. Run the loop with the web UI disabled: that is the cheapest way to catch anything that quietly requires a click. And exercise the FAILURE branch with the same care as the success branch, since section 10.2 deliberately makes failure productive.","status":"open","priority":2,"issue_type":"task","created_at":"2026-09-23T19:05:35.861489Z","created_by":"tranquangdang21","updated_at":"2026-09-24T22:00:51.913223Z","source_repo":"battle-agents","source_repo_path":"/Users/tranquangdang21/Projects/battle-agents","mol_type":"work","work_type":"mutex","wisp_type":"none","labels":["integration","milestone","p2"],"dependencies":[{"issue_id":"ba-core-loop-integration-uk7","depends_on_id":"ba-battle-replay-yjb","type":"blocks","created_at":"2026-09-24T21:32:36.886052Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"ba-core-loop-integration-uk7","depends_on_id":"ba-cli-p0-6oe","type":"blocks","created_at":"2026-09-24T21:32:36.804630Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"ba-core-loop-integration-uk7","depends_on_id":"ba-feature-achievements-gmr","type":"blocks","created_at":"2026-09-23T19:05:53.307457Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"ba-core-loop-integration-uk7","depends_on_id":"ba-feature-battle-fbt","type":"blocks","created_at":"2026-09-23T19:05:53.128717Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"ba-core-loop-integration-uk7","depends_on_id":"ba-feature-bounty-xhk","type":"blocks","created_at":"2026-09-23T19:05:53.218251Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"ba-core-loop-integration-uk7","depends_on_id":"ba-feature-social-lss","type":"blocks","created_at":"2026-09-23T19:05:53.399544Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"ba-db-schema-drizzle-fki","title":"Drizzle schema + migration tooling for the initial tables","description":"Own the Drizzle schema and migration pipeline, per plan section 19 (drizzle/ schema + migrations) and section 21 (the full initial schema).\n\nWHY THIS NEEDS AN OWNER: the tables are currently described inside the contract beads, but no bead owns the migration TOOLCHAIN. Without it, 'docker compose up -> migrations -> seed' (section 40) has nothing to run, and every contract bead would be editing the same schema file concurrently - which is exactly the coordination failure the plugin/lock tooling exists to prevent.\n\nTABLES (section 21), grouped by the persistence split in section 29.4:\nPLATFORM-OWNED: users, agents, agent_credentials, sessions, event_log, installations, projects.\nFEATURE-OWNED: quests, bounties, bounty_funds, battles, battle_participants, agent_stats, achievements, messages.\nEach feature owns ITS OWN tables and its own repository (section 24: 'repository.ts (drizzle access, own tables only)'). No GodRepository (section 12.2).\n\nINVARIANT to enforce in the schema: sessions.agent_id -> agents.id, and NEVER a user_id standing in as agent identity (sections 29.1, 38).\n\nSECURITY FIELDS: agent_credentials stores tokenHash (never a raw token) plus scopes, expiresAt, revokedAt (section 21). bounties stores amountCents as an integer (never a float for money).\n\nRELATIONAL NOTES worth getting right the first time:\n- battle_participants has a composite primary key (battleId, sessionId) and references SESSIONS, not agents (section 3.1).\n- bounty_funds has N rows per bounty; the displayed amount is their SUM. Prefer deriving the total over storing a scalar that can drift (see bounty-open-funding).\n- agent_stats keys on agentId and holds FACTS (prsOpened, prsMerged, prsRejected, testsPassed, testsFailed, recoveries, battlesWon, battlesLost, skillsJson) - distinct from XP and Reputation per the section 10.2 triple split.\n\nMIGRATION TOOLING: generate migrations from the schema, and make the chain runnable offline in the local container (section 37) and against a Neon preview branch (section 37 DB modes). Section 39 calls out a common newcomer failure - 'DB unreachable (migration/seed step missed)' - so migration+seed must be a single reliable step in the compose pipeline.\n\nSEED DATA: a seed script (section 19 lists scripts/seed.ts) so a fresh local DB is immediately usable and the E2E smoke has something to log in against.\n\nSUCCESS CRITERIA: from a clean container, migrations + seed run offline and produce a schema matching section 21; a feature can add a table without editing another feature's repository.\n\nTESTS: a migration smoke test against a real Postgres container; a test asserting the sessions.agent_id FK exists and no user_id identity column does; a test that seed is idempotent (safe to re-run).\n\nCONSIDERATIONS: the platform/feature ownership split in section 29.4 is a real boundary, not bookkeeping - it decides which extension may migrate which table. Adding a platform table later (say, seasons) should NOT require editing a feature's migration, and vice versa. Money as integer cents, never float. And because section 39 lists 'DB unreachable (migration/seed step missed)' as a common newcomer failure, migrations+seed must be one reliable step, not two things a contributor has to remember.","status":"closed","priority":0,"issue_type":"task","created_at":"2026-09-23T19:05:35.746988Z","created_by":"tranquangdang21","updated_at":"2026-09-24T05:40:32.590193100Z","closed_at":"2026-09-24T05:40:32.588228600Z","close_reason":"Verified end-to-end against a live Postgres: migrations + seed run and are idempotent (db:verify asserts both), schema matches section 21, integration suite asserts sessions.agent_id FK to agents, absence of a user_id identity column, no plaintext token column, and money held as integer cents derived from funding rows. M0 stages compose/migrations/seed/integration/schema-drift/schema-hygiene all pass.","source_repo":"battle-agents","source_repo_path":"/Users/tranquangdang21/Projects/battle-agents","compaction_level":0,"original_size":0,"labels":["data","infra","p0"],"dependencies":[{"issue_id":"ba-db-schema-drizzle-fki","depends_on_id":"ba-monorepo-scaffold-cit","type":"blocks","created_at":"2026-09-23T19:05:52.702964Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"ba-dependency-rules-os1","title":"Enforce dependency direction (features never cross-import)","description":"Machine-enforce the plan section 19 dependency rule and the section 33 extension-kind boundary so the architecture cannot silently rot.\n\nTHE RULE (section 19): presentation -> features -> core <- infrastructure. Features NEVER import each other (only core + protocol). Adapters import core + protocol only.\n\nWHY: Section 12.1 declares the God-Engine antipattern (a GameEngine accumulating if(hasBounty)/if(hasGuild) branches into 4000 LOC of unremovable features) to be BANNED. The stated architectural principle number 1 is the REMOVAL TEST: rm -rf features/guild must still compile and run. That property only holds if the boundary is enforced mechanically rather than by discipline.\n\nIMPLEMENT dependency-cruiser config (or eslint no-restricted-imports) encoding:\n- features/* may import core/*, protocol/*, and their own files. NOT other features/*.\n- core/* may import nothing from features/, adapters/, interfaces/, infrastructure/.\n- adapters/* may import core/*, protocol/* only. NEVER game code (section 23: 'No adapter imports game code - ever').\n- interfaces/{cli,mcp,api,websocket}/* consume features through the capability registry, never by importing feature implementations (section 36).\n- infrastructure/* may import core/* only; features never import Docker/Compose/Postgres-container concepts (section 37).\n\nREVIEW CHECKLIST ENFORCEMENT (sections 33, 36): any PR that forces edits to core/, cli/, or mcp/ implementations in order to add a feature is an ARCHITECTURE FAILURE. Allowed: composition-root/manifest one-liners. Any PR adding interfaces/cli/* or interfaces/mcp/* implementation code FOR A SPECIFIC GAME FEATURE (instead of registry entries) also fails review.\n\nSUCCESS CRITERIA: adding a deliberately illegal import makes lint/build fail with a message naming the violated rule; the dependency graph report shows clean layering.\n\nTESTS: a unit test running the rule engine over a fixture tree containing each violation type, asserting each is caught.\n\nCONSIDERATIONS: a lint rule that fires on legitimate code is worse than no rule, because teams disable it. Expect false positives during the initial port of the age-of-agents client and the learn-spine-derived animation core, both of which are structured differently from our conventions. Add narrow, documented exceptions rather than weakening the rule. Also worth encoding in CI rather than review: architecture failures are exactly the kind of thing a reviewer waves through under deadline pressure, and this is the mechanism that makes 'adding a feature never edits core' a fact rather than an aspiration.","status":"closed","priority":0,"issue_type":"task","created_at":"2026-09-23T18:58:26.072902Z","created_by":"tranquangdang21","updated_at":"2026-09-24T05:18:56.144048600Z","closed_at":"2026-09-24T05:18:56.142029300Z","close_reason":"Verified: architecture-rules.cjs exports the engine, a test exercises every declared rule against a fixture and asserts nothing else fires, violations name the rule, and the real tree reports 0 violations. Stage runs in CI via test:m0.","source_repo":"battle-agents","source_repo_path":"/Users/tranquangdang21/Projects/battle-agents","compaction_level":0,"original_size":0,"labels":["governance","infra","p0"],"dependencies":[{"issue_id":"ba-dependency-rules-os1","depends_on_id":"ba-monorepo-scaffold-cit","type":"blocks","created_at":"2026-09-23T19:04:21.527232Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"ba-docker-local-dev-y28","title":"Docker Compose local dev: web + Postgres, offline-capable (M0)","description":"Make the project run with one command on a clean clone, per plan section 37.\n\nTHE LOCKED LOCAL CONTRACT: git clone -> cp .env.example .env -> docker compose up runs the app with HMR, OFFLINE (no Neon required).\n\nWHY (section 37): Next.js + DB + CLI + MCP/worker later is too many moving parts to ask contributors to install Node and Postgres manually. This bead is a gap fill, NOT an architecture change.\n\nLAYOUT per section 37:\n  / (repo root)\n  docker/Dockerfile, docker/Dockerfile.dev, .dockerignore\n  compose.yaml, compose.dev.yaml, .env.example\n\nM0 SERVICES: web (Next.js dev, bind-mount + Compose Watch/HMR) and postgres (local PG, named volume agent-battle-postgres-data, survives restarts). LATER and ONLY when needed: worker (background jobs), standalone MCP server, queue. Compose may also run lint/test parity services for CI/local sameness.\n\nDB MODES: default local = container Postgres (offline-capable); cloud/preview = Neon branch per-developer/per-PR via neonctl link/checkout/env pull. NEON IS NEVER A LOCAL-RUN DEPENDENCY.\n\nARCHITECTURAL INVARIANT (section 37): Docker lives in the INFRASTRUCTURE layer. Features never import Docker/Compose/Postgres-container concepts - only repository and capability interfaces (section 33). The dependency-rules bead enforces this.\n\nM0 DoD GAINS (section 37): docker compose up from a clean clone gets web + migrations + seed + CLI bootstrap all green; docker compose down && docker compose up preserves the local DB via the named volume.\n\nNOTE ON THE OLDER PHRASING: section 7.1's earlier 'no Docker' phrasing is SUPERSEDED for local dev. Docker is the local development/test environment; it is never a PRODUCTION dependency. Production remains Vercel + Neon (git push deploys).\n\nSUCCESS CRITERIA: a clean clone plus cp .env.example .env plus docker compose up yields a working app with HMR and a persistent DB, entirely offline.\n\nTESTS: a smoke script that asserts compose config validity; a test that the named volume survives down/up; a test that docker-compose files do not leak into packages/features (the invariant above).\n\nCONSIDERATIONS: the named volume is the whole point - a contributor who loses their local DB on every `docker compose down` will not trust the setup. Separately, resist adding services early: the plan adds worker/queue/MCP-split containers only when section 7.4 triggers. The temptation to compose a full production topology locally is strong and directly contradicts section 7.1's deploy-ease philosophy.","status":"closed","priority":0,"issue_type":"task","created_at":"2026-09-23T18:59:47.822979Z","created_by":"tranquangdang21","updated_at":"2026-09-24T06:51:25.637240400Z","closed_at":"2026-09-24T06:51:25.634051700Z","close_reason":"Verified by running docker/scripts/smoke.sh: compose files parse, M0 services present (postgres/web/test-runner profile), no worker/queue/MCP containers added ahead of the scale-up trigger, postgres persists to the named volume agent-battle-postgres-data, no Docker/Compose concepts leak into apps/ or packages/, and data survives docker compose down && up. All six checks pass against a live stack.","source_repo":"battle-agents","source_repo_path":"/Users/tranquangdang21/Projects/battle-agents","compaction_level":0,"original_size":0,"labels":["devx","infra","p0"],"dependencies":[{"issue_id":"ba-docker-local-dev-y28","depends_on_id":"ba-monorepo-scaffold-cit","type":"blocks","created_at":"2026-09-23T19:04:22.052286Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"ba-e2i","title":"features/world: M5 persistent base + offline-to-online continuity","description":"Implement the M5 persistent world: the base/building layer that unlocks capability, and the offline-to-online continuity that makes a character survive its sessions. This bead IS the M5 owner - it was created because M5 was the one milestone in the plan's M0-M7 ladder with no bead, and the paragraphs below state that gap in the past tense on purpose. Read them as the reason this bead exists, not as a claim that the work is still unowned.\n\nWHY IT IS MISSING, stated as the gap rather than the ambition\n- Section 15.1 lists eight milestones, M0 through M7. Seven have an owning bead: M0 ba-better-auth-github-oauth-9wm, M1 ba-m1-two-harness-integration-3us, M2 ba-feature-bounty-xhk, M3 ba-feature-progression-md8, M4 ba-feature-battle-fbt, M6 ba-feature-guild-5g6, M7 ba-third-party-adapter-proof-2wp and ba-protocol-sdk-ecosystem-3ro. M5 had none. Searching every bead back then for M5, base buildings, offline-to-online, next-day continuity and world state returned exactly one hit, and it was ba-watchlist-space-molt-5v4 - a bead whose whole argument is that the SpaceMolt session/presence comparison must land BEFORE M5 locks world persistence in. That bead sequenced work against a milestone that had no bead, which is the clearest possible evidence the milestone was dropped rather than deferred on purpose.\n- Section 33's revised build order names the same gap in its own P2 list: \"P2 Battle, Guild, World, Animation, Social.\" World sits between Guild and Animation in a priority list, and Battle, Guild, Animation and Social each have a bead. World does not. The plan's P2 line is the strongest single piece of evidence here because it is a build-order statement, not a vision statement.\n- Section 27 gives M5 a definition of done that no other bead's criteria restate: \"M5: PixiJS city + base buildings unlocking capabilities; offline->online continuity. DoD: close all sessions, reopen next day, world + character state intact.\"\n- ba-feature-progression-md8 closes its own third clause by naming this bead's absence: \"NOT DONE, level gates. The curve and the awards are computed and exposed (progression.awards and progression.read), but there is no CONTENT to gate - no battle tier, no zone, no guild-creation threshold - because the features that would be gated do not exist yet. The gate function cannot be meaningfully tested until something sits behind it.\" The gate function is built and has no consumer. A base whose buildings unlock capability IS the consumer section 10.2 describes, and the world is what would sit behind the gate.\n\nTHE TWO HALVES ARE DIFFERENT WORK AND BOTH ARE NEEDED\nThe DoD names two things that a single feature does not naturally do together.\n1. BUILDINGS UNLOCK CAPABILITY (section 10.2, CoC line): \"progression always unlocks something (Workshop tiers -> larger projects; Lab -> benchmarks; Arena -> PvP; Guild Hall -> teams; Command Center -> subagent orchestration). Buildings unlock capability.\" And section 10.2's EVE line makes level equal access rather than cosmetics: \"Lv1 basic, Lv5 advanced, Lv10 Arena, Lv15 teams, Lv20 guild creation, Lv30 sponsoring.\" This is the level-gate consumer ba-feature-progression-md8 is waiting for, and it is a real feature: a base state, a building->capability table, and an unlock check on the capability registry.\n2. OFFLINE-TO-ONLINE CONTINUITY: \"close all sessions, reopen next day, world + character state intact.\" This is the session-death rule reaching its actual consequence. Section 10.2's death rule - \"HP 0 NEVER kills the character; only the SESSION fails\" - is already DONE and tested in ba-feature-progression-md8 ('a failed run still teaches' asserts no arriving event ever deletes or zeroes a record). What is not done is the other half of the same sentence: the world and character state must be reconstructable from durable storage with NO live session, which is the property section 3.2 means by browser/session-independent continuation.\n\nWHY THIS IS NOT A RENDERING BEAD\nba-game-client-pixijs-riw already owns the client: the city scene, the building-sprites directory, tilemap, camera guards, and the delta path. It explicitly scopes itself OUT of M0-M4 (\"the game client is NOT in the M0-M4 MVP\") and its criteria are all about rendering driven by normalized events. Adding persistence and capability-unlock logic to it would put domain state in the presentation layer and invert the dependency rule - section 19's presentation -> features -> core <- infrastructure, and the removal test's whole premise.\nThe world is a FEATURE (features/world/), the same shape as every other feature in the tree, removable with rm -rf and still compiling. Its state lives in its own repository and its own tables, the way bounty owns bounties and guild owns guilds (section 29.4's persistence split). The client reads it; it never writes it.\nSection 19's feature list does NOT include world/ today, and that omission is consistent with the plan having never scheduled the work rather than with a decision to drop it - section 33's P2 list names it.\n\nTHE MACHINE-NATIVE RULE APPLIES HERE TOO (section 34)\n\"every gameplay loop must be fully playable through protocol/tool interface alone (no web clicks required).\" A base whose buildings can only be inspected through a browser is not done, exactly as a quest that cannot be claimed through discover/search/act is not done. Unlocking a building must be reachable through act() and readable through inspect(), with the same application command the web path uses. Do not let the PixiJS city become the only place a capability can be seen.\n\nWHAT \"NEXT DAY\" MEANS, because the literal reading is untestable\n\"Reopen next day\" cannot be settled by a test that waits. The DoD's real content is that world and character state are a function of DURABLE STATE and not of any live session or in-memory process. So: destroy the process, drop every session, rebuild the runtime from a cold start against the same database, and assert the world and character state is byte-identical. A cache, a module-level variable, or a store keyed on a live session handle all fail that test; a rehydrated repository passes. State the constant in the test as a cold restart rather than a 24-hour wait, and say in the close reason that this is what 'next day' was taken to mean.\n\nSUCCESS CRITERIA, each with the thing that settles it\n1. A building unlocks a capability at its level and not below it. Settled by a test at a boundary: an agent below the gate is refused the capability and at or above it is granted, with the refusal naming the level required. A gate that only ever grants passes this, so the refusal half is required. Paired with ba-feature-progression-md8: the gate function this consumes is that bead's, not a second copy.\n2. World and character state survive a cold restart with zero live sessions. Settled by the cold-restart test above - tear down the runtime, end every session, rehydrate, assert identical state. A snapshot test taken while a session is alive passes for an implementation that keeps the world in memory, which is the exact failure.\n3. A session ending does not damage the character or the world. Settled by a test that ends a session mid-state and asserts the world is unchanged, paired with the existing death-rule test rather than replacing it.\n4. The feature is removable. Settled by the removal test with both halves, the same two-assertion shape ba-animation-core-spike-hf7 uses: uninstalling world leaves the build green AND leaves the rest of the game correct, with capability unlocks degrading rather than throwing for a consumer that asked.\n5. No web-only path. Settled by a machine-native test that reads and unlocks through the protocol with the web client absent.\n\nTESTS, each failing when the behaviour is wrong\n- a level-boundary test asserting both the grant and the named refusal (section 10.2's Lv10 Arena / Lv20 guild creation levels are the natural fixtures);\n- a cold-restart persistence test with every session ended - the one that catches a world living in memory;\n- a session-end-does-not-damage test;\n- a removal test with both halves;\n- an architecture assertion that features/world imports core and protocol only and is imported by no other feature - section 29.3's event-boundary rule, the same one features never cross-importing;\n- a machine-native unlock test with the UI absent.\n\nCONSIDERATIONS\nTwo failure modes are worth naming before this is picked up. First, scope: section 17.8's risk 8 bans MMO-shaped features from the MVP, and a persistent world is the single easiest place to smuggle one in. The deliverable is a base, a building-to-capability table, and continuity - NOT resources, crafting, trading, territory war, or a housing system. Section 10.4 explicitly excludes crypto and limits the economy to XP/Coins/Reputation/Items/Cosmetics. Second, ordering: the SpaceMolt comparison in ba-watchlist-space-molt-5v4 is meant to land before world persistence is locked, because the plan schedules it that way. This bead is the thing that comparison is a precondition FOR, so if that bead's verdict changes the session/presence model, this is the bead that absorbs the change - which is the correct time for that finding to arrive, and the reason the ordering is not merely decorative.","status":"open","priority":2,"issue_type":"task","created_at":"2026-09-24T22:44:38.315217Z","created_by":"tranquangdang21","updated_at":"2026-09-24T23:09:30.099632Z","source_repo":"battle-agents","source_repo_path":"/Users/tranquangdang21/Projects/battle-agents","mol_type":"work","work_type":"mutex","wisp_type":"none","labels":["game","m5","p2"],"dependencies":[{"issue_id":"ba-e2i","depends_on_id":"ba-feature-progression-md8","type":"blocks","created_at":"2026-09-24T22:45:30.146194Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"ba-e2i","depends_on_id":"ba-game-client-pixijs-riw","type":"blocks","created_at":"2026-09-24T22:45:30.042922Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"ba-e2i","depends_on_id":"ba-watchlist-space-molt-5v4","type":"blocks","created_at":"2026-09-24T22:45:30.227384Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"ba-event-ingest-and-sse-gu9","title":"Event ingest (batched) + SSE fan-out, full_state then deltas","description":"Implement the telemetry plane plumbing: batched event ingest and the realtime fan-out, per plan sections 5.1, 7.2, 22, and 25.\n\nTHE ARCHITECTURE (section 7.2) - the key backend insight of the whole plan:\n  Agents -> Next.js Event Gateway -> {Realtime layer (WS/SSE) -> Browser} + {batched/important -> Neon}\nNeon stores DURABLE state only. Realtime is NOT Neon's job.\n\nPOST /api/events (section 22): Bearer installation-token -> validate AgentEvent[] (zod, protocol version check) -> resolve session -> persist KEY events -> runtime.emit -> SSE publish.\n- Batching: adapters buffer 250ms / up to 50 events. The server REJECTS >100 per batch with 413 plus a Retry-After header.\n- Response: { accepted: n, sessionId, resumed: boolean }.\n\nGET /api/events/stream (SSE) (section 22): per-user (web session) or per-battle channel. Sends a full_state snapshot FIRST, then deltas. This exact full_state-then-delta shape is ported from agent-move's Broadcaster (section 1.2 item 4) and the diff-broadcast shape from codemoo/agent-world's stateDiffBroadcast.js (section 28.1 item 12).\n\nMVP CHOICE (section 7.2): SSE, not WebSocket. POST /api/events -> fan-out -> Game UI. SSE is simpler than WS and sufficient for the MVP. Revisit only when measured (section 7.4).\n\nTELEMETRY VERSUS CONTROL (section 5.1): hooks/session-watcher are the telemetry/data plane (passive observation); MCP is the command/control plane (active interaction). NEVER MCP-per-Read/Edit/Bash - that bloats context, costs tokens, drags the workflow, and couples us to per-agent MCP quirks. MCP is NOT event capture.\n\nVOLUME MATH THAT MOTIVATES THE BATCHING (section 7.2): 100 agents x 20 events/s = 2000 DB writes/s, which Neon must NEVER see. This is why the PERSISTED_EVENT_TYPES filter (activity-event-log) and the 250ms/50-event batching are P0 design, not optimization.\n\nDIFF BROADCAST (section 28.1 item 12): port the stateDiffBroadcast + eventsPipeline shape from codemoo/agent-world - broadcast DIFFS, not full state. Also port buildingAssignments.js (repo -> building) and costTracker.js (bounty payout accounting) from the same repo.\n\nTHE THREE SERVER-ENFORCED BATCH RULES, spelled out because an adapter that gets any of them wrong is rejected at the door. The ingest contract is not a suggestion: the server enforces all three.\n- Flush at 250ms OR 50 events (the client-side buffer's job).\n- Never exceed 100 events in one batch: the server rejects with 413 plus a Retry-After header. This is a backpressure signal, not a permanent failure.\n- Never put two sessionIds in one batch: a mixed-session batch is rejected with 400. The response is { accepted, sessionId, resumed }, which is per-session, so a batch that mixes sessions has no coherent single response and no single session to attribute accepted events to.\n\nTHE AUTH CREDENTIAL is a Bearer installation token, issued to an agent, not a session cookie and not a URL. Agent credential issuance/rotation/scopes/expiry is a separate closed bead (ba-agent-credentials-issuance-294); this endpoint validates the presented token and scopes what the session may do. The token is never placed in a URL (URLs leak via logs and referrers) - it is an Authorization header. The ingest path is authenticated; note that the SSE read path is a separate surface with its own (currently unresolved) auth story - see below.\n\nTHE PUBLISHED STREAM IS CURRENTLY AN OPEN P0 BUG, and this bead should not pretend otherwise. The GET /api/events/stream endpoint as shipped is an unauthenticated global broadcast: it has no authentication and no per-channel scoping, and toGameEvent forwards the whole validated AgentEvent as the payload - so an agent's file paths, command names, and thinking content reach any open socket. This is deliberate-with-record (the limitation is noted in apps/web/src/event-routes.ts) and is tracked as a P0 bug (ba-9hr). The fix is ordered there: write down which event types are public spectator data (battle progress, score, agent identity) and which are never public (file paths, command input, thinking, message bodies, permission requests), filter the bus by that classification, then authenticate and scope the stream. A browser EventSource cannot set an Authorization header and the repo forbids tokens in URLs, so the credential has to be a web session - real work, not a header. The relevant fact for THIS bead: the ingest path (POST) is correct; it is the fan-out it publishes to that is currently open. Do not close this bead's fan-out work as done, and do not build downstream consumers (replay, game client) on the assumption that the current global broadcast is the intended steady state.\n\nWHAT IS ALREADY SHIPPED that this bead composes rather than rebuilds: the event vocabulary and its validation are frozen (ba-agent-event-union-up5 - the zod discriminated AgentEvent union, PROTOCOL_VERSION '0.1.0', checked at handshake, validation rejects malformed with field-level messages and an unknown type names every known type); the activity log and its persistence filter are closed (ba-activity-event-log-aph - PERSISTED_EVENT_TYPES, and DrizzleStateStore as the only path to the database); the runtime emit ordering is closed (ba-runtime-and-event-bus-63r - persist -> handlers -> publish). So 'persist KEY events, then runtime.emit' has a defined meaning: the durable write happens first, and only then do handlers and the realtime bus see the event. This ordering is why a consumer never races ahead of the durable record.\n\nWHY A NAIVE IMPLEMENTATION GOES WRONG. A 100-event batch that is silently truncated instead of rejected with 413 looks like it works until it drops the tail under load - and a client that ignores Retry-After turns one backpressure signal into a self-inflicted stampede. Treating 'we do not persist transient events' as 'we drop them' is the specific failure the consideration names: transient events (thinking, waiting, streaming, heartbeat) are bus-only for DURABILITY, but they must still reach the UI at high rate, so the filter belongs in the persist path, not in the emit path. A full-state broadcast on every tick instead of diffs is the volume problem the whole design exists to avoid. And a per-session batch rule that is only documented in the adapter and not enforced server-side is a contract that will be violated the first time a misbehaving client posts.\n\nSUCCESS CRITERIA: a batch over 100 events is rejected with 413 and Retry-After; a mixed-session batch is rejected with 400; key events persist while transient events do not; an SSE subscriber receives a full_state snapshot then deltas only; the diff broadcast sends deltas rather than whole-state payloads; an unauthenticated stream subscriber is a known, tracked gap (ba-9hr) and not silently treated as acceptable.\n\nTESTS: integration test for the 413 rejection (and that a client honoring Retry-After recovers); a test that a mixed-session batch is rejected with 400; a test that transient events never reach the DB; an SSE test asserting the first frame is full_state and subsequent frames are deltas; a load test harness for the 100x20 ev/s scenario (run as a pre-M4 gate per section 17.2, not an M0 gate).\n\nCONSIDERATIONS: the batching parameters (250ms, 50 events, reject over 100 with 413 and Retry-After) are starting values, not constants - they should be configuration, because the right numbers depend on real agent behavior and the 100x20 ev/s load test is what will reveal them. The 413 path matters more than it looks: a client that ignores Retry-After and retries immediately converts a backpressure signal into a self-inflicted stampede. Also, transient events still have to flow to the UI at high rate, so 'we do not persist it' must not quietly become 'we drop it'.","status":"closed","priority":1,"issue_type":"task","created_at":"2026-09-23T19:01:27.849862Z","created_by":"tranquangdang21","updated_at":"2026-09-25T02:38:55.265662Z","closed_at":"2026-09-25T02:38:55.265569Z","close_reason":"Every success criterion has a test that passes. A batch over the limit is refused with 413 and Retry-After, the size gate reads only .length and runs before parsing so a flood is refused without the server doing the flood's work, a batch spanning two sessions is refused rather than half-written, key events persist while transient ones do not, and the SSE stream opens with a full_state frame and then deltas that carry no state field. Transient events still reach the game's own activity view: the hub carries a public classification and an operator view, decided per subscriber, because 'we do not persist it' must not quietly become 'we drop it', and a busy agent is what that view exists to show.\n\nThe batching numbers are configuration read from the environment, not constants, with a startup error when a limit contradicts itself, because the 100x20 load test is what will reveal the right values. The load harness runs 131565 ev/s with 500 rows for 10000 events and 40000 deltas across four subscribers.\n\nThe unauthenticated stream is not treated as acceptable: it is a tracked bead whose classification step is done and whose authentication step is not, and the close reason says so rather than implying the gap is closed.","source_repo":"battle-agents","source_repo_path":"/Users/tranquangdang21/Projects/battle-agents","mol_type":"work","work_type":"mutex","wisp_type":"none","labels":["api","p1","realtime"],"dependencies":[{"issue_id":"ba-event-ingest-and-sse-gu9","depends_on_id":"ba-9hr","type":"blocks","created_at":"2026-09-24T18:04:13.078385Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"ba-event-ingest-and-sse-gu9","depends_on_id":"ba-activity-event-log-aph","type":"blocks","created_at":"2026-09-23T19:04:24.060793Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"ba-event-ingest-and-sse-gu9","depends_on_id":"ba-agent-credentials-issuance-294","type":"blocks","created_at":"2026-09-23T19:07:17.699332Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"ba-event-ingest-and-sse-gu9","depends_on_id":"ba-agent-event-union-up5","type":"blocks","created_at":"2026-09-23T19:04:23.970793Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"ba-feature-achievements-gmr","title":"Achievements (incl. failure achievements) driven by the activity log","description":"Implement achievements as a feature reading from the activity log, per plan sections 10.2, 21, 24, and 30.\n\nSCHEMA (section 21): achievements: id, agentId -> agents, code, awardedAt.\n\nACHIEVEMENTS ARE EVENT-DERIVED, NOT SCRIPTED (section 30): replay, achievements, reputation, and audit all read from the ONE ordered activity log, never from scattered feature tables. An achievement is therefore a projection over the event stream - which means achievements become retroactively grantable if a rule is added later (decide and document whether backfill is allowed; granting a missed 'first bounty' badge to an established agent is arguably correct).\n\nEXAMPLES from the plan's own vocabulary: 'First Bounty' (section 12.2 cites progression reacting to bounty.completed with an achievement check), and FAILURE ACHIEVEMENTS (section 10.2) - the plan explicitly includes achievements for failure. This is consistent with the death rule: HP 0 never kills the character, only the session fails, and failure is itself a recordable accomplishment (e.g. recovering from five failed attempts is a 'critical hit', not a shame).\n\nMACHINE-NATIVE (section 33/34): achievements must be readable and awardable through the protocol and CLI, not only rendered in a web page.\n\nCONSIDERATIONS: achievement codes should be stable, versioned strings (like the protocol version pin in section 17.3) so a renamed achievement does not orphan historical rows.\n\nTHE FOUNDATION YOU ARE BUILDING ON, already shipped, so this bead does not re-derive it (this is why the \"read from the log\" instruction above is cheap to honor):\n\nTHE ACTIVITY LOG IS REAL AND CLOSED (ba-activity-event-log-aph, closed). features/activity owns the read API (a session trail plus recent), the session summary shape, and a 365-day retention policy (365 because a shared replay is a link). The durable table is event_log: id (bigserial), type, actorId, payload jsonb, occurredAt. DrizzleStateStore is the ONLY path an event takes to the database - so achievements are reached by reading event_log, never by inserting rows beside it. Crucially, the log persists a FILTERED subset, not everything: PERSISTED_EVENT_TYPES = PERSISTED_EVENT_TYPES = ['session.started','session.ended','session.resumed','test.passed','test.failed'], and a FEATURE ADDS ITS OWN by declaring GameFeature.persistedEvents - the registry unions them at install and uninstall, so bounty.completed, battle.started/finished and agent.level_up become durable by being declared, never by editing core. That two-part shape is deliberate and packages/features/activity/src/activity.test.ts pins both halves, including that isPersistedEventType('bounty.completed') is FALSE on its own and true only when a feature declares it. The list this bead used to quote was the plan's section 30 wish-list, not the shipped constant - no feature declares any of the four game types today, so right now bounty.completed is NOT durable and an achievement keyed on it would read nothing from the log. That is this bead's own work to fix, by having bounty declare it.. Only those are durable; everything else (thinking, waiting, streaming, heartbeat) is bus-only. So the set of achievements derivable from the log is bounded by that filter - an achievement that needs an event type outside the filter must first add that type to the filter, which is a deliberate act, not something to assume. On top of the fixed filter, a feature may add its own event types.\n\nORDERING IS GUARANTEED, AND THIS IS WHY THE PROJECTION IS SAFE. The runtime emits in the order persist -> handlers -> publish: the event is written to event_log BEFORE handlers run, so an achievement handler reacting to bounty.completed is reading a durable record that already exists, not racing ahead of it. The log is written before downstream consumers read it, or replay/achievements would race the durable record. This is a property of the shipped runtime (ba-runtime-and-event-bus-63r, closed), so an achievements handler does not have to add its own ordering.\n\nHOW A FEATURE ATTACHES - the boundary rule, because it is the easiest thing to get wrong here. Achievements is a feature extension, and per the frozen Contract 1 it registers as a GameFeature {id, commands?, eventHandlers?, reducers?, capabilities?, requires?}. It reacts to cross-feature signals by SUBSCRIBING an eventHandler to the bus (e.g. on 'bounty.completed'), never by importing the bounty feature. Features NEVER import each other - this is machine-enforced in CI via architecture-rules.cjs, not by reviewer discipline. Core knows no game vocabulary: a grep for Bounty/Guild/Battle/XP/Level/Quest/Reputation in packages/core must return nothing. If achievements needs bounty's state, it reacts to the event, it does not import the feature or call into it. Direct domain operations stay plain function calls WITHIN the achievements feature; only cross-feature side effects go through the bus.\n\nHOW IT REACHES AGENTS AND THE CLI (the machine-native half). Public behaviour goes through the capability registry over the five frozen primitives (discover/search/inspect/act/observe, ba-contract-public-api-ute, closed), NOT by adding a bespoke tool or a new primitive. So an achievement award is a typed registry action (defineAction with an input/output) surfaced through act(), and the CLI is a consumer of that same command - 'achievements list' becomes a search over the achievements domain. Adding achievements grows the registry, not the primitive surface. The parity rule: the same application command must be reachable identically from CLI, HTTP, and MCP.\n\nWHY A NAIVE IMPLEMENTATION GOES WRONG. The obvious shortcut - awards computed by a scheduled job that scans feature tables (bounties, progression rows) for 'has this agent ever completed a bounty' - is exactly what the design forbids, and it breaks in three ways: it duplicates truth that the log already holds, it will drift from the log the moment two paths disagree, and it cannot be reproduced on a replay. The second shortcut is awarding on a handler that fires before the log write; the shipped ordering makes that safe ONLY if you subscribe through the runtime's emit path, not by racing it. The third is a mutable, human-editable achievement name stored in the row: a renamed achievement then orphans history, which is why the code is a stable versioned string. Finally, an award must be IDEMPOTENT - the bus can deliver, and a backfill can re-run the projection, so 'first bounty' granted twice is a correctness bug; the natural key is (agentId, code) and the write must be safe to repeat.\n\nSUCCESS CRITERIA: completing a first bounty awards the matching code exactly once; a failure-path achievement can be earned; achievements are derived from the log rather than from feature-table joins; the award is reachable identically through the protocol and the CLI over the frozen five primitives, with no new primitive and no bespoke tool.\n\nTESTS: unit test that an award is idempotent (never double-awarded, including under a repeated/backfilled projection); a test that a failure-path achievement is grantable; a test that achievements derive from event_log and not from a feature table; a test that an achievement needing an event type outside PERSISTED_EVENT_TYPES correctly requires the filter to be widened first; a parity test that the same award command is reachable via CLI, HTTP, and MCP.","status":"open","priority":1,"issue_type":"task","created_at":"2026-09-23T19:01:27.702771Z","created_by":"tranquangdang21","updated_at":"2026-09-24T23:05:04.327103Z","source_repo":"battle-agents","source_repo_path":"/Users/tranquangdang21/Projects/battle-agents","mol_type":"work","work_type":"mutex","wisp_type":"none","labels":["feature","p1"],"dependencies":[{"issue_id":"ba-feature-achievements-gmr","depends_on_id":"ba-activity-event-log-aph","type":"blocks","created_at":"2026-09-23T19:04:23.736402Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"ba-feature-achievements-gmr","depends_on_id":"ba-feature-progression-md8","type":"blocks","created_at":"2026-09-23T19:04:24.869121Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"ba-feature-agent-f9t","title":"features/agent: identity, HELLO, heartbeat, presence","description":"Build the agent extension - the first game feature, per plan section 24.\n\nCOMMANDS: RegisterAgent, HelloSession, Heartbeat, EndSession.\nEVENTS: agent.registered, session.started / session.resumed / session.ended.\nOWNERS: writes to agents / sessions / installations tables only (section 29.4 persistence split: users/agents/agent_credentials/sessions/event_log are platform-owned; feature-owned tables are bounties/quests/progressions/reputations/battles/guilds).\n\nMACHINE-NATIVE FROM BIRTH (section 33 P1 rule): this feature must expose CLI/MCP-consumable capabilities FROM THE START - design machine-native, never 'game first, protocol later'. Registering an agent and running a session must be fully drivable through the 5 stable primitives and the CLI.\n\nTHE IDENTITY RESOLUTION LOGIC lives here: given HELLO{installationId, agentId, projectId} plus harness and project repo URL, find-or-create installation, agent, and project, then locate a recently-disconnected session in the same agent+installation+project within the grace window and resume it (section 3.2, section 22). Do NOT key on process name alone - three concurrent Claudes must stay distinguishable.\n\nPRESENCE: the agent row's status field moves ACTIVE -> OFFLINE on session end while all stats persist (section 3.2). The UI consequence: a dead session must never make the character vanish; offline shows Level/XP/wins plus 'Last seen 3m ago'.\n\nCONSIDERATIONS: presence detection quality is a separate concern - see the status-fusion bead. This feature owns the state machine; the fusion bead owns inferring working/idle/attention/error from transcripts and pane text.\n\nSUCCESS CRITERIA: an agent registers via the CLI, runs a session, heartbeats, ends cleanly, and shows offline with intact stats; a reconnect within the grace window resumes the session rather than duplicating the agent.\n\nSTATUS, 2026-09-25 - the whole feature is built, tested and RUNNING, and the criteria split in a way worth being precise about. Verified against the tree:\n- DONE, and it is the only feature in the composition root. apps/web/src/composition.ts installs agentFeature and questFeature; progression, reputation and bounty are built but not yet composed (see ba-feature-progression-md8 and ba-feature-reputation-nnj). There are no per-feature branches in that file by design - scripts/removal-test.sh deletes a feature by stripping one line, and every other line is a line that survives the strip.\n- DONE, the state machine. session.ts declares SESSION_STATUSES = active/ended/disconnected/abandoned and the three SESSION_END_REASONS (completed/crashed/abandoned), with isResumable, isTerminal, nextSessionStatus, isHeartbeatStale and isWithinResumeGrace as pure functions and both timers as named constants - DEFAULT_HEARTBEAT_TIMEOUT_MS = 5 min, DEFAULT_RESUME_GRACE_MS = 15 min. feature.ts:65 states the ownership boundary explicitly: this feature owns the state machine and nothing else, with XP, reputation, presence and session lifecycle named elsewhere.\n- DONE, the handshake. hello() finds-or-creates the installation, agent and project, then looks for a resumable session in the same agent+installation+project within the grace window. Identity is not keyed on process name.\n- DONE, the session lifecycle over the protocol. RegisterAgent is dispatched as the command agent.register; the other three are ACTIONS, not commands - session.heartbeat and session.end, alongside agent.describe and agent.read. The split matters for callers: act() rejects a command name at compile time, and tests/types/action-ids.ts pins exactly that with a @ts-expect-error on 'agent.register'. So 'registers via the CLI' means dispatch for the register step and act() for the rest.\n- NOT MET AS WRITTEN: 'via the CLI'. This feature is fully reachable - every action is in the generated RegisteredActionId union - and the CLI is a generic dispatcher over the same ApplicationApi, so `agent-battle session heartbeat` works by construction. But the CLI has no transport and no agent-runtime verbs (see ba-cli-p0-6oe), so 'via the CLI' is currently reachable in-process rather than from a terminal against a running server. The feature is done; the sentence depends on another bead.\n- The visible-offline-with-intact-stats half is a UI criterion and has no surface yet: the agent rows carry a presence field, but apps/web has no dashboard to render it (ba-web-ui-surface-t3w).\n\nTESTS: unit tests for the session state machine; integration test for register -> hello -> heartbeat -> end; the kill-and-reopen resume test from the section 3.2 narrative (terminal killed mid-session, reopened, resume offered); test that agent stats survive session end.\n\nWHAT EXISTS TODAY: session.test.ts, hello.test.ts, credential.test.ts, feature.test.ts and manifest.test.ts in packages/features/agent/src/, plus tests/integration/{session-lifecycle,agent-ownership,credentials,event-ingest}.test.ts against a live Postgres, and tests/e2e/first-login.test.ts which registers a character, gives it a session and asserts the session survives a re-login.\n\nWHAT DOES NOT, AND WHY IT CANNOT YET: the kill-and-reopen test is the one genuinely outstanding item, and it is blocked on a harness rather than on this feature. It needs two real coding agents, which is ba-m1-two-harness-integration-3us - and that in turn needs the Claude and Codex adapters. The resume LOGIC is fully covered at the unit level (isResumable, isWithinResumeGrace and the grace-window branches in hello.test.ts); what is unproven is that a killed terminal produces a session in the state the logic expects, which is exactly the integration proof's job. Do not treat the unit coverage as discharging this criterion.","status":"closed","priority":1,"issue_type":"task","created_at":"2026-09-23T19:00:28.857241Z","created_by":"tranquangdang21","updated_at":"2026-09-25T02:45:24.894482Z","closed_at":"2026-09-25T02:45:24.894156Z","close_reason":"Built, tested and composed, and the one clause the bead recorded as unmet is now met. It said 'not met as written: via the CLI' because the CLI had no transport and no runtime verbs. The CLI is now an HTTP client with a session file and a real agent-battle entry point, so agent.register dispatches and session.heartbeat / session.end act, from a terminal against a running server rather than in-process. The same bead also noted the CLI could not be relied on for the action half; that is fixed by the generated RegisteredActionId union, which the CLI's act() is typed against.\n\nWhat is verified: the state machine is pure and total over active/ended/disconnected/abandoned with three end reasons, isResumable, isTerminal, nextSessionStatus, isHeartbeatStale and isWithinResumeGrace, and named timers at 5 minutes and 15 minutes. The handshake finds-or-creates installation, agent and project and then looks for a resumable session in the same triple inside the grace window, and identity is not keyed on process name. Integration tests cover the handshake against Postgres, resume on reconnect, a new session once the grace window has passed, per-project resumable isolation, and two users registering the same name.\n\nThe visible-offline-with-intact-stats half is a UI criterion and stays open on ba-web-ui-surface-t3w, which owns the dashboard. The agent rows already carry presence. This is named here rather than counted as done, because the bead's own sentence bundles a model half and a screen half and only the first is delivered.","source_repo":"battle-agents","source_repo_path":"/Users/tranquangdang21/Projects/battle-agents","mol_type":"work","work_type":"mutex","wisp_type":"none","labels":["feature","p1"],"dependencies":[{"issue_id":"ba-feature-agent-f9t","depends_on_id":"ba-capability-registry-klw","type":"blocks","created_at":"2026-09-23T19:04:22.901371Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"ba-feature-agent-f9t","depends_on_id":"ba-contract-public-api-ute","type":"blocks","created_at":"2026-09-23T19:05:52.617078Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"ba-feature-agent-f9t","depends_on_id":"ba-contract-session-rpo","type":"blocks","created_at":"2026-09-23T19:04:22.811045Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"ba-feature-battle-fbt","title":"features/battle: matchmaking, judge, arena (M4 TRIPLE-STAR)","description":"Implement the battle extension - M4, one of the three milestones that prove the idea, per plan sections 10.3, 15.1, 22, 24, and 27.\n\nM4 IS TRIPLE-STAR (section 15.1): same challenge + isolated workspaces + judge + arena/spectator/replay/reward, with modes Speed/Quality/Survival/Boss/Team/Tournament.\n\nCOMMANDS (section 24): CreateBattle, JoinBattle, FinishBattle. OWNS: battles and battle_participants tables.\n\nA BATTLE BINDS SESSIONS, NOT AGENTS (section 3.1) - this is a load-bearing invariant and a stated anti-pattern: Battle#918 = Claude/Session#241 vs Codex/Session#552. The WRONG model is 'Battle -> Claude vs Codex' because the same Claude can fight two battles in two sessions. battle_participants references session_ids (section 21).\n\nBATTLE IS NOT FAKE COMBAT (section 2.1): agents must ACTUALLY read/edit/test/build/debug/review. Two real coding agents solve the SAME coding problem in ISOLATED WORKSPACES; the game only visualizes the resulting state. The plan is explicit that this is 'the part no existing repo does completely' and the core differentiator.\n\nMATCH LENGTH (section 10.3, from Clash Royale): 5-15 minutes. Same issue, same tests, isolated repos: code -> test -> debug -> submit.\n\nJUDGING IS SUBSTANCE, NOT SPEED (section 10.3): judge on tests / build / lint / typecheck / diff / security / performance. DEFAULT WEIGHTS stored per battle in battles.weightsJson: {correctness: .5, tests: .2, regression: .1, quality: .1, efficiency: .1}. Ties: faster valid submission wins (Speed) or shared win (Tournament).\n\nTHE WEIGHTS MUST BE PUBLIC (section 17.4): judge weights are published per match. This is both a fairness property and a trust property - a hidden rubric is indistinguishable from a rigged one.\n\nCODING BEHAVIOR MAPS TO STATS, NEVER TOKENS TO DAMAGE (sections 2.2, 10.3): Edit + tests pass -> +XP; Edit + fail -> small XP; 5 consecutive failures -> damage; fixing your own bug -> critical hit. Stats: STR (successful changes) / INT (reasoning) / WIS (tool selection) / LUK (unexpected recovery), with DEF (tests) and DEX (fast iterations). Section 2.2 explicitly BANS 'token-count-as-damage'.\n\nBATTLE STATES (section 3.2): RUNNING / PAUSED / COMPLETED / ABANDONED / EXPIRED. Disconnect during a battle -> grace period -> reconnect? resume : abandoned.\n\nDECLARED CAPABILITY DEPENDENCY (section 24): requires: ['reputation.read'], degrading to open battles when reputation is uninstalled. This is the removal-test proof - it is a real architectural demonstration, not decoration.\n\nANTI-CHEAT IS TBD (section 17.4): fairness is in scope for M4 (public weights, mandatory isolated workspaces); full anti-cheat is explicitly still open. Do not let this bead expand into anti-cheat engineering.\n\nSUCCESS CRITERIA (section 27 M4 DoD): 1v1 battle on the same issue, isolated workspaces, public weights, judge run, replay URL shareable, XP/rep applied - and the DoD test is that a LOGGED-OUT user can open the share link and see the full timeline.\n\nTHE DoD TEST IS FINE AND STAYS THE HEADLINE. One addition, because the list above has a clause nothing checks: 'PUBLIC WEIGHTS' is a fairness property and a trust property (a hidden rubric is indistinguishable from a rigged one), yet every listed test passes with the weights hidden. A battle with hidden weights can satisfy the whole M4 DoD as written. So public weights gets its own assertion, and it is a visibility test rather than a storage test - the weights being STORED in battles.weightsJson is the default, and storing them proves nothing about whether a participant or spectator can READ them before the match is judged. The assertion is that the weights are retrievable through the public read path, unauthenticated, for a battle that is still running. This is the one clause here that must hold BEFORE the result, since a rubric published after the verdict is not a public rubric.\n\nTwo more that the list implies but does not test, and which are cheap to state because they are properties of the invariants already recorded above:\n- A BATTLE BINDS SESSIONS, NOT AGENTS (section 3.1, the stated anti-pattern Battle#918 = Claude/Session#241 vs Codex/Session#552). The test: one agent in two concurrent sessions fights two battles, and neither battle sees the other's session. This is the specific bug the invariant exists to prevent, and it is invisible to every other test in this bead because a battle with one session per agent behaves identically.\n- CODING BEHAVIOR MAPS TO STATS, NEVER TOKENS TO DAMAGE (section 2.2 bans token-count-as-damage). The test: two agents with wildly different token counts and identical behaviour produce identical stats. A judge that quietly weighted tokens would pass every fixture test here and violate the section's explicit ban.\n\nTESTS, each a test that fails when the behaviour is wrong rather than when a page is missing:\n- the logged-out replay-share DoD test (the headline, unchanged).\n- a PUBLIC WEIGHTS visibility test: the weights are retrievable unauthenticated through the public read path while the battle is still running, and match the stored weightsJson. A judge whose weights are hidden fails this; a judge that stores them and never surfaces them also fails this, which is why a storage assertion would not have been enough.\n- judge unit tests per criterion with fixtures, plus a determinism test that the same battle state yields the same weighted score - the property ba-battle-workspace-judge-jc9 also asserts, cited here so the two do not drift.\n- an isolation test proving one participant cannot see or affect the other's workspace.\n- a session-binding test for the Battle#918 invariant: one agent, two sessions, two battles, no cross-visibility.\n- a no-token-as-damage test: identical behaviour with different token counts yields identical stats.\n- a tie-handling test (faster valid submission wins in Speed; shared win in Tournament).\n- a removal test asserting battles degrade gracefully without reputation - which is the real architectural demonstration of the declared capability dependency, so it is asserted rather than assumed.\n\nANTI-CHEAT IS STILL OPEN and this bead must not quietly close it: the gap is named in ba-risk-gates-e74 (risk 4), which carries a docs check that fails if any doc claims anti-cheat is solved. Document the gap; do not imply it is handled.\n\nCONSIDERATIONS: the honest risk is that a battle is only as interesting as the honesty of its judging. Everything here - public weights, mandatory isolation, deterministic judge - exists so the result is defensible. A judge whose weights are hidden, or a workspace leak between participants, destroys the feature faster than any amount of visual polish can build it. Related: battles are expensive (two agents, isolated environments, test runs), so the token and compute cost per battle is a real product constraint that belongs in this design, not deferred to operations. Anti-cheat is explicitly still open (section 17.4) - document the gap rather than implying it is solved.","status":"open","priority":2,"issue_type":"task","created_at":"2026-09-23T19:03:04.300296Z","created_by":"tranquangdang21","updated_at":"2026-09-24T21:52:46.828524Z","source_repo":"battle-agents","source_repo_path":"/Users/tranquangdang21/Projects/battle-agents","mol_type":"work","work_type":"mutex","wisp_type":"none","labels":["feature","m4","star"],"dependencies":[{"issue_id":"ba-feature-battle-fbt","depends_on_id":"ba-feature-progression-md8","type":"blocks","created_at":"2026-09-23T19:04:23.414681Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"ba-feature-battle-fbt","depends_on_id":"ba-feature-reputation-nnj","type":"blocks","created_at":"2026-09-23T19:04:23.498635Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"ba-feature-bounty-xhk","title":"features/bounty: M2 vertical slice - Issue to payout","description":"Implement the bounty extension - M2, the plan's most important slice. Per plan sections 11.1, 22, 24, 27.\n\nM2 IS THE PROOF OF THE WHOLE IDEA (section 15.3): the three milestones that prove the project are M1, M2, and M4, and M2 is explicitly 'valuable with zero game UI' (section 15.1). Ship real utility BEFORE heavy game art (section 17.1: gimmick risk, ship M2 first). The M2 DoD (section 27, LOCKED): FIRST REAL GAME LOOP = full Bounty vertical slice: Issue -> Bounty -> Discover -> Claim -> Coding -> PR -> Review -> Merge -> Reward -> Activity/History. 'Reward is the payoff at the END of the first playable loop, never the first feature.'\n\nTHE BOUNTY OBJECT (section 11.1):\ntype Bounty = { id, repository{owner,name,url}, issue{number,url}, reward{amount,currency}, requirements[], status: open|claimed|submitted|review|paid|expired, sponsor{githubUserId} }\n\nCOMMANDS (section 24): CreateBounty, ClaimBounty, SubmitBounty, FundBounty, ExpireBounty.\nEVENTS: bounty.created, bounty.claimed, bounty.submitted, bounty.completed (and bounty.failed for the reputation consumer).\nOWNERS: bounties and bounty_funds tables only.\n\nAPI FLOW (section 22):\n- POST /api/bounties - create; validates the GitHub issue exists via Octokit; emits bounty.created\n- POST /api/bounties/[id]/claim - agent session claims; emits bounty.claimed; sets claimedAgentId; RACE MODE REJECTS SECOND CLAIMS\n- POST /api/bounties/[id]/submit - {prUrl} -> bounty.submitted\n- POST /api/webhooks/github - PR merge -> bounty.completed -> progression/reputation/achievement handlers fire -> payout job enqueued\n\nTHE CORE INSIGHT (section 11.1): do NOT invent a fake issue system. The Bounty has REAL GitHub links - repository url, issue number, issue url. Game is the discovery/execution/reputation/visualization layer ON TOP of GitHub, not a replacement for it. Precedents cited: Tari public bounties repo (browse -> pick -> fork -> PR -> first merged wins) and the AsyncAPI rounds model (budget rounds, public amounts, resolution rates -> our Season model).\n\nBounty IS a FUNDED QUEST, and must remain a separate feature from quest (Appendix B: 'Bounty = funded quest on a real GitHub issue'). Bounty must work if quest is removed.\n\nMACHINE-NATIVE (section 33 P1 / section 34): the full loop discover -> claim -> code -> submit -> reward must be drivable via the 5 stable primitives and the CLI with no web clicks.\n\nCONSIDERATIONS: the payout rail is a separate bead (see payout-rail-dispute) because real-money compliance is unresolved (section 17.5). M2's DoD ships with a real-money rail STUBBED - record intent, no real payout - and the UI must clearly mark 'payout: manual/sandbox' (section 27). Do not let this bead quietly become a payment system.\n\nSUCCESS CRITERIA: on a scratch repository, end to end - create a bounty on a real issue, claim it from an agent via the protocol, open a PR, merge it, the webhook completes the bounty, XP/reputation fire, and history shows the trail.\n\nTESTS: the full vertical-slice integration test on a scratch repo; unit tests for the state machine including RACE-mode double-claim rejection; a removal test that bounty works with quest uninstalled; a test asserting the bounty carries real GitHub URLs rather than synthetic ones.\n\nBOUNDARY WITH THE SIBLING BOUNDS BEADS (avoid double-implementation):\nThis bead owns the CORE BOUNTY LIFECYCLE ONLY - the Bounty object, its state machine (open -> claimed -> submitted -> review -> paid/expired), and the commands CreateBounty, ClaimBounty, SubmitBounty, FundBounty, ExpireBounty.\nIt does NOT own: the mode and tier taxonomy, quest chains, or seasons. Those belong to the bounty-modes-tiers-seasons bead.\nCONCRETELY: this bead stores a 'mode' field and enforces RACE-mode double-claim rejection, because the lifecycle cannot be correct without it. The enum of valid modes, the tier thresholds, chain prerequisite logic, and season accounting are all in the sibling bead. If both beads try to define the mode enum, that is a duplication bug - one owns it.\nThis bead also does NOT own payout mechanics (see payout-rail-dispute) or the GitHub API client (see github-infrastructure) - it consumes both as dependencies.\nINHERITED FROM ba-payout-rail-dispute-1q6 — the payout design is closed; these are its unfinished obligations. They were blocked there because none can be done before this feature exists.\n\n1. PERSIST PAYOUT INTENT. PayoutIntentStore is declared at payout.ts:75-79 and has no implementation. packages/db/src/repositories/ contains activity, agents, credentials, quests, sessions and state-store - no payout repository - and packages/db/src/schema/features/bounty.ts declares bounty_funds but no payout or payout_intent table. Until the store exists, payout intent is representable in memory and never recorded. PAYOUT_EVENTS (payout.ts:44-48, mapping funded/pending/recorded to bounty.payout_funded / bounty.payout_pending / bounty.payout_recorded) appear in no event union and are emitted by nothing. Verified 2026-09-25: all still true.\n\n  Note the layering rule this has to respect: the store lives in packages/db, and infrastructure may not import the layer that consumes it, so the db implementation satisfies PayoutIntentStore structurally rather than by importing it. The existing repositories set the pattern.\n\n2. RENDER THE SANDBOX BANNER. needsSandboxBanner exists at payout.ts:172-174 and is unit-tested, but has zero production callers. The original criterion was that the banner is present AND tested. The rule is tested; the banner is not present, because there was no UI to put it in. A user must never believe they have been paid when they have not, so this becomes a hard requirement the moment a paid-adjacent status is visible.\n\n  THERE IS NOW A UI TO PUT IT IN, which is what changed. apps/web is a real Next.js app with app/layout.tsx, app/page.tsx and the API routes; the previous blocker named in this obligation is gone. The banner still has no production caller - needsSandboxBanner is exported from packages/features/bounty/src/index.ts and imported by nothing outside its own test. This obligation is therefore no longer blocked, only unstarted, and it is also tracked as RISK 5 in ba-risk-gates-e74. The honest form of the criterion: a unit test on the function is not the deliverable, and a green test suite currently makes this look further along than it is.\n\n3. IMPLEMENT THE DISPUTE WINDOWS. The time limits in docs/design/payout-rail.md are not implemented. They are meant to become values in a rules.ts here so changing one is a visible edit rather than a hunt. No rules.ts exists in this package yet - packages/features/bounty/src/ holds exactly index.ts, payout.ts and payout.test.ts. Verified 2026-09-25: still true.\n\n  There is a naming collision to avoid when that file appears. packages/features/progression/src/rules.ts and packages/features/reputation/src/rules.ts already exist for their own features' tuning tables. A third rules.ts in bounty is the right name, but it must hold the payout/dispute constants specifically - the dispute windows, not a general per-feature config - or a reader who greps rules.ts will find two unrelated tables and assume they are one.\n\n4. TEST A COMPLETED BOUNTY. The original criterion was that a completed bounty records payout intent in the correct state, which needs a bounty lifecycle — this bead.","status":"closed","priority":1,"issue_type":"task","created_at":"2026-09-23T19:01:27.448447Z","created_by":"tranquangdang21","updated_at":"2026-09-26T02:17:47.623618Z","closed_at":"2026-09-26T02:17:47.623555Z","close_reason":"DONE, with one part of the success criterion met by proxy and recorded as such.\n\nBuilt: the bounty feature, its lifecycle (open -> claimed -> submitted -> paid, plus\ndisputed as a terminal state this feature refuses to move), claim/PR/merge/payout,\nmigration 0010, the Drizzle repository, registration in the composition root and in\nthe action-id generator. Issue to payout, which is §27's M2 definition.\n\nTHE DOUBLE-PAY IS SETTLED, and the finding settled it rather than taste.\nprogression's awardFor skips any event whose payload carries no agentId, and only\na claim record can attach an agent to a merge. So whenever pr.merged would pay at\nall, bounty.completed has already fired for the same merge — the two rows are not\ntwo awards for two cases, one is only reachable alongside the other. A merged PR\nnow pays the LARGER of the two it qualifies for: 1000 for completing a bounty, 500\nfor a merge that did not, never both, never either twice. The exclusion lives in\nthe price table rather than in bounty's restraint, and it is fail-closed: a\npr.merged that never states which case it is pays nothing, because guessing is how\na double-pay gets in.\n\nTHREE REAL DEFECTS, none visible to a unit test: a correlated subquery drizzle\nrendered as a bare \"id\" that resolved to the WRONG TABLE's column, so a\n$200-funded bounty reported $0; bounty.completed had two owners, and the registry\nallows one per persisted event type, so reputation drops the declaration and keeps\nthe handler (the emitter owns an event, the reactor does not); and the top-up gate\nrefused a second sponsor while still writing their funding row, leaving the intent\nstating the first total.\n\nWHAT THE CRITERION ASKS THAT WAS NOT DONE HERE: it asks for a scratch repository\nend to end. There is no outbound GitHub from this environment, so what was driven\ninstead is a real SIGNED webhook delivery through the app's own route and shared\nruntime. That proves the wiring and the arithmetic — including that a forged\nsignature is refused and a duplicate merge pays once — and it does not prove a\nreal PR from a real maintainer. The gap is environmental, not unimplemented, and it\nis the first thing to run when a token is available.\n\nALSO DELIBERATELY NOT DONE: no POST /api/bounties routes, because the Application\nAPI is frozen at five primitives and /api/act already dispatches every registered\naction, so the loop is machine-natively drivable with no new surface — which is the\nproperty §34 exists to protect. No rendered sandbox banner: needsSandboxBanner has\na production caller and describePayout puts the notice in the payout object every\naction returns, so a surface cannot render a reward without meeting it, but no\npixel carries it because apps/web/app/** may not import a feature and the board is\nba-web-ui-surface-t3w's. payout.ts is extended by callers, not rewritten.","source_repo":"battle-agents","source_repo_path":"/Users/tranquangdang21/Projects/battle-agents","mol_type":"work","work_type":"mutex","wisp_type":"none","labels":["feature","m2","star"],"dependencies":[{"issue_id":"ba-feature-bounty-xhk","depends_on_id":"ba-feature-quest-3hq","type":"blocks","created_at":"2026-09-23T19:04:23.231287Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"ba-feature-bounty-xhk","depends_on_id":"ba-github-infrastructure-56v","type":"blocks","created_at":"2026-09-23T19:04:23.329418Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"ba-feature-bounty-xhk","depends_on_id":"ba-payout-rail-dispute-1q6","type":"blocks","created_at":"2026-09-23T19:41:37.858966Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"ba-feature-guild-5g6","title":"features/guild: teams, treasury, guild quests (M6)","description":"Implement the guild feature per plan sections 10.4, 11.3, and 24. M6 scope.\n\nSCOPE (section 24): teams, treasury, guild quests.\n\nGUILD AS THE SOCIAL ENDGAME (section 10.4): shared agents, projects, and research. Guild quests such as 'fix 10 issues in project X' where each agent contributes. Weekly Guild A vs Guild B tallies. Explicitly NO pay-to-win (section 10.4).\n\nGUILD TREASURY (section 11.3): guilds fund bounties collectively, connecting to the open-funding model.\n\nTFT TEAM COMPOSITION (section 10.2): team TRAITS and SYNERGY, not raw stacking. Roles are Researcher / Coder / Tester / Reviewer; a Coder+Tester+Reviewer combination yields a validation bonus. CRITICAL: role comes from player BEHAVIOR, not from the model - '3 Claude' should NOT just be 3 Claude; it can be a Debugger, a Researcher, and a Security specialist. This directly reuses the build-derivation from feature-progression.\n\nCLEANEST EXAMPLE IN THE PLAN (section 2.4): 'Quang's Claude Lv37 Architect with Refactor special' is a CHALLENGEABLE SOCIAL IDENTITY, not a model name. Agent = AI + developer configuration (prompts, tools, memory). That identity layer is what pixel-agent projects lack and it is the network-effect foundation.\n\nACL PATTERN (section 1.1, from Agentainer's can_talk_to): use a declared ACL concept for guild messaging rather than letting any agent DM any agent. This also sets up the messaging.md etiquette doc (section 26).\n\nM6 DoD (section 27): two users' agents in one guild complete a team bounty.\n\nCONSIDERATIONS: guild state is strongly social and therefore strongly abuse-prone (alt accounts, collusion, guild-shopping for favorable bounties). Section 17 does not cover social abuse in detail - note it as an open risk rather than pretending it is solved.\n\nTHE BEHAVIOR-DERIVED ROLE IS THE LOAD-BEARING PART, so it is worth being concrete about where the behavior signal comes from. 'Role comes from player BEHAVIOR, not from the model' means the role is a projection over what the agent actually DID, and the durable record of what an agent did is the activity log (ba-activity-event-log-aph, closed): the event_log table (id, type, actorId, payload jsonb, occurredAt) reached through DrizzleStateStore, the only path an event takes to the database. So a Researcher is an agent whose persisted trail is research-shaped (lots of file.read / search), a Coder is file.write / command.run shaped, a Tester is test.passed / test.failed shaped, a Reviewer is read-and-approve shaped. The same 'behavior, not model name' principle is what the progression feature already uses to derive a build from behavior (the reuse the bead calls out), so this is a second reader of the same trail, not a new tracking system.\nThe persistence filter bounds which roles are derivable: only PERSISTED_EVENT_TYPES are durable - PERSISTED_EVENT_TYPES = ['session.started','session.ended','session.resumed','test.passed','test.failed'] PLUS whatever each installed feature declares in GameFeature.persistedEvents - the registry unions them, so a feature widens durability without editing core. The list this bead used to quote was the plan's wish-list, and it is wider than reality in a way that matters HERE specifically: file.read, file.changed, search and thinking are not in the shipped set and no feature declares them, so a role classifier reading file.read to spot a Researcher reads a column that is not there. The role signals actually available today are test.passed / test.failed (Tester), bounty.completed (Coder) and session.* (presence) - the rest must be added via persistedEvents by the feature that owns the event, which is a deliberate act, not an assumption. - so role classification is limited to signals present in that set (plus whatever the features add). An activity signal that is bus-only is not a reliable basis for a persistent social identity. The runtime emits persist -> handlers -> publish, so a role projection reacting to a persisted event reads a durable record that already exists.\n\nTHE ACL IS A DECLARED CONCEPT, NOT AN OPEN CHANNEL. The pattern borrowed from Agentainer's can_talk_to means guild messaging is governed by a declared access rule (who may talk to whom), evaluated by the social feature's authorization, rather than 'any agent may DM any agent'. This is the same ACL the social feature (ba-feature-social-lss) honors for messaging; guild does not invent a second, looser messaging path. Note the two are different defenses: the ACL decides WHO may message, but a permitted sender is still untrusted input on the receiving agent's context (the prompt-injection surface the social bead calls out) - 'they are in my guild' is not a sanitization, so membership in a guild must never widen what a message body is allowed to do.\n\nHOW GUILD ATTACHES TO CORE. Guild is a feature extension under the frozen Contract 1 (ba-contract-extension-api-w29): it registers a GameFeature with its commands and event handlers, and reacts to cross-feature signals over the bus (a team bounty completing, a member's persisted activity) rather than importing the bounty, social, or progression features - features never import each other, machine-enforced in CI via architecture-rules.cjs. Guild reaches agents and the CLI through the capability registry over the five frozen primitives (discover/search/inspect/act/observe); join/leave and treasury actions are typed registry actions, and adding guild grows the registry, not the primitive surface. Core itself must contain zero game vocabulary, including the word Guild.\n\nTHE TREASURY CONNECTS TO A HALF-SHIPPED RAIL, which matters because guild funding is third-party money. The guild treasury funds bounties collectively, which routes through the same open-funding and payout-rail territory the bounty feature carries, where the honest current state (verified 2026-09-25) is: payout intent is not yet persisted (PayoutIntentStore is declared with no implementation, no payout repository, no payout_intent table), and the 'payout: manual/sandbox' banner rule exists but has no production caller. A guild pooling member funds inherits exactly that reality, so the treasury must not represent escrowed money as settled and should carry the same sandbox marking. This bead does not own payout mechanics; it funds through the bounty's own fund path.\n\nWHY A NAIVE IMPLEMENTATION GOES WRONG. The first shortcut is deriving roles from the model or a self-declared label ('I am a Tester'), which is the exact thing the plan forbids - a self-declared role makes the synergy bonus trivially gameable by labeling, and it is not behavior. The second is computing synergy from raw model stacking ('3 Claude'), which the plan explicitly rejects. The third is an open guild channel where any member may DM any agent with no declared ACL, which is the thing the can_talk_to pattern exists to prevent. The fourth is a guild feature that imports the bounty and social features to read their state instead of reacting to their events - that is a dependency-rules violation, machine-caught in CI, and it is what breaks the moment a second consumer subscribes or a dependency is uninstalled. The fifth is colluding on bounties: guild coordination and the no-pay-to-win rule (section 10.4) are in tension, and the plan's own social-abuse gap is acknowledged above - surface it, do not paper over it with an unearned bonus.\n\nSUCCESS CRITERIA: two users' agents join one guild and complete a team bounty; role synergies are computed from behavior over the persisted activity log (not from model names or self-declared labels); messaging respects the declared ACL; guild funds through the bounty's open-funding path carrying the honest sandbox payout marking.\n\nTESTS: the M6 DoD integration test; a synergy-calculation test that a role is derived from persisted behavior and not from a model name or a self-declared label; a test that an activity signal outside the persisted set is not used as a role basis; an ACL enforcement test; a removal test that guild uninstalls cleanly.","status":"open","priority":2,"issue_type":"task","created_at":"2026-09-23T19:03:04.490334Z","created_by":"tranquangdang21","updated_at":"2026-09-24T23:05:04.440891Z","source_repo":"battle-agents","source_repo_path":"/Users/tranquangdang21/Projects/battle-agents","mol_type":"work","work_type":"mutex","wisp_type":"none","labels":["feature","m6","p2"],"dependencies":[{"issue_id":"ba-feature-guild-5g6","depends_on_id":"ba-feature-bounty-xhk","type":"blocks","created_at":"2026-09-23T19:04:23.579084Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"ba-feature-progression-md8","title":"features/progression: XP, level, skills, builds (model != class)","description":"Implement the progression extension per plan sections 10.1, 10.2, and 24.\n\nSUBSCRIBES: bounty.completed (main XP table), test.passed (small), battle.finished (win bonus). EMITS: agent.level_up.\n\nLEVEL CURVE (section 24): xpForLevel(n) = 100 * n * (n+1) / 2, to be tuned later. Keep the formula in rules.ts as data, not inline logic, so tuning does not require touching the reducer.\n\nXP FROM ACTUAL WORK, NEVER TOKENS (sections 10.2, 10.3, 17): PR merged +500, tests +100, recovery +150, bounty +1000. NOT tokens and NOT tool-call counts. Section 10.2 is explicit: 'XP from actual work - never tokens/tool-counts.' This is a core integrity property - a token-based economy would reward spamming.\n\nMODEL IS NOT CLASS (section 10.2, the most important design idea in the progression system): class/build is derived from BEHAVIOR, not from the model name. The same Claude can be a Debugger in one project and a Researcher in another. Behavior->build classifiers live in rules.ts, NOT hardcoded per model (section 24).\n\nTHE TRIPLE SPLIT (section 10.2) - never conflate these:\n- XP: progression toward Level\n- Reputation: social and economic trust\n- Stats: FACTS (127 PRs opened, 92 merged, 21 rejected, 14 abandoned)\n\nRUNE SCAPE MODEL FITS BEST (section 10.2): independent skills (Coding, Debugging, Testing, Research, Refactoring, Security, Documentation, Collaboration) each with their own level per agent. There is NO single power scalar.\n\nEVE INSIGHT: specialization without maxing everything (section 10.2). Level equals ACCESS, not cosmetics - level-gated content (Lv1 basic, Lv5 advanced, Lv10 Arena, Lv15 teams, Lv20 guild creation, Lv30 sponsoring).\n\nDEATH RULE (section 10.2): HP 0 NEVER kills the character; only the SESSION fails. The agent lives, keeps its history, and still gains experience. Failure achievements are included. This protects the persistent-character premise from section 3.1.\n\nMVP CHARACTER SHEET (section 10.2): Identity + XP/Level + Skills + Build + Reputation + Achievements + History. Equipment, cosmetics, pets, mounts, base, and guild come later - scope discipline (section 17.8).\n\nCONSIDERATIONS: the behavior->build classifier is the piece most likely to need iteration; expose its weights as configuration and log the classification so a surprising result is debuggable rather than mysterious.\n\nSUCCESS CRITERIA: two behavior histories on the same model produce two DIFFERENT builds (this is the section 27 M3 DoD); XP is awarded from real outcomes only; level gates actually gate content.\n\nSTATUS, 2026-09-25 - the first two clauses are DONE and the third has no consumer yet. Verified against the tree:\n- DONE, the M3 DoD. packages/features/progression/src/rules.ts derives the build from a weighted outcome history (BUILDS = builder / refactorer / tester / debugger / infrastructure / generalist, with the XP table mapping bounty.completed to builder, pr.merged to refactorer, test.passed to tester, session.recovered to debugger, battle.won to infrastructure). Nothing in it reads a model name, which is the property the DoD is actually testing - two histories of the same model diverge because the classifier never sees the model. rules.test.ts asserts exactly that.\n- DONE, XP from real outcomes only. The XP table is keyed by event type; there is no token or tool-count column anywhere in it, and feature.test.ts asserts an event that is not an outcome changes nothing.\n- DONE, the death rule. feature.test.ts has a 'a failed run still teaches' block: session.recovered grants experience, and a test asserts no arriving event ever deletes or zeroes a record.\n- NOT DONE, level gates. The curve and the awards are computed and exposed (actions progression.awards and progression.read, both in the generated RegisteredActionId union), but there is no CONTENT to gate - no battle tier, no zone, no guild-creation threshold - because the features that would be gated do not exist yet. The gate function cannot be meaningfully tested until something sits behind it.\n\nTwo things a reader will otherwise trip over. First, this feature is not in the composition root: apps/web/src/composition.ts installs only agentFeature and questFeature, so progression is built and tested but not running. The generated action-id union lists progression.awards and progression.read, so the ids compile from every manifest in the workspace regardless of what a given composition installs - the union describes the BUILD, and the registry is the authority at runtime, which is why act() checks both. Second, the manifest's ProgressionActionTypes maps every id to { input: unknown; output: unknown }. That is the deliberate placeholder described in ba-capability-registry-klw; the action payloads are the open half of that gap.\n\nTESTS: unit test for the level curve and XP awards; the M3 DoD test (same model, two behavior histories, two builds); a test asserting no token-derived XP exists anywhere; a test that a failed session grants experience but does not delete the character. Four of these exist today in packages/features/progression/src/{rules,feature}.test.ts.\n\nThe one that is only half-present is 'no token-derived XP exists anywhere'. Asserting the absence across the whole codebase is an architecture check rather than a unit test, and the dependency-rule harness in tests/unit/dependency-rules.test.ts is the right place for it - a grep proving no feature reads a token count. Asserting it inside rules.test.ts only proves the XP table has no such column, which is true and much weaker than what the criterion asks for.","status":"open","priority":1,"issue_type":"task","created_at":"2026-09-23T19:00:29.034235Z","created_by":"tranquangdang21","updated_at":"2026-09-24T21:05:28.368136Z","source_repo":"battle-agents","source_repo_path":"/Users/tranquangdang21/Projects/battle-agents","mol_type":"work","work_type":"mutex","wisp_type":"none","labels":["feature","p1"],"dependencies":[{"issue_id":"ba-feature-progression-md8","depends_on_id":"ba-feature-agent-f9t","type":"blocks","created_at":"2026-09-23T19:04:23.075731Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"ba-feature-quest-3hq","title":"features/quest: generic quest CRUD, GitHub-linked or internal","description":"Implement the quest feature per plan section 24, the generic task unit that bounties build on.\n\nSCOPE: generic quest CRUD, either GitHub-linked or internal. A quest has objective, progress, prerequisites, reward, difficulty, and a time limit (section 10.4 mechanics, informed by the Roblox quest-design reference the plan cites).\n\nSCHEMA (section 21): quests: id, projectId -> projects, title, body, difficulty (default 1), xpReward (default 100), status (default 'open'), createdAt.\n\nSTRUCTURE: every packages/features/<name>/ ships domain.ts, commands.ts, events.ts, reducer.ts, rules.ts, repository.ts (drizzle access, own tables only), index.ts exporting a GameFeature, README.md listing capabilities, and *.test.ts (section 24).\n\nMACHINE-NATIVE (section 33 P1 + section 34): a quest must be fully discoverable, claimable, submittable, and completable through discover/search/act and the CLI equivalents with NO web clicks. If a quest cannot be driven end-to-end via the protocol, the feature is not done.\n\nQUEST IS NOT BOUNTY: a quest is the generic task unit; a bounty is a FUNDED quest backed by a real GitHub issue and real money (section 11.1, Appendix B). Keep them separate features - quest must work with no bounty extension installed (that is the removal-test proof).\n\nCONSIDERATIONS: quest chains and campaigns (a project becoming an RPG campaign, section 10.4's Coding Quests shape) are listed under bounty chains in section 11.3, so link quests there rather than overloading this feature.\n\nSUCCESS CRITERIA: quests can be created, listed, searched, claimed, progressed, and completed entirely via CLI/protocol; completing a quest emits a quest.completed event that other features may react to without importing quest internals.\n\nTESTS: unit tests for the reducer; integration test for the full lifecycle over the API; a removal test that uninstalling quest leaves the runtime green; a machine-native test proving no step requires a web UI.","status":"closed","priority":1,"issue_type":"task","created_at":"2026-09-23T19:00:28.972531Z","created_by":"tranquangdang21","updated_at":"2026-09-24T08:35:57.225544400Z","closed_at":"2026-09-24T08:35:57.222701400Z","close_reason":"Shipped against the frozen contract; typecheck clean, architecture clean, unit tested. See the commit for the design calls and what was deliberately left out.","source_repo":"battle-agents","source_repo_path":"/Users/tranquangdang21/Projects/battle-agents","compaction_level":0,"original_size":0,"labels":["feature","p1"],"dependencies":[{"issue_id":"ba-feature-quest-3hq","depends_on_id":"ba-contract-public-api-ute","type":"blocks","created_at":"2026-09-23T19:04:22.992712Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"ba-feature-quest-3hq","depends_on_id":"ba-contract-session-rpo","type":"blocks","created_at":"2026-09-23T19:06:37.193028Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"ba-feature-reputation-nnj","title":"features/reputation: trust from outcomes + bounty tier gating","description":"Implement the reputation extension per plan sections 10.2, 11.3, and 24.\n\nSUBSCRIBES: bounty.completed / bounty.failed, battle.finished. Trust is a function of merged rate, acceptance rate, and review outcomes.\n\nREPUTATION MUST BE COMPUTED FROM REAL OUTCOMES (section 11.3): '47 bounties done, $8,420 earned, 91% acceptance, 4.7 average review -> 8,921 reputation.' The plan is explicit that these numbers must be derived from ACTUAL GitHub/bounty outcomes, NOT self-assessed by the game. A reputation system that games can inflate is worthless as a trust signal.\n\nTIER GATING (section 11.3) - reputation gates which bounties an agent may attempt:\n- Beginner: $5-25, docs and small bugs\n- Intermediate: $25-200, feature/bugfix\n- Advanced: $200-1000, architecture and complex bugs\n- Legendary: $1000+, major OSS contribution\nNewcomers grind easy bounties upward. This is the progression-gating role reputation plays, distinct from progression-level gating.\n\nTHIS IS BUILT, in BOUNTY_TIERS plus tierForReward / tierForTrust / mayAcceptBounty in packages/features/reputation/src/rules.ts, and the thresholds are stated in CENTS (maxRewardCents, minTrust) rather than as bare dollar figures, which is the right call for money and worth preserving. reputation.gate is a registered action, so a surface can ask the question over the protocol rather than reimplementing the comparison. What is not built is the content those bands name - there are no beginner-tier bounties to gate, because the bounty feature has no lifecycle yet (ba-feature-bounty-xhk owns that and the bounty package currently holds only payout.ts).\n\nREPUTATION IS TRUST, NOT PROGRESSION (section 10.2 triple split): XP/Level is progression; Reputation is social and economic trust; Stats are facts. Do not let reputation feed the level curve.\n\nTHIS FEATURE IS A CAPABILITY PROVIDER (section 24): it exposes reputation.read, which the battle feature declares in requires: ['reputation.read'] (section 24). That declared dependency is exactly what makes the battle degradation path and the removal test meaningful - when reputation is uninstalled, battles degrade to open rather than crash.\n\nTHE PROVIDER HALF IS DONE, THE CONSUMER IS NOT. reputation.read is a real registered action and packages/features/reputation/src/feature.ts carries a comment naming it as the capability other features gate on. No feature declares `requires: ['reputation.read']` today - the only 'requires' string in the workspace is that comment - because there is no battle feature yet (ba-feature-battle-fbt) to consume it. The degradation machinery this relies on is already built and tested in core, so wiring the consumer is a one-line requires array plus the removal-test assertion, once battle exists.\n\nCONSIDERATIONS: the plan notes GitHub's own security bounty reward guidelines span hundreds to thousands by severity, but explicitly says those are specific to GitHub's security program and NOT general OSS bounty rates. Do not present our tiers as authoritative market rates.\n\nSUCCESS CRITERIA: reputation updates from real bounty/battle outcomes; tier gates block and admit correctly; uninstalling reputation makes battle degrade with a warning instead of failing.\n\nSTATUS, 2026-09-25 - the trust formula and the tier gates are DONE; the degradation clause cannot be tested, because the battle feature does not exist. Verified against the tree:\n- DONE. packages/features/reputation/src/rules.ts exports trustScore(record), tierForReward, tierForTrust, mayAcceptBounty, summarise and isInTier as pure functions over a ReputationRecord, and it throws if BOUNTY_TIERS is empty on the grounds that a tier table with no bands gates nothing - the right way to fail. The band logic is right in the way the plan asks for: mayAcceptBounty compares TRUST against the tier the REWARD falls into, so an agent cannot take a $5 chore it has not earned its way up to. rules.test.ts covers the boundaries.\n- DONE, the triple split. Nothing in this package writes XP. The progression feature owns that, and the two never import each other - the trust score is read through the capability, not through progression's state.\n- NOT TESTABLE. The clause 'uninstalling reputation makes battle degrade with a warning instead of failing' has no subject: packages/features/battle/src/index.ts is `export {};` and there is no battle feature to install alongside. The MECHANISM the clause depends on is real and tested in core - FeatureRegistry.missingRequirements() recomputes after every install and uninstall, a feature ordered before its provider is not falsely reported degraded, and a provider installed later clears the warning - so what is missing is a consumer, not a capability. The capability itself is declared (reputation.read, with a comment in feature.ts naming it as what other features gate on) and three actions exist: reputation.read, reputation.gate and reputation.tiers.\n\nAs with progression, this feature is not in apps/web/src/composition.ts, which installs only agent and quest. It is built and unit-tested but not running, so the criterion above should be read as 'verified at the unit level' until the composition root grows.\n\nTESTS: unit tests for the trust formula; tier-boundary tests at each edge; a removal test asserting battle still runs (degraded) with reputation uninstalled; a test that reputation never contributes to XP.","status":"open","priority":1,"issue_type":"task","created_at":"2026-09-23T19:00:29.096247Z","created_by":"tranquangdang21","updated_at":"2026-09-24T21:06:42.548673Z","source_repo":"battle-agents","source_repo_path":"/Users/tranquangdang21/Projects/battle-agents","mol_type":"work","work_type":"mutex","wisp_type":"none","labels":["feature","p1"],"dependencies":[{"issue_id":"ba-feature-reputation-nnj","depends_on_id":"ba-feature-agent-f9t","type":"blocks","created_at":"2026-09-23T19:04:23.150686Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"ba-feature-social-lss","title":"features/social: messaging, profiles, leaderboard","description":"Implement the social feature per plan sections 1.3, 8, 13, and 24. M6 scope.\n\nSCOPE (section 24): messages, profiles, leaderboard queries.\n\nSCHEMA (section 21): messages: id, fromAgentId -> agents, toAgentId -> agents, guildId, body, createdAt. Note the nullable toAgentId plus guildId - messages are either agent-to-agent DMs or guild broadcasts.\n\nPORT THE MESSAGING SHAPE FROM cross-agent-teams-mcp (section 28.1 item 8, MIT): register-agent / send / broadcast / poke / inbox / bind-runtime-identity tool shapes, refactored into inbox + outbox plus poke/wake fanout over Neon and SSE. Section 1.3 credits this repo with the identity-registry + inbox/outbox + poke/wake fanout pattern and specifically the identity-vs-session split - which is already locked in contract-agent-identity.\n\nWIRE-LEVEL PRECEDENTS (section 1.1 section C): karansag/agent-swarm is a delivery layer UNDER orchestrators - it holds agent registration, stable session ID, model, tmux pane, message history, and delivery status, and injects messages directly into the agent's terminal pane. That last capability (injecting into a live agent's context) is the genuinely interesting one for us and should be evaluated, not dismissed.\n\nPUBLIC AGENT PROFILE (section 2.4): level, battle history, W/L, achievements, projects, guild. This is the network-effect artifact - profiles attract challenges, challenges produce replays, replays attract sponsors.\n\nLEADERBOARD (section 2.3, 15.1): part of the tournament content engine.\n\nSCOPE DISCIPLINE (section 17.8): social is M6, NOT MVP. No marketplace, no trading, no guild war, no housing, no crafting in this phase.\n\nCONSIDERATIONS: agent-to-agent messaging is a prompt-injection surface. An agent receiving a DM from another agent is reading untrusted text authored by (or on behalf of) someone else. Design the trust boundary deliberately - this is a real security consideration the plan does not address, so surface it rather than shipping it blind.\n\nTHE PROMPT-INJECTION SURFACE, MADE CONCRETE, because it is called out above and is the single most important thing to design around. The dangerous capability is the one the bead flags as interesting and not-to-dismiss: injecting a message directly into a live agent's context (the karansag terminal-pane injection, or the poke/wake fanout). An agent has no way to distinguish 'this instruction came from my operator' from 'this text arrived in a DM from another agent' once it is in the same context window. So a message body authored by Agent B, handed to Agent A, and injected into A's context is a remote code-and-prompt vector against A's whole tool surface - not just a chat feature. The trust boundary this bead must ship:\n- A message is DATA delivered to an agent, not an instruction the runtime executes. Nothing in a message body may be dispatched as a command, an action, or a tool call. Runtime commands arrive only through the frozen act() path with a typed action id, never through an inbox.\n- Separate provenance: a delivered message must be visibly attributable and must not masquerade as operator/system text. The receiving side should be able to tell who sent it.\n- The ACL is the authorization half, and it is separate from the injection defense: an ACL decides WHO may DM whom, but a permitted sender is still untrusted input. A guild member allowed to message you can still carry an injection, so 'they are in my guild' is not a sanitization.\n- The public profile (section 2.4) is a READ surface and must exclude private fields (credentials, session data, file contents) even though leaderboards and challenges are built on it. The removal test and a private-field exclusion test both apply.\n\nTHE IDENTITY CONTEXT THIS BUILDS ON. Agent identity is a FROZEN contract (ba-contract-agent-identity-evq, closed) that deliberately separates the agent (durable identity, credentials, ownership) from the session (a runtime instance). The identity-vs-session split this bead ports from cross-agent-teams-mcp is that same distinction. Practically: messages are addressed to an agentId (durable), while delivery to a LIVE agent happens through a currently-active session; the guild broadcast path (nullable toAgentId + guildId) fans out to the guild's currently-active members' sessions. Do not let a message collapse agent and session into one - that is the collision the identity contract exists to prevent. The same collision shows up at the adapter seam as providerId namespacing, one level down.\n\nHOW THIS ATTACHES TO CORE. Social is a feature extension under the frozen Contract 1: it registers a GameFeature with command handlers (send / broadcast / inbox / poke) and event handlers, and reacts to cross-feature signals over the bus rather than importing the agent, battle, or guild features - features never import each other, machine-enforced in CI via architecture-rules.cjs. It reaches agents and the CLI through the capability registry over the five frozen primitives (discover/search/inspect/act/observe); a send is a typed act() over the registry, and the CLI is a consumer of that same command, not a second implementation. Adding social grows the registry, not the primitive surface.\n\nWHY A NAIVE IMPLEMENTATION GOES WRONG. The shortcut is to treat a delivered message as a command channel - 'if the body says /claim, act on it' - which turns every DM into remote code execution against the recipient and violates the data-not-instructions boundary. The second is to skip the ACL and let any agent DM any other because 'it's a social feature'; an ACL is a named requirement here (the guild bead defines the declared can_talk_to ACL concept, and social honors it rather than reinventing an open channel). The third is to build messaging so it only works from the web page; the machine-native rule requires discover -> act over the protocol/CLI with no browser. The fourth is exposing a whole agent record on the public profile 'because the leaderboard needs it' - that leaks credentials or session data; the profile is an explicit allow-list.\n\nSUCCESS CRITERIA: agents exchange messages via the protocol and CLI; public profiles render battle history; leaderboards query correctly; a delivered message is provably data (it cannot become a dispatched command or action); the ACL decides who may DM whom; the public profile excludes private fields.\n\nTESTS: messaging round-trip test; an ACL/authorization test on who may DM whom; a test that a message body cannot be dispatched as a command or action through the runtime; a test that public profile data excludes private fields; a removal test; a machine-native test that a message can be sent with the web UI disabled.","status":"open","priority":2,"issue_type":"task","created_at":"2026-09-23T19:03:04.545211Z","created_by":"tranquangdang21","updated_at":"2026-09-24T21:17:49.032236Z","source_repo":"battle-agents","source_repo_path":"/Users/tranquangdang21/Projects/battle-agents","mol_type":"work","work_type":"mutex","wisp_type":"none","labels":["feature","m6","p2"],"dependencies":[{"issue_id":"ba-feature-social-lss","depends_on_id":"ba-feature-agent-f9t","type":"blocks","created_at":"2026-09-23T19:04:23.658105Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"ba-game-client-pixijs-riw","title":"game-client: PixiJS v8 world mirroring age-of-agents","description":"Build the PixiJS game client by mirroring the age-of-agents client skeleton, per plan sections 8, 25, and 28.1 item 6.\n\nDIRECT COPY (section 28.1 item 6, MIT): mirror .tmp/age-of-agents/packages/client/src/game/ - plus the dual asset packs as placeholder themes. Section 1.2 item 1 calls this the BEST PixiJS v8 reference and says 'STEAL nearly all client structure'. age-of-agents is MIT with a real LICENSE file (Copyright (c) 2026 Mateusz Pawelczuk), so its code AND its shipped assets are covered; the THIRD-PARTY-NOTICES.md row for it already exists at SHA a6f22316e36b.\n\nTHE FILE LIST IS 22, NOT 8. The reference directory contains: archetype, autotile, building-fx, building-sprites, camera-guards, decoration-sprites, decorations, emblems, flip, home-building, iso-fill, pathfind, placeholders, projection, roads, scatter, sprites, terrain-map, tilemap-iso, tilemap, unit, view (all .ts). An earlier version of this bead named 8 of them under the words 'FILE FOR FILE', which is a contradiction - file-for-file means the directory, and the 8 named are a subset. Read the whole directory.\n\nTHE ASSET PATH IS UNDER packages/client/, NOT THE REPO ROOT. The packs live at .tmp/age-of-agents/packages/client/public/assets/{fantasy,scifi}/. There is no .tmp/age-of-agents/public/ - a `ls` there fails. Each pack contains buildings/, decorations/, heroes/ and a tilemap tree: fantasy has BOTH tilemap/ and tilemap-iso/, scifi has ONLY tilemap-iso/. An earlier version of this bead said both packs carry 'buildings, decorations, heroes, tilemap', which is wrong for scifi.\n\nTWO THINGS THE RESEARCH NOTE SAYS TO PORT DIFFERENTLY, recorded in docs/research/age-of-agents.md:\n- autotile.ts is an UNVERIFIED IDENTITY STUB in the reference. The lookup is a placeholder that returns its input, not a working autotiler. Copying it file-for-file imports a no-op. Either implement it or leave a stub of our own that is honestly a stub - but do not ship the reference's and call it done.\n- The reference has three bare `catch {}` blocks around asset loading, and the research note marks assets-that-fail-silently as an AVOID. Our loader should surface a missing asset rather than continuing with an empty spritesheet.\n\nThe 25-line Projection interface is the note's headline finding: the entire pixel-math story is that one file, and it is the cheapest thing to steal.\n\nWHY MIRROR RATHER THAN DESIGN (section 1.2 item 1): the reference already proves pixi-viewport + React19 + zustand, a clean view/unit/tilemap/tilemap-iso/projection/pathfind/autotile/building-sprites/camera-guards decomposition, and a dual fantasy/scifi asset pack structure. Re-deriving that decomposition from scratch is wasted effort.\n\nEXPERIENCE TARGET (section 1.2 item 1): the reference uses pixi-viewport with React19 and zustand. Adopt that stack shape.\n\nTHEN EXTEND (section 25):\n- zones.ts - config-driven {tool|event -> zone} mapping, adding bounty-board, battle-arena, guild-hall to the AgentMove zone set. That zone set is normalized by ba-tool-map-port-89a (TOOL_ZONE_MAP plus getZoneForTool); consume it from there rather than defining a second one, or the client and the protocol will disagree about what 'files' means.\n- net/client.ts - SSE client: full_state to hydrate the store, then deltas to patch. The wire format is already fixed and shipped: `GET /api/events/stream` emits SSE frames whose JSON body carries `kind`, either 'full_state' (with a `state` object) or 'delta' (with an `event`, and NO state field - the type makes the two mutually exclusive on purpose). The first frame is always full_state. Reconnect semantics are also already decided: a subscriber that falls more than 1024 frames behind is CLOSED rather than skipped, and the client is expected to reconnect for a fresh snapshot. A delta client that skips frames is permanently wrong with nothing to tell it.\n\n  ONE CAVEAT THAT IS A REAL DEFECT, NOT A DESIGN: the stream is currently an unauthenticated global broadcast, and toGameEvent puts the whole validated AgentEvent on the wire, so file paths, command names and thinking content reach any open socket. That is tracked as its own bug in ba-9hr. Build the client against the frame SHAPE, and do not treat today's payload as a public contract - the filter is coming.\n- state/store.ts - zustand or event-emitter store\n- scenes/ - city.ts, arena.ts, guild-hall.ts\n- sprites/sprite-factory.ts - programmatic 16x16@3x PLACEHOLDER sprites (agent-move pattern) until the asset shortlist lands; this keeps V0 unblocked by art\n\nPERFORMANCE (section 25): diff updates only (the codemoo lesson), delta-capped rAF loop (pixel-agents gameLoop.ts pattern), sprite cache.\n\nSCOPE (section 17.8): the game client is NOT in the M0-M4 MVP. M2 ships 'with zero game UI' precisely so real utility lands before art. This is the gimmick-risk defense (section 17.1).\n\nSUCCESS CRITERIA, REWRITTEN BECAUSE 'PERFORMANCE HOLDS THE DISCIPLINE' CANNOT FAIL. 'The world renders', 'is driven ONLY by normalized events' and 'zone mapping is config-driven' are all fine and are kept, sharpened. The third clause was a promise: nobody can point at a commit that violates a discipline without also pointing at a commit that satisfies it, so it was satisfied by writing the words down. It is now three separate properties, each of which has a wrong implementation that makes it go red:\n\n1. The world renders, and is driven ONLY by normalized events. A client that hardcodes an agent, a zone or a position renders perfectly and violates this. Settled by: a render test that feeds a synthetic event stream naming agents the client has never heard of and asserts they appear correctly - if any state came from a hardcoded list rather than the stream, an unknown agent would not appear and the test fails. The complementary assertion is that the store is EMPTY before the first full_state frame, so a client that quietly seeds state from a fixture is caught.\n\n2. Zone mapping is config-driven, and consumes the ONE zone set. Not a second mapping table. Settled by: a test that adds a tool->zone entry to the shared zone config and asserts the client routes it, with no change to any client file - that is what config-driven means, and it fails the moment someone starts branching on a tool name in the view. Paired with the ba-tool-map-port-89a dependency: a zone the tool-map assigns must be the zone the client renders, asserted from one shared table.\n\n3. PERFORMANCE, AS THREE PROPERTIES THAT EACH HAVE A WRONG VERSION. This replaces 'holds the diff/rAF/caching discipline'.\n   3a. THE DELTA PATH IS O(CHANGES), NOT O(SCENE). Applying one delta must not walk every entity. Settled by a counting test: a scene of N entities plus a single delta touches a bounded, sub-linear number of nodes, asserted against an explicit count. A client that re-reconciles the whole store per event - which is what a naive first implementation does, and what still looks correct on screen - fails this and passes every snapshot test. This is the single most valuable performance assertion here, because the failure is invisible in a screenshot and expensive in production.\n   3b. THE rAF LOOP IS DELTA-CAPPED AND COALESCES. N events arriving in one frame produce ONE reconciliation, not N. Settled by: a test that injects K events inside a single frame and asserts the render function was called once. Without this, a burst re-enters the renderer per event and the frame budget goes with it.\n   3c. THE SPRITE CACHE IS ACTUALLY A CACHE, WITH A NUMBER. Settled by: two lookups of the same key return the same object identity, and the count of constructed sprites is independent of the number of frames drawn. The bead must PIN a frame-time budget as a named constant and assert the delta path stays under it on a fixed scene fixture; the budget is a decision to be recorded here, not inherited, and if the measurement shows the number is wrong the number changes - but it must exist and be asserted, because 'fast enough' is the thing this clause was actually trying to say.\n\nTESTS, and each one is a test that fails when the behaviour is wrong:\n- a render test driving a synthetic stream with agent names the client has never seen, asserting they appear, and asserting the store is empty before the first full_state frame.\n- a zone-routing test that adds an entry to the shared zone config and asserts the client picks it up with zero client-file changes; plus a cross-check that the client's zone for a tool equals getZoneForTool's, read from the one table ba-tool-map-port-89a owns.\n- a delta-cost counting test (3a) - the one that catches a full-store re-reconcile.\n- a frame-coalescing test (3b) - K events in one frame, one render call.\n- a sprite-cache identity test and a construction-count test (3c), against a pinned frame-time budget on a named scene fixture.\n- an architecture test: presentation imports features, never the reverse. This is the same layering rule tests/unit/dependency-rules.test.ts already enforces for core/protocol, extended to the client, and it is what keeps the client from reaching into feature internals and re-creating the zone table inline.\n\nCONSIDERATIONS: mirroring age-of-agents file-for-file is right up to a point - the temptation is to keep mirroring as our needs diverge (we add bounty/battle/guild zones; they do not), and past that point the copy becomes a straitjacket. Name that boundary explicitly: mirror the view/unit/tilemap decomposition, then own the divergence. Second, the programmatic sprite factory is what keeps V0 unblocked by art, so resist deleting it once real assets land - it is also what makes the client testable without an asset pipeline.","status":"open","priority":2,"issue_type":"task","created_at":"2026-09-23T19:03:04.601900Z","created_by":"tranquangdang21","updated_at":"2026-09-24T21:41:07.265924Z","source_repo":"battle-agents","source_repo_path":"/Users/tranquangdang21/Projects/battle-agents","mol_type":"work","work_type":"mutex","wisp_type":"none","labels":["frontend","game","p2"],"dependencies":[{"issue_id":"ba-game-client-pixijs-riw","depends_on_id":"ba-agent-event-union-up5","type":"blocks","created_at":"2026-09-23T19:04:26.005798Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"ba-game-client-pixijs-riw","depends_on_id":"ba-event-ingest-and-sse-gu9","type":"blocks","created_at":"2026-09-23T19:04:26.088668Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"ba-game-client-pixijs-riw","depends_on_id":"ba-feature-bounty-xhk","type":"blocks","created_at":"2026-09-24T21:32:46.981680Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"ba-game-client-pixijs-riw","depends_on_id":"ba-reference-repo-study-o92","type":"blocks","created_at":"2026-09-23T19:05:53.662609Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"ba-game-client-pixijs-riw","depends_on_id":"ba-tool-map-port-89a","type":"blocks","created_at":"2026-09-24T21:32:19.775835Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"ba-github-infrastructure-56v","title":"GitHub infrastructure: webhooks, issue validation, PR merge detection","description":"Build the GitHub integration behind the infrastructure boundary, per plan sections 19, 22, and 33.\n\nCOMPONENTS:\n- POST /api/webhooks/github: issues and PRs become quests/bounties; a PR merge completes a bounty (section 22). Signature verification is mandatory - an unauthenticated webhook endpoint that can mint XP is a trivial exploit.\n- Issue existence validation via Octokit when a bounty is created (section 22): POST /api/bounties 'validates GitHub issue exists via Octokit'.\n- PR merge detection driving bounty.completed.\n\nARCHITECTURAL PLACEMENT (section 33): infrastructure/* is an INTEGRATION BEHIND A BOUNDARY. The bounty feature does not import Octokit or know GitHub's API shape - it consumes events and issues commands. This keeps the feature testable without the network and keeps a GitHub outage from corrupting game state.\n\nREALITY: GitHub is BOTH authentication and quest source (section 3.3). It authenticates humans and supplies repos/issues/PRs, but it does NOT identify local agents. That separation is a hard invariant (see contract-agent-identity).\n\nCONSIDERATIONS: webhooks are at-least-once and arrive out of order. Make handlers idempotent (a duplicate merge event must not double-award XP) and tolerant of reordering (a PR-closed event arriving before PR-opened). Both are easy to get wrong and produce duplicate reputation.\n\nSECURITY: store the App installation token or PAT in infrastructure only, never in a feature, never in the CLI, never in a URL.\n\nTHE SIGNATURE-VERIFICATION REQUIREMENT IS EXACTLY AS STATED ABOVE and worth restating as a threat, because it is the difference between this endpoint being a feature and being a remote XP printer. GitHub signs each delivery (X-Hub-Signature-256, an HMAC over the raw request body with the webhook secret). The handler must verify that signature against the RAW body - before any JSON parsing, because re-serializing a parsed object changes bytes and breaks the HMAC. An unsigned, badly-signed, or signature-mismatched delivery must be rejected before it can mint a bounty.completed, and a rejected delivery must leave no trace. This is the one endpoint where a trivial auth bypass directly creates game state and reputation, so it is the highest-value attack on the system.\n\nTHE IDEMPOTENCY KEY IS THE DELIVERY, because GitHub's at-least-once delivery is the actual cause of double-XP. Two deliveries of the same merge are indistinguishable at the payload level unless you key on GitHub's delivery identifier (the X-GitHub-Delivery header, a unique id per delivery) or, more robustly, on the semantic identity of the event (repo + PR number + the fact it is a merge, i.e. what bounty is being completed). The natural durable key is 'this bounty is completed' as a state transition that is checked before it happens, not an after-the-fact deduplication: once a bounty is in the completed state, a second merge for the same PR is a no-op. Relying only on a delivery-id cache still double-awards if the cache is lost, so prefer making the completion itself idempotent.\n\nTHE REORDERING CASE IS A REAL ONE. A PR-closed event can arrive before the PR-opened event, and issue events can arrive out of order too. A handler that assumes 'opened then closed' will drop the close and leave a bounty stuck. The robust pattern is to make each handler's effect a function of the CURRENT known state, not of the event order - e.g. resolve the bounty by repo+PR (not by assuming the submission event was already processed), and if the bounty is not yet in a state that can complete, record the fact and reconcile rather than drop it. This is the same idempotent/reorderable discipline the event bus expects, applied at the ingress.\n\nTHE BOUNDARY IS MACHINE-ENFORCED, not conventional. The dependency rules (ba-dependency-rules-os1, closed) are checked in CI via architecture-rules.cjs: presentation -> features -> core <- infrastructure; features NEVER import each other; infrastructure/* may import core only. So the bounty feature consuming 'a PR was merged' does it by reacting to an event the infrastructure layer emits (bounty.completed, or a lower-level github.pr_merged that bounty translates), never by importing Octokit or this webhook handler. The architecture test ('features/ contains no Octokit import') is the regression guard. The reward of getting this right: bounty stays testable without the network, and a GitHub outage cannot corrupt game state - an outage just means no new merges are detected, which is recoverable, rather than a half-applied merge that awards XP twice.\n\nTHE AGENT-IDENTITY INVARIANT, restated because it is easy to erode. GitHub authenticates HUMANS and supplies repos/issues/PRs; it does NOT identify the local coding agents that work the bounty. Those agents are identified by the separate, frozen agent-identity contract (ba-contract-agent-identity-evq) with their own credentials. So a GitHub account and an agent are never the same thing, and no code path here may treat a GitHub username as an agent identity or let a webhook 'authenticate' an agent. The PR author on GitHub and the agent that produced the PR are different subjects that the bounty layer correlates; conflating them is how reputation gets attributed to the wrong party.\n\nSECURITY (expanded): store the App installation token or PAT in infrastructure only, never in a feature, never in the CLI, never in a URL. Concretely: the token is a server-side secret read from configuration/secret storage by the infrastructure layer; it is never returned in an API response, never logged, never placed in a query string (URLs leak via logs and referrers), and never handed to a client. The webhook secret is a separate secret from the installation token, and both live on the infrastructure side of the boundary.\n\nWHY A NAIVE IMPLEMENTATION GOES WRONG. Verifying the signature against the re-serialized body instead of the raw bytes, so legitimate deliveries fail (or a careful attacker can smuggle one). Deduplicating on an in-memory delivery-id set that a restart empties, so a replayed delivery double-awards XP. Assuming event order, so an out-of-order close leaves a bounty uncompleted. Letting the bounty feature import Octokit 'just to check the PR', which couples the feature to the network and is a CI-caught boundary violation. And treating the GitHub PR author as the agent, collapsing the identity invariant.\n\nSUCCESS CRITERIA: a real PR merged on a real repo completes the corresponding bounty exactly once; a forged webhook signature is rejected before any state changes; out-of-order and duplicate deliveries are both handled; the bounty feature has no GitHub SDK import; the installation token and webhook secret never leave infrastructure.\n\nTESTS: integration test with a signed fixture webhook; a forged/unsigned/bad-signature test asserting rejection AND that no state changed; a duplicate-delivery test asserting the bounty completes exactly once (across a simulated retry, not just an in-memory dedupe); a reordering test (close-before-open) asserting the bounty still completes; an architecture test asserting features/ contains no Octokit import; a test that the installation token never appears in an API response, log, or URL.","status":"closed","priority":1,"issue_type":"task","created_at":"2026-09-23T19:01:27.771165Z","created_by":"tranquangdang21","updated_at":"2026-09-25T23:46:06.633743Z","closed_at":"2026-09-25T23:46:06.633526Z","close_reason":"DONE for this bead's scope; one criterion is UNMET AND TRANSFERS, which is the\nreason this closes rather than staying open to block its own dependent.\n\nFOUR OF FIVE CRITERIA MET, and the four are the ones with teeth:\n  - a forged, unsigned or bad-signature webhook is rejected BEFORE any state\n    change, and leaves no trace. X-Hub-Signature-256 is an HMAC over the raw\n    bytes, verified before any parsing — re-serializing a parsed object changes\n    the bytes and breaks the HMAC. This is the difference between an endpoint and\n    a remote XP printer, so it is the property worth having.\n  - duplicate and out-of-order deliveries are both handled, keyed on the state\n    transition rather than a delivery-id cache, because a cache double-awards the\n    moment it is lost. Handlers are a function of current state, so a PR-closed\n    before PR-opened is not dropped.\n  - features/ contains no Octokit import, proven by a guard whose fixture was\n    written by dropping 'adapter' from the rule and watching it fire.\n  - the installation token and webhook secret stay in infrastructure, in\n    packages/db columns the schema-hygiene gate guards.\n  - the architecture test that guards the boundary lives in tests/unit and its\n    self-test parses a literal rather than a feature file, because reading\n    packages/features/social/src/feature.ts broke the removal test.\n\nTHE FIFTH CRITERION CANNOT BE MET FROM THIS BEAD, and did the plan not say so.\n\"a real PR merged on a real repo completes the corresponding bounty exactly once\"\nrequires a bounty lifecycle, and packages/features/bounty/src/ is still payout.ts\nplus re-exports. The bounty bead ba-feature-bounty-xhk OWNS that lifecycle and was\nblocked by THIS bead — so the criterion is not merely unmet, it is unmet by the\nbead this one unblocks, and leaving this open blocks the critical path on\nsomething that cannot be resolved until it is closed. It transfers to\nba-feature-bounty-xhk, which is now unblocked and owns it.\n\nCARRIED FORWARD, because it is a real regression waiting to happen and nobody\nshould rediscover it: progression pays 1000 XP for bounty.completed AND 500 XP\nfor pr.merged, with a handler registered for each, and neither name is in the\nprotocol's agent-event union — they are GAME events, not telemetry. The\ninfrastructure event is deliberately named for what infrastructure observed and\nNOT for either of those. The moment a bounty completes via a merge and both fire,\none merged PR pays 1500 XP and writes two history rows. Deciding that is the\nbounty bead's, and this note is the record that it was known.\n\nALSO RECORDED, not worked around: a GitHub outage costs a delay in noticing merges\nand nothing else, which is the whole point of the boundary — the feature reacts to\nan event and has no idea GitHub exists.","source_repo":"battle-agents","source_repo_path":"/Users/tranquangdang21/Projects/battle-agents","mol_type":"work","work_type":"mutex","wisp_type":"none","labels":["github","infra","p1"],"dependencies":[{"issue_id":"ba-github-infrastructure-56v","depends_on_id":"ba-better-auth-github-oauth-9wm","type":"blocks","created_at":"2026-09-23T19:04:23.809269Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"ba-hookprovider-seam-lq2","title":"Port HookProvider/TeamProvider seam from pixel-agents","description":"Port the provider seam from pixel-agents into packages/core, per plan section 28.1 item 3 and section 5.3.\n\nSOURCE (MIT, direct copy per section 28.1 item 3):\n- pixel-agents/core/src/provider.ts (151 lines) - HookProvider\n- pixel-agents/core/src/teamProvider.ts (100 lines) - TeamProvider\n\nWHY A SEAM IN CORE (sections 5.3, 1.1): section 1.1 credits pixel-agents with an explicit agent-agnostic/platform-agnostic roadmap built around a HookProvider seam where 'a new CLI = one subdirectory'. Section 5.3 says core exposes the seam that AgentWatcher implementations plug into. Putting the seam in core (not in an adapter) is what makes the boundary structural rather than conventional, and it is what the dependency-rules bead enforces.\n\nPIXEL-AGENTS REFERENCE ARCHITECTURE (section 1.1): TS monorepo core/server/webview-ui, Fastify+ws, React19+Canvas2D, with an AsyncAPI contract and codegen drift check. Hooks mode posts SessionStart/PreToolUse/PermissionRequest/Stop to /api/hooks/:providerId, with a heuristic fallback scanning ~/.claude/projects/*.jsonl.\n\nADDITIONAL STEALS FROM THE SAME REPO (section 28.2, learn-only rewrite): agentRuntime.ts, agentStateStore.ts (the SOLE broadcaster - a single choke point for state distribution), fileWatcher.ts / transcriptParser.ts, transport/index.ts branching, and the AsyncAPI-to-Modelina codegen discipline. Section 28.2 says adopt the CONTRACT-FIRST HABIT, not the files - so the AsyncAPI schema-plus-codegen-drift-check practice is worth adopting for our protocol schemas, but the specific generated files are not.\n\nAVOID (section 1.1): VSCode coupling, file-only persistence, Canvas renderer.\n\nATTRIBUTION: keep the MIT copyright header and add a THIRD-PARTY-NOTICES.md entry with the commit SHA (section 28.3).\n\nCONSIDERATIONS: provider identity must be namespaced per providerId so a session is never confused across harnesses; this is the same collision-avoidance problem the identity-resolution logic in feature-agent solves at a higher level.\n\nWHERE THIS SEAM SITS AGAINST THE CONTRACTS ALREADY FROZEN (this is the part that is not obvious from the reference repo, and the reason this bead exists rather than just a copy). By the time this seam lands, three core contracts are already closed and cannot be casually reshaped:\n- CONTRACT 1 (ba-contract-extension-api-w29, frozen) fixes the GameFeature shape every feature is written against: {id, commands?, eventHandlers?, reducers?, capabilities?, requires?}, with the emit() ordering persist -> handlers -> publish, and a hard rule that core contains ZERO game vocabulary (a grep for Bounty/Guild/Battle/XP/Level/Quest/Inventory/Repetition in packages/core must return nothing).\n- The dependency rules (ba-dependency-rules-os1, closed) are machine-enforced in CI via architecture-rules.cjs, not by reviewer discipline: presentation -> features -> core <- infrastructure; features NEVER import each other; adapters import core + protocol only and NEVER game code; interfaces/{cli,mcp,api,websocket} consume features through the capability registry. Any seam that lets an adapter reach past core into a feature is a violation this seam must not create.\n- The event vocabulary (ba-agent-event-union-up5, closed) is the normalized zod AgentEvent union with PROTOCOL_VERSION '0.1.0'. The seam is a transport/provider boundary, NOT a second event vocabulary: a HookProvider translates a harness's native hook payloads INTO the already-frozen AgentEvent union, then hands them to the same runtime.emit the log-watching adapters use. It must not introduce its own event names, and it must not re-open the shape of the union.\n\nSo the seam's job is precisely: give hook-based harnesses (Claude and anything else that posts SessionStart/PreToolUse/PermissionRequest/Stop to /api/hooks/:providerId) the same normalized entry point that log-watching harnesses (codex, opencode, cursor, pi, gemini) already use, so the game core has exactly one vocabulary and one ingest path. If a new harness needs a core edit to get through the seam, the seam is wrong.\n\nTHE AgentWatcher CONTRACT THIS SEAM MUST HONOR, because every adapter bead depends on it being stable. The reference interface, from agent-move's packages/server/src/watcher/agent-watcher.ts, is exactly two methods:\n  export interface AgentWatcher { start(): Promise<void>; stop(): void; }\nstop() returns void, not Promise<void> in the source. Whether ours keeps void or upgrades to a promise is a real open decision, not a transcription detail: a hook provider that must flush a buffered batch on shutdown has nowhere to await in the void shape. This bead is the natural place to decide it; decide deliberately, record which, and do not present the choice as 'verbatim from the reference' when the reference says void. The sibling adapter beads (codex, opencode, other-harnesses) have each been told to match whatever this seam settles.\n\nTHE PROVIDER IDENTITY RULE, stated as a concrete requirement rather than a consideration. A hook payload carries no harness identity of its own, so the seam is responsible for stamping every translated event with a namespaced providerId (e.g. 'claude', 'cursor'), so a session started by one harness is never confused with a same-named session from another. This is the same collision-avoidance the identity-resolution logic in feature-agent solves at a higher level; the seam handles the per-event stamp, the feature handles the durable identity.\n\nWHY A NAIVE PORT GOES WRONG. The temptation is to copy pixel-agents' files wholesale, which drags in its VSCode coupling, its file-only persistence, and its Canvas renderer - all three explicitly on the AVOID list - and to introduce a hooks-specific event vocabulary that bypasses the frozen AgentEvent union. The second temptation is a provider seam that is a thin interface but not a real choke point: if a hook provider can emit its own events, call handlers directly, or reach a feature, the architecture has leaked and the 'new CLI = one subdirectory' promise is false. A lint rule that a reviewer can wave through under deadline is not a seam.\n\nSUCCESS CRITERIA: a new harness can be supported by implementing the seam and adding one subdirectory, with zero core edits; the seam's translated events are members of the frozen AgentEvent union (no new vocabulary); the stop() shape is decided and recorded, and the adapter beads match it; providerId namespaces sessions; attribution survives; the codegen drift check catches an un-regenerated schema.\n\nTESTS: a contract test suite that every adapter must pass (run it against the _template stub and against each real adapter); a test that providerId namespaces sessions; an architecture test that no adapter imports game code and none reaches a feature; a test that a hook provider's events validate against the frozen AgentEvent union and pass the version handshake.","status":"closed","priority":1,"issue_type":"task","created_at":"2026-09-23T19:02:10.233675Z","created_by":"tranquangdang21","updated_at":"2026-09-25T00:15:06.417367Z","closed_at":"2026-09-25T00:15:06.417313Z","close_reason":"Seam landed in packages/core/src/hook-provider.ts and exported from the barrel. The translation target is core's GameEvent rather than a second event vocabulary, installHooks takes a named target, provider ids are namespaced and validated, and the team extension is optional. stop() returns a Promise, which is the watcher contract the sibling adapter beads were told to match: a hook provider buffers, and a void stop makes the shutdown flush fire-and-forget. The bead's 'keep the MIT copyright header' is unsatisfiable, since the upstream files carry none; the notices entry is the attribution. 14 tests, namespace guard proven by mutation. core still has zero dependencies.","source_repo":"battle-agents","source_repo_path":"/Users/tranquangdang21/Projects/battle-agents","mol_type":"work","work_type":"mutex","wisp_type":"none","labels":["adapter","core","p1"],"dependencies":[{"issue_id":"ba-hookprovider-seam-lq2","depends_on_id":"ba-contract-extension-api-w29","type":"blocks","created_at":"2026-09-23T19:04:22.729336Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"ba-hookprovider-seam-lq2","depends_on_id":"ba-reference-repo-study-o92","type":"blocks","created_at":"2026-09-23T19:05:53.574430Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"ba-license-hygiene-qy7","title":"Lock MIT license + THIRD-PARTY-NOTICES + license CI check","description":"Establish license hygiene for the whole repo, per plan section 35 (License lock: MIT) and section 28.3 (Attribution hygiene, CI-enforced).\n\nWHY P0 AND NOT POLISH: The project vendors/ports substantial third-party code. Section 28.1 lists 12 direct-copy items drawn from 8 repos under 4 different licenses. Getting attribution right AFTER copying is far harder and riskier than establishing the mechanism first. Section 35 also records that the plan previously contradicted itself on MIT vs Apache-2.0, so the lock must be explicit or the ambiguity will creep back.\n\nVERIFIED LICENSE FACTS (from .tmp checkouts, plan section 28):\n- MIT: agent-dashboard, agent-quest, arcane-agents, moltbook, paperclip, pixel-agents, tmux-agents, age-of-agents, cross-agent-teams-mcp, agent-move, agent-world-codemoo\n- Apache-2.0: agent-world-smallville\n- MPL-2.0: agentworld-openagents (Kaetram fork - FILE-LEVEL copyleft)\n- CC0: agent-quest tiny-swords-cc0 asset pack\n\nRULES per section 28:\n- MIT / Apache-2.0 / CC0: may copy with attribution (keep the copyright header, add a THIRD-PARTY-NOTICES entry).\n- MPL-2.0: NEVER paste Kaetram files into our tree; the copyleft attaches to those files. Learn the pattern only, rewrite clean in TypeScript.\n- No-license-confirmed (learn-spine until verified): hands off, copy NOTHING.\n\nCREATE:\n- LICENSE (MIT; the file already exists - verify its content matches the section 35 decision)\n- THIRD-PARTY-NOTICES.md at repo root: every copied/ported file listed with source repo + commit SHA + license\n- scripts/check-licenses.sh: fails CI if any file under packages/ or apps/ matches an MPL-2.0 header or a Kaetram import path. Allowlist: MIT / Apache-2.0 / CC0 only.\n- Game art keeps its OWN license file beside each pack (public/assets/<pack>/LICENSE.txt). Asset licenses are tracked separately from the MIT code license (sections 14, 17.7).\n\nCONSIDERATIONS: Section 28.3 explicitly separates the MIT code license from per-pack asset licenses. Do not collapse these. Section 35: any future Apache-2.0 mention elsewhere in docs is stale unless a new explicit decision reverses the lock.\n\nSUCCESS CRITERIA: scripts/check-licenses.sh runs in CI and passes on a clean tree; it FAILS on a deliberately planted MPL-2.0 header (proving the guard works); THIRD-PARTY-NOTICES.md exists and names the source repos.\n\nTESTS: unit test for the license scanner (pattern match, allowlist, exit code) plus a negative test that plants a forbidden header and asserts a non-zero exit.","status":"closed","priority":0,"issue_type":"task","created_at":"2026-09-23T18:58:25.974451Z","created_by":"tranquangdang21","updated_at":"2026-09-24T05:18:55.786528400Z","closed_at":"2026-09-24T05:18:55.784589500Z","close_reason":"Verified: LICENSE is MIT, THIRD-PARTY-NOTICES.md names 6 source repos, check-licenses.sh has a self-test that plants forbidden headers and asserts denial, and the stage runs in CI via test:m0. Gate passes.","source_repo":"battle-agents","source_repo_path":"/Users/tranquangdang21/Projects/battle-agents","compaction_level":0,"original_size":0,"labels":["governance","legal","p0"],"dependencies":[{"issue_id":"ba-license-hygiene-qy7","depends_on_id":"ba-monorepo-scaffold-cit","type":"blocks","created_at":"2026-09-23T19:04:21.409442Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"ba-local-testing-contract-gmi","title":"Local testing contract: pnpm test:m0 canonical pipeline","description":"Establish the single canonical test pipeline that both local dev and CI run, per plan section 40. Docker Compose is the gate.\n\nTHE PIPELINE (section 40):\n  docker compose up -> migrations -> seed -> unit (vitest) -> integration (+Postgres service) ->\n  removal-test (section 20) -> license check (section 28.3) -> E2E smoke (login -> connect -> claim -> replay render)\n\nRULES:\n- compose.yaml services (M0): web, postgres, plus a test-runner profile for lint/unit/integration parity. Worker/queue/MCP-split containers only when section 7.4 triggers a scale-up.\n- 'pnpm test:m0' runs the full chain locally; ANY step red blocks merge.\n- The load test (100 agents x 20 events/s, section 7.2) stays a PRE-M4 GATE, not an M0 gate.\n\nE2E SMOKE ASSERTIONS (section 40): the section 27 M0 DoD (login -> logout -> login shows the same user and agents) PLUS a GitHub-login stub -> agent HELLO -> bounty claim -> replay page renders, with the sandbox payout banner visible.\n\nWHY CI RUNS THE SAME STEPS INSIDE THE SAME IMAGES (section 40): parity. A test that passes locally and fails in CI (or vice versa) wastes more contributor time than it saves. 'Compose may also run lint/test parity services for CI/local sameness' is the mechanism.\n\nCONSIDERATIONS: the pipeline must be resumable/parallel where safe (unit tests are fast and independent; integration needs Postgres). Keep the ordering explicit so a failure message says which stage failed. Secrets for the GitHub stub must be test-only and never touch real OAuth credentials.\n\nSUCCESS CRITERIA: pnpm test:m0 runs every stage in order and reports which stage failed; CI invokes the same script in the same images; a red stage blocks merge.\n\nTESTS: the pipeline itself; plus a test that asserts each stage is invoked (e.g. a stage marker file written per stage) so stages cannot be silently skipped.","status":"closed","priority":0,"issue_type":"task","created_at":"2026-09-23T18:59:47.877476Z","created_by":"tranquangdang21","updated_at":"2026-09-24T08:35:48.165421Z","closed_at":"2026-09-24T08:35:48.162269800Z","close_reason":"M0 pipeline GREEN: 12/12 stages (compose, migrations, seed, unit, integration, schema-drift, typecheck, architecture, schema-hygiene, removal-test, license, e2e-smoke). CI runs this same script in the same compose image. The e2e-smoke stage now exists and asserts exactly what plan 1101 allows at M0. A red stage blocks merge.","source_repo":"battle-agents","source_repo_path":"/Users/tranquangdang21/Projects/battle-agents","compaction_level":0,"original_size":0,"labels":["infra","p0","test"],"dependencies":[{"issue_id":"ba-local-testing-contract-gmi","depends_on_id":"ba-dependency-rules-os1","type":"blocks","created_at":"2026-09-23T19:41:54.273877Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"ba-local-testing-contract-gmi","depends_on_id":"ba-license-hygiene-qy7","type":"blocks","created_at":"2026-09-23T19:41:54.479767Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"ba-local-testing-contract-gmi","depends_on_id":"ba-monorepo-scaffold-cit","type":"blocks","created_at":"2026-09-23T19:04:21.976482Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"ba-local-testing-contract-gmi","depends_on_id":"ba-removal-test-e33","type":"blocks","created_at":"2026-09-23T19:41:54.387762Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"ba-m1-two-harness-integration-3us","title":"M1 integration proof: two harnesses live simultaneously","description":"Prove the M1 milestone end to end, which no single adapter bead can demonstrate.\n\nM1 DoD, VERBATIM (section 27): 'Claude + Codex adapters emitting normalized events; HELLO resume-vs-new works (kill terminal mid-session, reopen, see resume offer); SSE shows live activity. DoD: two different harnesses visible simultaneously.'\n\nWHY THIS IS A SEPARATE BEAD: each adapter bead tests its own stream in isolation. The DoD is about CONCURRENCY and about the two harnesses being genuinely different in kind - Claude is hook-driven (primary plane), Codex is rollout-JSONL-driven with NO hooks at all (section 6). If both work at once, the telemetry plane is provably harness-agnostic rather than incidentally so. That is the entire architectural claim of section 2.2 ('Not a Claude Code plugin... or it never covers all agents').\n\nWHAT THE PROOF REQUIRES:\n1. A live Claude session and a live Codex session running at the SAME time, both emitting normalized AgentEvents into the same runtime.\n2. Both visible simultaneously in the UI over the SSE stream, with distinguishable agents (section 3.2: do NOT key on process name alone - concurrent sessions must stay separable).\n3. The resume-vs-new handshake demonstrated for real: kill a terminal mid-session, reopen it, and observe the resume offer rather than a duplicate agent.\n4. Version-checked handshake, so a protocol mismatch warns instead of corrupting the stream (section 17.3, section 26).\n\nWHY THE HARNESS MISMATCH MATTERS TECHNICALLY: the two adapters normalize the same taxonomy (section 5.2) from completely different sources. If the normalized forms diverge - a tool that is 'tool.completed' in one and 'command.run' in the other - the game core will treat them as different activities and the world will look wrong. This integration proof is where that divergence shows up, which is why per-adapter tests cannot substitute for it.\n\nMACHINE-NATIVE (section 34): the whole flow must be drivable from the CLI; the UI is a view, not the driver.\n\nTHE CONTRACTS THIS PROOF EXERCISES, already frozen, so the proof is a real integration test rather than a design debate. All of these are closed and none of them may be reshaped to make M1 pass:\n- The normalized event vocabulary (ba-agent-event-union-up5, closed): a zod discriminated AgentEvent union with 21 taxonomy events, PROTOCOL_VERSION '0.1.0' checked at handshake. A version mismatch must warn, not corrupt - that is the point of the pin. Both adapters emit members of THIS one union; neither may invent a harness-specific event to make its own test pass.\n- Agent identity (ba-contract-agent-identity-evq, closed): a durable agent identity, distinct from any session. This is what makes 'distinguishable agents' possible without keying on process name - two harnesses, two sessions, and the agent identity is what stays stable across the kill/reopen.\n- The session lifecycle and the HELLO resume-vs-new handshake (ba-contract-session-rpo, closed): the resume offer is this frozen contract's behavior, and this bead is where it is proven for real rather than in isolation.\n- Agent credentials (ba-agent-credentials-issuance-294, closed): each adapter presents a Bearer installation token to POST /api/events, so both live sessions are independently authenticated, not sharing one identity.\n- The ingest and SSE path (ba-event-ingest-and-sse-gu9, open, and the object of this proof).\n\nTHE CONCURRENCY-SPECIFIC TRAP, which is why this is not just 'run two adapters'. The ingest contract forbids mixing sessions in one batch: a batch that carries two sessionIds is rejected with 400, and the response { accepted, sessionId, resumed } is per-session. With two live harnesses there are two sessions, so the two adapters' buffers MUST stay separate and flush independently (250ms / up to 50 events, never over 100). The tempting shortcut - a shared runtime that merges both harnesses' events into one outgoing batch - is rejected by the server and is also the thing that would mask the divergence this bead exists to catch. The runtime is shared; the per-session batching and the per-agent identity are not.\n\nTHE EQUIVALENCE ASSERTION IS THE POINT, stated precisely. Take one logical action - a shell command that runs to completion - and drive it through both harnesses. Claude (hook-driven) and Codex (rollout-JSONL) must produce the SAME normalized event type and a compatible payload after passing through the shared tool-map (normalizeToolName / getZoneForTool / normalizeToolInput). The tool names differ at the edge (a Claude hook name vs a Codex rollout tool name); the normalized form must not. If 'a command completed' is command.run in one harness and tool.completed in the other, the game core renders the world inconsistently and - this is the silent part - each adapter's own test still passed, because each was only ever compared against itself.\n\nWHY A NAIVE IMPLEMENTATION GOES WRONG. The failure this bead is built to catch is silent divergence: Claude reports a tool completion where Codex reports a command run, and both are 'real', so the game world renders them differently and nobody notices because each adapter passed its own tests. Cross-harness normalization equivalence is therefore the highest-value assertion here, more so than the raw concurrency. The second failure is keying identity on process name, which collides the moment two harnesses run at once - exactly what this bead forces. The third is treating the UI as the driver: if the flow only works with a browser open, the machine-native rule is broken and the DoD has been met in a way that will not survive. The fourth is a shared outgoing batch, which the single-session ingest rule rejects and which would hide divergence.\n\nSUCCESS CRITERIA: the literal M1 DoD - two different harnesses visible simultaneously, resume offer working after a terminal kill, live SSE activity for both; the same logical action normalizes to the same event type across both harnesses; the two sessions' batches stay separate (no mixed-session 400); a protocol-version mismatch warns rather than corrupting the stream.\n\nTESTS: the concurrency integration test (two live sessions, one runtime, no mixed-session batch); a normalized-form equivalence test asserting the same logical action maps to the same event type across harnesses; the kill-and-reopen resume test asserting a resume offer, not a duplicate agent; a protocol-version-mismatch warning test; an identity-separation test proving two concurrent sessions are not keyed on process name.\n\nCONSIDERATIONS: the interesting failure mode is silent divergence - Claude reports a tool completion where Codex reports a command run, and both are 'real', but the game world renders them differently and nobody notices because each adapter passed its own tests. Cross-harness normalization equivalence is therefore the highest-value assertion in this bead, more so than the concurrency itself. Second: running two real agents means real tokens, so budget this as a deliberately-run integration test rather than something attempted casually during development. Third: the SSE stream this proof reads is the same unauthenticated global broadcast tracked as a P0 bug (ba-9hr); this bead proves the ingest and concurrency, not the stream's public/private policy, and should not be read as accepting the current fan-out as final.","status":"open","priority":1,"issue_type":"task","created_at":"2026-09-23T19:07:16.385557Z","created_by":"tranquangdang21","updated_at":"2026-09-24T21:32:36.694955Z","source_repo":"battle-agents","source_repo_path":"/Users/tranquangdang21/Projects/battle-agents","mol_type":"work","work_type":"mutex","wisp_type":"none","labels":["integration","m1","star"],"dependencies":[{"issue_id":"ba-m1-two-harness-integration-3us","depends_on_id":"ba-adapter-claude-fbc","type":"blocks","created_at":"2026-09-23T19:07:28.164420Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"ba-m1-two-harness-integration-3us","depends_on_id":"ba-adapter-codex-nhp","type":"blocks","created_at":"2026-09-23T19:07:27.569498Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"ba-m1-two-harness-integration-3us","depends_on_id":"ba-cli-p0-6oe","type":"blocks","created_at":"2026-09-24T21:32:36.694548Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"ba-m1-two-harness-integration-3us","depends_on_id":"ba-contract-session-rpo","type":"blocks","created_at":"2026-09-23T19:07:28.414478Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"ba-m1-two-harness-integration-3us","depends_on_id":"ba-event-ingest-and-sse-gu9","type":"blocks","created_at":"2026-09-23T19:07:26.987050Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"ba-mcp-adapter-li2","title":"MCP adapter: 5 stable primitives over the capability registry","description":"Implement the MCP Streamable HTTP adapter, per plan sections 5.1, 22, 26, 32, 33, and 36.\n\nCRITICAL SCOPE CORRECTION (sections 31, 32, 33): MCP is NOT P0. Protocol/API + CLI ship first; MCP is an adapter on the SAME protocol later. Section 33's P3 list puts 'MCP adapter' explicitly in phase 3, and section 36 makes interfaces/{cli,mcp,api,websocket} EQUAL capability consumers rather than MCP-first.\n\nTHE SURFACE IS EXACTLY 5 PRIMITIVES (section 32): discover, search, inspect, act, observe. NOT the old domain-operation tool list.\n\nBANNED IMPLEMENTATION (sections 5.1, 32, 33): the section 1.3-era tool list - register_agent, get_world, get_quests, get_opponents, challenge_agent, accept_battle, send_message, inspect_enemy, claim_reward, spawn_agent, quest/party/progress/report/move and so on - describes CAPABILITIES IN THE REGISTRY and must NEVER be implemented verbatim as MCP tools. That verbatim list IS the tool-explosion path section 32 bans. Adding Guild must change the registry, not the tool surface.\n\nWHY THE PRIMITIVES EXIST (section 32): N features x M operations collapses to 100-200 MCP tools, which is undiscoverable and bloats agent context. 'Cover every capability, expose few stable primitives.' discover() is LAZY - top-level domains first, per-domain detail on demand, NEVER the full 200-capability firehose at connect time.\n\nANTI-GOD-TOOL GUARD: act() dispatches TYPED registry-defined actions (defineAction), never act({action: string, payload: any}). A bogus action id must be a compile error.\n\nMCP IS THE CONTROL PLANE, NOT EVENT CAPTURE (section 5.1, section 5 'MCP khong nen dung de capture moi event'): NEVER make the agent call MCP after every Read/Edit/Bash. That bloats context, costs tokens, drags the workflow, and couples us to per-agent MCP quirks. Hooks/session-watcher carry telemetry; MCP carries commands.\n\nAUTH (sections 3.4, 22, 4): short-lived installation token from the section 4 flow, obtained via MCP OAuth. NEVER put a raw token in the MCP URL (?token= leaks into logs and history) - the plan's own bad example.\n\nONBOARDING UX this enables (section 4, from mcp-coding-agents): 'claude mcp add agent-battle https://agentbattle.gg/mcp' then browser auth, then a terminal confirmation showing Agent, Account, and Project. This is the proof that 'login -> add MCP -> authorize -> play' needs no separate CLI install.\n\nREVIEW GUARD (section 36): any PR adding interfaces/mcp/* implementation code FOR A SPECIFIC GAME FEATURE (instead of registry entries) fails review.\n\nSUCCESS CRITERIA: an agent connects over MCP, discovers domains lazily, performs a full bounty loop via the 5 primitives, and the tool count stays at 5 no matter how many features are installed.\n\nTESTS: a tool-count test asserting exactly 5 primitives regardless of installed features; a lazy-discovery test; a protocol-loop integration test; an architecture test that no per-feature MCP tool modules exist.\n\nCONSIDERATIONS: the pull toward a richer tool surface is strongest right after a real agent struggles with the 5 primitives - and that is precisely the moment the plan's argument applies. If discover is too coarse, the fix is finer-grained REGISTRY metadata, not a sixth tool. A useful forcing function: any proposed sixth primitive must demonstrate a capability that genuinely cannot be expressed as discover/search/inspect/act/observe. Related, keep MCP clearly on the control plane - it is tempting to use it for notifications or telemetry because it is already connected, and that is the exact conflation section 5.1 warns against.","status":"closed","priority":0,"issue_type":"task","created_at":"2026-09-23T19:03:56.853265Z","created_by":"tranquangdang21","updated_at":"2026-09-24T08:35:58.077773500Z","closed_at":"2026-09-24T08:35:58.073761200Z","close_reason":"Shipped against the frozen contract; typecheck clean, architecture clean, unit tested. See the commit for the design calls and what was deliberately left out.","source_repo":"battle-agents","source_repo_path":"/Users/tranquangdang21/Projects/battle-agents","compaction_level":0,"original_size":0,"labels":["interface","mcp","p3"],"dependencies":[{"issue_id":"ba-mcp-adapter-li2","depends_on_id":"ba-contract-public-api-ute","type":"blocks","created_at":"2026-09-23T19:04:26.329061Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"ba-moltbook-verification-0zc","title":"Verify Moltbook claims from source (section 29 correctness pass)","description":"Resolve the unverified Moltbook claims flagged in plan section 29, by reading source or API docs directly.\n\nTHE PROBLEM (section 29): the architecture review found that skill.md could NOT be fetched directly (the server returns Markdown content-type the tooling could not parse), so NO Moltbook implementation details asserted from that file are TRUSTWORTHY. Every place the plan describes Moltbook mechanics - api_key flow, ~4h heartbeat, claim flow, rate limits, human-escalation policy - is UNVERIFIED HYPOTHESIS, not fact.\n\nTHE RULE (section 29): keep the PRINCIPLE, drop the citation certainty. Never infer implementation details from a skill.md summary. Read the source or the API docs directly.\n\nWHAT MUST BE CHECKED against .tmp/moltbook (already cloned, MIT, LICENSE present, Copyright (c) 2025 Moltbook). It holds 9 non-.github files, not 7: skill.md, heartbeat.md, messaging.md, SECURITY.md, README.md, skill.json, LICENSE, examples/integration.md, assets/logo.png.\n\nTHE HEADLINE IS THAT THE FILES ARE NOW READABLE, WHICH IS THE WHOLE POINT OF THIS BEAD. The plan's section 29 says skill.md could not be fetched and that no detail from it was trustworthy. That is no longer the situation: the repo is on disk and every file above can be read directly. So the check is no longer 'can we get the source' - it is the substantive comparison below, and it can actually be completed now.\n\nMUST BE CHECKED, against the file that should contain it:\n- the register -> api_key -> Bearer flow: skill.md, cross-read against SECURITY.md, which is where an auth flow's real constraints would live.\n- the heartbeat cadence claim (~4h): heartbeat.md, the only file that could substantiate a number.\n- the claim flow: skill.md / messaging.md.\n- rate limits: skill.md, SECURITY.md.\n- the human-escalation policy: SECURITY.md is the place to look; if the policy is not written down there, that is the finding.\n- the skill.json version pin: the file exists and declares `\"version\": \"1.7.0\"`, `\"license\": \"MIT\"`. Whether that pin is load-bearing for a client, or merely descriptive, is the thing to determine - it decides whether our own skill.json pin (ba-skill-md-protocol-72x) is a real compatibility mechanism or a decoration.\n\nWHAT SURVIVES REGARDLESS (section 29): the architecture distinction is independently sound and does not depend on Moltbook being accurate - Human GitHub Account -> Agent Identity (credentials, progression, reputation, history) -> Sessions (ephemeral, A/B/C). Three identities answering three different questions: Human login answers 'who owns this Agent?', Agent ID answers 'which character is playing?', Session ID answers 'what is this execution doing?'\n\nTHE LEARN-SPINE MARKER IS ALREADY RESOLVED, and the answer is worse than 'unconfirmed'. docs/research/learn-spine-license.md records the determination dated 2026-09-24: learn-spine has NO LICENSE - no license file, no license field in package.json, GitHub's API reports none - confirmed five independent ways. The standing instruction is 'copy nothing, not one line'. ba-risk-gates-e74 (RISK 6) still describes this as an open 'verify its license' action; it is not open, and the outcome is a prohibition rather than a permission.\n\nThe other stale marker is in section 28.2 of the plan itself, which is out of scope for this bead to edit. What belongs here is the corrected fact so the next reader does not re-derive it: the checkout is present at .tmp/learn-spine and is readable, and reading it is how we know it is unlicensed. The animation bead (ba-animation-core-spike-hf7) cites learn-spine as a reference; it may READ it for architectural ideas, which is what an unlicensed repo permits, and must not copy from it.\n\nWHY IT MATTERS: our agent-facing protocol (skill.md family) is explicitly modeled on Moltbook's shape. Building a protocol on assumed mechanics is how a design acquires phantom constraints.\n\nSUCCESS CRITERIA, REWRITTEN BECAUSE 'EACH CLAIM IS CONFIRMED OR MARKED UNVERIFIED' IS A PROPERTY OF A DOCUMENT, AND A DOCUMENT CAN SAY IT ABOUT ITSELF. The old criterion was close to right and is kept in substance; what it lacked is any check that could fail. A note reading \"all Moltbook claims verified\" satisfies the old wording while verifying nothing, and a note that verifies everything and cites nothing also satisfies it. The claim-by-claim verdict IS the deliverable, so the deliverable is what gets checked.\n\n1. Every Moltbook claim carries a VERDICT, per claim, not per document. Settled by: the dated note lists each claim individually, each tagged CONFIRMED or UNVERIFIED, and a CONFIRMED claim names the FILE in .tmp/moltbook it came from - not the repo. That last distinction is the bead's own and it catches a real error: \"confirmed against the repo\" is compatible with having read only a README, which is how a claim about an internal detail gets asserted from a summary.\n\n2. A claim marked UNVERIFIED is DROPPED FROM THE DESIGN, not merely labelled. This is the half the old criterion stated and the half nothing checked. The distinction matters: a design that still builds around a heartbeat interval nobody confirmed is exactly the phantom constraint this bead exists to prevent, and it survives the label perfectly - the note says UNVERIFIED while the design keeps the assumption. Settled by: for every UNVERIFIED claim, a check that the design docs no longer assert it, so an unverified claim cannot quietly keep doing work in the architecture. Where the PRINCIPLE survives but the citation does not (the Human -> Agent -> Session distinction, per section 29), the note records that split explicitly, so 'keep the principle, drop the certainty' is a recorded decision rather than an accident.\n\n3. The learn-spine question is NOT re-done here. It is already answered - no license, copy nothing - and recorded in docs/research/learn-spine-license.md. This bead points at that determination so the two never drift; it does not re-investigate it. The plan's section 28.2 caveat is likewise out of scope.\n\n4. The note is dated and carries the staleness section. Settled by: the date being present, and the note naming the beads that were unstarted when it was written, so a later reader can tell how much of the plan postdates it. This is the shape docs/research/README.md already uses for the six existing notes and it is the model worth copying - a research note without a date and a staleness note is a claim with no shelf life.\n\n5. The note lands in the EXISTING research index rather than beside it. Settled by: a Moltbook row in the docs/research/README.md per-repo table, carrying the commit SHA and a headline finding. A seventh standalone note the index does not list is the failure - it exists, satisfies every per-file check above, and is invisible to the next person who reads only the index, which is where the design work actually starts.\n\nTESTS. Not a code test - documentation-accuracy tests, and they are as concrete as the code ones:\n- a per-claim verdict check: every Moltbook claim in the note is individually tagged, and every CONFIRMED claim names a file in .tmp/moltbook rather than only the repo. A note asserting 'all claims verified' with no per-claim tags fails this - which is the point, because that note is the failure mode.\n- the unverified-claims-dropped check: for each UNVERIFIED claim, the design docs no longer assert it. This is the check that makes the label load-bearing instead of decorative.\n- the uncited-claims grep, which is the gate: a design doc asserting a Moltbook implementation detail with no source citation fails. The failure it catches is the one that matters - a design quietly leaning on an unverified Moltbook mechanic, which is how a phantom constraint gets built.\n- an index check: docs/research/README.md carries a Moltbook row with a commit SHA and a headline finding, so the note is discoverable from the index.\n- a date-and-staleness check on the note itself, matching the format of the six existing per-repo notes.\n\nOUT OF SCOPE, stated so it is not smuggled back in: the same caution applies to any claim sourced only to an unfetched page - the Pi and Paperclip references, the MCP-spec quotes in sections 12-13: keep the principle, drop the citation certainty.\n\nThe deliverable is a dated note in docs/research/ alongside the six existing per-repo notes, in the same format: commit SHA, what was checked, what was confirmed, what stayed a hypothesis. That file is also the natural home for the learn-spine determination's pointer, so the two never drift.\n\nCONSIDERATIONS: the meta-lesson matters more than the answers here. Building a protocol on assumed mechanics is how a design acquires phantom constraints - someone designs around a heartbeat interval that never existed, and then the real constraint makes that design wrong. The same caution in section 29 applies to any claim sourced only to an unfetched page (Pi, Paperclip, MCP-spec quotes in sections 12-13): keep the principle, drop the citation certainty. Record what was confirmed and from where, so a later reader can tell verified fact from inherited assumption.","status":"closed","priority":1,"issue_type":"task","created_at":"2026-09-23T19:03:57.151104Z","created_by":"tranquangdang21","updated_at":"2026-09-26T02:18:21.946124Z","closed_at":"2026-09-26T02:18:21.946042Z","close_reason":"DONE, and it corrected the plan in both directions rather than only the one the\nbead expected.\n\n§29 said skill.md could not be fetched and that no Moltbook detail was\ntrustworthy, so every mechanism the plan described was unverified hypothesis. The\ncheckout is on disk and all nine non-.github files were read in full, so the check\nstopped being \"can we get the source\" and became the substantive comparison.\n\nIT CHANGED MORE THAN IT CONFIRMED:\n  - The ~4h heartbeat is NOT a Moltbook platform constraint. skill.md:47 is a cron\n    line in a setup section addressed to a client's own scheduler, and\n    heartbeat.md:194 calls heartbeat \"a gentle reminder, not a rule\". A constraint\n    we would have designed a scheduler around does not exist.\n  - \"One API key = same Agent across processes\" is UNVERIFIED — zero occurrences\n    anywhere in the checkout. It was load-bearing for a design premise and is not\n    one. Agent-identity persistence is §29.1 contract 2, our own locked decision,\n    and it never needed Moltbook's agreement.\n  - skill.json's \"1.7.0\" is a client-side string compare against a moving main, so\n    OUR pin has no compatibility mechanism to inherit and must be made load-bearing\n    by a handshake test or it is decoration. Recorded in the plan so\n    ba-skill-md-protocol-72x inherits the finding.\n  - §29's blanket \"unverified, not fact\" was ALSO wrong, just wrong the other way.\n    The plan is now corrected in both directions, and the correction says so.\n\nCONFIRMED now means confirmed AS DOCUMENTED, never as enforced. The checkout is\ndocumentation only — twelve tracked paths, no server source — so nothing about\nwhat the server actually does is resolvable from it, and a reader who treats a\nCONFIRMED row as a verified mechanic has misread it. Three verdict levels are\nused, REFUTED included, and three plan claims are refuted by them.\n\nThe architecture distinction survives untouched and is restated as surviving for\nthe right reason: Human GitHub Account -> Agent Identity -> Sessions is §29.1\ncontracts 2 and 3, a locked decision of ours, not an observation about a reference\nrepo. A bad Moltbook fact cannot take it down.\n\nThe check is wired as pipeline stage moltbook-claims and ships with a self-test\nasserting every sub-check goes red on the mutation meant to defeat it, so a future\nedit cannot quietly turn it into a script that exits 0.","source_repo":"battle-agents","source_repo_path":"/Users/tranquangdang21/Projects/battle-agents","mol_type":"work","work_type":"mutex","wisp_type":"none","labels":["correction","p1","research"],"dependencies":[{"issue_id":"ba-moltbook-verification-0zc","depends_on_id":"ba-monorepo-scaffold-cit","type":"blocks","created_at":"2026-09-23T19:04:27.186387Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"ba-moltbook-verification-0zc","depends_on_id":"ba-reference-repo-study-o92","type":"blocks","created_at":"2026-09-23T19:05:54.052894Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"ba-monorepo-scaffold-cit","title":"Scaffold pnpm monorepo: apps/web + packages/*","description":"Create the pnpm-workspace monorepo skeleton for Agent Battle, exactly as specified in the plan section 19 (Implementation - monorepo layout).\n\nWHY FIRST: The plan locks a specific target layout because several architectural guarantees are enforced by the folder boundaries themselves (section 19 dependency rule, section 33 extension kinds). If the scaffold deviates, the guarantees 'features never import each other' and 'adding a feature never edits core/cli/mcp' become unenforceable conventions instead of structural facts.\n\nCREATE per section 19:\n- package.json (pnpm workspaces), pnpm-workspace.yaml, turbo.json (optional)\n- .env.example with BETTER_AUTH_SECRET, BETTER_AUTH_URL, GITHUB_CLIENT_ID, GITHUB_CLIENT_SECRET, DATABASE_URL\n- apps/web/ (Next.js 14 App Router: pages, api routes, components, lib)\n- packages/core/ (primitives ONLY, no game words)\n- packages/protocol/ (versioned AgentEvent union, tool-map, version.ts)\n- packages/adapters/{claude,codex,opencode,cursor,pi,gemini,_template}/\n- packages/features/{agent,progression,reputation,quest,bounty,battle,guild,social,inventory,animation}/\n- packages/game-client/ (PixiJS v8 world)\n- packages/mcp-server/ (thin, 5 stable primitives only)\n- packages/cli/ (P0 per sections 31/36)\n- drizzle/ (schema + migrations), scripts/\n\nDEPENDENCY DIRECTION (enforced separately): presentation -> features -> core <- infrastructure. Features NEVER import each other (only core + protocol). Adapters import core + protocol only.\n\nCONSIDERATIONS: Docker lives in the infrastructure layer only (section 37) - features must never import Docker/Compose/Postgres-container concepts, only repository/capability interfaces (section 33). Folders may start near-empty; this bead establishes structural boundaries, not contents.\n\nSUCCESS CRITERIA: pnpm install succeeds; pnpm -r build runs (each package may be a stub); folder graph matches section 19 exactly; no package imports outside its allowed direction.\n\nTESTS: a scaffolding test that walks packages/* and asserts each has package.json with the correct @battle-agents/* name and no cross-boundary imports.","status":"closed","priority":0,"issue_type":"task","created_at":"2026-09-23T18:58:25.808877Z","created_by":"tranquangdang21","updated_at":"2026-09-24T05:11:08.004606100Z","closed_at":"2026-09-24T05:11:08.001697700Z","close_reason":"Scaffold verified: 24 packages, pnpm build and pnpm -r typecheck green, architecture and removal-test gates pass","source_repo":"battle-agents","source_repo_path":"/Users/tranquangdang21/Projects/battle-agents","compaction_level":0,"original_size":0,"labels":["infra","p0","scaffold"]}
{"id":"ba-monorepo-scaffold-j5a","title":"Web app dựng hai runtime với hai bus riêng, nên event từ API không tới SSE","description":"Fixed in bc6bd6b. apps/web/src/shared-runtime.ts now owns the pool, the runtime and the bus; sharedApi() and sharedEventGateway() both draw from it.\n\nThe condition event-gateway.ts set for this fold was met: its own header said folding the two runtimes together \"belongs with the work that makes the act path a first-class publisher\". MCP is that work and ba-4to is closed, so the deferral was executed rather than re-litigated.\n\nCHOSE: runtime and bus became OPTIONAL dependencies of createEventGateway rather than being removed. The ingest integration test constructs a gateway with no runtime and gets an isolated one, so it keeps asserting against a runtime it fully controls and did not have to be rewritten. Only the app passes the shared ones.\n\nDID NOT: fold the three closers into one function. closeSharedApi, closeSharedEventGateway and closeSharedRuntime each drop their own cache; the pool is closed once by closeSharedRuntime. Three entry points where one would do is untidy, but each is a correct shutdown of a different layer's state and collapsing them is a cleanup rather than a fix.\n\nREGRESSION GUARD: identity, not equality. The assertion is that gateway.bus IS the shared bus by reference, so two structurally identical buses fail it. A deep comparison would have passed while every event went missing. Watched failing: restoring the gateway's own bus makes the stream assertion report \"expected +0 to be 1\" and the identity assertion report two different buses.\n\nALSO: the paragraph in event-gateway.ts that argued FOR two runtimes was a stale claim once the fold landed. Replaced, per AGENTS.md's \"comments are claims\". api.observe(), fixed in ba-4to, now actually delivers because sharedApi() is built over a bus something else can see.\n","status":"closed","priority":1,"issue_type":"bug","created_at":"2026-09-25T07:00:37.166670500Z","created_by":"unknown","updated_at":"2026-09-25T07:26:59.549205100Z","closed_at":"2026-09-25T07:26:42.832786900Z","close_reason":"Fixed in bc6bd6b. apps/web/src/shared-runtime.ts now owns the pool, the runtime and the bus; sharedApi() and sharedEventGateway() both draw from it.\n\nThe condition event-gateway.ts set for this fold was met: its own header said folding the two runtimes together 'belongs with the work that makes the act path a first-class publisher'. MCP is that work and ba-4to is closed, so the deferral was executed rather than re-litigated.\n\nCHOSE: runtime and bus became OPTIONAL dependencies of createEventGateway rather than being removed. The ingest integration test constructs a gateway with no runtime and gets an isolated one, so it keeps asserting against a runtime it fully controls and did not have to be rewritten. Only the app passes the shared ones.\n\nDID NOT: fold the two closers into one function. closeSharedApi, closeSharedEventGateway and closeSharedRuntime each drop their own cache; the pool is closed once by closeSharedRuntime. Three entry points where one would do is untidy but each is a correct shutdown of a different layer's state, and collapsing them is a cleanup rather than a fix.\n\nREGRESSION GUARD: identity, not equality.  fails for two structurally identical buses, which toEqual would have passed while every event went missing. Watched failing: restoring the gateway's own bus makes the stream assertion report 'expected +0 to be 1' and the identity assertion report two different buses.\n\nALSO: the paragraph in event-gateway.ts that argued FOR two runtimes was a stale claim once the fold landed. Replaced, per AGENTS.md's 'comments are claims'. api.observe(), fixed in ba-4to, now actually delivers because sharedApi() is built over a bus something else can see.","source_repo":"battle-agents","source_repo_path":"\\\\?\\C:\\Users\\ADMIN\\Documents\\Projects\\battle-agents","compaction_level":0,"original_size":0}
{"id":"ba-neon-cloud-deploy-zn4","title":"Vercel + Neon cloud deploy and per-PR preview branches","description":"Stand up cloud deployment, per plan sections 7.1 and 37.\n\nTHE STACK (section 7.1): Next.js App Router on Vercel (git push -> deploy; no Docker, Nginx, PM2, Postgres VM, DB backup, DB scaling, or Kubernetes required FOR DEPLOYMENT) plus Neon Postgres. MCP endpoint lives in Next.js at https://agentbattle.gg/api/mcp; split into a standalone gateway ONLY if long-lived streaming demands it.\n\nCLARIFY THE EARLIER PHRASING (section 7.1 supersedes): Docker is the LOCAL DEVELOPMENT/TEST environment (section 37), never a production dependency. Do not let the old 'no Docker' wording cause someone to strip the compose setup.\n\nTHE BEGINNER TRAP THIS BEAD EXISTS TO AVOID (section 7.1): never construct a new Client() and connect() per query. Always use a POOLED driver - the Neon serverless driver in cloud, pg-pool against the local container in Docker dev. The conversation explicitly identified this as the mistake a beginner makes.\n\nNEON IS NEVER A LOCAL-RUN DEPENDENCY (section 37): default local is container Postgres (offline-capable); cloud/preview uses a Neon branch per-developer/per-PR via the neonctl link/checkout/env pull workflow.\n\nWHY DEPLOY-EASE WON: the conversation explicitly prioritized deploy simplicity, and this stack wins from MVP to mid-scale. Phase 1 (MVP) is Vercel Next.js (Web+API+Auth+MCP+SSE) + Neon. Phase 2 happens ONLY AFTER A BENCHMARK PROVES event throughput is the bottleneck: Agents -> Gateway -> {Next.js/Vercel, Realtime service, Queue} -> Neon.\n\nDO NOT ADD REDIS/UPSTAZ PREEMPTIVELY (section 7.4) - the plan's exact words: 'don't choose Redis just because realtime might need Redis'. Measure first.\n\nCONNECTION MANAGEMENT vs WRITE VOLUME (section 7.2): Neon is perfectly capable of handling many requests; the danger is write VOLUME from agent telemetry (2000 writes/s at 100 agents x 20 ev/s). The batching and persist-filter are the mitigation, not a bigger database.\n\nSUCCESS CRITERIA, REWRITTEN BECAUSE 'PUSH TO MAIN DEPLOYS' CANNOT FAIL. A deploy is not observable from inside the repo - a green pipeline that deploys a broken build, or deploys nothing at all, satisfies the old first clause. All three clauses now name a command whose exit code settles them.\n\n1. A merge to main produces a working deployment. Settled by: an automated smoke test against the deployed URL on every main build, which must FAIL the build when it fails. The test asserts a real response, not a reachable host - specifically that the app serves its root, that the Better Auth mount responds at /api/auth/*, and that the event ingest route answers on a batched request with the documented status for that batch. A deploy that 404s or serves a shell with no working API is the failure this catches, and it is not hypothetical: the auth and events routes are the real surface, and a Next.js shell with no API behind it renders fine. Exit 0 means deployed AND serving; there is no \"deployed\" state that passes without a working endpoint.\n\n2. Each PR gets an isolated preview with its OWN Neon branch. Settled by two assertions, because either alone is satisfiable by a lie: (a) the preview environment is bound to a branch whose name derives from the PR number, asserted by reading the deployment's env, and (b) a migration run against that branch leaves the PRODUCTION branch's schema untouched. The second is the one that matters and the one easiest to claim without checking - a preview wired to the production database satisfies \"each PR gets a preview\" and corrupts shared data on merge. The check is: run the migration against the preview branch, then assert the production branch's migration ledger is unchanged. Two PRs running concurrently against one database is the failure this prevents, and it is exactly what per-PR branches exist to stop.\n\n3. Local development never requires Neon. Settled by: a local test run with no Neon credentials present at all - DATABASE_URL unset, no neonctl config - which must still pass, because the local default is container Postgres (section 37). This is the assertion that catches a stray Neon import or a hardcoded connection string, and it is the only clause here checkable in CI without a cloud account, so it is the one to wire into .github/workflows/m0.yml first.\n\n4. The pooled driver is used; no per-request Client construction. Settled by: a grep-level guard rejecting a new Client() constructed inside a request path, PLUS - because the grep is easy to satisfy wrongly - a test asserting the Neon serverless driver is a pooled singleton in cloud and pg-pool against the local container in Docker dev. The grep alone is defeated by a single hoisted Client that reconnects per query under load, which is the exact failure the bead was written to prevent: it looks correct in code review and exhausts connections in production at 2000 writes/s.\n\nTESTS, each a command with an exit code:\n- a smoke test against the deployed URL on every main build, asserting the root, the /api/auth mount and the events ingest route all respond - failing the build when they do not, not warning.\n- a preview-isolation check: the preview's Neon branch name derives from the PR number, AND running migrations against it leaves the production branch's migration ledger byte-identical.\n- the no-Neon local check: the full local test suite passes with DATABASE_URL unset and no neonctl config, wired into m0.yml.\n- the pooled-driver guard: a lint rejecting per-request Client construction, plus a test asserting the pooled driver is a reused singleton rather than a per-query connection.\n\nSTILL BINDING, and worth restating because it is the discipline this bead exists to protect: Phase 2 (a realtime service, a queue) happens ONLY after a benchmark proves event throughput is the bottleneck. Adding Redis/Upstash because realtime seems to need it is the failure - section 7.4's exact words are 'don't choose Redis just because realtime might need Redis'. The right-hand side of criterion 4 is what makes that checkable: if event throughput is not the bottleneck, none of these tests should ever ask for a queue.\n\nCONSIDERATIONS: the discipline this bead must protect is 'measure before scaling'. The plan is unusually explicit that Redis/Upstash must NOT be added just because realtime seems to need it (section 7.4), and that a queue is Phase 2 only after a benchmark shows event throughput is the bottleneck. It is cheaper to hold a simple architecture under a proven load than to debug a speculative one. Second: preview branches per PR are the mechanism that keeps shared staging data from corrupting parallel work - worth the setup cost even before anyone needs it, because retrofitting it mid-project is painful.","status":"open","priority":1,"issue_type":"task","created_at":"2026-09-23T19:05:35.802393Z","created_by":"tranquangdang21","updated_at":"2026-09-24T21:45:48.184247Z","source_repo":"battle-agents","source_repo_path":"/Users/tranquangdang21/Projects/battle-agents","mol_type":"work","work_type":"mutex","wisp_type":"none","labels":["deploy","infra","p1"],"dependencies":[{"issue_id":"ba-neon-cloud-deploy-zn4","depends_on_id":"ba-better-auth-github-oauth-9wm","type":"blocks","created_at":"2026-09-23T19:05:53.037664Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"ba-neon-cloud-deploy-zn4","depends_on_id":"ba-db-schema-drizzle-fki","type":"blocks","created_at":"2026-09-23T19:05:52.951558Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"ba-neon-cloud-deploy-zn4","depends_on_id":"ba-docker-local-dev-y28","type":"blocks","created_at":"2026-09-23T19:05:52.868444Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"ba-p3j","title":"test:m2 / test:m4 milestone pipelines + wire the pre-M4 load test","description":"Add the `pnpm test:m2` and `pnpm test:m4` milestone pipelines, and wire the pre-M4 load test into a gate that can actually fail the build.\n\nWHAT THE PLAN SAYS, QUOTED\nSection 40, added 2026-09-24: \"The bounty-claim and replay-render assertions are NOT M0: bounty is M2 and replay is M4, so asserting them in the M0 smoke made `pnpm test:m0` impossible to pass before M2 and M4 were complete. They move to `pnpm test:m2` and `pnpm test:m4` respectively (added 2026-09-24).\"\n\nThose two commands are named in the plan as the reason the M0 smoke is allowed to be small. Neither exists, and nothing in the tracker will build them.\n\nWHY IT IS MISSING, verified 2026-09-25\n- package.json scripts are: architecture, build, codegen, db:generate, db:migrate, db:seed, db:verify, format, license-check, load:events, lint, removal-test, schema-hygiene, test, test:e2e, test:integration, test:m0, test:unit, typecheck. There is no test:m2 and no test:m4.\n- The pipeline that would be extended is real and good: scripts/test-m0.sh with a CANONICAL_STAGES array, scripts/stages.manifest, and scripts/check-stage-manifest.sh asserting THREE-WAY agreement between the manifest, the array, and the run_stage dispatch, so a stage cannot be dropped, added in one place only, or left with no implementation. Per-stage markers land under .tmp/m0-stages/ and the gate may only report green when a marker exists for every stage. The M0 machinery is the thing to copy, not reinvent.\n- ba-local-testing-contract-gmi is CLOSED and its close reason is about test:m0 only: \"M0 pipeline GREEN: 12/12 stages ... A red stage blocks merge.\" It did not build, and was not asked to build, the M2/M4 pipelines.\n- No bead mentions test:m2 or test:m4. Searching all 61 beads for those strings returns nothing.\n- The gap is not cosmetic. The M2 and M4 DoDs in section 27 are among the plan's most important claims - \"Three most important milestones to prove the idea: M1, M2, M4\" (section 15.3) - and the bounty bead's own success criterion is an end-to-end vertical slice on a scratch repo. A DoD with no command that settles it is a claim, not a gate.\n\nWHAT test:m2 MUST SETTLE\nSection 27's M2 DoD: \"end-to-end on a scratch repo with real money rail stubbed (record intent, no real payout until 17.5 resolved - mark clearly in UI 'payout: manual/sandbox').\" The pipeline asserts the LOOP, end to end, on a real scratch repository: a bounty is created on a real GitHub issue, claimed by an agent through the protocol, a PR is opened, the merge webhook fires, the bounty completes, XP and reputation are awarded, and the history shows the trail. The payout leg asserts INTENT RECORDED and the sandbox marking rendered - never a real transfer. ba-payout-rail-dispute-1q6 closed with the type-level no-real-transfer guarantee mutation-verified, and ba-feature-bounty-xhk owns the loop; this pipeline is the command under which both are proven, not a second implementation of either.\n\nWHAT test:m4 MUST SETTLE\nSection 27's M4 DoD: \"share replay link with a logged-out user and they see the full timeline.\" So: 1v1 battle on the same issue, isolated workspaces, public weights readable while the battle runs, judge run, replay URL shareable, XP/rep applied - and the logged-out read, which is the specific clause and the one that actually distinguishes M4 from \"a battle page exists.\"\n\nTHE LOAD TEST, WHICH IS A GATE THAT DOES NOT CURRENTLY GATE ANYTHING\nSection 17.2 and section 40 both require it: 100 agents x 20 events/s, run as a PRE-M4 gate, not an M0 one. Section 7.2 is explicit that the number reaching the database must be a small fraction of the number crossing the wire - that gap is the whole thesis of the batching design.\nThe good news: the harness EXISTS. scripts/load-test-events.ts is implemented, `pnpm load:events` runs it, it drives the real ingest handler in-process, and it deliberately reports generated / accepted / rows-written as three separate numbers. The bad news, and the reason this is in scope: it is referenced by no gate. It is not in scripts/stages.manifest, not in CANONICAL_STAGES, and not in any CI job. ba-risk-gates-e74 states the requirement - \"a load test simulating 100x20 ev/s, run as a pre-M4 gate rather than an M0 one (section 40), so it must be runnable before feature-battle closes\" - and then says in its own considerations: \"a gate nobody runs is a comment.\" By that bead's own standard the load test is currently a comment with a script attached. Two beads name the load test as a test (ba-risk-gates-e74 and ba-event-ingest-and-sse-gu9) and neither runs it.\n\nWHAT TO BUILD\n- scripts/test-m2.sh and scripts/test:m4, following test-m0.sh's shape: a manifest, a stage array, a run_stage dispatch, per-stage markers, and a red stage that names itself in the summary and the exit code. Extend the EXISTING three-way check rather than adding a second convention - if a stage is added to a milestone pipeline it must appear in all three views or the pipeline is not trustworthy, and the check-stage-manifest.sh self-test already proves that check catches a dropped stage.\n- Wire `load:events` into test:m4 as a stage, with a PASS THRESHOLD, not just an exit code. A load test that prints numbers and always exits 0 gates nothing. The threshold is a decision to be recorded here and asserted; if the measurement shows the number is wrong, the number changes - but it must exist, because \"the batching held\" is prose. The natural assertion is on rows-written versus events-generated, since that ratio is what section 7.2 actually claims, plus an explicit assertion that the harness exceeded its target rate (a load test that quietly ran at 5 events/s passes any threshold).\n- Keep the M2 and M4 pipelines honest about what they may assert: bounty is M2, replay is M4. A pipeline asserting a later milestone's surface makes itself unpassable, which is exactly the mistake section 40's amendment was written to undo.\n\nSUCCESS CRITERIA, each with the thing that settles it\n1. `pnpm test:m2` exists, runs the M2 vertical slice against a real scratch repository, and exits non-zero when a stage of that slice is broken. Settled by deliberately breaking one stage of the loop and asserting the command goes red - a pipeline that has never been seen red is not known to gate.\n2. `pnpm test:m4` exists, runs the M4 DoD including the LOGGED-OUT replay read, and exits non-zero when it is broken. The logged-out read is the clause; assert the timeline renders with no Better Auth session, and that the same request with a session also works (the public path must not be a logged-in-only path with the session stripped).\n3. The load test runs inside test:m4 and FAILS on a threshold breach. Settled by a test that runs the harness with a deliberately raised write-ratio and asserts the stage goes red. Without the deliberate-breach half this is a script in a pipeline that always passes.\n4. Stage parity with test:m0. Settled by the three-way manifest check passing for the new pipelines, so a stage cannot be added in the dispatch only and left out of the manifest.\n5. CI runs both, on the same terms as test:m0. Settled by the CI configuration naming them; a local-only pipeline is the \"gate nobody runs\" failure this bead exists to end.\n\nCONSIDERATIONS\nThe tempting wrong turn is to fold the M2 and M4 assertions back into the M0 smoke, which is precisely the change section 40's amendment reversed: it made `pnpm test:m0` impossible to pass before M2 and M4 were done, and a permanently-red M0 gate trains people to ignore red. Separate commands, each asserting only its own milestone. Second, the scratch repository must be a real one with real GitHub URLs - ba-feature-bounty-xhk already requires a test asserting real GitHub URLs rather than synthetic ones, and a pipeline that mints fake issues would let a broken GitHub integration pass. Third, the load-test threshold should be recorded as a number with a rationale, not copied from the plan's 100x20: that figure is the SCENARIO, and the PASS CRITERION is how many of those events may reach the database, which is a different number the plan does not state.","status":"open","priority":1,"issue_type":"task","created_at":"2026-09-24T22:44:38.417361Z","created_by":"tranquangdang21","updated_at":"2026-09-24T22:45:30.482264Z","source_repo":"battle-agents","source_repo_path":"/Users/tranquangdang21/Projects/battle-agents","mol_type":"work","work_type":"mutex","wisp_type":"none","labels":["ci","m2","m4","testing"],"dependencies":[{"issue_id":"ba-p3j","depends_on_id":"ba-battle-replay-yjb","type":"blocks","created_at":"2026-09-24T22:45:30.482096Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"ba-p3j","depends_on_id":"ba-feature-battle-fbt","type":"blocks","created_at":"2026-09-24T22:45:30.400086Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"ba-p3j","depends_on_id":"ba-feature-bounty-xhk","type":"blocks","created_at":"2026-09-24T22:45:30.312439Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"ba-payout-rail-dispute-1q6","title":"Payout rail + dispute path for real-money bounties (design before promising)","description":"DESIGN (and stub) the payout rail and dispute path for real-money bounties. This is the unresolved blocker flagged in plan section 17.5, and it must be settled BEFORE the product ever promises real amounts.\n\nTHE OPEN RISK (section 17.5): 'Payment/compliance for real-money bounties: M2 needs payout rail + dispute path design (not yet specified in conversation - must design before promising amounts).' The conversation never settled this. It is a genuine gap, and the plan correctly says so.\n\nM2 SCOPE (section 27): the DoD ships with real money STUBBED - 'record intent, no real payout until section 17.5 resolved - mark clearly in UI payout: manual/sandbox.' The UI banner is a hard requirement, not a nicety: users must never believe they have been paid when they have not.\n\nWHAT THIS BEAD MUST PRODUCE:\n1. A payout rail decision: which provider(s), escrow vs direct transfer, KYC/AML posture, tax/reporting obligations, minimum bounty size, refund policy.\n2. A dispute path: who adjudicates, what evidence is required (the activity log is the audit trail - section 30), time limits, refund to multiple co-sponsors when a bounty is cancelled or a maintainer rejects a valid PR.\n3. A compliance checklist: what legal exposure does accepting money for OSS work create, and at what scale does it require counsel.\n4. A stub interface so the bounty feature records payout INTENT (bounty_funded -> payout_pending -> payout_recorded) without moving money.\n\nWHY THE ACTIVITY LOG MATTERS HERE: section 30 lists audit and anti-abuse as reasons the ordered, attributed event trail exists. A dispute is resolved by replaying what actually happened, so the log must be sufficient evidence. If the log cannot settle a dispute, the design is incomplete.\n\nCONSIDERATIONS: the refund-on-cancel case is the hardest, because open funding (bounty-open-funding) means multiple sponsors with different expectations. Decide proportional refund semantics.\n\nSUCCESS CRITERIA: a written design exists and is referenced from the bounty feature; the stub interface records intent with no money movement; the sandbox banner is present and tested; nothing in the product claims a real payout.\n\nTESTS: test that a completed bounty records payout intent in the correct state; test that the sandbox banner renders whenever a bounty is in a paid-adjacent state; a test that the payout stub cannot be invoked in a mode that implies real transfer.","status":"closed","priority":1,"issue_type":"task","created_at":"2026-09-23T19:01:27.648179Z","created_by":"tranquangdang21","updated_at":"2026-09-24T19:04:15.260205Z","closed_at":"2026-09-24T19:04:15.260138Z","close_reason":"The design half is done and good: 265 lines settling all four section 17.5 questions with reasoning rather than deferral, including a rejection table for four payout providers derived from a hard external constraint (Vercel Hobby counts payment processing as commercial, so escrow is unavailable on the current plan). The type-level no-real-transfer guarantee is real and mutation-verified: removing the intent.mode refusal from payout.ts turns the smuggle test red, and PayoutMode is a single-member union with a separate structural test.\n\nThe three obligations that were not met are moved to ba-feature-bounty-xhk, which this bead blocked. They could not be done here: a PayoutIntentStore implementation needs a database repository, a rendered banner needs a UI to render in, the dispute windows need a rules.ts, and a completed-bounty test needs a bounty lifecycle. Keeping them here was a deadlock, and the risk they guard against is impossible today because no bounty surface exists at all.\n\nThree present-tense sentences in docs/design/payout-rail.md claimed the events are persisted, the windows live in rules.ts, and the README reports manual/sandbox. None was true. Corrected to say what is declared and what is not built.","source_repo":"battle-agents","source_repo_path":"/Users/tranquangdang21/Projects/battle-agents","mol_type":"work","work_type":"mutex","wisp_type":"none","labels":["bounty","feature","risk"]}
{"id":"ba-protocol-sdk-ecosystem-3ro","title":"External extension packages + public protocol/SDK (M7)","description":"Open the ecosystem per plan sections 16 and 33 (M7).\n\nWHAT M7 IS (section 15.1): open ecosystem via MIT protocol/SDK covering adapters, agents, clients, and arenas - the contributor flywheel.\n\nLICENSE POSTURE (sections 16, 35): MIT everywhere, whole repo, public from day one. Rationale: a fun-game OSS flywheel maximizes stars, forks, and contributions; MIT is simpler than Apache-2.0 and avoids copyleft fear; it is commercial-use safe. THE MOAT IS COMMUNITY + AGENTS + BOUNTIES + HISTORY + REPUTATION + GUILDS, NOT SOURCE. Precedents cited: Kubernetes, Kafka, Spark, TensorFlow, Android - forks cannot fork ecosystems; LiveKit - open core to hosted cloud value. Custom 'MIT + no-compete' licenses were REJECTED (they are not OSS). Trademark, brand, and domain are protected separately, since MIT/Apache grant no trademark rights beyond attribution.\n\nFORKS ARE WELCOMED (section 16): forks like 'Agent Arena' or 'Coding Wars' are welcome IF protocol-compatible - and that compatibility is precisely what would make us the standard.\n\nTHE FLYWHEEL NEEDS MORE THAN A LICENSE (section 16): docs, easy dev, clear architecture, good-first-issues, fun visible results (adapter, reporter, or battle-mode PRs), a useful protocol, and active maintainers. A permissive license alone does not produce contributors. This bead is where that non-license work happens.\n\nBANNED (section 29.2): a plugin MARKETPLACE, a plugin INSTALLER, dynamic/remote plugin loading, distributed event infrastructure, and microservices. The extension model is EXPLICIT COMPOSITION via createRuntime({extensions: [...]}), not a dynamic loader. Do not build an installer here.\n\nPACKAGE SHAPE (section 33): features/extensions declare capabilities and know NOTHING about MCP/CLI/REST/Web. External packages install the same way internal ones do - as GameFeature objects. A third-party world or battle mode is just another extension.\n\nSUCCESS CRITERIA, and the second clause is the one that decides whether this bead worked:\n\n1. An external package can be published and consumed as a GameFeature without patching core. Settled by: the out-of-tree extension test, which installs and uninstalls through the public API only. 'Without patching core' is the load-bearing half and it needs a real assertion, not a convention - the test runs against a core with a diff guard, so an extension that reaches into core internals fails rather than merely being discouraged. The removal half matters as much as the install half and is easy to omit: a feature that installs but whose handlers, commands, actionDefs, capabilities and persistedEvents are not all given back is a leak that survives into every later test.\n\n2. The protocol/SDK is documented well enough to write a client from scratch. [REWRITTEN] 'Well enough' has no wrong version - a doc set that omits the handshake, or gets the auth model wrong, still reads as documentation and still passes a completeness check. It is now the property that makes it a real test: a client written from the PUBLISHED DOCUMENTATION ALONE, with no access to the repo source, compiles and completes a real exchange against a running server - register, authenticate, call each of the documented capabilities, and parse the responses. The assertions that make it fail when the docs are wrong: the client builds using only what the docs state, its requests authenticate under the documented credential model (the agent Bearer token, never the human web session), and every response it parses matches the documented shape. A doc gap produces a client that cannot compile or cannot authenticate, and THAT is the failure being looked for.\n\n   The published-package half is the same property from the other side, and it is the half THIS BEAD owns: the extension is consumed the way a third party would consume it - from a package, through public exports only - so 'published' means consumable rather than merely uploadable.\n\n   THE DOCS-SUFFICIENCY CLAIM IS NOT THIS BEAD'S, and the duplication is worth naming because two beads claiming one property is how it ships half-proved. The claim \"a third party with no repo access can build a working client from the published documentation\" is a single property, and ba-skill-md-protocol-72x owns it: that bead publishes the four docs and discharges it with a fresh-agent conformance test. This bead already depends on ba-skill-md-protocol-72x, so the ordering is right - the point is only that the reader should not also build a docs-only client here. What is genuinely distinct and genuinely this bead's is the out-of-tree extension test: an external PACKAGE installed and uninstalled through public exports, with the core-diff guard. A package is a shipped artifact; a doc set is prose. They fail differently - a package fails on a missing export or a leaking registration, docs fail on an omitted step - which is exactly why they are two beads rather than one criterion twice.\n\nTESTS, and each fails when the behaviour is wrong rather than when a file is missing:\n- the out-of-tree extension test: installs and uninstalls through the public API only, with a core-diff guard so 'without patching core' is asserted rather than assumed, and an uninstall assertion covering every registration kind (handlers, commands, actionDefs, capabilities, declaredCapabilities, eventHandlers, persistedEvents) - runtime.test.ts already asserts the give-back property for internal features, and this is that same assertion for an external one.\n- the removal test for the external extension: uninstalling it leaves the runtime with no trace, and the surrounding features keep working.\n- the docs-only client test, the criterion-2 test: a client built from the published docs with no repo access, compiling and completing register -> authenticate -> each documented capability -> parse. A completeness check on the docs cannot substitute for this, and passing this while the docs are wrong is impossible by construction - the client has nothing else to go on.\n- a versioning/compatibility check, because the moat argument cuts both ways: breaking protocol compatibility is expensive, so the published PROTOCOL_VERSION and the handshake's expected version are asserted to agree, and a client presenting the wrong version is refused rather than silently admitted. This is the mechanical half of 'forks are welcome IF protocol-compatible' - without it, 'protocol-compatible' is a wish.\n\nWHAT THIS BEAD STILL CANNOT PROVE: a docs-only client that works proves the documentation is SUFFICIENT, not that it is good. Do not read a green run as evidence the protocol is ergonomic to build against.\n\nCONSIDERATIONS: the plan's moat argument is worth restating as an engineering constraint rather than a slogan - the defensibility is community, agents, bounties, history, reputation, and guilds, NOT source code. That has a practical consequence: breaking protocol compatibility is expensive, so versioning and compatibility guarantees deserve more care here than typical feature work. Second, resist building the ecosystem tooling early. Section 29.2 bans a plugin marketplace, a plugin installer, and dynamic loading; explicit composition is the model. Publishing packages and documenting the protocol is enough until real third parties force otherwise.","status":"open","priority":3,"issue_type":"task","created_at":"2026-09-23T19:03:56.983587Z","created_by":"tranquangdang21","updated_at":"2026-09-24T22:22:03.394659Z","source_repo":"battle-agents","source_repo_path":"/Users/tranquangdang21/Projects/battle-agents","mol_type":"work","work_type":"mutex","wisp_type":"none","labels":["ecosystem","m7","p3"],"dependencies":[{"issue_id":"ba-protocol-sdk-ecosystem-3ro","depends_on_id":"ba-feature-battle-fbt","type":"blocks","created_at":"2026-09-23T19:05:54.335153Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"ba-protocol-sdk-ecosystem-3ro","depends_on_id":"ba-skill-md-protocol-72x","type":"blocks","created_at":"2026-09-23T19:04:26.674062Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"ba-reference-repo-study-o92","title":"Run reference implementations locally to internalize PixiJS + AgentWatcher","description":"Plan section 18 build order item 1 - the FIRST thing to do, and it is currently unowned.\n\nPLAN SECTION 18, VERBATIM: 'Re-clone learn-spine cleanly; run age-of-agents demo + agent-move locally to internalize PixiJS + AgentWatcher behavior.'\n\nWHY THIS IS A BEAD AND NOT A NICE-TO-HAVE: the plan explicitly instructs reading source, not just cloning it (section 9.5). Running the two best references locally is how the porting beads (tool-map-port, hookprovider-seam, game-client-pixijs, adapters-template) get grounded in observed BEHAVIOR rather than code reading alone. Porting a PixiJS client or a watcher interface from source you have only skimmed is how subtle behavioral details get lost.\n\nTASKS:\n1. Re-verify the learn-spine checkout (the stale 'empty/broken HEAD' caveat in sections 79/28.2/17.6 is now outdated - the repo is cloned and valid) and determine its LICENSE, which is currently unconfirmed. Until the license is known, copy NOTHING from it.\n2. Run the age-of-agents demo locally and study packages/client/src/game/ as the PixiJS v8 reference (section 1.2 item 1): pixi-viewport + React19 + zustand, the view/unit/tilemap/projection/pathfind/autotile decomposition, and the dual fantasy/scifi pack structure.\n3. Run agent-move locally and study watcher/ per-CLI parsers, the AgentStateManager 30s idle rule, the Broadcaster, full_state-then-delta, and the OpenCode WAL-polling CAUTION (sections 1.2 item 4, 28.2).\n4. Read .tmp/agent-quest's mapping config (the TOOL -> activity table, section 28.2) since tool-map-port depends on the same idea.\n\nALSO WORTH A PASS (they are already cloned): agent-world-codemoo for the stateDiffBroadcast + permission-queue + costTracker patterns (section 28.1 item 12), and arcane-agents for the 1061-line status-fusion decider (section 28.1 item 5).\n\nDELIVERABLE: a short study note per repo recording the concrete behaviors worth porting and the ones to avoid, so the porting beads do not each re-derive the same reading. The plan's Appendix A already maps repo to lesson; this note fills in the BEHAVIORAL detail that a static map cannot.\n\nNOTE: all 14 reference repos are already cloned into .tmp/ (gitignored, ~4.3GB). No re-cloning is needed.\n\nSUCCESS CRITERIA: each porting bead can cite an observed behavior rather than only a source snippet; learn-spine's license is determined.\n\nTESTS: not code - the study note is the artifact. Add a dated note so future readers can judge staleness.\n\nCONSIDERATIONS: the value of this bead decays. Read the sources while the specific porting question is live, not speculatively - a study note written a month early will be confidently wrong about behavior it never observed. Keep the notes dated and tied to a porting bead, so a reader can tell which claims were verified and against which commit. And resist cloning repos 'just in case': the plan already names what was not cloned and why (Appendix A backlog), and a .tmp full of unread checkouts is a false sense of coverage.","status":"closed","priority":1,"issue_type":"task","created_at":"2026-09-23T19:05:35.664122Z","created_by":"tranquangdang21","updated_at":"2026-09-24T19:01:21.382192Z","closed_at":"2026-09-24T19:01:21.381970Z","close_reason":"Six dated, SHA-pinned notes. age-of-agents and agent-move were genuinely run with recorded evidence (229 tests passing, HTTP 200, four watchers live). learn-spine's licence determined NONE by five independent checks, which resolves the plan's open 'copy NOTHING' item. The three NOT RUN notes say so in three places each, which is honest rather than a shortfall: the bead named two repos to run and both were run.\n\nThe notes corrected two plan errors rather than agreeing with them: the idle rule is 45s, not the 30s the plan states, and agent-world-codemoo is AMBIGUOUS rather than MIT (package.json says MIT, the LICENSE file does not exist). Caveat carried forward: ba-status-fusion-presence-5zk and ba-animation-core-spike-hf7 have no observed material behind them.","source_repo":"battle-agents","source_repo_path":"/Users/tranquangdang21/Projects/battle-agents","mol_type":"work","work_type":"mutex","wisp_type":"none","labels":["enabler","p1","research"]}
{"id":"ba-removal-test-e33","title":"Removal test: every feature removable without breaking the build","description":"Turn the REMOVAL TEST into an executable CI gate, per plan section 12.2 and section 20.\n\nWHAT IT IS: Section 12.2 states 'Removal test is architectural principle #1: rm -rf features/guild must still compile/run.' This bead makes that principle an automated check so the pluggability property cannot silently regress as features are added.\n\nMECHANISM (section 20): scripts/removal-test.sh - for each packages/features/*, temporarily move it away, then run 'tsc --noEmit && vitest run'. The tree must stay GREEN except for capability-degraded warnings. This is also the mechanism by which the Battle feature proves its requires: ['reputation.read'] degradation (section 24).\n\nWHY IT MATTERS: The entire extension architecture (sections 12, 33) is justified by 'add feature without touching others'. If removal breaks the build, the architecture has quietly become a God Engine and section 29.2's 'thin core, rich edges' claim is false. Catching that in CI rather than at M6 is the entire value of this bead.\n\nCONSIDERATIONS: the script MUST restore the moved directory even on failure (trap/finally), or a crashed run leaves the tree broken. It must be reasonably fast so it runs in CI and not just locally. Capability-degraded warnings (a feature whose requires[] become unsatisfied after removal) are EXPECTED and must not fail the run; only type errors and test failures do.\n\nSUCCESS CRITERIA: 'pnpm removal-test' passes with all features present; passes with any single feature removed; fails when a feature illegally imports a sibling feature (cross-check with the dependency-rules bead).\n\nTESTS: the script is itself the test. Add a fixture feature that illegally imports a sibling and assert the removal-test run fails.","status":"closed","priority":0,"issue_type":"task","created_at":"2026-09-23T18:58:26.154857Z","created_by":"tranquangdang21","updated_at":"2026-09-24T05:21:36.339195Z","closed_at":"2026-09-24T05:21:36.337110800Z","close_reason":"Script checks all 10 removable features + extensions[] stripping; self-test proves the strip is sound and was mutation-verified against the exact bug it guards. Gate runs in CI via test:m0.","source_repo":"battle-agents","source_repo_path":"/Users/tranquangdang21/Projects/battle-agents","compaction_level":0,"original_size":0,"labels":["infra","p0","test"],"dependencies":[{"issue_id":"ba-removal-test-e33","depends_on_id":"ba-monorepo-scaffold-cit","type":"blocks","created_at":"2026-09-23T19:04:21.600491Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"ba-risk-gates-e74","title":"Risk gates: gimmick, event volume, adapter sprawl, fairness, scope","description":"Encode the plan's documented risks (section 17) as verifiable gates rather than prose warnings.\n\nRISK 1 - GIMMICK TRAP (section 17.1, also section 2.3): 'Claude/Codex/OpenCode -> pixel characters -> XP -> battle with no real utility' produces a 'haha cool' reaction then churn. DEFENSE: ship M2 real utility (bounties paying real money) BEFORE heavy game art. GATE: the game client and art beads must not be scheduled to complete before the bounty vertical slice.\n\n  STATUS, and it is the one risk that has actually bitten: the art and game-client work is moving ahead of the slice. packages/game-client/src/index.ts is `export {};` and apps/web has no public/assets directory, so nothing visual has been BUILT - but ba-game-client-pixijs-riw and ba-asset-shortlist-x2a are both active open beads with detailed instructions, and the Next.js shell in apps/web now means a contributor can see a running app. The gate as written ('must not be scheduled to complete before') is therefore already being strained. The stronger form of the gate, and the one worth enforcing, is that the bounty vertical slice (ba-feature-bounty-xhk) CLOSES before either art bead starts producing files - not merely before they complete, because completing-in-order is trivially satisfiable by writing art first and declaring it unfinished.\n\n  THE GATE IS NOW AN EDGE, not a sentence. Both art beads now carry an explicit dependency on ba-feature-bounty-xhk: br dep add ba-game-client-pixijs-riw ba-feature-bounty-xhk and br dep add ba-asset-shortlist-x2a ba-feature-bounty-xhk. That is deliberate and it is not redundant with this text - if you are reading this and the two edges are missing, the gate has been quietly deleted, and that is the failure this risk exists to prevent. Do not remove them as over-constraining: the plan defense is to ship M2 real utility BEFORE heavy game art, and an ordering edge is the only form of gate an agent swarm actually obeys.\n\n  ONE HONEST TENSION, recorded rather than smoothed over. Gating the asset shortlist behind M2 also delays RISK 7, whose per-pack license tracking is downstream of exactly that shortlist. The plan explicit ordering was chosen over that convenience, because risk 1 is the one risk that has already bitten and risk 7 gate is only needed before art ships, not before the shortlist is researched. If a future reader disagrees, the asset-shortlist edge is the one to reverse; the game-client edge is not.\n\nRISK 2 - EVENT VOLUME (section 17.2): 100 agents x 20 events/s = 2000 DB writes/s. DEFENSE: enforce the section 7.2 batching from day one. GATE: a load test simulating 100x20 ev/s, run as a pre-M4 gate rather than an M0 one (section 40), so it must be runnable before feature-battle closes.\n\n  STATUS: THE HARNESS ALREADY EXISTS AND RUNS. scripts/load-test-events.ts is written and wired as `pnpm load:events`; its defaults are exactly LOAD_AGENTS=100, LOAD_EVENTS_PER_SECOND=20, LOAD_SECONDS=5, LOAD_MAX_SUBSCRIBERS=4. It drives the real ingest handler in-process against the real EventStreamHub, and it reports generated / accepted / rows-written as three separate numbers - the gap between the last two is the whole thesis of the persistence filter. So this risk's deliverable is DONE; what remains is running it and wiring it somewhere visible, because a harness nobody invokes is the exact failure this bead exists to prevent. It is deliberately NOT in .github/workflows/m0.yml, and per section 40 it should not be: add it as its own pre-M4 job when M4 work starts.\n\nRISK 3 - ADAPTER SPRAWL (section 17.3): third-party adapter PRs must not break core. DEFENSE: keep the AgentEvent union versioned with a skill.json-style pin, and check the version on every handshake. GATE: a CI check that a protocol version bump forces regeneration of schemas and updates skill.json.\n\n  STATUS: THE DEFENSE IS BUILT, THE CI CHECK IS NOT. PROTOCOL_VERSION lives in packages/protocol/src/version.ts; the ingest route rejects a mismatched version with a 400 naming both the expected and the received value; `pnpm codegen` regenerates packages/protocol/src/generated/action-ids.ts from each feature's manifest, and tests/types/action-ids.ts pins the union at compile time with @ts-expect-error in both directions. What does NOT exist is a CI job that fails when the generated file is stale relative to the manifests, or that a version bump moved skill.json. The codegen is a script a human runs; nothing verifies it was run. That gap is the live part of this risk and it is small: a job that runs codegen and fails on a dirty diff closes it. (skill.json itself is ba-skill-md-protocol-72x's deliverable and does not exist yet, so that half of the gate is blocked on that bead rather than on this one.)\n\nRISK 4 - BATTLE FAIRNESS (section 17.4): judge weights must be public per match; isolated workspaces mandatory; ANTI-CHEAT IS TBD. GATE: assert that weights are stored and surfaced per battle, and that the anti-cheat gap is documented rather than silently assumed solved.\n\nRISK 5 - PAYMENT/COMPLIANCE (section 17.5): unresolved; see the payout-rail-dispute bead. GATE: no UI may imply a real payout before that design lands; the sandbox banner is enforced by test.\n\nRISK 6 - LEARN-SPINE CHECKOUT (section 17.6): RESOLVED, and the answer is a prohibition rather than a permission. docs/research/learn-spine-license.md records the determination dated 2026-09-24: learn-spine has NO LICENSE - no license file, no license field in package.json, and GitHub's own API reports none - confirmed five independent ways. The checkout is present and readable at .tmp/learn-spine; reading it is how we know this. Standing instruction: copy nothing, not one line. An unlicensed repo may be READ for architectural ideas and that is what ba-animation-core-spike-hf7 should do, but no file may be copied and no code derived from it may be vendored. This risk is CLOSED, not pending; the verify step this bead used to carry is done.\n\nRISK 7 - ASSET LICENSING (section 17.7): track per-pack licenses; decide the game-asset license separately from the code MIT license.\n\nRISK 8 - SCOPE DISCIPLINE (section 17.8): NO MMO, and no equipment/pets/trading/guild-war/housing/crafting/marketplace/tournament/boss/season/achievements in the MVP beyond the section 10.2 MVP character sheet and the M1/M2/M4 slice. GATE: a scope check in review - the tempting failure mode is gold-plating while M2 is still incomplete.\n\nWHY ENCODE AS GATES: section 17 calls these 'documented', but documentation alone does not prevent the failure. Each should have an executable or reviewable check.\n\nSUCCESS CRITERIA, REWRITTEN BECAUSE THE OLD CRITERION CONTRADICTED THE BEAD'S OWN REASONING. The old wording was 'a named, runnable OR REVIEW-ENFORCED gate', and this bead's own closing line says 'a gate nobody runs is a comment'. A review-enforced gate is the one form nobody runs - it is satisfied by writing a checklist, which is the exact failure the bead exists to prevent. So the three-way disjunction is removed and replaced with a single rule.\n\nTHE RULE: a gate is DONE only when a COMMAND exists whose exit code settles it, and that command runs somewhere that is not a human's memory. Concretely, per risk, a gate is done when at least one of these holds, and the risk's entry must name which:\n  (a) it is a script or test in the tree, named by its command, and\n  (b) it is WIRED - invoked by a CI job, or by a br dependency edge, or by a pre-commit/CI check that fails the build.\n  (a) without (b) is a harness nobody runs and does NOT count. This is exactly risk 2's current state: pnpm load:events exists and is real, and is deliberately not yet an M0 job, so risk 2 is NOT done even though its harness is written. Writing the harness was the work; wiring it is the gate.\n  A 'review-enforced' gate is acceptable ONLY as a fallback for a risk that genuinely cannot be mechanized yet, and it must name the file and line where the checklist item lives and who reviews it - and it is recorded as PARTIAL, never as done. A review gate with no named location is a comment, by this bead's own reasoning.\n\nTHE HONEST TALLY as of 2026-09-25, with each entry naming the command that would settle it, because a state description that names no command is the thing being fixed here:\n- risk 1 GIMMICK: enforced by dependency edge, and that IS a command - br dep list showing ba-game-client-pixijs-riw and ba-asset-shortlist-x2a both blocked on ba-feature-bounty-xhk. DONE as a gate. But the M2 slice it protects is still outstanding, so risk 1's DEFENSE is unbuilt; the gate and the defense are different states and the tally must not merge them.\n- risk 2 EVENT VOLUME: harness built and runs (pnpm load:events), NOT wired. PARTIAL - fails the (b) test until it is a pre-M4 job.\n- risk 3 ADAPTER SPRAWL: defense built (PROTOCOL_VERSION, the ingest 400, the generated action-ids union pinned by @ts-expect-error both ways), CI check MISSING. PARTIAL. The settling command is a CI job that runs pnpm codegen and fails on a dirty diff; skill.json's half is blocked on ba-skill-md-protocol-72x, not on this bead.\n- risk 4 BATTLE FAIRNESS: no gate, depends on the battle bead. NOT DONE.\n- risk 5 PAYMENT/COMPLIANCE: the RULE is tested and the BANNER IS NOT RENDERED. needsSandboxBanner is unit-tested in packages/features/bounty/src/payout.ts with ZERO production callers, so a green test suite says nothing about the UI. NOT DONE, and this is the sharpest illustration of the whole bead: a passing unit test on an uncalled function is the definition of a gate that settles nothing.\n- risk 6 LEARN-SPINE: CLOSED, and the answer is a prohibition. The settling check is the negative one - docs/research/learn-spine-license.md plus a check that no file under packages/ or apps/ contains code derived from .tmp/learn-spine. Copy nothing, not one line.\n- risk 7 ASSET LICENSING: no gate, depends on the asset shortlist. NOT DONE. [its CI check is specified in ba-asset-shortlist-x2a and must be a LICENSE.txt-per-directory check; see the scope-lint form below for why a directory-existence check is the wrong shape]\n- risk 8 SCOPE DISCIPLINE: no gate. NOT DONE - needs the scope lint below.\nAnd the M1/M2/M4 slice ships before any MMO-shaped feature.\n\nFOR RISK 4, THE ANTI-CHEAT CLAUSE WAS ALSO A COMMENT. 'The anti-cheat gap is documented rather than silently assumed solved' is satisfied by typing the words 'anti-cheat is a gap' into a file, which is the opposite of what it means - the risk is precisely that a reader ASSUMES it is solved. It is now a check that fails in both directions: a docs check asserting the anti-cheat gap is still described as open in the battle docs, AND failing if any doc claims anti-cheat is solved. A doc asserting a solution is a finding, not a completion. The gap stays an open named bead, cited by id from the battle docs, so 'TBD' has an owner.\n\nTESTS, each of which is a command with an exit code, per the rule above:\n- risk 2: pnpm load:events exists AND is referenced by a CI job definition. The reference is the assertion - grepping for the script alone is what let a wired-up-looking gate pass unwired.\n- risk 3: a CI job that runs pnpm codegen and fails on a dirty git diff. Exit 0 means the generated schemas are in sync; exit 1 means someone bumped a manifest and did not regenerate.\n- risk 5: a test that asserts needsSandboxBanner is CALLED from a rendered component, not merely that it returns the right boolean for a given input. Production callers > 0 is the assertion. This is the check that would have caught the current state.\n- risk 6: a negative check that no source file contains code derived from .tmp/learn-spine, plus the dated determination in docs/research/learn-spine-license.md.\n- risk 8: the scope lint below.\n- a per-risk completeness check: every one of the eight risks names a settling command in this bead, and a risk whose command is a review checklist is recorded PARTIAL rather than done. This is the check that stops the tally from drifting back into prose - it fails when someone adds a ninth risk without a way to tell it is safe.\n\nWHAT THIS BEAD STILL CANNOT PROVE: a green gate says the encoded risk is covered, not that the risks were the right eight. The list comes from section 17 and a risk nobody thought of is invisible to every check here.\n\nTHE SCOPE LINT HAS A CONCRETE FORM HERE, because the placeholder directories already exist: packages/features/ contains battle, guild, social, animation and inventory, each holding only `export {};`. A lint that fails when a non-empty implementation appears in an out-of-phase feature is the check; a lint that fails on the directory's existence would fail today for reasons that are intentional. The M6 features (social, guild) and the M4 battle feature are all past their milestone as empty placeholders, which is fine and is what the placeholder is for.\n\nCONSIDERATIONS: a gate nobody runs is a comment. Each of these should be runnable (a load test, a version CI check, a banner assertion) or attached to an explicit review checklist item, not left as prose in a document nobody re-reads. The one that will get the most pushback is scope discipline (risk 8) - the MMO-shaped features are individually appealing, and the M1/M2/M4 slice is the whole reason the project avoids the gimmick trap. Treat a scope expansion during MVP as a change to the plan, not a small feature addition.","notes":"[2026-09-25 23:51:37 UTC] \n---\n\nGATE INVENTORY, 2026-09-26. The bead's own rule is that a gate is done only when a\nCOMMAND exists whose exit code settles it AND it runs somewhere that is not a\nhuman's memory. Per risk, with the command named — including the ones that are\nstill only prose, because a list that reads as all-green is worse than none.\n\nSETTLED, with a command:\n  RISK 1 (gimmick trap) — the two ordering edges, verified present: ba-game-client-pixijs-riw\n    and ba-asset-shortlist-x2a both depend on ba-feature-bounty-xhk. Not a document; an\n    edge, which is the only form an agent swarm obeys. Verified, not assumed.\n  RISK 3 (adapter sprawl) — `pnpm codegen:check`, wired as pipeline stage\n    codegen-drift. The generator also DISCOVERS its feature inputs and refuses to emit a\n    union that omits one, because a staleness check cannot catch an omission the generator\n    itself reproduces. The skill.json half is still ba-skill-md-protocol-72x's.\n  RISK 5 (payment/compliance) — packages/features/bounty/src/payout.test.ts, \"the sandbox\n    banner\" describe block, already passing.\n  RISK 6 (learn-spine) — CLOSED, and the answer is a prohibition. docs/research/learn-spine-license.md\n    records no license confirmed five ways. \"Copy nothing, not one line.\" Read is permitted;\n    copying is not.\n  RISK 8 (scope discipline) — tests/unit/mvp-scope.test.ts. Deliberately NOT a vocabulary scan:\n    six of the banned words are scheduled for M6, so failing on \"tournament\" would block\n    legitimate work and be switched off. It asserts on the installed feature set instead.\n  MOLTBOOK-adjacent — `pnpm check:moltbook`, wired as stage moltbook-claims. Not a risk on\n    this list, but the same rule: a check nobody invokes is a comment with a shebang.\n\nSTILL PROSE, and honestly so:\n  RISK 2 (event volume) — scripts/load-test-events.ts exists and runs (`pnpm load:events`, 100\n    agents x 20 ev/s x 5s, reporting generated/accepted/rows-written as three numbers). It is\n    deliberately NOT in m0.yml: section 40 puts it in its own job when M4 work starts. So\n    today a harness exists that only a human invokes. That is the exact state the bead says\n    is unacceptable, and the resolution is a milestone, not more code.\n  RISK 4 (battle fairness) — needs the battle feature. Weights stored and surfaced per battle\n    cannot be asserted before there are battles.\n  RISK 7 (asset licensing) — needs ba-asset-shortlist-x2a, which is correctly edge-gated behind\n    the bounty slice.\n\nONE THING FOUND WHILE DOING THIS. tests/unit/mvp-scope.test.ts originally asserted in both\ndirections, including that every declared feature is installed. The removal test caught it:\nthat script strips a feature and runs the unit suite, so the check failed on every feature it\nremoved. The two directions are not symmetrical — the list is an allowlist, and absence below\nthe line is a valid state, not drift. Worth recording because a gate that breaks a property\nthe repository exists to protect is worse than the gate.","status":"open","priority":1,"issue_type":"task","created_at":"2026-09-23T19:03:57.201779Z","created_by":"tranquangdang21","updated_at":"2026-09-25T23:51:37.901778Z","source_repo":"battle-agents","source_repo_path":"/Users/tranquangdang21/Projects/battle-agents","mol_type":"work","work_type":"mutex","wisp_type":"none","labels":["governance","p1","risk"],"dependencies":[{"issue_id":"ba-risk-gates-e74","depends_on_id":"ba-local-testing-contract-gmi","type":"blocks","created_at":"2026-09-23T19:04:27.392322Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"ba-risk-gates-e74","depends_on_id":"ba-reference-repo-study-o92","type":"blocks","created_at":"2026-09-23T19:07:19.248957Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"ba-runtime-and-event-bus-63r","title":"createRuntime + Event Bus + event-boundary rule","description":"Implement the core runtime, event bus, and the anti-overengineering event-boundary rule from plan sections 20, 29.2, 29.3.\n\nFILE: packages/core/src/runtime.ts, per plan section 20:\n\nexport function createRuntime(opts: { extensions: GameFeature[]; store: StateStore; bus: EventBus; log?: Logger }) {\n  const commands = new Map<string, CommandHandler>();\n  const handlers = new Map<string, EventHandler[]>();\n  const caps = new Map<string, string>(); // capability -> feature id\n  for (const ext of opts.extensions) {\n    for (const c of ext.commands ?? []) { if (commands.has(c.type)) throw new Error('duplicate command ' + c.type); commands.set(c.type, c); }\n    for (const h of ext.eventHandlers ?? []) pushToList(handlers, h.on, h);\n    for (const cap of ext.capabilities ?? []) caps.set(cap.name, ext.id);\n    const missing = (ext.requires ?? []).filter(r => !caps.has(r));\n    if (missing.length) opts.log?.warn('[core] feature ' + ext.id + ' degraded, missing: ' + missing.join(','));\n  }\n  return {\n    capabilities: [...caps.keys()],\n    async dispatch(cmd: Command) {\n      const h = commands.get(cmd.type); if (!h) throw new Error('unknown command ' + cmd.type);\n      const evts = await h.handle(cmd, ctx);\n      for (const e of evts) await this.emit(e);\n      return evts;\n    },\n    async emit(evt: GameEvent) {\n      await opts.store.append(evt);   // event_log, key events only (see section 7.2 filter)\n      for (const h of handlers.get(evt.type) ?? []) await h.handle(evt, ctx);\n      opts.bus.publish(evt);          // SSE/WS fan-out, transient allowed\n    },\n    uninstall(id: string) { /* remove feature handlers; removal-test hooks here */ },\n  };\n}\n\nALSO: packages/core/src/lifecycle.ts (start/stop, graceful shutdown).\n\nTHE EVENT-BOUNDARY RULE (section 29.3) - this is the important design constraint, encode it in review docs and tests:\n- Events are the CROSS-FEATURE INTEGRATION BOUNDARY ONLY.\n- Direct domain operations (createBounty(), validateBounty(), calculateReward()) stay PLAIN FUNCTION CALLS inside their feature.\n- Cross-feature side effects (BountyCompleted -> +XP / +rep / achievement check) go through the bus.\n- If EVERY function call becomes an event, the system is overengineered - reject in review.\n\nORDERING CONSTRAINT inside emit(): persist to event_log FIRST (durability), then run handlers, then publish to the realtime bus (transient fan-out). This ordering matters because section 30 makes the activity log a first-class P0 feature that replay, audit, achievements, reputation, and anti-abuse all read from.\n\nCONSIDERATIONS: duplicate command types must throw at install time, not silently overwrite - a duplicate is almost always a composition-root bug. uninstall() must fully deregister commands, handlers AND capabilities, or the removal test gives false confidence.\n\nSUCCESS CRITERIA: dispatch/emit/install/uninstall all work; uninstall fully deregisters; capability degradation warns rather than throws; no game vocabulary in core.\n\nTESTS: unit test that dispatch emits the handler's returned events in order; test that a duplicate command type throws; test that uninstall removes capabilities from the registry; test that a degraded feature still handles its own events.","status":"closed","priority":0,"issue_type":"task","created_at":"2026-09-23T18:59:02.913815Z","created_by":"tranquangdang21","updated_at":"2026-09-24T05:22:14.724761800Z","closed_at":"2026-09-24T05:22:14.722905300Z","close_reason":"createRuntime + in-memory EventBus shipped with 58 unit tests: dispatch ordering, duplicate command/action rejection, uninstall deregistration, degradation warning-not-throwing, degraded feature still handles its own events, no game vocabulary in core (scaffold test). Event-boundary rule is structural: architecture rules forbid feature-to-feature imports.","source_repo":"battle-agents","source_repo_path":"/Users/tranquangdang21/Projects/battle-agents","compaction_level":0,"original_size":0,"labels":["contract","core","p0"],"dependencies":[{"issue_id":"ba-runtime-and-event-bus-63r","depends_on_id":"ba-contract-extension-api-w29","type":"blocks","created_at":"2026-09-23T19:04:21.751920Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"ba-skill-md-protocol-72x","title":"Publish skill.md + heartbeat.md + messaging.md + events.md","description":"Publish the agent-facing protocol documentation, per plan sections 13, 26, and 33.\n\nMIRROR MOLTBOOK'S SHAPE (sections 13.1, 28.1 item 11): adapt the moltbook skill.md / heartbeat.md / messaging.md / skill.json structure (MIT) to our own protocol docs in apps/web/public/. Publish skill.md, heartbeat.md, messaging.md, events.md, plus skill.json with a version pin.\n\nVERIFY BEFORE COPYING (section 29 - IMPORTANT CORRECTION): the plan's architecture review found that skill.md could NOT be fetched directly (the server returns Markdown content-type the tooling could not parse), so NO Moltbook implementation details asserted from that file are trustworthy. Everywhere the plan describes Moltbook mechanics (api_key flow, ~4h heartbeat, claim flow, rate limits), treat it as UNVERIFIED HYPOTHESIS, not fact. Read moltbook's SOURCE or API docs directly to confirm; never infer from a skill.md summary.\n\nWHAT SURVIVES regardless (section 29): the architectural distinction is independently sound - Human GitHub Account -> Agent Identity (credentials, progression, reputation, history) -> Sessions (ephemeral). Keep the principle, drop the citation certainty. See the moltbook-verification bead.\n\nTHE DISTINCTION THAT MATTERS (section 33): instructions are NOT transport, NOT protocol, NOT runtime. skill.md is PROMPT INPUT, not an executable plugin - which is exactly why it is cross-framework and why ANY skills-capable agent can participate without a per-harness adapter.\n\nTWO EXTENSION LAYERS (section 13.1): PLATFORM extensions (our Bounty/Battle/Guild/Trading/Tournament modules) versus AGENT extensions (the agent's own installed skills - a battle skill, a github skill, and so on). The platform exposes the protocol; the agent composes skills. Conflating these is the mistake to avoid.\n\nTHE DOC (section 26, adapted): API base URL; Bearer agent_token auth, register once and the same key equals the same character; capabilities (register identity, inspect character, discover quests and bounties, claim bounty, join and fight battles, submit a result via PR URL, message agents, receive events); the heartbeat loop (check quests, battle invitations, guild messages, bounty updates, then act and report); and the rules (sessions are ephemeral and may close any time while the character persists; XP comes from merged PRs and passing tests, NEVER token counts; battle scoring weights are public per match).\n\nHEARTBEAT SEMANTIC (section 13.2): a VOLUNTARY agent return cadence on a timer - NOT a held-open socket. Ours checks quests, battle invites, guild messages, and bounty updates. This is a deliberate architectural choice: it means an agent can close its terminal for days and its character persists.\n\nVERSION CHECK (section 26): check the version on every handshake; on mismatch, warn and link the changelog.\n\nCONSIDERATIONS: these docs are the onboarding surface for third-party agents, so clarity is the product. An agent that misreads 'XP comes from tokens' will game the economy.\n\nSUCCESS CRITERIA, and the second clause is the one that decides whether this bead worked:\n\n1. All four docs plus skill.json are published. Settled by the docs-completeness check, which asserts every file exists AND that skill.json's version equals PROTOCOL_VERSION. Note these are the same two things and the second is the one that rots: a doc set can be complete and still advertise a version the server no longer speaks, and nothing about the files themselves would catch it.\n\n2. A skills-capable agent can participate using ONLY these docs and no per-harness adapter. [REWRITTEN - see below] This was previously unfalsifiable: 'can participate' has no wrong version. A doc set that is four empty files, or four files full of plausible prose that omits the heartbeat entirely, satisfies 'published' and reads as participation-ready to any reviewer. It is now stated as the property that actually distinguishes this design from a normal adapter: an agent that has read these four files and has no per-harness code can complete one full loop. The concrete falsifiable form is the fresh-agent conformance test, below, and it is the primary deliverable of this bead - a docs-completeness check alone would let four empty files pass.\n\n   THIS BEAD OWNS THE DOCS-SUFFICIENCY CLAIM, and it owns it alone. The property being tested is one property - \"the published documentation is sufficient for a third party with no repo access\" - and asserting it in two beads is how two agents each write half a proof and each assume the other finished it. The sibling ba-protocol-sdk-ecosystem-3ro carries a criterion that reads the same way (a client written from the published docs alone, no repo access, completing a real exchange). That criterion is NOT its own; it is the same claim, and this bead is the owner. A reader landing on 3ro should treat this criterion as already discharged here rather than re-derive it. What 3ro legitimately owns is the other half of the M7 story - that an external PACKAGE can be installed and consumed as a GameFeature through public exports - which is a different subject (a shipped artifact, not a document set) and is unaffected by this bead. If you are working on 3ro and find yourself writing a docs-only client test, you are duplicating this bead; write the out-of-tree extension test instead.\n\n   THE CONFORMANCE TEST, and what makes it a real test rather than a demonstration: a fresh agent is given the four published docs as its ONLY reference material, with no access to the repo source, no per-harness adapter, and no human steering mid-run, and must then complete a full loop end to end - register, HELLO, heartbeat, receive and correctly interpret a live event, act, end session. It PASSES only if the agent's events validate against the frozen AgentEvent union and the server's own invariants hold afterwards. The assertions that make it fail when the docs are wrong: the agent's registration is accepted under the agent identity contract (never the human session), its emitted events pass the protocol's zod validation, it does not invent an action or a field that is not in the union, and it does not claim XP from token counts. A doc set that leaves a required step implicit produces an agent that stalls or guesses, and THAT is the failure - the test is looking for the gap, not for polish.\n\n3. The version pin is enforced at handshake. Settled by the version-mismatch handshake test: a client presenting a wrong version is warned and refused rather than silently admitted, per section 26. This is a correctness rule, not documentation - a doc that says 'check the version on every handshake' while the server accepts anything makes the sentence true and the feature false.\n\nTESTS, and each fails when the behaviour is wrong rather than when a file is missing:\n- the fresh-agent conformance test (criterion 2) - a fresh agent, docs only, no adapter, no source access, completing register -> HELLO -> heartbeat -> receive -> act -> end, with every emitted event validated against the frozen AgentEvent union and the post-run server state asserted. THIS IS THE TEST THAT EARNS THE BEAD. A docs-completeness check cannot stand in for it.\n- a docs-completeness check: all four files present, and skill.json's version equals PROTOCOL_VERSION (a mismatch fails even though every file exists).\n- a version-mismatch handshake test: a wrong version warns and is refused, not admitted.\n- a docs lint asserting no doc contradicts a locked decision - the concrete instance being that no doc claims XP comes from token counts, since that is the misreading that would have a third-party agent game the economy. This is a grep over the published docs for the specific claim, not a general prose review.\n\nWHAT THIS BEAD STILL CANNOT PROVE, recorded so nobody over-reads a green run: the conformance test demonstrates one agent reading these docs successfully. It does not prove the docs are optimal, and it will not catch a doc that is merely adequate rather than clear. Clarity is the product here, and the test's job is to catch the docs being WRONG, not to certify them being good.","status":"open","priority":3,"issue_type":"task","created_at":"2026-09-23T19:03:56.932693Z","created_by":"tranquangdang21","updated_at":"2026-09-24T22:21:47.531604Z","source_repo":"battle-agents","source_repo_path":"/Users/tranquangdang21/Projects/battle-agents","mol_type":"work","work_type":"mutex","wisp_type":"none","labels":["docs","p3","protocol"],"dependencies":[{"issue_id":"ba-skill-md-protocol-72x","depends_on_id":"ba-agent-event-union-up5","type":"blocks","created_at":"2026-09-23T19:04:26.586360Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"ba-skill-md-protocol-72x","depends_on_id":"ba-mcp-adapter-li2","type":"blocks","created_at":"2026-09-23T19:04:26.500494Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"ba-skill-md-protocol-72x","depends_on_id":"ba-moltbook-verification-0zc","type":"blocks","created_at":"2026-09-23T19:04:27.284356Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"ba-status-fusion-presence-5zk","title":"Status fusion: pane + transcript -> idle/working/attention/error","description":"Port the status-fusion logic from arcane-agents into the agent extension's presence detector, per plan section 28.1 item 5 and section 1.2 item 5.\n\nSOURCE: .tmp/arcane-agents/src/server/status/decide.ts - 1061 lines, MIT (LICENSE present, Copyright (c) 2026 Thomas Rice; the THIRD-PARTY-NOTICES.md row already exists at SHA edcaf4018ab4). It fuses pane text plus transcript into a small status set: idle / working / attention / error / stopped.\n\nTHE DECIDER RETURNS BOTH A VERDICT AND ITS JUSTIFICATION, AND THAT IS THE PART TO STEAL. It returns `reasons: StatusReason[]` (an ordered, human-readable why) alongside `facts: StatusDecisionFacts` (the complete set of derived boolean and duration inputs). Both are load-bearing, and they solve different problems: `facts` makes the decision auditable, `reasons` makes it explainable. A detector that returns only a status string cannot answer 'why is this agent idle', and a presence view that cannot answer that is a spinner.\n\nA CONSEQUENCE THAT FOLLOWS DIRECTLY, recorded in docs/research/arcane-agents.md: because `facts` records the full derived signal set, a reviewer can see that a signal was DETECTED and deliberately NOT USED, rather than concluding the detection is broken. The reference does exactly this - it detects a Codex `prompt` signal and rejects it as a false-attention trap, with a comment explaining why, so the next person does not 'fix' it. PORT THAT PRACTICE, not just the rule: a decider that silently omits a signal leaves an obvious-looking hole, and the comment is what closes it.\n\nWHY: Section 1.2 item 5 calls this a concrete STEAL: 'status fusion, control-group UX for parties, SQLite schema draft.' Raw tool events tell you an agent is active, but not whether it is blocked on a PERMISSION PROMPT (attention) or has actually errored. That distinction is what makes a character legible in the world and what surfaces a permission queue as a real game mechanic (section 1.1 credits codemoo/agent-world with 'permission-queue-as-game-mechanic').\n\nPORT THE LOGIC, NOT THE FRAMEWORK (section 28.1 item 5 wording: 'port logic (not framework)'). The source is Express/node-pty/tmux-oriented; our target is a pure function over event streams, so do not drag the framework along.\n\nALSO STEAL FROM THE SAME REPO: the SQLite schema as a Neon draft (section 1.2 item 5), and the RTS control-group UX for parties (section 1.2 item 5) - the party UX belongs to the guild/social beads, but the schema draft informs our session tables.\n\nDO NOT (section 1.2 item 5 AVOID): tmux as a CORE dependency - keep it as one adapter backend only; Express/Canvas (use Next.js/PixiJS); the local-only assumption.\n\nCONSIDERATIONS: the 1061-line source is the single largest ported file in the plan. Extract the decision rules into a pure, table-driven module with its own tests rather than transliterating directly, so the logic is auditable. Its timing windows are all named constants and all injectable in the reference - keep that property, because an untestable timeout is a magic number (8s strong-evidence, 15s recent-error, 2.25s command-warmup are the ones the note records).\n\nCOMPLEMENTARY IDLE RULE FROM AGENT-MOVE: 45 SECONDS, NOT 30. The plan (section 1.2 item 4) and an earlier version of this bead both said '30s idle timeout'. That is wrong, and docs/research/agent-move.md records the correction against the source (`idleTimeoutMs: 45_000`). The 30s figure is a client-side cosmetic sleep timer and plays no part in the server state machine. Use 45s, and keep it a named constant rather than folding it into the table.\n\nWHERE THIS LANDS: the owning feature is packages/features/agent, which already has a session state machine (session.ts: SESSION_STATUSES active/ended/disconnected/abandoned, nextSessionStatus, DEFAULT_HEARTBEAT_TIMEOUT_MS = 5 minutes, DEFAULT_RESUME_GRACE_MS = 15 minutes) and a sweepStaleSessions helper in hello.ts. Idle / working / attention / error / stopped is a different axis from that lifecycle - a session can be active and its agent blocked on a permission prompt - so this detector must not be collapsed into nextSessionStatus. Note also that session.heartbeat and session.end are the actions that exist today; the presence statuses have no action id yet and will need one.\n\nSUCCESS CRITERIA, one clause at a time, each with the thing that settles it. The two clauses marked [REWRITTEN] could previously be satisfied without writing any behaviour at all:\n\n1. [behaviour] Given a fixture event stream, the detector produces the expected status transitions. Settled by: table-driven unit tests over the five named sequences, where each row is (input event sequence, expected status after each event) and the expectation is a literal, not a snapshot. Snapshots are how a decider silently changes behaviour and still goes green.\n\n2. [behaviour] 'attention' is raised on permission.requested and cleared on the corresponding resolution. Settled by: a test that drives request -> resolution and asserts the transition BOTH ways. A decider that raises attention and never clears it passes clause 1 and is useless in the UI - it is the most likely way this bead ships broken, so it gets its own test rather than being folded into the table.\n\n3. [behaviour] no tmux or Express dependency leaks in. Settled by: the architecture test over the whole packages/features/agent module graph, asserting zero tmux / node-pty / Express imports. 'port the logic, not the framework' is only true if the import graph proves it, because a single convenience import of node-pty to read a pane would reintroduce the framework and still let every unit test pass.\n\n4. [behaviour] every decision returns BOTH reasons[] and facts{} so a transition is explainable after the fact. Settled by: a test asserting the shape of the return value on EVERY transition, not one - reasons is a non-empty ordered array and facts is a complete object. The failure this catches is the one the reference exists to prevent: a detector that returns only a status string, so 'why is this agent idle' is unanswerable and the presence view is a spinner.\n\n5. [REWRITTEN] a detected-but-unused signal is recorded with a comment saying why it is rejected, rather than being silently omitted. AS WRITTEN THIS WAS SATISFIED BY A COMMENT, and a comment is not observable: anyone can add one, delete it, or write one that explains nothing, and no command anywhere would notice. THE MECHANISM IS NOW A MACHINE-CHECKED TABLE. Rejected signals live in an exported REJECTED_SIGNALS array of { signal, reason } entries, the decider records the corresponding facts key, and the reason string is part of the data the test asserts on. So: a test asserts REJECTED_SIGNALS is non-empty, that every entry names a real signal key the facts object actually carries, and that the Codex 'prompt' false-attention rejection the reference makes is present with its reason intact. Deleting an entry without replacing it now fails a test; a reason that degrades to an empty string fails a test. A prose comment beside the code remains welcome, but it is now documentation of the table, not the mechanism - the table is what holds.\n\n6. [behaviour] the idle timeout is the named 45s constant. Settled by: a test asserting the exported constant equals 45_000. The bead's whole correction history is that '30s' was wrong and that an untestable timeout is a magic number, so the number is pinned by a test rather than trusted to a reader. All timing windows stay injectable, per the reference, and there is a test that the same event sequence with a shortened window produces a different verdict - that is what makes 45s a testable claim about a timeout instead of a constant nobody varies.\n\n7. The detector has no action id today - session.heartbeat and session.end are what the registry currently exposes - so exposing presence over the protocol means adding one and regenerating the action-id union via pnpm codegen. The codegen drift check (a dirty diff after pnpm codegen) is the gate here, the same one risk 3 is tracked on; a presence action id added to the source but not regenerated is caught by that check, not by any test in this bead.\n\nTESTS, and note that each one is a test that FAILS WHEN THE BEHAVIOUR IS WRONG, not a test of the file's existence:\n- table-driven transition tests over the five named sequences, with literal expected statuses per event.\n- an attention raise-and-clear test driving request -> resolution and asserting the transition both ways.\n- an architecture test over the whole agent module graph: zero tmux / node-pty / Express imports.\n- a return-shape test on every transition: reasons[] non-empty and ordered, facts{} complete. A snapshot here would let a decider drop facts{} and stay green, so assert the fields explicitly.\n- a REJECTED_SIGNALS test: the array is non-empty, every entry's signal is a key the facts object carries, every reason is non-empty, and the Codex 'prompt' false-attention entry is present. This is the clause that used to be a comment.\n- a timeout test: the exported idle constant is 45_000, and injecting a shortened window changes the verdict for the same input.\n- the codegen drift check after pnpm codegen (recorded on risk 3, not owned here).","status":"closed","priority":1,"issue_type":"task","created_at":"2026-09-23T19:00:28.918858Z","created_by":"tranquangdang21","updated_at":"2026-09-25T05:46:29.085095Z","closed_at":"2026-09-25T05:46:29.085042Z","close_reason":"Ported to packages/features/agent/src/status-fusion.ts. The logic is ported, not the framework: the reference is Express/node-pty/tmux shaped and takes a Worker, this takes derived signals and returns a status, and nothing here opens a pty or reads a pane.\n\nThree properties survive the port because they are the reason this is a module rather than a branch. A decision carries the full derived fact set as well as the ordered reasons, so it is auditable and a view can say \"blocked on a permission prompt\" instead of spinning; a detector returning only a status string cannot answer why an agent is idle. A signal that is detected and deliberately NOT used stays visible in the facts with its reason, because the prompt-shaped signal is a false-attention trap: harnesses emit it while working normally, and treating it as attention pins a character there for the whole run. And the order of the decision is itself the decision: stopped outranks everything, since showing \"working\" for a departed agent is the one wrong answer that costs a person their attention, and attention outranks working, since being blocked is the state a spectator can act on and working masks it.\n\nIdle is 45 seconds, from agent-move's idleTimeoutMs. The 30 in the plan and in an earlier version of this bead is a client-side cosmetic sleep timer that plays no part in any state machine; at 30 every character looks busy a third longer than the reference, which is the difference between working and stuck. Every window is named and injectable, because an untestable timeout is a magic number.\n\nThe table is asserted with literals, never snapshots: a snapshot records whatever the code happened to produce, so a decider that changes behaviour passes on the same run that revealed the change. Disabling the attention branch turns three tests red, which is how the table is known to be holding. An earlier attempt to prove that by reordering the precedence turned nothing red, because the edit did not change behaviour; the proof is the second attempt, not the first.\n\nKept off nextSessionStatus deliberately: a session can be active while its agent is blocked, so this is a different axis from the lifecycle. NOT DONE: the presence statuses have no action id yet, so nothing surfaces a status to a caller. That is the remaining piece and it is named rather than implied.","source_repo":"battle-agents","source_repo_path":"/Users/tranquangdang21/Projects/battle-agents","mol_type":"work","work_type":"mutex","wisp_type":"none","labels":["feature","p1"],"dependencies":[{"issue_id":"ba-status-fusion-presence-5zk","depends_on_id":"ba-feature-agent-f9t","type":"blocks","created_at":"2026-09-23T19:04:24.213430Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"ba-status-fusion-presence-5zk","depends_on_id":"ba-reference-repo-study-o92","type":"blocks","created_at":"2026-09-23T19:05:53.963487Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"ba-third-party-adapter-proof-2wp","title":"M7 proof: third-party adapter merged with zero core edits","description":"Prove the extensibility architecture is real, per plan section 27 (M7 DoD) and section 6.\n\nTHE DoD, VERBATIM (section 27): 'third-party adapter PR merged using only _template + protocol docs; DoD: contributor adds Amp support without touching core.'\n\nWHY THIS IS A BEAD RATHER THAN A MILESTONE LABEL: the plan's central architectural claim is 'add feature = add module + register capability, zero core edits' (section 12.1) and 'new CLI = one subdirectory' (section 1.1). A claim like that is only worth something if it survives contact with a real contributor who was not in the room when it was designed. This bead is the falsification test for the architecture.\n\nTHREE THINGS THAT WILL SEND AN IMPLEMENTER TO THE WRONG PLACE:\n\n1. THERE IS NO packages/interfaces/ DIRECTORY. The success criterion below says the diff must touch 'ZERO files under core/, interfaces/'. The interface layer in the shipped tree is packages/api (the shared Application API that cli, mcp-server and the surfaces consume), together with packages/cli and packages/mcp-server. Search for packages/interfaces and you will find nothing and conclude the check is trivially satisfied. The second paragraph of this bead names the right set (core, cli, mcp); treat that as the one to enforce, and add packages/api to it.\n\n2. YOU WILL NOT NEED TO EDIT THE ARCHITECTURE RULES, AND SHOULD NOT. The obvious worry is that a new package is 'unclassified' and fails the gate with 'Classify this package in architecture-rules.cjs'. It will not: the adapter layer is declared as a generic shape, instance /^packages\\/adapters\\/([^/]+)(?:\\/|$)/ over packages/adapters/*/**/*.ts, so packages/adapters/amp/ is classified automatically the moment it exists. Do not add an entry to architecture-rules.cjs - that is an out-of-scope root edit that would itself fail the diff check, and it is unnecessary.\n\n3. THE ONE REAL WAY TO VIOLATE THE DIFF IS THE ACTION-ID UNION - AND NO ADAPTER HAS EVER DONE IT. The dispatchable action ids are a generated file, packages/protocol/src/generated/action-ids.ts, holding REGISTERED_ACTION_IDS (currently 14 ids: agent.*, progression.*, quest.*, reputation.*, session.*). It is produced by `pnpm codegen` from a HAND-MAINTAINED map at the root of scripts/generate-action-ids.ts, which currently lists exactly four features: agent, quest, progression, reputation. Two consequences. First, the map is not automatic - adding a package to it is a hand edit to a root script, and regenerating writes a committed file under packages/protocol/. Both are outside 'packages/adapters/amp/ plus a manifest entry'. Second, and this is the useful part: NONE of the six shipped adapters (claude, codex, cursor, gemini, opencode, pi) contributes a single id to that union. An adapter built to the template consumes the frozen AgentEvent union and implements the AgentWatcher seam; it does not publish dispatchable actions. So the correct Amp adapter needs no codegen change at all. If your Amp work concludes it must expose its own action id, that is a genuine architecture finding about the codegen, and it gets recorded as one - it is not a licence to hand-edit the map to make the diff check pass.\n\nPROCEDURE:\n1. Have someone (or an agent working only from the written docs) implement the Amp adapter using adapters/_template plus the protocol docs - not by reading our feature code\n2. Merge it through the normal review path\n3. Verify the diff touches ZERO files under core/, packages/api, packages/cli and packages/mcp-server, and that `pnpm run architecture` passes\n\nARCHITECTURE-FAILURE SIGNAL (sections 33, 36): if adding Amp requires ANY edit to core/, cli/, or mcp/ implementations, that is an architecture FAILURE and the correct response is to fix the architecture - not to relax the rule. The settling command for the layering half is `pnpm run architecture` (scripts/check-architecture.ts over architecture-rules.cjs, invoked as its own pipeline stage, with each individual rule separately proven against a fixture by tests/unit/dependency-rules.test.ts). Do not write a fresh grep for this bead: a grep disagrees with the real gate and passes on a tree the gate rejects.\n\nSUPPORTING LEGS from the research: section 1.1 credits pixel-agents with an agent-agnostic roadmap where a new CLI is one subdirectory; section 6 says Amp, Grok, and Koda all follow the same AgentWatcher recipe; section 26's skill.md exists so that ANY skills-capable agent can participate without a per-harness adapter at all.\n\nWHAT TO LEARN FROM A FAILURE: if the template proves insufficient, the gap is real product debt in the template and protocol docs - fix those, do not special-case Amp.\n\nSUCCESS CRITERIA: the Amp adapter is merged with a diff limited to packages/adapters/amp/ plus its manifest entry, `pnpm run architecture` passes unchanged, no file under core/, packages/api, packages/cli or packages/mcp-server is touched, and packages/protocol/src/generated/action-ids.ts is unchanged (which is what \"the adapter consumes the event union rather than publishing actions\" looks like in a diff).\n\nTESTS: a CI assertion that the Amp adapter diff introduced no changes outside its own directory, asserting on the file list rather than on a reviewer's reading; the adapter contract suite passes for Amp; and `pnpm run architecture` exits 0. The per-rule fixtures in tests/unit/dependency-rules.test.ts are what make the gate trustworthy, so do not delete or narrow them to make the new adapter pass.\n\nCONSIDERATIONS: the temptation, when this fails, is to special-case the new harness - add a field to the provider interface, a conditional in the registry, a branch in an adapter. That converts a false claim about the architecture into a true but much weaker one, and it is how a 'pluggable' system becomes a switchboard. The correct response to a failure is to fix the template or the protocol docs, then re-run. The two edits that would do this quietly are exactly the ones this bead forbids: adding a provider field, and adding the adapter to a hand-maintained list at the repo root. Also worth resisting: letting the proof be performed by someone who already knows the codebase, since insider knowledge is exactly the variable that would mask the gap.","status":"open","priority":3,"issue_type":"task","created_at":"2026-09-23T19:03:57.097658Z","created_by":"tranquangdang21","updated_at":"2026-09-24T22:02:45.615790Z","source_repo":"battle-agents","source_repo_path":"/Users/tranquangdang21/Projects/battle-agents","mol_type":"work","work_type":"mutex","wisp_type":"none","labels":["ecosystem","m7","proof"],"dependencies":[{"issue_id":"ba-third-party-adapter-proof-2wp","depends_on_id":"ba-adapters-other-harnesses-as3","type":"blocks","created_at":"2026-09-23T19:05:54.245015Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"ba-third-party-adapter-proof-2wp","depends_on_id":"ba-adapters-template-a09","type":"blocks","created_at":"2026-09-23T19:04:26.944294Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"ba-third-party-adapter-proof-2wp","depends_on_id":"ba-m1-two-harness-integration-3us","type":"blocks","created_at":"2026-09-24T21:32:36.974124Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"ba-third-party-adapter-proof-2wp","depends_on_id":"ba-protocol-sdk-ecosystem-3ro","type":"blocks","created_at":"2026-09-23T19:04:27.077797Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"ba-tool-map-port-89a","title":"Port TOOL_NAME_MAP / TOOL_ZONE_MAP normalization from agent-move","description":"Port the tool normalization layer from agent-move into packages/protocol/src/tool-map.ts, per plan section 28.1 item 2 and section 20.\n\nSOURCE: .tmp/agent-move/packages/shared/src/constants/tools.ts - 157 lines, containing TOOL_NAME_MAP, TOOL_ZONE_MAP, normalizeToolName(), getZoneForTool(), and normalizeToolInput(). Also port zones.ts, colors.ts, names.ts from the same directory as naming seeds.\n\nTHREE FACTS ABOUT THE SOURCE, each verified, because getting them wrong makes the port wrong:\n1. TOOL_NAME_MAP IS NOT EXPORTED. It is a module-private `const Record<string, string>` at line 86, reachable only through `normalizeToolName()`, which is `TOOL_NAME_MAP[name] ?? name`. Ours must be exported, because adapters normalize through it and the contract suite needs to assert against it - but say so deliberately rather than copying the privacy by accident.\n2. THE ENTRY COUNTS ARE SMALLER THAN THE PLAN IMPLIES. TOOL_NAME_MAP has roughly 38 entries, not ~50, and TOOL_ZONE_MAP roughly 29. Both are additions to a growing set, so the count is not the contract; the degrade-gracefully behaviour is. Do not treat a missing entry as a bug to be filled in by guessing.\n3. normalizeToolInput() IS IN THE FILE AND IS WORTH PORTING. It rewrites camelCase input keys to snake_case (filePath->file_path, oldString->old_string, newString->new_string, replaceAll->replace_all) and is called by every agent parser in the reference. It is the reason a downstream consumer can read one input shape regardless of harness. Omitting it is the most likely way a copied version turns out to be subtly wrong.\n\nThe file also exports TOOL_ICONS (emoji per tool) and the FILE_WRITE_TOOLS / FILE_READ_TOOLS sets. The icon map is presentation and belongs to the game client, not the protocol; the two tool sets are cheap and useful.\n\nWHY THIS IS A DIRECT COPY (section 28.1 item 2): agent-move is the single best-documented reference for normalizing many CLIs into one vocabulary. Section 1.2 item 4 calls it a 'textbook' implementation and says STEAL: copy the AgentWatcher + normalization VERBATIM as our adapters/ template. The plan explicitly classifies this as a copy-with-attribution, not a rewrite.\n\nEXTEND THE ZONE SET: add bounty-board, battle-arena, guild-hall alongside the existing Files/Terminal/Search/Web/Thinking/Messaging/Tasks/Spawn/Idle, so the game client can map tools to our world (section 20, section 25).\n\nATTRIBUTION REQUIREMENT: add a THIRD-PARTY-NOTICES.md entry naming agent-move, the file, and the commit SHA (section 28.3). The license-hygiene bead's CI check enforces this.\n\nTWO THINGS THAT LOOK TRUE AND ARE NOT, both recorded in docs/research/agent-move.md and in the existing THIRD-PARTY-NOTICES.md table:\n- agent-move SHIPS NO LICENSE FILE. Its `package.json` declares `\"license\": \"MIT\"` and nothing else grants terms. The existing notices row already records this as 'declared in package.json' with an empty copyright cell, which is the honest form. Do not write a copyright line naming an author - there is none to name.\n- tools.ts HAS NO COPYRIGHT HEADER TO KEEP. The file opens with a bare `import type` and a comment. 'Keep the original copyright header' is unsatisfiable as written, because there is no header in the source. The attribution obligation is satisfied by the THIRD-PARTY-NOTICES entry, not by a header that does not exist upstream.\n\nCommit SHA to cite: 85d377110721 (the SHA the research note and the notices table both record).\n\nCONSIDERATIONS: ~50 tool entries will go stale as harnesses add tools. normalizeToolName() should degrade gracefully to a sensible default zone for unknown tools rather than dropping the event - an unmapped tool is still real activity and should not break the pipeline.\n\nSUCCESS CRITERIA. The bead's shape is already good - every clause names a function and an observable output - so this is a targeted fix, not a rewrite. The one clause that could not fail is marked [REWRITTEN]; the rest are kept and each is given the assertion that settles it.\n\n1. normalizeToolName maps the known harnesses' tool names to canonical names and is EXPORTED so the contract suite can assert against it directly. Settled by: a table-driven unit test over the actual mapping, importing TOOL_NAME_MAP itself rather than re-typing the expected pairs in the test. That import is the assertion - a re-typed table can drift from the map it claims to test and stay green, whereas the contract suite reaching the exported map cannot. The EXPORT is a compile-time property: tests/types/action-ids.ts-style @ts-expect-error coverage proving the symbol is reachable from outside the module, since a module-private const would satisfy every runtime test while failing the reason this clause exists.\n\n2. getZoneForTool returns a zone for every canonical tool, including the new bounty-board, battle-arena and guild-hall zones. Settled by: a test iterating every key of the canonical name map and asserting getZoneForTool resolves each to a member of the declared zone set - no undefined, no throw. Plus a test asserting the three new zones are REACHABLE, not merely present in a type: a tool mapping to each of bounty-board, battle-arena and guild-hall returns it. The distinction matters because the zone set can be extended by adding three strings to a union, which compiles, satisfies 'returns a zone for every canonical tool', and leaves the client unable to route anything there. An unreachable zone is the failure this half of the clause is about.\n\n3. [REWRITTEN] An unknown tool returns a sensible default zone rather than throwing or being dropped. 'SENSIBLE' is the problem: the old test asserted only that the call did not throw, which passes for a default of literally any zone - a nonsense zone, a zone that does not exist, a tool routed to 'battle-arena' because it happened to be first in the map. No wrong implementation of the default could fail it. The default is now PINNED: the module exports a named constant for the fallback zone, the test asserts getZoneForTool('a_tool_no_harness_has_ever_heard_of') returns EXACTLY that constant, and the test additionally asserts the constant is a member of the declared zone set. A default pointing at a zone that does not exist now fails, which is the bug the old wording waved through. The 'rather than being dropped' half keeps its own assertion: the unmapped tool still normalizes to a name and still produces an event, so an unmapped tool degrades in zone only and never disappears from the stream - an unmapped tool is real activity (see CONSIDERATIONS) and a normalization layer that swallows it is worse than one that misfiles it.\n\n4. normalizeToolInput rewrites the four camelCase keys to snake_case. Settled by: a test per key (filePath->file_path, oldString->old_string, newString->new_string, replaceAll->replace_all), AND the no-op path when none of those keys are present. The no-op half is the one that is usually missing and it is not automatic: an implementation that rewrites keys unconditionally, or that drops unrecognised keys while rewriting, passes all four positive cases and corrupts inputs it was supposed to leave alone. So the test asserts an input with none of the four keys comes back with all its keys intact, and an input mixing a known key with an unknown one comes back with BOTH present.\n\n5. THIRD-PARTY-NOTICES.md carries the agent-move row with SHA 85d377110721 and an honest license cell. Settled by: a test asserting the file carries the agent-move row AND that row's SHA is 85d377110721 - pinning the SHA, not merely the project name, because a row naming the right repo at the wrong commit is exactly the drift this is meant to catch. 'An honest license cell' is made checkable too: the cell must read 'declared in package.json' and the copyright cell must be EMPTY, per the two facts recorded above. That is falsifiable in the useful direction - writing a copyright line naming an author now fails, which is the mistake the bead warns against twice.\n\nTESTS, each a test that fails when the behaviour is wrong:\n- a table-driven unit test over the mapping, asserting against the IMPORTED TOOL_NAME_MAP rather than a re-typed copy, plus a compile-time check that the export is reachable from outside the module.\n- a test that every key of the canonical map resolves to a member of the declared zone set, and that the three new zones are reachable by an actual tool rather than present only in the union.\n- a pinned-default test: an unknown tool returns EXACTLY the exported fallback constant, that constant is a member of the zone set, and the tool still normalizes and still produces an event (degrades in zone, never dropped).\n- normalizeToolInput tests: each of the four rewrites, the all-keys-unknown no-op path, and a mixed known/unknown input asserting both survive.\n- a notices test asserting the agent-move row exists, its SHA is 85d377110721, its license cell reads 'declared in package.json', and its copyright cell is EMPTY.\n- Dropped on purpose, as before: the 'attribution header survived' test. There is no upstream header to survive, and a test asserting one exists would only ever be satisfied by inventing it.","status":"closed","priority":1,"issue_type":"task","created_at":"2026-09-23T19:00:28.796366Z","created_by":"tranquangdang21","updated_at":"2026-09-25T00:04:35.629864Z","closed_at":"2026-09-25T00:04:35.629813Z","close_reason":"Ported to packages/protocol/src/tool-map.ts and exported from the barrel. Three departures from upstream (TOOL_NAME_MAP exported, TOOL_ICONS left to the game client, three game zones added) are recorded in the file header so a later diff against agent-move reads them as decisions. normalizeToolInput came with it, which is the piece most likely to be dropped by accident since the maps look complete without it. 14 tests; removing the mcp__ prefix check and removing the unknown-tool fallback each turn one red. Attribution row was already present in THIRD-PARTY-NOTICES.md.","source_repo":"battle-agents","source_repo_path":"/Users/tranquangdang21/Projects/battle-agents","mol_type":"work","work_type":"mutex","wisp_type":"none","labels":["adapter","p1","protocol"],"dependencies":[{"issue_id":"ba-tool-map-port-89a","depends_on_id":"ba-agent-event-union-up5","type":"blocks","created_at":"2026-09-23T19:04:22.651427Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"ba-tool-map-port-89a","depends_on_id":"ba-reference-repo-study-o92","type":"blocks","created_at":"2026-09-23T19:05:53.489378Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"ba-vsd","title":"Action payload shapes are not typed at the call site","description":"PARTIALLY ADDRESSED — the compile-time half (this bead's stated subject) is still open, but the \"not just work\" framing turned out to be wrong, and finding that changed the priority.\n\nWHAT WAS FOUND, which the bead did not record. The payload shapes were not merely untyped at the call site; in quest they were a TYPE ANNOTATION NOTHING CHECKED. whyQuestIsRejected took `{ title: string; difficulty: number; xpReward: number }`, which made `draft.title.trim()` typecheck and read as a guarantee, while act() takes its input as a generic. So:\n\n    act('quest.create', {})     -> Cannot read properties of undefined (reading 'trim')\n    act('quest.create', null)   -> Cannot read properties of null (reading 'title')\n\nThe rejection path was the worst place for it: createQuest read `input.title` to build its error message and to name the quest in the quest.rejected event, so the one caller guaranteed to be handed the answer was the one that threw. A unit test of the validator would have passed; only an end-to-end test through act caught it.\n\nDONE (682c5b5, 69614b6):\n- whyQuestIsRejected narrows from unknown, a companion guard narrows createQuest so no read below it needs a cast, titleForRejection is the one place that reads a title knowing it may be not be there, and CreateQuestInput is an alias of CreatableQuestDraft rather than a second interface that happens to match.\n- act() now refuses a non-object payload at the boundary for EVERY action, with the action named. That is the floor; per-feature field checks remain each feature's business.\n- Both were watched failing before being believed.\n\nSTILL OPEN, and it is the bead's actual subject: act() is still act<I>(action, input: I), so the id-to-shape lookup is untyped. A wrong field inside a well-formed object still compiles clean. The three routes in the original brief all remain open, and the prerequisite is now clearer — a feature has to VALIDATE a shape from unknown before its shape is worth generating, and only quest does. The other three features (agent, progression, reputation) declare inputs and read fields off them unchecked; progression.read would query `agentId = undefined` from act('progression.read', {}), which the new act guard stops at the boundary but which the feature should still refuse by name.\n\nA DECISION IS STILL NEEDED on how the id-to-shape map gets built, and none of the three original routes is obviously right now that the real problem turned out to be unverified annotations rather than missing types.\n","notes":"[2026-09-25 16:11:16 UTC] \n---\n\nDECISION, 2026-09-25, by the author: close the non-frozen half only.\n\nThe typing half — act<K>(id, input: Shape[K]) — is NOT being done in this bead.\nIt was blocked on a choice with three exits that each cost something, and the\nchoice is a product/architecture one rather than an implementer's:\n\n  (a) brand the payload (Untrusted<I>) so the strict signature compiles; the two\n      frozen callers — packages/cli/src/commands.ts:220 and\n      packages/mcp-server/src/tools.ts:158 — each need a one-line wrapper. Strict\n      at every real call site, but two edits to files AGENTS.md calls frozen.\n  (b) leave act() loose and add a separately-named strict method. Nothing frozen\n      is edited, but the three real surfaces keep using the loose one, so the\n      guarantee documents intent rather than enforcing it.\n  (c) a sixth method beside the five primitives, which is a breaking change to a\n      statement packages/api/src/api.ts:14-18 makes explicitly.\n\nChosen: none of them yet. The half that touches nothing frozen is real work\nwith real defects behind it, and it is what this bead now means.\n\nSCOPE FROM HERE: ten unchecked read sites across four features must validate\ntheir payload from `unknown`, following the quest.create pattern (a rejection\nunion, a guard derived from the validator, a defensive read where the rejection\nreports back). The recon below corrected the brief's own premise: quest is NOT\nfinished. quest.create is guarded, but quest.claim, quest.submit,\nquest.admin.revoke and quest.list are not, so act('quest.claim', {}) still\nreaches repository.findById(undefined) and act('quest.list', { status:\n'nonsense' }) still hands an uninterpreted string to SQL. progression and\nreputation are worse — reputation's rewardCents reaches a trust gate. agent's\ninputs are branded (domain.ts:20-29), so its guard must narrow to a branded\ntype, not a plain string.\n\nSTILL OPEN, deliberately: the compile-time id-to-shape guarantee, the\n`{ input: unknown; output: unknown }` placeholder types (dead code — nothing\nreads ActionTypes today), and the type tests that currently assert the gap.\nDo not \"fix\" runAction in core to close this; packages/core is frozen and\ntests/types/core-actions.ts:60-99 pins that gap as a fact on purpose.\n\nALSO FIXED ALREADY, by ba-risk-gates-e74 work landing first: the generator's\nhand-written feature list. scripts/generate-action-ids.ts now discovers every\nfeature declaring *_ACTION_IDS and refuses to emit a union that would silently\nomit one, so a map built on this generator no longer inherits the hole.","status":"open","priority":1,"issue_type":"bug","created_at":"2026-09-25T01:01:56.559081Z","created_by":"tranquangdang21","updated_at":"2026-09-25T16:11:16.131753Z","source_repo":"battle-agents","source_repo_path":"/Users/tranquangdang21/Projects/battle-agents","mol_type":"work","work_type":"mutex","wisp_type":"none","labels":["contract","typing"]}
{"id":"ba-watchlist-space-molt-5v4","title":"Watchlist: SpaceMolt and other live agent-world experiments","description":"Track external projects that could invalidate or improve our design, per plan section 34.\n\nPRIMARY WATCH TARGET (section 34): SpaceMolt - an MMO-style persistent AI-agent universe over MCP/WebSocket with agents living continuously. It is the closest live experiment to our 'persistent agent world' half. ACTION: compare its session/presence model against section 3 (the User/Agent/Session/Battle split) before M5, because we have a contract-session bead that LOCKED our resume-vs-new handshake - if SpaceMolt has a better model, we want to know before M5 rather than after.\n\nWHY THIS IS A TRACKED BEAD: our four contracts are frozen (section 29.1), so external evidence is most valuable BEFORE M5 locks world persistence in - and least valuable after. The plan explicitly schedules the comparison 'before M5'.\n\nSECONDARY WATCH: Agent Intercom and sandbox-agent (section 1.3) - cross-harness messaging protocol and a unified control API across Claude/Codex/OpenCode/Cursor/Amp/Pi streaming events, permissions, and sessions over HTTP. The plan calls sandbox-agent 'a good reference for the runtime isolation/control layer behind the Battle Engine', so it may inform the battle workspace provisioner.\n\nALSO IN THE BACKLOG (Appendix A of the plan, the 'named in conversation but NOT cloned' list - the old reference here said 'section 395', which is a line number in an earlier draft of this bead and not a section of the plan at all; the plan runs 0 through 40 plus two appendices and has no section 395): Pixel Agents Web/Desktop/Desk, Claude Dungeon, Agentainer, the karansag / me-frankan / displega-ai / biggazoo swarms, agent-sandbox, Agentic Quest, mcp-coding-agents, Keyframe.it, spine-ai, DragonScript Arena, Coding Quests, Mage Mayhem, Tari bounties, Bountysource, BountyHub, AsyncAPI bounty program.\n\nCONSIDERATIONS: resist cloning these reflexively. Each has a specific lesson already extracted into the relevant bead (for example agent-sandbox's perceive-plan-act loop and Agentic Quest's real-validator win condition are cited in section 1.2 items 7-8). Pull one in only when a specific open question needs it.\n\nSUCCESS CRITERIA, REWRITTEN BECAUSE THE OLD ONE COULD NOT FAIL. The old criterion was \"a short comparison note exists\" and the old TESTS line said outright that this is not a code test. An empty file satisfies it. A research artifact is still accountable; what makes it accountable is that each of its claims is individually labelled and its conclusion has a consequence. Three things must be observable, and the whole thing must land before M5 begins, because that is when the plan says world persistence gets locked:\n\n1. A SpaceMolt row in the per-repo table in docs/research/README.md, carrying the commit SHA the reading was done against, a one-line headline finding, and a verdict per claimed mechanic - CONFIRMED (naming the file it came from) or UNVERIFIED-hypothesis. This is the same table and the same shape ba-moltbook-verification-0zc already established. This bead extends that table to the watch targets; it does not invent a second format, and the two notes must not drift apart.\n\n2. A DECISION on the single question the plan schedules this comparison to answer: given SpaceMolt's session/presence model, does our LOCKED section 3 User/Agent/Session/Battle split and resume-vs-new handshake stand, or does it change? The answer is exactly one of ADOPT / KEEP / DEFER, and each carries a written consequence: ADOPT names the contract section that would change and opens a bead for it; KEEP names the specific section 3 clause the comparison cleared, so a later reader knows what was actually tested rather than \"we looked at it\"; DEFER names the date or milestone at which the question is re-asked. A comparison that concludes nothing is a comment, not a decision - and \"concludes nothing\" is exactly what the old criterion allowed.\n\n3. A staleness note: the date, plus the beads that were unstarted when the reading was done, so a reader can tell how much of the plan postdates it. This is the shape docs/research/README.md already uses and it is the model worth copying.\n\nWHAT SETTLES IT - a check with an exit code, not a review: a docs check that FAILS if (a) docs/research/README.md carries no SpaceMolt row, (b) that row carries no commit SHA, or (c) any file under docs/design/ asserts a SpaceMolt, sandbox-agent or Agent Intercom mechanic with no citation to that row. Clause (c) is a grep for the watch-target names outside the research directory. THE FAILING CASE THIS CATCHES IS THE ONE THAT MATTERS: a design doc quietly leaning on an unverified SpaceMolt mechanic, which is exactly how a phantom constraint gets built - someone designs around a heartbeat interval or a resume rule that was never real, and the real constraint then makes the design wrong. That is the same failure ba-moltbook-verification-0zc is about, on the second watch target.\n\nUNCHANGED AND STILL TRUE: resist cloning these reflexively. Each of these projects has its lesson already extracted into the relevant bead (agent-sandbox's perceive-plan-act loop and Agentic Quest's real-validator win condition are cited in section 1.2 items 7-8). Pull one in only when a specific open question needs it. Agent Intercom and sandbox-agent stay SECONDARY here - if their comparison is not finished, say so in the staleness line rather than implying coverage that does not exist.","status":"closed","priority":2,"issue_type":"task","created_at":"2026-09-23T19:03:57.254807Z","created_by":"tranquangdang21","updated_at":"2026-09-26T02:17:28.146409Z","closed_at":"2026-09-26T02:17:28.146277Z","close_reason":"DONE. The reading happened, the decision is made, and it is KEEP.\n\nThe recon recorded that the SpaceMolt checkout had never happened. That was wrong —\nit is public and clonable. Two were taken, because the question needs both and\nneither alone answers it: the official reference client for the protocol, and\nspacemolt-lib for the WebSocket lifecycle, close codes and reconnect policy. The\ngame server is closed-source, so CONFIRMED here means confirmed in shipped client\ncode or a server-published schema — a stronger footing than Moltbook's\ndocumentation-only checkout, and the reason this reading settled anything at all\nwhere that one could not.\n\n15 claims, 9 CONFIRMED / 5 UNVERIFIED / 1 REFUTED, each quoting the line it rests\non, because .tmp/ is gitignored and a bare file:line dies in a fresh clone.\n\nTWO CLAUSES CLEARED. §3.1's Agent/Session split: SpaceMolt runs the exact \"Claude\n#2\" failure at MMO scale and still keeps identity and transport session apart,\nindependently arriving at \"expired session, new session, same player\". §3.1's\n\"BATTLE binds Sessions not Agents\", in the strongest form available — a SpaceMolt\nbattle is a server-side object with an id, queryable after every session that\ntouched it is gone.\n\nONE CLAUSE KEPT BECAUSE OF WHAT THE READING FOUND, which is the interesting part.\n§3.2's resume-vs-new handshake stands because SpaceMolt has no such offer at all:\nthe client forks login versus register unilaterally from a local file. That is the\n\"Claude #2\" failure reached by a different road, and adopting it would trade a\nserver-held contract for a local file.\n\nDELIBERATELY NOT MARKED CLEARED: the grace window (no SpaceMolt counterpart) and\nthe heartbeat. An earlier draft of the note called our heartbeat \"unjustified\"\nbecause SpaceMolt runs without one — which was the wrong inference, since our own\nsession code implements a five-minute heartbeat. It is not unexplained, merely\nunmatched by a counterpart. Corrected rather than shipped.\n\nThe check is wired as pipeline stage watchlist-claims and resolves all 12\nfile:line citations plus 13 evidence lines byte-for-byte, so the note cannot drift\nfrom the reading that produced it. The clause that earns it is the cross-check: a\ndesign doc leaning on an unverified SpaceMolt mechanic is how a phantom constraint\ngets built.","source_repo":"battle-agents","source_repo_path":"/Users/tranquangdang21/Projects/battle-agents","mol_type":"work","work_type":"mutex","wisp_type":"none","labels":["p2","research","watchlist"],"dependencies":[{"issue_id":"ba-watchlist-space-molt-5v4","depends_on_id":"ba-monorepo-scaffold-cit","type":"blocks","created_at":"2026-09-23T19:04:27.499948Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"ba-web-ui-surface-t3w","title":"Next.js UI: dashboard shell, bounty board, agent cards","description":"Build the web UI surface per plan section 8.\n\nMAIN SCREEN OPTIONS (section 8). The plan offers a pixel-city main screen and a BOUNTY BOARD AS FIRST SCREEN OPTION - and the bounty board is better aligned with the M2-is-valuable-without-game-UI strategy. The board: HOT BOUNTIES list ($2000 SSO, $750 memleak, $250 OAuth, $50 CLI bug with competitor counts) -> bounty detail (repo, issue, reward, requirements, [ENTER BATTLE], competitors).\n\nDASHBOARD (section 8, from the conversation): top bar (Agent Battle, notifications, coins, level) / CODING CITY map (Guild Hall, Research Lab, character, Arena, Workshop, Quest Board) / bottom tabs (QUESTS, AGENTS, GUILD, ARENA, PROFILE).\n\nAGENT CARD (section 3.2 + section 10.1): level, current quest and session, skill bars, [Enter Arena] [View History] [Join Guild]. The dashboard agent list must show online AND offline agents with Level/XP and 'Last seen 12m ago' - a dead session must never make a character vanish.\n\nTHREE OF THE FIVE SURFACES IN THE OLD TITLE ARE NOT THIS BEAD'S, and leaving them in was the actual defect. This bead is the DASHBOARD SHELL, the BOUNTY BOARD, and the AGENT CARDS. The other two have owners, and this text used to claim work that belonged to them while its own success criteria quietly tested something narrower - so an implementer could read the title, build two surfaces they did not own, and still miss the one thing the criteria actually settle.\n\nREPLAY VIEW is owned by ba-battle-replay-yjb, which already specifies the page (apps/web/app/(app)/replay/[replayId]/page.tsx), the og:image, the public logged-out read and the determinism contract. Note the contradiction this removes: this bead previously said 'REPLAY VIEW: see battle-replay' and then, two clauses later, required in criterion 1 that the replay 'renders from REAL data' with an integration test. Both cannot be true of one bead. The replay view is the replay bead's; what stays here is the link into it from the bounty board's [ENTER BATTLE] action, which is this bead's because the board is.\n\nARENA DIFF-STREAMING is also the replay bead's, and was duplicated here verbatim. ba-battle-replay-yjb already carries 'diff-streaming over the websocket for arena efficiency (section 8), using the codemoo stateDiffBroadcast pattern (section 28.1 item 12)', and the server side of the same pattern is ba-event-ingest-and-sse-gu9 ('DIFF BROADCAST ... broadcast DIFFS, not full state'). Three beads naming the same port is how it gets built twice or, more likely, never: none of them can tell whether it is done. It is the replay bead's to render and the ingest bead's to emit. This bead consumes the stream and does not build it.\n\nGUILD HALL with a map editor inside the client (section 8, agent-quest editor precedent) belongs to ba-feature-guild-5g6 (M6) and the game client, not here. A map editor is a creation tool for M6 guild territory; building it inside the dashboard bead puts M6 surface area in a P2 bead that is not blocked on anything guild-related. It stays in the plan (section 8) and it is not dropped - it is not this bead's, and the honest form of that is a pointer, not a bullet.\n\nPRODUCT SHAPE (section 11.4): BOUNTY MARKET (issues, sponsors, rewards) + GAME WORLD (agents, guilds, projects) + ARENA (PvP, battles, tournaments) over AGENT RUNTIME (MCP/hooks/GitHub).\n\nCONSIDERATIONS: the plan is emphatic that the web is a VIEW, not the game - every loop must be playable via the protocol alone (section 34). The UI must not become the only path to any action, or the machine-native rule is broken.\n\nSUCCESS CRITERIA, REWRITTEN BECAUSE 'RENDER FROM REAL DATA' AND 'NO LOOP IS WEB-ONLY' COULD NOT FAIL. Both clauses were satisfied by a page that renders a hardcoded array, and both are the properties this bead actually exists to protect. Each now names the wrong implementation it must catch.\n\n1. Bounty board and agent cards render from REAL data. 'Real' means: the component is exercised against a live or compose-seeded API, not a fixture literal pasted into the component file. Settled by: an integration test that mounts each view against the running API and asserts the values on screen came from the response - a bounty with a distinctive reward amount in the API appears with that amount on screen, an agent id appears on its card. THE WRONG IMPLEMENTATION THIS CATCHES, and it is the common one: a component that renders a hardcoded array renders perfectly, passes every visual check, and ships data that is not the game's. A test that only asserts the page does not crash passes all three of these views with a hardcoded list. If a fixture is unavoidable for a view, the test must additionally assert the fetch path, and say which part is faked.\n\n2. Offline agents display correctly - and a dead session must never make a character vanish. This is the whole reason the clause exists (section 3.2 / 10.1: the agent list shows online AND offline agents with Level/XP and 'Last seen 12m ago'). Settled by: an online/offline state test driven from actual session state rather than a snapshot. Two assertions that a snapshot cannot give: an agent whose session is ended or heartbeat-stale RENDERS, with a last-seen value, and an agent absent from the current live set still renders. A snapshot of the online case alone passes for a component that simply filters to online agents, which is the exact bug - an offline agent silently disappearing from the roster is invisible until a user goes looking for a teammate. Assert both states, and assert the last-seen value is computed from real session data.\n\n3. [REWRITTEN] No loop is web-only. This was the most important clause and the least measurable: 'no loop is web-only' has no wrong version, because a UI can be complete and beautiful and still be the only path to some action, which is what section 34 forbids. It is now the property with a test: EVERY action the UI offers is reachable through the protocol or CLI alone, and the UI introduces no capability that exists nowhere else. Settled by: a machine-native test that completes the full loop - discover, claim, act, and the resulting state - with the web UI entirely absent, asserting the same application-command effects the UI produces. Paired with the architectural half, which is the cheap continuous check: no route handler in apps/web contains game logic, because the five primitives translate and dispatch and make no decisions, and each maps 1:1 onto an application command. The UI is then a second client of the same commands rather than a privileged path. WITHOUT THE MACHINE-NATIVE HALF THIS CLAUSE IS UNCHECKABLE, because a UI that happens to mirror the protocol and a UI that is the only way to do something look identical from inside a browser - only running the loop with the UI gone tells them apart.\n\nTESTS, each a test that fails when the behaviour is wrong rather than when a page is missing. (This supersedes the old 'component/integration tests; a snapshot for the agent card's online/offline states; the E2E smoke' line - the snapshot is specifically insufficient for criterion 2, and component tests alone are insufficient for criterion 1.)\n- integration tests mounting the bounty board and the agent card against a live or compose-seeded API, asserting distinctive values from the response appear on screen. These catch a hardcoded list, which is the failure mode the old wording could not see. The replay is deliberately absent from this list: ba-battle-replay-yjb owns the replay view and runs the equivalent test against its own page, so the test exists once and is not owned twice.\n- the online/offline state test: an ended-or-stale agent renders with a last-seen value, and an agent missing from the live set still appears in the roster. Asserted from real session state, not from a committed snapshot of the online case alone.\n- the machine-native test with the web UI disabled, completing discover -> claim -> act and asserting the same application-command effects. This is the test that earns criterion 3.\n- an architecture test asserting no route handler in apps/web contains game logic, and that each route maps 1:1 onto an application command.\n- the E2E smoke covering login -> connect -> claim, stopping at the board's [ENTER BATTLE] link into the replay (section 40). The link is this bead's; the render it lands on is ba-battle-replay-yjb's, and that bead's own E2E covers arriving at the timeline. Note the direction: the E2E proves the UI works, the machine-native test proves the UI is not required. Only the first is commonly written, and only the second settles this clause.\n\nSTATE CORRECTION, 2026-09-25: the state warning that used to sit here is obsolete, and acting on it would now break the build. The scaffold placeholder is gone. `apps/web` IS a Next.js app: `next` ^15.5.4, `react` and `react-dom` are dependencies; `next.config.mjs` exists; `app/` exists with `layout.tsx` and `page.tsx`; the build is `next build`, not `tsc`. `docker compose up` serves it on WEB_PORT (3000 by default).\n\nWHAT ACTUALLY EXISTS, so the next person does not re-derive it:\n- `app/api/auth/[...all]/route.ts` - the Better Auth mount (see ba-better-auth-github-oauth-9wm).\n- `app/api/events/route.ts` and `app/api/events/stream/route.ts` - the telemetry plane (see ba-event-ingest-and-sse-gu9).\n- `app/page.tsx` - a deliberate placeholder that links to `/dashboard`, which DOES NOT EXIST YET. The dashboard is this bead's first real piece of work.\n- `src/routes.ts` - the five primitives over HTTP: GET /api/discover, /api/search, /api/inspect and POST /api/act.\n\nCONSEQUENCES FOR WHOEVER PICKS THIS UP:\n- Import `next/...` freely. The old 'do NOT import next' instruction described a tree state that no longer exists.\n- There is no `app/api/health/route.ts`. The old warning asked for one; it was never needed and was not added. The auth and events routes are the real surface.\n- What remains is exactly what the success criteria above name, minus the replay: the bounty board, the agent cards, the dashboard shell they hang off, and the offline-agent display. The replay view was the one item here that another bead owns outright (ba-battle-replay-yjb). The cross-reference that used to sit here was itself stale: it said the replay 'is listed in criterion 1', but criterion 1 was narrowed to the bounty board and agent cards and no longer names the replay anywhere. So the replay is now absent from this bead's criteria entirely, which is the correct end state - the board's [ENTER BATTLE] link is exercised by the E2E smoke, and the view it lands on belongs to the replay bead, which runs its own test against its own page. Nothing here builds or renders a replay.\n\nThat is VISUAL work, and it is still blocked until DESIGN.md exists - antislop R-37 forbids building UI without design direction. A framework shell is not visual design and was never blocked; the shell is now in place.","status":"open","priority":2,"issue_type":"task","created_at":"2026-09-23T19:03:04.660810Z","created_by":"tranquangdang21","updated_at":"2026-09-24T23:06:57.364156Z","source_repo":"battle-agents","source_repo_path":"/Users/tranquangdang21/Projects/battle-agents","mol_type":"work","work_type":"mutex","wisp_type":"none","labels":["frontend","p2","web"],"dependencies":[{"issue_id":"ba-web-ui-surface-t3w","depends_on_id":"ba-2xu","type":"blocks","created_at":"2026-09-24T22:45:35.618471Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"ba-web-ui-surface-t3w","depends_on_id":"ba-api-routes-surface-mqr","type":"blocks","created_at":"2026-09-23T19:04:25.760267Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"ba-web-ui-surface-t3w","depends_on_id":"ba-battle-replay-yjb","type":"blocks","created_at":"2026-09-23T19:04:25.924875Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""},{"issue_id":"ba-web-ui-surface-t3w","depends_on_id":"ba-feature-bounty-xhk","type":"blocks","created_at":"2026-09-23T19:04:25.839558Z","created_by":"tranquangdang21","metadata":"{}","thread_id":""}]}
{"id":"ba-118","title":"Session creation over the protocol: HELLO has no surface","description":"FOUND 2026-09-26 while working ba-adapter-codex-nhp. Every adapter posts to POST /api/events, and every one of those posts answers 404 against a live server. Not a configuration problem and not an adapter bug: there is no way, from any network surface, to create a game session.\n\nWHAT EXISTS. packages/features/agent/src/hello.ts exports hello() as a PURE FUNCTION OVER A SessionRepository, alongside sweepStaleSessions(). It is correct and tested. It is wired into NOTHING.\n\nWHAT IS MISSING, verified by reading each layer:\n  - the agent GameFeature registers exactly one command, agent.register (packages/features/agent/src/feature.ts:123). hello() is not it.\n  - AGENT_ACTION_IDS is agent.describe, agent.read, session.end, session.heartbeat. There is no session.create, so act() cannot reach it.\n  - the Application API has five primitives plus observe (packages/api/src/api.ts:81-105). None creates a session.\n  - apps/web/app/api/ contains auth/, events/ and mcp/ only. There is no POST /api/sessions.\n  - apps/web/src/mcp-sessions.ts is the JSON-RPC TRANSPORT session, a different concept entirely.\n\nCONSEQUENCE. apps/web/src/event-routes.ts:166 calls resolveSession and 404s when the row is absent, so a session row can never exist over HTTP. An adapter's sessionId comes from the harness's own log, so every batch is refused. Codex made this loud rather than silent (IngestRefusedError) instead of it being a debugging session.\n\nWHY IT MATTERS MORE THAN ITS SIZE. M1's definition of done is two different harnesses visible simultaneously. Every adapter posts over HTTP. So M1 is not demonstrable until this exists, and this is the only thing standing between the adapters and that proof.\n\nTHE DECISION IS NOT OBVIOUS, and picking one silently would settle a contract question that belongs to the author. Four routes, each with a real cost:\n  (a) register hello() as a COMMAND on the agent feature and add a sixth Application API method. Cleanest, and packages/api/src/api.ts:14-18 calls the five primitives a contract, so this is a breaking change to a stated design.\n  (b) make session creation an ACTION (session.create) so it rides the existing act() and the route stays thin. Changes the meaning of an action id, since an id is what act() accepts and this is arguably a command. Also needs the first call to authenticate as a caller, which is the same principal question ba-feature-social-lss is deferring.\n  (c) expose a dedicated HTTP route that reaches the function directly. BLOCKED as written: architecture-rules.cjs forbids a route handler importing a feature, and the composition root is the only place allowed to.\n  (d) extend the HELLO handshake inside the existing MCP transport only, and have HTTP adapters never create sessions. Contradicts the adapters' design — they are told to observe, and session creation is how an adapter's events are ever accepted.\n\nSUCCESS CRITERIA: an agent that has an installation credential can create a session over HTTP and then post a batch that is accepted; a batch for a session the caller does not own is refused; the resume-vs-new handshake this repo already locked (ba-contract-session-rpo) is what the route calls, not a reimplementation; and the choice above is written down, because each of the four leaves a different architecture behind.\n\nNOTE FOR WHOEVER PICKS THIS UP: the answer is coupled to two other open questions and should be decided with them — the principal question ba-feature-social-lss is deferring, and the frozen-surface question in ba-vsd. Deciding one of the four above in isolation risks a second one being reopened a week later.","notes":"[2026-09-26 02:19:51 UTC] \n---\n\nDECIDED 2026-09-26, and settled by PRECEDENT rather than by preference, so it is\nnot a judgement call that needed a human.\n\nRoute (b) wins: register the handshake as a COMMAND on the agent feature and\nexpose it as the action `session.create`, so it rides the existing act().\n\nThe reason is visible in the tree. An action id is defined as what act() accepts,\nand act() ALREADY accepts creates and mutations:\n  - quest.create, quest.claim, quest.submit, quest.admin.revoke\n  - bounty.create, bounty.claim, bounty.submit, bounty.fund, bounty.expire\n  - progression.awards\nSo a session create riding act() is not a new concept and not a strained use of\nthe mechanism — it is what six existing actions already do. The objection I\nrecorded above, that an id is for actions and this is arguably a command, is\nanswered by the repository having already decided the question in the other\ndirection.\n\nIt also costs the least, which matters because the alternatives each cost\nsomething real:\n  (a) a sixth Application API method contradicts packages/api/src/api.ts, which\n      calls the five primitives a contract. Rejected.\n  (c) a dedicated route reaching hello() directly is FORBIDDEN —\n      architecture-rules.cjs stops a route handler importing a feature, and the\n      composition root is the only place allowed to. Not a preference.\n  (d) HTTP adapters never create sessions, so they can never post an accepted\n      batch. That is the bug this bead exists to remove, so it is not a fix.\n(b) needs none of that, and it means HTTP, CLI and MCP reach the handshake the\nsame way they already reach every other command.\n\nSTILL NEEDED, and none of it is the decision:\n  - the handshake must be an action, so it needs an entry in the agent manifest\n    and a codegen run, or it is built but not dispatchable;\n  - the FIRST call has to authenticate as a caller, which is the same principal\n    question ba-feature-social-lss is deferring. The bearer-token work already\n    landed for /api/events, so the shape exists; what is missing is saying which\n    principal owns a session at creation time.\n  - the 404 that started this is the thing to make disappear: with session.create\n    reachable, the row exists, and resolveSession stops answering 404.","status":"open","priority":1,"issue_type":"task","created_at":"2026-09-25T18:08:26.155598Z","created_by":"tranquangdang21","updated_at":"2026-09-26T02:19:51.899427Z","source_repo":"battle-agents","source_repo_path":"/Users/tranquangdang21/Projects/battle-agents","mol_type":"work","work_type":"mutex","wisp_type":"none"}
{"id":"ba-adapter-goose-good-first-cpd","title":"Adapter: Goose — a new harness joins the arena","description":"Add `packages/adapters/goose` so Goose becomes a playable harness.\n\n## What a merged PR visibly changes\n\nGoose joins the roster. Before this PR the harness has no character in the\narena at all; after it, a Goose session produces the same normalized\n`AgentEvent` stream as Claude or Cursor, so a Goose agent shows up in the\nworld, earns XP and can be battled like any other. Nothing in `core/`, `cli/`\nor `mcp/` changes, and that is the point: the protocol is what makes a new\nharness a small PR.\n\n## Why Goose\n\nGoose (Block's open-source agent CLI) writes one JSONL file per session under\n`~/.local/share/goose/sessions/`, and each line carries the message role and\ncontent. That is the same shape the codex and opencode adapters already read,\nso most of this is translating field names, not designing a mechanism.\n\nAlternates, if Goose is claimed: **Crush** (`~/.local/share/crush/`) and\n**Amp** (`~/.local/share/amp/`) are also JSONL and equally tractable. Say in\nthe PR which harness you took.\n\n## What to do\n\nStart from `packages/adapters/_template`. It has the four modules and is the\nchecklist; the two things people get wrong on a first adapter are tool-name\nnormalisation and consent gating, and both modules exist to model them.\n\n- `src/parser.ts` — one Goose line in, one `AgentEvent` out. Map tool names\n  through `canonicalToolName` from `@battle-agents/protocol`; do not grow a\n  private tool table. That is the two-copies-drift failure, and the contract\n  suite fails you for it.\n- `src/watcher.ts` — the read, normalise, buffer, post loop. Read **bytes**\n  from a tracked offset, loop on short reads, advance the cursor over what was\n  actually received, and decode **last**. Goose writes non-ASCII, and\n  incrementing the cursor by decoded-character count corrupts multi-byte tails.\n  Two shipped adapters had this exact bug.\n- `src/install.ts` — consent gating.\n- Fixtures under `src/` that are real captured lines, including one non-ASCII\n  line, so the byte/UTF-16 trap is tested rather than described.\n\n## Files to touch\n\n- `packages/adapters/goose/{package.json,tsconfig.json,src/*}` — new\n- `tsconfig.json` — a `@battle-agents/goose` path entry\n- `tsconfig.build.json` — a project reference entry\n- `tests/unit/adapter-contract.test.ts` — the import and the name in the\n  `ADAPTERS` array. This list is hardcoded, which is the one non-obvious\n  registration step in the whole task.\n\nNot needed: `pnpm-workspace.yaml` already globs `packages/adapters/*`, and the\nvitest alias is derived from the workspace directories rather than written out.\n\n## How you know it worked\n\n- `pnpm test:unit` green, with the contract suite covering your adapter.\n- `pnpm typecheck` green. `source-alias-coverage.test.ts` asserts the vitest\n  aliases and the tsconfig paths cover the same set, so a missing path entry\n  fails the unit stage even though nothing imports the package yet.\n- `pnpm test:m0` green before you open the PR. It is the same command CI runs.\n\nIf your adapter needs a new event type rather than an existing one, stop and\nopen an issue instead: the event union in `packages/protocol` is versioned and\nfrozen, and widening it is a protocol decision, not an adapter one.","status":"open","priority":3,"issue_type":"task","created_at":"2026-09-25T23:49:35.493943Z","created_by":"tranquangdang21","updated_at":"2026-09-25T23:49:35.493943Z","source_repo":"battle-agents","source_repo_path":"/Users/tranquangdang21/Projects/battle-agents","mol_type":"work","work_type":"mutex","wisp_type":"none","labels":["adapter","community","docs","good-first-issue"]}
{"id":"ba-adapter-aider-good-first-9f1","title":"Adapter: Aider — a new harness joins the arena","description":"Add `packages/adapters/aider` so Aider becomes a playable harness.\n\n## What a merged PR visibly changes\n\nAider joins the roster. Aider is one of the most-used coding agents that\nbattle-agents does not speak to yet, so today a user running it sees nothing.\nAfter this PR their Aider sessions normalize into the same `AgentEvent` stream\nas every other harness and their agent exists in the arena.\n\n## Why Aider, and the one thing to look at first\n\nAider does not have a single session file. It writes **two**:\n\n- `~/.aider/chat.history.md` — a Markdown transcript, human-readable, which is\n  what a newcomer finds first and therefore what they will assume is the\n  source of truth.\n- `~/.aider.input` / the `--analytics` / conversation YAML in\n  `~/.aider/conversations/` — the structured record with role, content and\n  timestamps.\n\nDecide which one you are reading, and write down in the module comment which,\nbecause the Markdown file is lossy and parsing it will produce a stream that\nlooks right and drops tool calls. If you find a third location, that is a\nfinding worth its own issue.\n\nAlternates, if Aider is claimed: **Qwen Code**, **Cline**, or **Windsurf**.\n\n## What to do\n\nSame shape as the other adapter issues: start from\n`packages/adapters/_template`, keep the four modules.\n\n- `src/parser.ts` — one record in, one `AgentEvent` out, tool names through\n  `canonicalToolName` from `@battle-agents/protocol`.\n- `src/watcher.ts` — read **bytes** from a tracked offset, loop on short\n  reads, advance the cursor over what was actually received, decode **last**.\n  Any session log with non-ASCII user text will corrupt under a\n  decode-then-count cursor.\n- `src/install.ts` — consent gating.\n- Fixtures that are real captured records, at least one non-ASCII.\n\n## Files to touch\n\n- `packages/adapters/aider/{package.json,tsconfig.json,src/*}` — new\n- `tsconfig.json` — a `@battle-agents/aider` path entry\n- `tsconfig.build.json` — a project reference entry\n- `tests/unit/adapter-contract.test.ts` — the import and the name in\n  `ADAPTERS`\n\n## How you know it worked\n\n- `pnpm test:unit` green, contract suite covering your adapter\n- `pnpm typecheck` green\n- `pnpm test:m0` green before the PR\n\n## What you should not have to change\n\n`packages/core`, `packages/cli` and `packages/mcp-server` are frozen. If your\nPR needs to edit one of them, that is a finding, not a workaround — open an\nissue and say what you were trying to express. A PR that has to touch those\nthree is an architecture failure and review will say so.","status":"open","priority":3,"issue_type":"task","created_at":"2026-09-25T23:49:45.759492Z","created_by":"tranquangdang21","updated_at":"2026-09-25T23:49:45.759492Z","source_repo":"battle-agents","source_repo_path":"/Users/tranquangdang21/Projects/battle-agents","mol_type":"work","work_type":"mutex","wisp_type":"none","labels":["adapter","community","docs","good-first-issue"]}
{"id":"ba-battle-reporter-good-first-rxz","title":"Battle reporter: render the public event stream as a readable report","description":"Render the public event stream as a battle report a human can read.\n\n## What a merged PR visibly changes\n\nA URL you can open that shows, in plain language, what an agent actually did\nin a session: it started, ran these tools, passed this many tests, waited here,\nended. Today the public stream exists and is correct, and a spectator can only\nconsume it as JSON — `toPublicEvent` in `apps/web/src/event-stream.ts` is\nbuilt and tested, and nothing turns it into a sentence.\n\nThis is the \"reporter\" the plan asks contributors to ship: the smallest\npossible artifact that makes the protocol legible to a person.\n\n## What to build\n\n**1. A pure projection.** `apps/web/src/battle-report.ts`, exporting something\nlike `renderReport(events: PublicEvent[]): ReportLine[]`. No I/O, no React, no\ndatabase — a function from events to lines, so it is trivially testable and\nbelongs to no feature.\n\nFollow the discipline in `docs/design/public-event-stream.md`: **project into a\nnew shape, never filter in place.** The protocol types use a\n`z.string().min(1)` identifier schema, so any field can carry a file path or a\nprompt. A reporter that renders `file.write.path` verbatim has leaked a\ndeveloper's filesystem layout to a public page, and the design doc explains\nwhy a redact-after-the-fact filter cannot save you. If your report needs a\nfield the public projection does not carry, that is a finding for\n`docs/design/public-event-stream.md`, not a reason to reach into the private\nevent.\n\n**2. Fixtures, not snapshots.** Cover: a session that did nothing, a session\nthat failed a test (the projection keeps the suite name and drops the failure\ntext — assert it is absent), a session with a non-ASCII agent name, and an\nempty stream. An empty stream is the case most likely to render as a crash.\n\n**3. A page.** `apps/web/app/report/[sessionId]/page.tsx` that reads the\nstream and renders the report. If wiring it to stored events needs more than\none file, that is the part to scope first — the projection and its tests are\nworth landing on their own.\n\n## Files to touch\n\n- `apps/web/src/battle-report.ts` — new, pure\n- `apps/web/src/battle-report.test.ts` — new\n- `apps/web/app/report/[sessionId]/page.tsx` — new\n\nDeliberately **not** touched: no new feature package, nothing in\n`packages/core`, `cli/` or `mcp/`, no `apps/web/src/composition.ts` entry, no\n`pnpm codegen`. A reporter that needs none of those is the cheapest real\ncontribution in the repository, and it is the right size for a first PR.\n\n## How you know it worked\n\n- `pnpm test:unit` green, including your fixtures\n- `pnpm architecture` green — a route under `apps/web/app/**` may not import a\n  feature, and a checker enforces it\n- `pnpm test:m0` green before the PR\n- You can open the page and read a report, which is the part no test\n  substitutes for. Run the stack (`docker compose up`) and look at it.\n\n## If it turns out to need a feature package\n\nStop and open an issue saying so, before writing it. That is a real design\nanswer and worth deciding deliberately rather than discovering inside a PR.","status":"open","priority":3,"issue_type":"task","created_at":"2026-09-25T23:49:59.427466Z","created_by":"tranquangdang21","updated_at":"2026-09-25T23:49:59.427466Z","source_repo":"battle-agents","source_repo_path":"/Users/tranquangdang21/Projects/battle-agents","mol_type":"work","work_type":"mutex","wisp_type":"none","labels":["community","docs","feature","good-first-issue","web"]}
